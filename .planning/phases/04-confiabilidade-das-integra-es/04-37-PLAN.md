---
phase: 04-confiabilidade-das-integra-es
plan: 37
type: tdd
wave: 30
depends_on: [04-36]
gap_closure: true
autonomous: true
requirements: [REL-03, REL-06]
files_modified:
  - backend/test/scheduler.categoriaIndecidivel.test.js
  - backend/src/scheduler.js
  - .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md

must_haves:
  truths:
    - "Uma rodada em que a borda de organizações está inteiramente fora dispara o alarme de supressão total MESMO quando algum dos negócios já havia sido notificado hoje (dedup)"
    - "Uma rodada com um negócio deduplicado ao lado de um notificável com sucesso continua SILENCIOSA — o conserto não troca mudez por ruído diário"
    - "A mensagem do alarme não afirma nada sobre o dia inteiro: ela fala da RODADA, porque um negócio contado pode ter sido pulado por outra guarda"
    - "O comportamento por negócio não muda: nenhum `continue` foi acrescentado, removido ou condicionado pelo contador"
    - "O residual que SOBRA (negócio sem organização a consultar) está documentado pelo mecanismo, e a correção que não o fecha está marcada como rejeitada"
  artifacts:
    - path: "backend/src/scheduler.js"
      provides: "Contador `results.categoriaIndecidivelNaRodada` no TOPO do laço + condição do alarme sobre ele + mensagem de escopo estreito"
      contains: "categoriaIndecidivelNaRodada"
    - path: "backend/test/scheduler.categoriaIndecidivel.test.js"
      provides: "Cenários L (rodada MISTA por dedup, alarme dispara) e M (dedup ao lado de notificável, alarme cala) + helper marcarComoNotificadoHoje"
    - path: ".planning/todos/pending/cr4-01b-limiar-de-supressao-total.md"
      provides: "Todo REESCRITO: nomeia o mecanismo, registra a causa fechada por este plano, e marca a correção antiga como rejeitada com medição"
  key_links:
    - from: "backend/src/agendor.js"
      to: "backend/src/scheduler.js"
      via: "campo `categoriaIndecidivel` por negócio, agora contado no topo do laço de runCheck"
      pattern: "categoriaIndecidivelNaRodada"
    - from: "backend/src/scheduler.js"
      to: "frontend/src/components/Dashboard.jsx"
      via: "`results.errors` — a superfície que a UI renderiza, e que o plano 04-36 levou também ao caminho manual"
      pattern: "results\\.errors\\.push"
---

<objective>
Fechar **WR5-01**: o contador `results.skippedCategoriaIndecidivel` incrementa **dentro da segunda
guarda** do laço de `runCheck`, então **qualquer `continue` anterior desarma o alarme de supressão
total** — e o mais banal deles é a **dedup do dia**, que nenhum plano nomeou.

Sonda do revisor: apagão TOTAL da borda `/organizations`, 3 negócios parados, **zero e-mails**,
`skippedCategoriaIndecidivel: 2` contra `stale: 3` → `results.error` **undefined**, `results.errors`
**vazio**, alarme **calado**. A rodada fica indistinguível de um dia calmo — que é literalmente o
enunciado do CR4-01, reaberto por um caminho que nenhum plano nomeou.

Purpose: a regra geral já está **escrita no código**, no comentário do contador do 04-35, no topo
deste mesmo laço — *"um contador que só incrementa depois de outra guarda ter feito `continue` não
consegue alcançar o denominador"*. O 04-35 aplicou-a ao contador novo e não a aplicou de volta ao
contador irmão, 60 linhas abaixo, no mesmo arquivo. Este plano aplica.

E fecha a metade documental do achado: o todo `cr4-01b` documenta a causa **errada**, e a "Correção
proposta" escrita dentro dele mexe no **denominador** quando o defeito está no **numerador**. Quem o
executasse como está fecharia o todo **com o defeito de pé**. A reescrita é obrigatória e faz parte
deste escopo.

Output: contador `results.categoriaIndecidivelNaRodada` incrementando no topo do laço, condição do
alarme sobre ele, mensagem com escopo de afirmação estreito, o par de cenários L (dispara) / M (cala)
e o `cr4-01b` reescrito pelo mecanismo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md
@.planning/phases/04-confiabilidade-das-integra-es/04-28-SUMMARY.md
@.planning/phases/04-confiabilidade-das-integra-es/04-35-SUMMARY.md
@.planning/todos/pending/cr4-01b-limiar-de-supressao-total.md

Convenções que valem aqui e não se negociam: comentários e mensagens em português; Biome
(`lineWidth` 80) é o único formatador — `npm run format` decide a quebra de linha, não o executor;
`node:test` nativo, um processo por arquivo de teste; referência a outro trecho **por âncora
nomeada** (função, identificador, nome de caso de teste), **nunca** por número de linha (WR2-06). Os
gates deste plano que usam número de linha são **comandos de verificação**, não texto de artefato —
WR2-06 fala de âncoras em comentário e mensagem de asserção, não de shell.
</context>

<interfaces>
O executor NÃO precisa explorar o repositório. Tudo abaixo foi medido no arquivo real em 2026-08-05,
na entrada deste plano (estado de ENTRADA = depois do plano 04-36).

**`backend/src/scheduler.js` — a cadeia de guardas de `runCheck`, na ordem real**

1. `results.checked++`
2. `if (deal.funilAusente) results.funilNaoAvaliado++;` — **NÃO faz `continue`**; é o contador do
   04-35, no topo do laço de propósito (D-IN3-08-f), e é o mecanismo CERTO que este plano replica
3. leitura de `users`, montagem de `dealResult`
4. **guarda de DEDUP** — `if (jaNotificadoHoje) { dealResult.skipped = true; results.skipped++;
   results.deals.push(dealResult); continue; }` ← **é este `continue` que desarma o alarme**
5. **guarda de CATEGORIA** — `if (deal.categoriaIndecidivel) { ...; results.skipped++;
   results.skippedCategoriaIndecidivel++; ...; continue; }` ← o incremento mora aqui
6. **guarda de FUNIL** — `if (!shouldNotifyOwner(deal)) { ...; continue; }`
7. ramo de envio / `else` de "sem destinatário"

Depois do laço, nesta ordem: **alarme de FORMA do funil** (04-35) e **alarme de supressão TOTAL por
categoria** (04-28), este último com a condição
`results.stale > 0 && results.skippedCategoriaIndecidivel === results.stale`.

**Contagens não-comentário na ENTRADA:** `skippedCategoriaIndecidivel` = **3** ocorrências (literal,
incremento, condição do alarme); `funilNaoAvaliado` = **3**; `results.skipped++` = **4** linhas;
`results.notified++` = **2**; `continue;` = **3** linhas; `results.error = ` = **3** linhas;
`categoriaIndecidivelNaRodada` = **0** (símbolo novo); `results.stale = dealsToNotify.length` = **1**,
atribuído **antes** do laço e **depois** do filtro de tarefas futuras.

**A mensagem atual do alarme de categoria** afirma: *"Nenhum dos N negócio(s) parado(s) **do dia** foi
notificado: a categoria da organização não pôde ser consultada em nenhum deles. (…)"*. Medido:
`grep -c 'do dia foi notificado' backend/src/scheduler.js` = **1**. Nenhum teste assere o texto dessa
mensagem — o cenário D assere apenas `typeof r.error === 'string' && r.error.length > 0`.

**Por que `categoriaIndecidivel` é a marca certa a contar no topo** (medido em
`backend/src/agendor.js`): no laço de enriquecimento de `getStaleDeals`,
`categoriaBruta = categoriaPorOrg.get(deal.organization?.id) ?? null` e
`categoriaIndecidivel = categoriaBruta === CATEGORIA_INDECIDIVEL`. O campo viaja por negócio na lista
enriquecida, é o MESMO que a guarda lê, e não depende de nenhuma guarda do scheduler.

**`backend/test/scheduler.categoriaIndecidivel.test.js` — a armação (estado de ENTRADA)**

- `servirDeals(idPrimeiro, idSegundo)` monta dois clones do molde 101 com donos/autores **distintos**
  (`DONO_1`/`AUTOR_1`, `DONO_2`/`AUTOR_2`) e organizações distintas (`organizacaoDe(id) = id + 100`).
- `orgsQueFalham` é um `Set` de ids de organização que respondem **429 persistente** — a única falha
  que sobrevive ao retry da borda e chega ao agendador como indecidível.
- `envios(destinatario)`, `linhasDoDeal(dealId)` (via `db.getNotificationLogs`), `avancarRelogioAte`,
  `idsPrometidosPelaPrevia` e `idsNotificadosDeFato` completam a armação.
- `const db = require('../src/db')` já está no arquivo: `db.logNotification` e
  `db.alreadyNotifiedToday` estão disponíveis sem import novo.
- O `beforeEach` rearma o relógio em `FIXED_NOW` (2026-06-01) e zera `orgsQueFalham`,
  `consultasPorOrg`, `transportesCriados` e `enviosPorDestinatario`.
- Ids já usados como `const primeiro/segundo`: **2301..2392**, de 10 em 10 (A–J), mais o par escolhido
  pelo cenário **K** no plano 04-36. Id reusado é caso contaminado — a dedup do próprio SUT acopla
  casos que compartilham id.
- **Nenhum caso da suíte arma hoje uma rodada com dedup**: medido, `db.logNotification` não aparece
  neste arquivo. O precedente da armação está em `backend/test/db.dedup.test.js`, no caso
  `alreadyNotifiedToday: deal notificado hoje (status "sent") -> true`.

**Baseline de ENTRADA (depois do 04-36):** `cd backend && npm test` → **194/194**; `npm run lint` →
exit 0, **44** warnings.
</interfaces>

<escopo_travado>
Decisão do usuário de 2026-08-05, vinculante. Este plano fecha **WR5-01 + a reescrita do `cr4-01b`**.

**Não reabrir, em hipótese nenhuma:**

- **CR4-01, limiar = supressão TOTAL** (decisão do usuário). O alarme é **ADITIVO**, mora **depois**
  do laço e **não** condiciona nenhum `continue` — nenhum limiar muda quem recebe. O conserto do
  WR5-01 é sobre **ONDE o contador incrementa**, **não** sobre o limiar. Propor limiar proporcional
  está **proibido** aqui.
- **D-IN3-08-f** — o contador do 04-35 incrementa no topo do laço de propósito. É o mecanismo CERTO,
  e este plano o replica; **não** movê-lo.
- **D-IN3-08-g** — o alarme do 04-35 vem **ANTES** do bloco do 04-28. A ordem é precedência do campo
  escalar `results.error`; **não** inverter.
- **Mensagem do alarme do 04-35 (funil) é deliberadamente ESTREITA**, com gate proibindo a frase
  larga. Este plano **não** a edita.
- **`runCheckOnly` não tem proteção de dedup** — decisão registrada; **não** acrescentar.

**Fora de escopo, com dono:** WR5-02 (aviso por negócio no caminho de leitura), WR5-03 (limiar do
alarme de forma do funil com N pequeno), WR5-04 (`console.log` vs `logger.warn` no resumo semanal) e
WR5-05 (asserções afrouxadas para `>= 1` nos cenários H, I e J) viram **todos pendentes** no plano
**04-38** desta mesma rodada, e **não** são consertados aqui. Em particular: **não** editar as
asserções dos cenários H, I e J.
</escopo_travado>

<inventario_de_irmaos>
Mandato estrutural da fase, com as **duas cláusulas novas da r5**. Cada item carrega trabalho
verificável. Item nomeado sem gate é a lacuna que reabriu esta fase cinco vezes.

## Grupo 1 — Irmãos do construto (os outros contadores agregados da rodada)

| # | Construção | Classificação | Trabalho verificável |
|---|---|---|---|
| 1 | `results.skippedCategoriaIndecidivel` (dentro da guarda de categoria) | **preservada, e DESACOPLADA do alarme** | Ela responde a outra pergunta — *quantos a GUARDA suprimiu* — e o cenário **L** a mede valendo **1** enquanto o numerador do alarme vale **2**: é essa diferença que torna o achado conferível. O cenário **D** continua asserindo `r.skippedCategoriaIndecidivel === 2` sem edição. Gate: ocorrências não-comentário caem de **3** para **2**, e `grep -c 'skippedCategoriaIndecidivel === results.stale'` = **0** |
| 2 | `results.funilNaoAvaliado` (topo do laço, 04-35) | **verificada-e-sã — é o PRECEDENTE que este plano replica** | Medido: incrementa antes de qualquer guarda, numerador e denominador percorrem `dealsToNotify` inteiro. O revisor confirmou por medição própria. Gate: nenhuma linha **não-comentário** dela entra no diff (comando na Task 2); o comentário dela PODE ser editado para registrar que a regra passou a valer nos dois contadores |
| 3 | `results.skipped` (4 incrementos, 4 causas) | **verificada-e-sã por medição** | Não é numerador de comparação nenhuma: `grep -cE 'results\.skipped ===\|results\.skipped >' backend/src/scheduler.js` = **0**. É contador de relatório, e o comentário do literal já enumera as quatro causas. Gate: `results.skipped++` não-comentário = **4** (inalterado) |
| 4 | `results.notified` (2 incrementos, nos dois ramos de status) | **verificada-e-sã por medição** | Reportado como valor absoluto, nunca comparado a `results.stale`: `grep -c 'results.notified ===' backend/src/scheduler.js` = **0**. A relação dele com o status gravado está pinada pelos cenários A e B de `notificationStatus.partialFailure.test.js` |
| 5 | `skippedFutureTasks` | **verificada-e-sã por medição** | Derivado por **diferença** (`staleDeals.length - dealsToNotify.length`) **antes** do laço: não pode ser desarmado por `continue` nenhum, por construção |
| 6 | `runCheckOnly` (a prévia) — não tem alarme agregado | **fora-de-escopo-com-medição** | A prévia marca **por negócio** (`seraNotificado`), e num apagão todos vêm marcados como não-notificáveis — o sinal existe, em outra forma. Gate: `git diff -U0 backend/src/scheduler.js \| grep -c 'runCheckOnly\|seraNotificado'` = **0**. A invisibilidade do motivo na UI já tem dono: `cr4-01c` |

## Grupo 2 — CLÁUSULA (a): DIREÇÃO REVERSA — o que pode NEUTRALIZAR o construto novo

A pergunta que o 04-28 **não** fez sobre a dedup, e que produziu este achado: *"o que faz o alarme que
eu acabei de construir ficar calado, sem deixar nenhum teste vermelho?"*.

| # | O que pode neutralizar o alarme | Por que | Disposição / gate |
|---|---|---|---|
| 1 | Uma guarda com `continue` **antes** do incremento | É o achado. Qualquer `continue` anterior subtrai do numerador sem subtrair do denominador | **FECHADO**: o incremento vai para o topo do laço, antes de todas as guardas. Gate de ordem: a linha de `results.categoriaIndecidivelNaRodada++` é **menor** que a linha do **primeiro** `continue;` do arquivo, e `continue;` continua = **3** |
| 2 | Um negócio que **nunca é marcado** `categoriaIndecidivel` porque **não tem organização** a consultar | O conjunto de organizações é montado com `.filter(Boolean)`, então um negócio sem organização não é consultado e sai **sem a marca**: o numerador não alcança o denominador e o alarme fica calado num apagão real | **NÃO FECHADO — é o residual verdadeiro.** Dono: `cr4-01b`, **reescrito na Task 3** para nomear o mecanismo. Gate: o todo reescrito nomeia esta causa como a que **sobra** e a distingue da que este plano fecha |
| 3 | Um negócio filtrado na **borda** (`EXCLUDED_CATEGORIES`, `EXCLUDED_OWNERS`, `isExcludedStage`, `dealStatus`) | Ele não entra em `dealsToNotify` | **Não neutraliza**: também não entra em `results.stale` — numerador e denominador encolhem juntos. Gate: `grep -c 'results.stale = dealsToNotify.length'` = **1**, atribuído depois do filtro |
| 4 | O filtro de **tarefas futuras** | Idem ao anterior | **Não neutraliza**, pelo mesmo motivo e pelo mesmo gate |
| 5 | `results.stale === 0` | Um dia sem negócio parado não dispara alarme | **Deliberado** e inalterado: a condição `results.stale > 0` fica byte a byte |
| 6 | Um consumidor que leia só o campo escalar `results.error` | Quando os DOIS alarmes valem na mesma rodada, quem escreve depois vence | **Preservado**: a ordem dos blocos (funil antes, categoria depois) não muda — D-IN3-08-g. Gate de ordem na Task 2 |
| 7 | `results.errors` não chegar à tela no caminho manual | O alarme existiria e ninguém o veria ao disparar | **FECHADO pelo plano 04-36** (declarado em `depends_on`), que leva `results.errors` ao toast de `sendNow`. É por isso que este plano vem **depois** dele |
| 8 | A mensagem afirmar mais do que o contador garante | Com o contador no topo, a condição total passa a valer também numa rodada **composta**; a frase "do dia foi notificado" fica **falsa** para o negócio que já recebera às 8h | **FECHADO nesta Task 2**: escopo de afirmação estreito, e o cenário **L** assere a redação |

## Grupo 3 — CLÁUSULA (b): RETROATIVIDADE DA JUSTIFICATIVA

O comentário novo vai condenar **duas** formas. A cláusula obriga a grepar o MESMO arquivo por
construções que cada frase condena, e classificar cada uma.

**Frase 1 condenada:** *"um contador que só incrementa depois de outra guarda ter feito `continue`
não alcança o denominador"*.

| Construção encontrada | Classificação | Medição |
|---|---|---|
| `results.skipped++` × 4, todas dentro de guardas | **verificada-e-sã** | Item 3 do Grupo 1: não é numerador de comparação nenhuma (grep = 0) |
| `results.notified++` × 2, dentro do ramo de envio | **verificada-e-sã** | Item 4 do Grupo 1: nunca comparado a `results.stale` |
| `results.skippedCategoriaIndecidivel++`, dentro da guarda | **preservada e desacoplada** | Item 1 do Grupo 1: continua onde está — a pergunta dela é sobre a GUARDA — e deixa de ser comparada com o denominador |
| `results.checked++` e `results.funilNaoAvaliado++`, no topo | **verificada-e-sã** | Já estão do lado certo; o segundo é o precedente literal |

**Frase 2 condenada:** *"uma mensagem de alarme não pode afirmar sobre o dia inteiro o que o contador
só garante sobre a rodada"*.

| Construção encontrada | Classificação | Medição |
|---|---|---|
| Alarme de FORMA do funil — *"Nenhum dos N negócio(s) parado(s) do dia trouxe funil"* | **verificada-e-sã por medição** | A afirmação é sobre a **forma do payload** dos negócios da rodada, não sobre notificação — e a própria mensagem diz por extenso que *"a supressão por funil não impediu nenhuma notificação"*. O contador dela já percorre o mesmo conjunto do denominador. O que é discutível ali é o **LIMIAR com N pequeno**, que é **WR5-03** e tem dono no plano 04-38. Gate: `git diff -U0 backend/src/scheduler.js \| grep -c 'trouxe funil'` = **0** |
| `logger.info('[Scheduler] Concluído: N negócios parados, M notificações enviadas')` | **verificada-e-sã** | Relata dois inteiros e não afirma causa nenhuma. É justamente a linha que o CR4-01 apontou como indistinguível entre apagão e dia calmo — e o alarme é a resposta a ela, não uma correção dela |
| Mensagem do alarme de **categoria** — *"Nenhum dos N (…) do dia foi notificado"* | **CORRIGIDA nesta Task 2** | Medido: `grep -c 'do dia foi notificado'` = **1** na entrada, **0** na saída. Nenhum teste assere o texto hoje (o cenário D mede só tipo e comprimento), então a mudança de redação **não** deixa nada vermelho por acidente — e o cenário L passa a pinar a redação nova |
</inventario_de_irmaos>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — a rodada MISTA por dedup, e o simétrico que impede o alarme de virar ruído</name>

  <read_first>
    - `backend/test/scheduler.categoriaIndecidivel.test.js` — o **cabeçalho** (a lista de cenários e o
      porquê de cada par), a armação inteira (`servirDeals`, `organizacaoDe`, `orgsQueFalham`,
      `erro429`, `installFakeAxios`, o `beforeEach`, `linhasDoDeal`, `envios`) e os cenários **D** e
      **E** por inteiro — D é o molde de L, E é o molde de M.
    - `backend/test/db.dedup.test.js` — o caso `alreadyNotifiedToday: deal notificado hoje` — é o
      precedente da armação de dedup (forma da chamada a `db.logNotification`).
    - `backend/src/scheduler.js` — a cadeia de guardas de `runCheck` (de `results.checked++` até o
      `else` final) e os DOIS blocos de alarme depois do laço. **Não editar** `backend/src/` nesta
      task.
    - As seções `<escopo_travado>` e `<inventario_de_irmaos>` deste plano.
  </read_first>

  <files>backend/test/scheduler.categoriaIndecidivel.test.js</files>

  <behavior>
    **Helper aditivo `marcarComoNotificadoHoje(dealId)`**, ao lado dos demais helpers de armação:
    grava uma linha `status: 'sent'` no `notification_log` real para o negócio, no molde do caso de
    `db.dedup.test.js`. Com o relógio do `beforeEach` fixo em `FIXED_NOW`, `sent_at` cai no mesmo dia
    que `alreadyNotifiedToday` consulta. O comentário registra por que a armação existe: **nenhum
    caso da suíte exercitava uma rodada com dedup**, e é exatamente o `continue` da dedup que desarma
    o alarme.

    **Cenário L — RODADA MISTA POR DEDUP: o apagão volta a ser audível.**
    Ids **2411 / 2412** (faixa nova; id reusado é caso contaminado). `servirDeals(primeiro, segundo)`,
    `orgsQueFalham` com as organizações dos **dois** (apagão total da borda, como em D), e
    `marcarComoNotificadoHoje(primeiro)` **antes** de `runCheck`.

    Asserções, nesta ordem — pré-condição e comportamento primeiro, instrumento depois:
    (a) pré-condição `db.alreadyNotifiedToday(primeiro) === true` — sem ela o caso mediria outra
    coisa se a armação falhasse em silêncio;
    (b) `r.stale === 2` — os dois negócios entraram na rodada;
    (c) `r.notified === 0`;
    (d) `envios(DONO_1) === 0`, `envios(AUTOR_1) === 0`, `envios(DONO_2) === 0`,
    `envios(AUTOR_2) === 0` — **igualdade exata**, nunca `>= 0`: zero e-mails é o fato irreversível
    que o caso existe para medir;
    (e) `linhasDoDeal(primeiro).length === 1` (só a linha da armação — nenhuma nova) e
    `linhasDoDeal(segundo).length === 0`;
    (f) `r.skipped === 2` e `r.skippedCategoriaIndecidivel === 1` — **a medição que torna o achado
    conferível**: o contador da guarda vê **um**, porque o `continue` da dedup veio antes; o
    comentário do caso escreve isso por extenso;
    (g) **o achado**: `r.errors.length === 1` — hoje vale **0**, e é aqui que o RED acontece;
    (h) `r.categoriaIndecidivelNaRodada === 2` — o numerador que percorre o mesmo conjunto do
    denominador;
    (i) `typeof r.error === 'string' && r.error.length > 0`;
    (j) a **redação**: `r.error.includes('desta rodada') === true` e
    `r.error.includes('do dia foi notificado') === false`. Esta asserção existe porque o conserto
    torna a condição alcançável numa rodada **composta**, em que um dos negócios contados **já havia
    sido notificado hoje** — a frase larga passaria a ser factualmente falsa, e é a mesma armadilha
    que o alarme do 04-35 evitou por redação. O comentário registra isso, para que a mensagem não
    seja "melhorada" de volta.

    **Cenário M — SIMÉTRICO: dedup ao lado de um notificável, e a rodada CALA.**
    Ids **2421 / 2422**. `servirDeals(primeiro, segundo)`, `orgsQueFalham` **VAZIA** (borda sã),
    `marcarComoNotificadoHoje(primeiro)`. Construção idêntica à de L mudando exclusivamente a saúde
    da borda — é isso que faz o par medir a **causa** e não a quantidade.

    Asserções, nesta ordem:
    (a) pré-condição `db.alreadyNotifiedToday(primeiro) === true`;
    (b) `r.stale === 2`, `r.skipped === 1`, `r.notified === 1`;
    (c) `envios(DONO_1) === 0` e `envios(AUTOR_1) === 0` — o negócio deduplicado **não recebe de
    novo**: é a proteção contra **duplicata**, a outra metade do Core Value;
    (d) `envios(DONO_2) === 1` e `envios(AUTOR_2) === 1` — **igualdade exata, deliberada**. O
    comentário registra o motivo: a forma frouxa `>= 1` detecta o envio a menos e **não** detecta o
    e-mail duplicado, e é exatamente o afrouxamento que a rodada 5 encontrou nos cenários mais novos
    deste mesmo arquivo (WR5-05, com dono no plano 04-38). Os cenários novos não repetem a forma
    frouxa;
    (e) `r.categoriaIndecidivelNaRodada === 0`;
    (f) `r.error === undefined`;
    (g) `r.errors.length === 0` — **sem este caso**, um conserto que ligasse o alarme por quantidade
    (`notified === 0`, `skipped > 0`, ou sempre) passaria por L, e o operador receberia um erro em
    todo dia em que alguém já tivesse sido notificado às 8h: o conserto teria trocado mudez por ruído
    diário.

    O cabeçalho do arquivo ganha as duas linhas novas na lista de cenários e um parágrafo curto
    dizendo por que L e M existem: L prova que o alarme sobrevive a um `continue` anterior; M prova
    que ele continua discriminando a causa.
  </behavior>

  <action>
    Acrescente o helper `marcarComoNotificadoHoje` e os cenários **L** e **M** ao final de
    `backend/test/scheduler.categoriaIndecidivel.test.js`, depois do último caso existente, cada um
    com o seu cabeçalho de seção no estilo de caixa do arquivo. Atualize a lista de cenários do
    cabeçalho.

    Reuse a armação existente inteira. **Não** altere `installFakeAxios`, `USUARIOS`, `MOLDE`,
    `servirDeals`, `servirDealsDoFunilBeefor`, `servirDealsComFunil`, o `beforeEach`, o `after`, nem
    qualquer asserção dos cenários já existentes — **inclusive H, I e J**, cujas asserções afrouxadas
    são o achado WR5-05 e têm dono no plano 04-38.

    Rode `npm run format` antes de medir qualquer contagem.

    NÃO edite `backend/src/` nesta task. O vermelho é o produto.

    Vermelho ESPERADO, para conferência: **L** falha na asserção **(g)**, com a forma `0 !== 1` —
    depois de (a) a (f) passarem, e é esse trecho verde que **reproduz a sonda do revisor dentro da
    suíte**: zero e-mails, dois negócios elegíveis suprimidos, contador da guarda em 1 e alarme
    calado. **M** falha na asserção **(e)**, com a forma `undefined !== 0`, depois de (a) a (d)
    passarem.

    Se **L** falhar em (a), **PARE e reporte**: a armação de dedup não produziu a linha esperada e o
    caso mediria outra coisa.
    Se **L** falhar em (b), (c), (d), (e) ou (f) — e SOMENTE nessas —, **PARE e reporte**: ou a
    armação do apagão não está produzindo indecidível nos dois, ou o comportamento por negócio
    regrediu, e nos dois casos a premissa do plano caiu.
    Se **L** ficar VERDE em (g), **PARE e reporte**: o alarme já dispara na rodada mista por um
    caminho não medido, e o achado não existe como descrito.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && node --test test/scheduler.categoriaIndecidivel.test.js; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | O RED existe e os demais continuam verdes | `cd backend && node --test test/scheduler.categoriaIndecidivel.test.js` | exit != 0, exatamente **2 fail** (L e M) e **11 pass** (A–K) |
    | L falha no achado, não na armação | saída do runner | a linha de falha de **L** é a de `r.errors.length` (`0 !== 1`) — nunca uma de `stale`, `notified`, `envios`, `linhasDoDeal` ou `skippedCategoriaIndecidivel` |
    | M falha no instrumento, não no comportamento | saída do runner | a linha de falha de **M** é a de `categoriaIndecidivelNaRodada` (`undefined !== 0`) |
    | Cenários no arquivo | `grep -c '^test(' backend/test/scheduler.categoriaIndecidivel.test.js` | **13** (era 11) |
    | O helper de dedup existe e é usado nos dois | `grep -o 'marcarComoNotificadoHoje' backend/test/scheduler.categoriaIndecidivel.test.js \| wc -l` | **3** (declaração + 2 chamadas) — ocorrências, não linhas |
    | Nenhum id foi reusado | `grep -oE 'const (primeiro\|segundo) = [0-9]+' backend/test/scheduler.categoriaIndecidivel.test.js \| sort \| uniq -d \| wc -l` | **0** |
    | Os cenários novos não repetem a forma frouxa de WR5-05 | `git diff -U0 backend/test/scheduler.categoriaIndecidivel.test.js \| grep -E '^\+' \| grep -c '>= 1'` | **0** |
    | O cabeçalho declara os cenários novos | `grep -cE '^//   (L\|M) ' backend/test/scheduler.categoriaIndecidivel.test.js` | **2** |
    | A task é ADITIVA — nenhuma asserção existente saiu | `git diff -U0 backend/test/ \| grep -c '^-[^-]'` | **0** |
    | H, I e J não foram tocados (WR5-05 tem dono) | `git diff -U0 backend/test/scheduler.categoriaIndecidivel.test.js \| grep -c 'servirDealsComFunil'` | **0** |
    | Nenhum código de produção mudou | `git diff --name-only -- backend/src/ frontend/` | **vazio** |
    | Referência por número de linha não entrou | `git diff -U0 backend/test/ \| grep -c '\.js:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    L está vermelho na asserção do alarme e M na do contador, A–K verdes, `backend/src/` intocado, e o
    commit `test(04-37): RED da rodada mista por dedup` registra o vermelho.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — contar onde numerador e denominador percorrem o mesmo conjunto</name>

  <read_first>
    - `backend/src/scheduler.js` — o literal de `results` (com os comentários dos dois contadores
      agregados), o laço de `runCheck` inteiro, e os DOIS blocos de alarme depois dele.
    - `.planning/phases/04-confiabilidade-das-integra-es/04-35-SUMMARY.md` — as seções que registram
      **D-IN3-08-f** (a posição do contador) e **D-IN3-08-g** (a ordem dos alarmes): é a redação a
      espelhar, e as decisões que **não** podem ser reabertas.
    - `.planning/phases/04-confiabilidade-das-integra-es/04-28-SUMMARY.md` — o inventário de irmãos do
      04-28, item **1** (o ramo de dedup classificado como `verificada-e-sã`): é a evidência
      **correta para a pergunta errada** que produziu este achado, e o comentário novo precisa
      nomeá-la.
    - As seções `<escopo_travado>` e `<inventario_de_irmaos>` deste plano.
  </read_first>

  <files>backend/src/scheduler.js</files>

  <action>
    **(a) O contador novo, no literal de `results`.** Acrescente `categoriaIndecidivelNaRodada: 0` ao
    lado de `skippedCategoriaIndecidivel: 0`, pelo mesmo motivo já escrito para os irmãos: o campo
    existe **sempre** no payload e nenhum consumidor precisa distinguir `undefined` de zero.

    **(b) O incremento, no TOPO do laço.** Imediatamente ao lado de
    `if (deal.funilAusente) results.funilNaoAvaliado++;`, acrescente
    `if (deal.categoriaIndecidivel) results.categoriaIndecidivelNaRodada++;`. Ele **não** faz
    `continue`, **não** escreve `dealResult.skipped`, **não** toca `results.skipped` e **não** decide
    destinatário nenhum — a guarda de categoria, mais abaixo, fica **byte a byte**.

    **(c) A condição do alarme.** Troque `results.skippedCategoriaIndecidivel === results.stale` por
    `results.categoriaIndecidivelNaRodada === results.stale`. A condição `results.stale > 0` fica.
    **Mantenha** `results.skippedCategoriaIndecidivel` e o seu incremento onde estão: ela responde a
    outra pergunta — quantos a **guarda** suprimiu — e o cenário D continua asserindo-a.

    **(d) A mensagem, com escopo de afirmação ESTREITO.** A redação atual afirma que *"nenhum dos N
    negócios parados **do dia** foi notificado"*. Com o contador no topo do laço, a condição passa a
    valer também numa rodada **composta**, em que algum dos negócios contados já havia sido
    notificado **hoje** pela rodada do cron — e ali a frase é **factualmente falsa**. Reescreva-a
    para afirmar exatamente o que o contador garante:

    - que **nenhum** dos N negócios parados **desta rodada** teve a categoria da organização
      consultada com sucesso, e que **nenhum e-mail saiu nesta rodada** (garantido por construção: um
      negócio marcado como indecidível é interceptado pela guarda de dedup ou pela de categoria, e as
      duas fazem `continue` antes do ramo de envio);
    - que a borda de organizações da Agendor pode estar indisponível;
    - que a rodada **CONCLUIU** — é alarme de supressão total, não interrupção;
    - e a ressalva que a redação larga esconderia: **alguns desses negócios podem ter sido pulados
      também por outra guarda (a dedup do dia)**, portanto a mensagem **não** afirma que ninguém foi
      notificado hoje.

    Apenas **inteiros e texto fixo** entram na mensagem — nenhum id, nome de negócio, nome de
    organização ou objeto de erro (CR-02 do 04-09).

    **(e) O comentário — é metade do conserto.** O bloco acima do alarme e o do contador novo
    precisam registrar, com âncoras nomeadas e sem número de linha:
    (i) que o incremento mora no **topo** do laço porque **qualquer** `continue` anterior — e o mais
    banal deles é a **dedup do dia** — subtrai do numerador sem subtrair do denominador, deixando a
    condição total **inalcançável**; que a regra já estava escrita no comentário do contador do 04-35
    (D-IN3-08-f) e não fora aplicada aqui; e que a sonda do revisor mediu o desfecho: apagão total,
    zero e-mails, alarme calado;
    (ii) que `skippedCategoriaIndecidivel` **fica**, respondendo a outra pergunta, e que o cenário
    **L** mede as duas valendo números **diferentes** na mesma rodada — é essa diferença que
    documenta a separação;
    (iii) que a afirmação da mensagem é **ESTREITA de propósito** e a redação larga é **PROIBIDA**:
    numa rodada composta, "nenhum negócio do dia foi notificado" seria falso para quem já recebera às
    8h, e uma mitigação que mente em parte dos casos é pior que nenhuma — mesma disciplina já adotada
    para o alarme de forma do 04-35 (T-04-35-05);
    (iv) que o bloco continua **ADITIVO**: mora depois do laço, não condiciona `continue` nenhum e
    **não decide quem recebe e-mail**; nenhuma escolha de limiar pode mudar isso, e o limiar
    "supressão TOTAL" é decisão do usuário que este plano **não** reabre;
    (v) que o par que impede o alarme de virar ruído diário são os cenários **L** e **M**, e que M
    existe para reprovar uma implementação que ligue o alarme por quantidade em vez de por causa;
    (vi) o residual que **sobra**: um negócio **sem organização** nunca é marcado como indecidível,
    então o numerador continua sem alcançar o denominador nesse caminho — dono `cr4-01b`, reescrito
    na Task 3.

    O comentário do contador do 04-35 **pode** ser atualizado para registrar que a regra passa a
    valer nos **dois** contadores (a frase atual diz que aquele plano "não conserta o `cr4-01b`;
    apenas não o reproduz", e isso deixou de ser o estado do arquivo). Nenhuma linha
    **não-comentário** daquele bloco muda.

    **Não escreva** a formulação que os planos anteriores rejeitaram por escrito ("um limiar menor
    mudaria o comportamento por negócio") — ela ficaria disponível como precedente para justificar,
    sob enquadramento errado, uma mudança real de comportamento numa rodada futura.

    Rode `npm run format` antes de medir contagens. Não instale nada (T-04-37-SC).
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && npm test && npm run test:coverage && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | Suíte inteira verde | `cd backend && npm test` | **196/196** (era 194; +L, +M) |
    | Cobertura | `cd backend && npm run test:coverage` | exit **0** — registrar o medido para `scheduler.js` no SUMMARY; divergência é registrada com o número, nunca forçada |
    | Lint | `cd backend && npm run lint` | exit **0**, **44** warnings (baseline) |
    | O contador novo existe nos três pontos | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'categoriaIndecidivelNaRodada' \| wc -l` | **3** (literal, incremento, condição do alarme) |
    | Ele incrementa ANTES de qualquer `continue` | `test $(grep -n 'results.categoriaIndecidivelNaRodada++' backend/src/scheduler.js \| head -1 \| cut -d: -f1) -lt $(grep -n 'continue;' backend/src/scheduler.js \| head -1 \| cut -d: -f1) && echo ok` | **ok** |
    | Ele não decide quem recebe | `grep -c 'continue;' backend/src/scheduler.js` | **3** (inalterado) |
    | O contador da guarda foi preservado e desacoplado | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'skippedCategoriaIndecidivel' \| wc -l` | **2** (era 3 — a condição do alarme deixou de usá-la) |
    | E não sobrou comparação antiga | `grep -c 'skippedCategoriaIndecidivel === results.stale' backend/src/scheduler.js` | **0** |
    | O denominador não foi mexido | `grep -c 'results.stale = dealsToNotify.length' backend/src/scheduler.js` | **1** (inalterado) |
    | A redação larga saiu | `grep -c 'do dia foi notificado' backend/src/scheduler.js` | **0** (era 1) |
    | E a estreita entrou | `grep -c 'desta rodada' backend/src/scheduler.js` | **≥ 1** |
    | A mensagem não vaza dado sensível | inspeção do texto do alarme | só inteiros e texto fixo — sem id, sem nome de negócio/organização, sem objeto de erro |
    | O alarme do funil (04-35) ficou intocado no CÓDIGO | `git diff -U0 backend/src/scheduler.js \| grep -E '^[-+][^-+]' \| grep -vE '^[-+][[:space:]]*//' \| grep -c 'funilNaoAvaliado'` | **0** (só comentário pode mudar) |
    | E a mensagem dele ficou byte a byte | `git diff -U0 backend/src/scheduler.js \| grep -c 'trouxe funil'` | **0** |
    | A ordem dos alarmes foi preservada (D-IN3-08-g) | `test $(grep -n 'funilNaoAvaliado === results.stale' backend/src/scheduler.js \| cut -d: -f1) -lt $(grep -n 'categoriaIndecidivelNaRodada === results.stale' backend/src/scheduler.js \| cut -d: -f1) && echo ok` | **ok** |
    | Os outros contadores não foram tocados | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'results.skipped++' \| wc -l` | **4** (inalterado) |
    | O CR5-01 não foi desfeito | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'execucaoIgnorada' \| wc -l` | **1** (inalterado, do plano 04-36) |
    | A prévia não foi editada | `git diff -U0 backend/src/scheduler.js \| grep -c 'runCheckOnly\|seraNotificado'` | **0** |
    | Nenhum teste foi editado para ficar verde | `git diff --name-only -- backend/test/` | **vazio** |
    | O frontend não foi tocado | `git diff --name-only -- frontend/` | **vazio** |
    | Nenhum pacote instalado | `git status --porcelain package.json package-lock.json backend/package.json backend/package-lock.json` | **vazio** |
    | Referência por número de linha não entrou | `git diff -U0 backend/src/ \| grep -c '\.js:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    L e M estão verdes sem que nenhum arquivo de teste tenha sido tocado, a suíte inteira passa em
    196/196, cobertura e lint saem 0, o alarme do 04-35 está fora do diff de código, e o commit
    `fix(04-37): GREEN` registra a mudança.
  </done>
</task>

<task type="auto">
  <name>Task 3: o todo cr4-01b REESCRITO — pelo mecanismo, não por uma de suas causas</name>

  <read_first>
    - `.planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` — **inteiro** (72 linhas). É o
      artefato a reescrever.
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md`, achado **WR5-01** inteiro,
      incluindo a saída literal da sonda e o parágrafo *"Por que isto não é o `cr4-01b` já
      registrado"*.
    - `.planning/todos/pending/in4-04-leitura-de-success-divergente-entre-ramos.md` — é o molde de
      formato dos todos desta fase (frontmatter + **Onde** / **O que acontece** / **Por que a
      prioridade é X** / **Correção proposta**).
    - O `<inventario_de_irmaos>` deste plano, Grupo 2, item **2** — é o residual que sobra.
  </read_first>

  <files>.planning/todos/pending/cr4-01b-limiar-de-supressao-total.md</files>

  <action>
    Reescreva o arquivo. **Não** o renomeie e **não** o mova para `completed/`: ele continua **aberto**
    (a causa que sobra é real), e o nome do arquivo é citado por artefatos históricos imutáveis —
    `04-28-SUMMARY.md`, `04-34-PLAN.md`, `04-34-SUMMARY.md`, `04-35-PLAN.md`, `04-35-SUMMARY.md`,
    `04-REVIEW-r5.md`, `STATE.md` e `ROADMAP.md`. Renomear deixaria essas referências penduradas. Em
    compensação, a **primeira linha do corpo** registra por escrito que o nome do arquivo fala em
    "limiar" e que isso é **vestígio do enquadramento errado**: o limiar é decisão do usuário e não
    está em discussão.

    O arquivo reescrito precisa conter, obrigatoriamente:

    1. **Frontmatter atualizado** — `id` inalterado; `status: pending`; `priority` **média**, com o
       motivo reescrito abaixo; `source` passando a citar a **rodada 5 (WR5-01)** e o plano **04-37**
       ao lado da origem original (inventário do 04-28); `tags` acrescidas de `wr5-01`.

    2. **Título pelo MECANISMO, não pela causa.** O título atual nomeia *"um negócio sem organização
       desarma o alarme"* — uma das causas. O novo nomeia o mecanismo: **o numerador do alarme não
       percorre o mesmo conjunto que o denominador**, e qualquer caminho que impeça um negócio parado
       de ser contado deixa a condição total inalcançável.

    3. **A causa FECHADA, com o desfecho medido.** A causa mais banal — **qualquer `continue`
       anterior**, e em especial a **dedup do dia** — foi medida por sonda pelo revisor (apagão total,
       três negócios, zero e-mails, alarme calado) e **fechada pelo plano 04-37**, movendo o
       incremento para o topo do laço. Registre os cenários que a pinam por **nome** (L e M), sem
       número de linha.

    4. **A causa que SOBRA, e que mantém este todo aberto.** Um negócio **sem organização** nunca
       chega a ser consultado — o conjunto de organizações únicas é montado filtrando valores falsos
       — e sai da lista enriquecida **sem a marca** de indecidível. Numa rodada em que a borda está
       inteiramente fora **e** existe pelo menos um negócio sem organização, o numerador continua
       estritamente menor que o denominador e o alarme continua calado. Essa é a única causa que
       resta, e é o motivo de o arquivo permanecer em `pending`.

    5. **A "Correção proposta" antiga marcada como REJEITADA, com o motivo.** O texto atual manda
       *"contar na borda, dentro de `getStaleDeals`, quantos negócios tinham organização a
       consultar"* — isso mexe no **DENOMINADOR**, e o defeito medido na r5 estava no **NUMERADOR**,
       gated por um `continue` anterior. Quem executasse aquela correção fecharia o todo, deixaria o
       oráculo verde e manteria o caminho aberto. Este parágrafo é o mais importante do arquivo: ele
       existe para impedir que a próxima pessoa feche o item **com o defeito de pé**.

    6. **A medição que rejeita o denominador derivado, PRESERVADA.** O parágrafo atual que mede por
       que contar o denominador na lista enriquecida não serve — o campo de organização carrega o
       **nome**, não o identificador, e uma organização com id válido e sem nome faria o alarme
       **falhar ABERTO por um caminho novo** — continua válido e não pode ser perdido na reescrita.

    7. **Correção proposta nova, coerente com o mecanismo.** Contar, **na borda**, quantos negócios
       parados tinham organização **a consultar**, e comparar numerador e denominador produzidos pelo
       **mesmo critério e no mesmo lugar** — agora explicitamente como conserto do caminho **sem
       organização**, e não como conserto do `continue`, que já está fechado. O oráculo é a rodada
       **mista por ausência de organização**: um negócio sem organização e todos os demais
       indecidíveis, exigindo o campo de erro preenchido; mais o simétrico — um negócio sem
       organização e nenhum indecidível — para garantir que o denominador novo não passe a disparar o
       alarme num dia normal.

    8. **A lição estrutural**, em uma frase: o inventário de irmãos do 04-28 classificou a dedup como
       `verificada-e-sã` com **evidência correta para a pergunta errada** — perguntou "este irmão
       está são?" e não "o que eu acabei de construir **depende** do comportamento dele?". É a
       cláusula de **direção reversa** que a rodada 5 acrescentou ao mandato.

    Convenção **WR2-06** aplicada ao próprio artefato: nenhuma referência por número de linha. Cite
    função, identificador, nome de caso de teste ou nome de plano. **Nenhum outro todo é editado** —
    há 33 pendentes, e o único arquivo que este plano pode tocar em `.planning/todos/` é este.

    Este plano **não** cria os todos de WR5-02..WR5-05 e IN5-01..IN5-04: são o plano **04-38**.
  </action>

  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor && test "$(git status --porcelain .planning/todos/ | wc -l | tr -d ' ')" = "1" && test "$(git status --porcelain backend/ frontend/ | wc -l | tr -d ' ')" = "0" && echo ok</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | O todo continua existindo e aberto | `ls .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | existe (não movido, não renomeado) |
    | Exatamente UM arquivo de todo mudou | `git status --porcelain .planning/todos/` | **1** linha, e é a modificação deste arquivo (` M`) |
    | A correção antiga está marcada como rejeitada | `grep -ciE 'rejeitad' .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | **≥ 1** |
    | O mecanismo está nomeado, não só a causa | `grep -c 'continue' .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | **≥ 1** (era 0 — o arquivo antigo não menciona o mecanismo) |
    | A causa fechada aponta o plano que a fechou | `grep -c '04-37' .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | **≥ 1** |
    | A medição preservada do denominador derivado | `grep -ci 'sem nome' .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | **≥ 1** |
    | Convenção WR2-06 no próprio artefato | `grep -cE '\.js:[0-9]\|linhas? [0-9]' .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | **0** |
    | O frontmatter continua válido | `head -12 .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | `id`, `type: todo`, `status: pending`, `priority`, `created`, `source`, `tags` presentes |
    | Nenhum outro todo foi editado | `git status --porcelain .planning/todos/pending/ \| grep -vc 'cr4-01b'` | **0** |
    | Nenhum código mudou nesta task | `git status --porcelain backend/ frontend/` | **vazio** |
    | Nenhum valor de segredo entrou no artefato | `grep -ciE 'AGENDOR_TOKEN[[:space:]]*=\|Bearer \|Token [A-Za-z0-9]{16}' .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md` | **0** (C8 / SEC-01) |
  </acceptance_criteria>

  <done>
    O `cr4-01b` está reescrito pelo mecanismo, com a causa fechada e a que sobra separadas, a correção
    antiga marcada como rejeitada e a medição do denominador derivado preservada; nenhum outro todo
    foi tocado; e o commit `docs(04-37): reescreve o todo cr4-01b` registra a mudança.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| API Agendor (`/organizations/:id`) → `getStaleDeals` → `runCheck` | A disponibilidade de uma borda de terceiro decide se um negócio é notificável; o alarme é o único sinal agregado de que ela caiu |
| `runCheck` → `results.errors` → Dashboard | O array de erros é renderizado na UI autenticada e, desde o plano 04-36, também no toast do disparo manual |
| `logger.error` → `logs/error.log` | Linha de log é artefato persistido; um objeto de erro do axios carrega `config.headers` com o `AGENDOR_TOKEN` |
| Operador → decisão de redisparar a rodada | O que ele lê no alarme decide se dispara de novo — e o disparo tem efeito irreversível (e-mail) |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-04-37-01 | Repudiation | Alarme de supressão total em `runCheck` | **mitigate** | Um apagão da borda de organizações numa rodada com **qualquer** negócio deduplicado ficava indistinguível de um dia calmo: zero e-mails, `results.error` undefined, `results.errors` vazio. O contador passa a percorrer o mesmo conjunto do denominador, e o par L/M prova que o alarme dispara na causa certa e cala nas demais |
| T-04-37-02 | Spoofing | Redação da mensagem do alarme | **mitigate** | Com o contador no topo do laço, a condição total passa a valer em rodada **composta**, e a frase "nenhum negócio parado **do dia** foi notificado" seria **falsa** para quem já recebera às 8h. Uma mitigação que mente em parte dos casos treina o operador a ignorá-la — e o operador que conclui ter perdido envios redispara e gera **duplicatas** (T-04-35-05). A afirmação passa a ser sobre a RODADA, com a ressalva escrita, e o cenário L assere a redação |
| T-04-37-03 | Information Disclosure | `logger.error` e `results.error` do alarme | **mitigate** | Apenas **inteiros e texto fixo** entram na mensagem — nenhum id, nome de negócio, nome de organização ou objeto de erro (regra CR-02 do 04-09). Critério de aceite na Task 2 |
| T-04-37-04 | Tampering | Comportamento por negócio (quem recebe) | **mitigate** | O incremento novo **não** faz `continue`, **não** escreve `dealResult.skipped` e **não** toca `results.skipped`; o bloco de alarme continua aditivo e depois do laço. Gates: `continue;` = **3** (inalterado), `results.skipped++` = **4** (inalterado), guardas fora do diff de código |
| T-04-37-05 | Denial of Service | Ruído de alarme diário | **mitigate** | Um alarme que dispara sem causa é um alarme que se aprende a ignorar, e aí o apagão real volta a passar despercebido. O cenário **M** (dedup ao lado de notificável, borda sã) exige `r.errors.length === 0` e reprova qualquer implementação que ligue o alarme por quantidade |
| T-04-37-06 | Information Disclosure | Todo reescrito em `.planning/todos/pending/` | **mitigate** | O artefato documental não pode carregar valor de segredo (C8 / SEC-01: o `AGENDOR_TOKEN` permanece com rotação pendente e o valor nunca é exibido). Gate de grep na Task 3 |
| T-04-37-SC | Tampering | Instalação de pacotes (npm/pip/cargo) | **mitigate** | Este plano **não instala nada** — nenhuma dependência nova, nenhum bump. Gate: `git status --porcelain` dos `package.json` e lockfiles vazio na Task 2. Não havendo instalação, o portão humano de legitimidade de pacote não se aplica |
</threat_model>

<verification>
1. `cd backend && npm test` — **196/196** verdes (era 194).
2. `cd backend && npm run test:coverage` — exit 0; registrar o medido para `scheduler.js`.
3. `cd backend && npm run lint` — exit 0, 44 warnings (baseline inalterado).
4. O par provado: cenário **L** (apagão com dedup dispara o alarme, com `skippedCategoriaIndecidivel`
   valendo 1 e o numerador valendo 2 na MESMA rodada) e cenário **M** (dedup ao lado de notificável
   não dispara nada).
5. A redação estreita provada por asserção, não por comentário: `r.error.includes('desta rodada')` e
   a ausência da frase larga.
6. Os cenários A–K verdes **sem edição**: `git diff --name-only -- backend/test/` vazio na Task 2.
7. O inventário de irmãos, incluindo as duas cláusulas novas: cada item classificado tem grep ou caso
   de teste, e os fora-de-escopo têm dono nomeado (`cr4-01b` reescrito aqui; `cr4-01c` já existente;
   WR5-02..WR5-05 e IN5-01..IN5-04 no plano 04-38).
8. O `cr4-01b` reescrito nomeia o mecanismo, separa a causa fechada da que sobra e marca a correção
   antiga como rejeitada — e nenhum outro todo foi tocado.
</verification>

<success_criteria>
- Um apagão da borda de organizações numa rodada que também tem negócio deduplicado deixa de ser
  indistinguível de um dia calmo, e isso está pinado por teste — não por comentário.
- Uma rodada com um deduplicado ao lado de um notificável continua silenciosa: o conserto não trocou
  mudez por ruído diário.
- A mensagem do alarme afirma exatamente o que o contador garante, e a frase larga tem gate.
- Nenhum comportamento de quem-recebe mudou: 3 `continue;`, 4 `results.skipped++`, guardas fora do
  diff de código, e o alarme do 04-35 byte a byte.
- O `cr4-01b` deixou de documentar a causa errada, e a correção que não fecha o defeito está marcada
  como rejeitada com o motivo medido.
</success_criteria>

<output>
Crie `.planning/phases/04-confiabilidade-das-integra-es/04-37-SUMMARY.md` ao terminar.

Registre obrigatoriamente: o RED literal da Task 1 (saída do runner, com a asserção em que L e M
falharam) e os valores medidos de `r.skipped`, `r.skippedCategoriaIndecidivel` e `r.errors.length` no
estado defeituoso — é a sonda do revisor reproduzida dentro da suíte; todos os números medidos ao
lado dos prescritos, **inclusive as divergências**, registradas com o número medido e o motivo, nunca
ajustadas para bater (esta fase acumulou onze divergências de contagem e nenhuma foi forçada); a
cobertura medida de `scheduler.js`; o inventário de irmãos com a classificação final de cada item dos
**três** grupos (irmãos, direção reversa, retroatividade da justificativa); o texto final da mensagem
do alarme; e o que este plano **não** fecha — o caminho do negócio **sem organização** (`cr4-01b`,
que permanece aberto e reescrito), WR5-02..WR5-05 e IN5-01..IN5-04 (plano 04-38).
</output>
