---
phase: 04-confiabilidade-das-integra-es
plan: 08
type: execute
wave: 1
depends_on: ["04-07"]
files_modified:
  - backend/src/agendor.js
  - backend/test/agendor.cacheConcurrency.test.js
  - backend/test/agendor.cacheInvalidation.test.js
autonomous: false
gap_closure: true
requirements: [REL-04]

must_haves:
  truths:
    - "Duas execuções SOBREPOSTAS de getStaleDeals devolvem, ambas, o golden [101, 103] — a organização 205 ('Parceiro') nunca entra na lista de notificação"
    - "O laço de enriquecimento de getStaleDeals não lê mais estado de módulo: a categoria de cada organização vem de um mapa local à execução"
    - "A limpeza por execução do orgCategoryCache (REL-04) continua valendo: recategorização no Agendor vale na execução seguinte"
    - "Dentro de uma execução, cada organização única continua sendo consultada exatamente uma vez"
  artifacts:
    - path: "backend/src/agendor.js"
      provides: "Mapa local à execução com a categoria por organização, consumido pelo laço de enriquecimento"
      contains: "categoriaPorOrg"
    - path: "backend/test/agendor.cacheConcurrency.test.js"
      provides: "Prova determinística do interleaving CR-01: execução B apaga o cache enquanto A ainda não leu"
      min_lines: 90
  key_links:
    - from: "backend/src/agendor.js Promise.all das organizações"
      to: "laço de enriquecimento de getStaleDeals"
      via: "Map local à execução (o retorno do Promise.all deixa de ser descartado)"
      pattern: "categoriaPorOrg\\.get\\("
    - from: "backend/test/agendor.getStaleDeals.test.js"
      to: "golden [101, 103]"
      via: "regressão que acusa afrouxamento da exclusão por categoria"
      pattern: "\\[101, 103\\]"
---

<objective>
Fechar **CR-01** (crítico, defeito introduzido pelo 04-07) e **WR-06**: o laço de enriquecimento de
`getStaleDeals` deixa de ler o dicionário de módulo `orgCategoryCache` e passa a ler um **mapa local à
execução**, construído a partir do retorno do `Promise.all` que hoje é descartado.

Purpose: hoje `getStaleDeals` (a) apaga as chaves do `orgCategoryCache` como primeira instrução
(`agendor.js:170-172`), (b) popula o dicionário via `await Promise.all(uniqueOrgIds.map(getOrgCategory))`
(`:211`) **descartando o retorno**, e (c) lê o **mesmo dicionário compartilhado** no laço de enriquecimento
(`:219`). Duas execuções sobrepostas quebram a regra de exclusão: a execução B apaga o que a execução A já
buscou, a leitura de A devolve `undefined`, `?? null` vira `null`,
`EXCLUDED_CATEGORIES.includes(null)` é `false` — e **uma organização de categoria excluída entra na lista de
notificação**. Reproduzido pelo orquestrador contra a fixture do projeto:

```
GOLDEN esperado    : [101,103]
execução A devolveu: [101,103,105]   ← deal 105, org 205 = "Parceiro" (categoria EXCLUÍDA)
```

Isto viola diretamente o Core Value do milestone ("nunca mais uma regressão silenciosa nas regras de quem é
notificado"). É a lacuna de maior prioridade desta rodada.

**Alcançabilidade (verificada, não inferida):** `getStaleDeals` tem **8 call-sites** sem lock compartilhado
— `scheduler.js:56` (`runCheck`, cujo `isRunning` só protege `runCheck` contra si mesmo), `scheduler.js:213`
(`runWeeklySummary`, sem lock), `scheduler.js:310` (`runCheckOnly`, sem lock), `routes/notifications.js:119,143,179`,
`routes/reports.js:11` e `routes/deals.js:15`. **As rotas HTTP não têm lock nenhum.** Cenário real: o cron das
8h enquanto alguém abre o dashboard. A janela dura segundos (paginação + `await new Promise(r => setTimeout(r, 1000))`
entre lotes em `agendor.js:196-197` + uma consulta por organização única).

**WR-06 (mesmo defeito, lado do teste e dos comentários):** os 3 cenários de
`agendor.cacheInvalidation.test.js` executam `getStaleDeals` **sequencialmente**, e tanto o cabeçalho do
arquivo (`:13-21`) quanto o comentário-bloco de `agendor.js:159-169` constroem um argumento detalhado sobre
`delete` vs. reatribuir que só vale sob uma premissa **nunca declarada e falsa**: que existe no máximo uma
execução em voo. Os comentários afirmam mais do que o código garante e precisam ser corrigidos junto com o
código.

Output: uma mudança de duas linhas efetivas em `agendor.js` (+ comentário reescrito), um arquivo de teste
novo de concorrência, e a correção do cabeçalho do teste de invalidação.

## O que NÃO muda

- A **limpeza das chaves na primeira instrução** de `getStaleDeals` permanece — é ela que garante o refetch
  entre rodadas (REL-04 / D-05) e que zera o `null` gravado pelo `catch` de `getOrgCategory`.
- `getOrgCategory` **não é tocada**: continua sendo a única consultante e a única escritora do dicionário de
  módulo, com a mesma assinatura e o mesmo formato de valor (`string | null`).
- `EXCLUDED_CATEGORIES`, o filtro de datas, `getDealType`, o `module.exports` — nada disso muda.
- O golden `assert.deepStrictEqual(ids, [101, 103])` de `agendor.getStaleDeals.test.js:61` **não pode ser
  editado**. Ele é o detector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-07-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-04. Não explorar o codebase. -->

`backend/src/agendor.js:47-60` — `const orgCategoryCache = {};` e `getOrgCategory(orgId)`: `if (!orgId) return null;`
retorno antecipado se `orgCategoryCache[orgId] !== undefined`; grava `orgCategoryCache[orgId] = category` no
sucesso e `= null` no `catch`. Valor guardado: `string | null`.

`backend/src/agendor.js:170-172` — limpeza atual (primeira instrução de `getStaleDeals`):
`for (const orgId of Object.keys(orgCategoryCache)) { delete orgCategoryCache[orgId]; }`

`backend/src/agendor.js:207-221` — o alvo desta correção:
```
const uniqueOrgIds = [...new Set(staleRaw.map((d) => d.organization?.id).filter(Boolean))];
await Promise.all(uniqueOrgIds.map((id) => getOrgCategory(id)));   // ← retorno DESCARTADO (:211)
...
const orgCategory = orgCategoryCache[deal.organization?.id] ?? null; // ← lê estado de MÓDULO (:219)
if (EXCLUDED_CATEGORIES.includes(orgCategory)) continue;
```

`EXCLUDED_CATEGORIES = ['Inativo (sem resposta)', 'Parceiro', 'Fornecedor']` (`agendor.js:79-83`).

`backend/test/fixtures/synthetic/deals-page.json` — fixture a **reusar**. Sob
`FIXED_NOW = 2026-06-01T00:00:00.000Z` e `staleDays = 15` (cutoff `2026-05-17`), os deals stale são
`101`(org 201) · `103`(org 203) · `105`(org 205) · `106`(org 206) · `107`(org 207) · `110`(org 210).
Exclusões: 205 = `'Parceiro'` (**categoria** — a única exclusão por categoria do golden), 206 = owner
`Maria Lobato`, 107 = `dealStatus.id 3`, 110 = etapa `Oportunidade Perdida`. Golden final: `[101, 103]`.

`backend/test/helpers/fakeAxios.js` — `installFakeAxios(routeHandler)` devolve `{ get: mock.fn(...), createArgs }`.
O `routeHandler` é chamado como `routeHandler(url, config)` dentro de uma função `async`; **o stub nunca pode ser
reinstalado depois do `require('../src/agendor')`** (a instância `api` nasce no load) — ramificar por variável
mutável de módulo.

`backend/test/agendor.cacheInvalidation.test.js:104-106` — `beforeEach(() => { fake.get.mock.resetCalls(); })`.
Padrão de leitura das urls: `fake.get.mock.calls.map((c) => c.arguments[0])`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — teste determinístico do interleaving que faz a organização excluída ser notificada</name>
  <files>backend/test/agendor.cacheConcurrency.test.js</files>
  <behavior>
    - Duas execuções SOBREPOSTAS de `getStaleDeals(15)`: a execução B começa (e apaga as chaves do cache) enquanto a execução A ainda está aguardando `/organizations/210`
    - A execução A devolve o golden `[101, 103]` — o deal `105` (org `205` = `'Parceiro'`) NÃO pode entrar
    - A execução B também devolve `[101, 103]`
    - Uma execução sequencial posterior continua devolvendo `[101, 103]` (o interleaving não deixou estado corrompido)
  </behavior>
  <read_first>
    - `backend/test/agendor.cacheInvalidation.test.js` (arquivo inteiro — bootstrap, `FIXED_NOW`, `ORG_CATEGORY`, uso de `fake.get.mock.calls`)
    - `backend/test/agendor.getStaleDeals.test.js` (arquivo inteiro — o golden `[101, 103]` e o molde do stub)
    - `backend/test/helpers/fakeAxios.js` (arquivo inteiro — formato do retorno e a armadilha de ordem)
    - `backend/src/agendor.js:47-60, 149-251` — `getOrgCategory`, a limpeza e o laço de enriquecimento
    - `backend/test/fixtures/synthetic/deals-page.json` — quais deals são stale e qual organização é `'Parceiro'`
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` §CR-01
  </read_first>
  <action>
    Criar `backend/test/agendor.cacheConcurrency.test.js`. Cabeçalho em PT-BR (padrão obrigatório do
    repositório) nomeando **CR-01/WR-06** e dizendo, em uma frase, o que o arquivo pina: que a decisão de
    exclusão por categoria não pode depender de estado de módulo compartilhado entre execuções sobrepostas,
    e que `getStaleDeals` tem 8 call-sites sem lock (cron das 8h + request do dashboard).

    Bootstrap na ordem canônica: `require('./setup')` → `installFakeAxios(...)` → `require('../src/agendor')`.
    `before(() => mock.timers.enable({ apis: ['Date'], now: FIXED_NOW }))` e `after(() => mock.timers.reset())`,
    com `FIXED_NOW = new Date('2026-06-01T00:00:00.000Z').getTime()`. **NÃO habilitar `setTimeout`** — a
    fixture tem 10 deals em uma página só, então nada dorme; habilitar congelaria a espera entre lotes de
    `agendor.js:196-197` sem necessidade.

    Reusar `./fixtures/synthetic/deals-page.json` e o dicionário `ORG_CATEGORY = { 205: 'Parceiro' }` com
    default `'Lead'`.

    **Stub com dois pontos de suspensão controlados** (ramificar DENTRO do `routeHandler`; nunca reinstalar
    o stub):
    - `/deals` → resposta imediata com a fixture inteira e `meta.totalCount = dealsPage.length`.
    - `/organizations/210` → na **primeira** chamada devolve uma promessa PENDENTE e guarda o resolvedor em
      `liberar210`; a partir daí responde imediatamente. A organização 210 é a escolha certa porque o deal
      `110` já é excluído pela etapa `Oportunidade Perdida` — suspendê-la controla o tempo sem alterar o
      resultado esperado.
    - `/organizations/205` → conta as chamadas em `chamadas205`. A **segunda** chamada (a da execução B)
      devolve uma promessa PENDENTE, com o resolvedor guardado em `liberar205DaExecucaoB`; as demais
      respondem imediatamente. Isto é o que impede a execução B de repopular `orgCategoryCache[205]` antes de
      a execução A ler — sem ele o teste ficaria dependente de ordem de microtask e seria intermitente.
    - qualquer outra organização → resposta imediata com `ORG_CATEGORY[id] || 'Lead'`.

    Helper local `async function cederEventLoop() { await new Promise((r) => setImmediate(r)); }` — um turno de
    `setImmediate` drena TODA a fila de microtasks (`setImmediate` é real: só `Date` está mockado). Comentar
    isso em PT-BR: é o que torna o passo (2) abaixo determinístico e não uma aposta de temporização.

    Helper local `async function esperarAte(condicao, descricao)`: até 20 iterações de `cederEventLoop()`;
    se a condição não ficar verdadeira, lançar `Error` com `descricao` (falha explícita em vez de travar a
    suíte — mesmo espírito de `avancarRelogioAte`, `emailer.timeout.test.js:98-113`).

    **Cenário único, na ordem exata:**
    1. `const execucaoA = getStaleDeals(15);` — sem `await`.
    2. `await cederEventLoop();` — A resolve `/deals`, filtra, dispara o `Promise.all` das 6 organizações;
       201/203/205/206/207 respondem e **gravam no `orgCategoryCache`**; 210 fica pendente e A trava ali.
    3. **Pré-condições explícitas** (sem elas o caso não diz nada — lição de WR-05):
       `assert.equal(chamadas205, 1, 'pré-condição: a execução A já consultou /organizations/205')` e
       `assert.notEqual(liberar210, null, 'pré-condição: a execução A está suspensa em /organizations/210')`.
    4. `const execucaoB = getStaleDeals(15);` — a primeira instrução de B **apaga as chaves** do cache,
       incluindo a `205` que A acabou de gravar.
    5. `liberar210();` — A retoma e roda o laço de enriquecimento síncrono.
    6. `const idsA = (await execucaoA).map((d) => d.id);`
       `assert.equal(idsA.includes(105), false, 'organização 205 = "Parceiro": o deal 105 não pode ser notificado')`
       e `assert.deepStrictEqual(idsA, [101, 103])`.
    7. `await esperarAte(() => liberar205DaExecucaoB !== null, 'a execução B não chegou a consultar /organizations/205');`
       depois `liberar205DaExecucaoB();`.
    8. `const idsB = (await execucaoB).map((d) => d.id); assert.deepStrictEqual(idsB, [101, 103]);`

    Segundo caso do arquivo, curto: uma `getStaleDeals(15)` sequencial depois do interleaving devolve
    `[101, 103]` — prova que o cruzamento não deixou o dicionário de módulo em estado ruim.

    Esta task é o passo **RED**: sem a correção, o passo (6) falha com `[101, 103, 105]`. Registrar a saída
    literal da falha no SUMMARY.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/agendor.cacheConcurrency.test.js; test $? -ne 0 &amp;&amp; echo "RED confirmado"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/agendor.cacheConcurrency.test.js` sai com código **diferente de 0**
    - A mensagem de falha mostra que a execução A devolveu `[ 101, 103, 105 ]` (o deal 105 entrou)
    - O arquivo reusa a fixture existente: `grep -c "fixtures/synthetic/deals-page.json" backend/test/agendor.cacheConcurrency.test.js` retorna 1
    - O stub é instalado uma única vez: `grep -c "installFakeAxios(" backend/test/agendor.cacheConcurrency.test.js` retorna 1 (só a chamada; a linha do `require` não casa com o parêntese) e `grep -c "mock.method(axios" backend/test/agendor.cacheConcurrency.test.js` retorna 0
    - `mock.timers.enable` é chamado apenas com `apis: ['Date']`: `grep -c "apis: \['Date'\]" backend/test/agendor.cacheConcurrency.test.js` retorna 1 e `grep -c "setTimeout'" backend/test/agendor.cacheConcurrency.test.js` retorna 0
    - As duas pré-condições do passo (3) existem como `assert` com mensagem começando por `pré-condição:`
    - `git diff --name-only backend/src/` está vazio nesta task
  </acceptance_criteria>
  <done>Existe uma reprodução determinística e auto-diagnosticável do CR-01, que falha hoje e nomeia exatamente o deal indevidamente notificado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — mapa local à execução + correção dos comentários que afirmam mais do que o código garante</name>
  <files>backend/src/agendor.js, backend/test/agendor.cacheInvalidation.test.js</files>
  <behavior>
    - O teste de concorrência da Task 1 fica verde nos dois casos
    - `agendor.cacheInvalidation.test.js` (3 cenários de REL-04) continua verde SEM alteração de asserção
    - `agendor.getStaleDeals.test.js` (golden `[101, 103]`) continua verde SEM edição
  </behavior>
  <read_first>
    - `backend/src/agendor.js:149-251` (a função `getStaleDeals` inteira, com o comentário-bloco de `:150-169`)
    - `backend/src/agendor.js:47-60` — `getOrgCategory`, que continua sendo a única consultante do dicionário
    - `backend/test/agendor.cacheConcurrency.test.js` (criado na Task 1)
    - `backend/test/agendor.cacheInvalidation.test.js:1-21` — o cabeçalho a corrigir
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` §CR-01 e §WR-06
  </read_first>
  <action>
    **(a) `backend/src/agendor.js` — substituir duas linhas.**

    Trocar `await Promise.all(uniqueOrgIds.map((id) => getOrgCategory(id)));` (`:211`) por um `Map` **local à
    execução** construído com o retorno do `Promise.all`, com o identificador `categoriaPorOrg`:
    cada item resolve para o par `[id, await getOrgCategory(id)]`, e o `Map` é montado a partir do array
    resultante.

    Trocar `const orgCategory = orgCategoryCache[deal.organization?.id] ?? null;` (`:219`) por
    `const orgCategory = categoriaPorOrg.get(deal.organization?.id) ?? null;`.

    Semântica preservada por construção: `Map.get(undefined)` devolve `undefined`, o `?? null` continua
    entregando `null` para deal sem organização, e `EXCLUDED_CATEGORIES.includes(orgCategory)` fica idêntico.

    **Nada mais pode mudar em `agendor.js`:** não tocar `getOrgCategory`, não tocar a limpeza das chaves da
    primeira instrução (é ela que entrega REL-04), não tocar `EXCLUDED_CATEGORIES`, o filtro de datas,
    `getDealType`, `getDealById`, `getDealsWithFutureTasks` nem `module.exports`.

    **(b) Reescrever o comentário-bloco de `agendor.js:150-169`.** O texto atual argumenta longamente que
    `delete` é seguro e reatribuir não é, **por causa da leitura direta do dicionário no laço** — leitura que
    esta task acaba de eliminar. O comentário novo (PT-BR, estilo do arquivo: explica a DECISÃO, não a
    mecânica) precisa dizer três coisas e nada além:
    1. por que a limpeza por execução existe (REL-04 / D-05: recategorização vale na rodada seguinte; e ela
       zera o `null` que o `catch` de `getOrgCategory` grava num erro transitório);
    2. que a partir de agora `getOrgCategory` é o **único** leitor e escritor do dicionário de módulo, e que
       por isso a limpeza continua sendo por `delete` de chave (a referência é compartilhada com ele);
    3. que o laço de enriquecimento lê o **mapa local à execução** — e que é isso, e não a forma da limpeza,
       que impede duas execuções sobrepostas de apagarem o dado uma da outra (CR-01). Citar
       `backend/test/agendor.cacheConcurrency.test.js` como o teste que pina essa propriedade.

    Remover do comentário qualquer afirmação sobre a linha de leitura direta que deixou de existir.

    **(c) Corrigir o cabeçalho de `backend/test/agendor.cacheInvalidation.test.js:13-21`** — o parágrafo
    "Por que a invalidação tem de DELETAR AS CHAVES e nunca reatribuir o objeto" descreve um mecanismo de
    proteção que não é mais o que protege. Reescrever para: o dicionário é lido só por `getOrgCategory`, a
    limpeza continua por `delete` porque compartilha a referência com ele, a garantia sobre execuções
    sobrepostas mora em `agendor.cacheConcurrency.test.js`, e este arquivo mede exclusivamente o refetch
    **entre** execuções sequenciais. **Edição de COMENTÁRIO apenas** — nenhuma linha de `assert`, nenhum
    cenário, nenhum dado de fixture pode mudar.

    Ao final: rodar o teste de concorrência, o de invalidação, o golden, a suíte completa com cobertura e o lint.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/agendor.cacheConcurrency.test.js &amp;&amp; node --test test/agendor.cacheInvalidation.test.js &amp;&amp; node --test test/agendor.getStaleDeals.test.js &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/agendor.cacheConcurrency.test.js` sai 0 com os 2 casos verdes
    - `node --test test/agendor.getStaleDeals.test.js` sai 0 e `git diff --name-only backend/test/agendor.getStaleDeals.test.js` está vazio (o golden `[101, 103]` não foi editado)
    - `npm run test:coverage` sai 0 (121 testes anteriores + os novos, nenhum piso violado) e `npm run lint` sai 0
    - Fora de linhas de comentário, `orgCategoryCache[` aparece em exatamente **4** linhas de `backend/src/agendor.js` (3 em `getOrgCategory` + 1 no `delete` da limpeza): `grep -v "^\s*//" backend/src/agendor.js | grep -c "orgCategoryCache\["` retorna 4
    - Fora de linhas de comentário, `categoriaPorOrg` aparece em exatamente **2** linhas: `grep -v "^\s*//" backend/src/agendor.js | grep -c "categoriaPorOrg"` retorna 2
    - `grep -c "delete orgCategoryCache\[" backend/src/agendor.js` retorna 1 (a limpeza de REL-04 continua existindo)
    - **Reprodução da corrida antiga:** aplicando SOMENTE o arquivo de teste sobre o `agendor.js` anterior à correção (`git stash` é proibido — use `git show <sha-do-commit-RED>:backend/src/agendor.js` num diretório descartável, ou reverta a correção temporariamente numa cópia), o caso concorrente falha com `[101, 103, 105]`. O RED precisa ser registrado no SUMMARY com a saída literal, não afirmado.
    - **Duas execuções sobrepostas passam:** com a correção aplicada, o caso concorrente devolve `[101, 103]` para AMBAS as execuções, e a asserção cobre as duas — não só a que termina por último.
    - **Nenhuma execução altera o estado usado pela outra:** o caso concorrente prova, no mesmo teste, que a execução B roda inteira (incluindo a limpeza de REL-04) entre a população e a leitura de A sem alterar o resultado de A. Determinismo garantido pelos dois pontos de suspensão descritos na Task 1, não por timing.
    - **Negócio de categoria excluída nunca é notificado:** em nenhuma das duas execuções o deal 105 (org 205, `'Parceiro'`) aparece no resultado. Asserção explícita `assert.ok(!ids.includes(105))` em ambas, além do `deepStrictEqual` do golden.
    - **Ausência de cache mutável compartilhado entre execuções, confirmada por diff:** `git diff backend/src/agendor.js` mostra que o laço de enriquecimento não lê mais `orgCategoryCache`. Fora de linhas de comentário, `orgCategoryCache` NÃO aparece em nenhuma linha entre o `Promise.all` das organizações e o fim do laço de enriquecimento; a única leitura de módulo remanescente está dentro de `getOrgCategory`. Verificação: `git diff backend/src/agendor.js | grep "^-" | grep -q "orgCategoryCache\[deal.organization"` deve encontrar a linha REMOVIDA `:219`.
    - O diff de `backend/test/agendor.cacheInvalidation.test.js` altera SOMENTE linhas de comentário: `git diff -- backend/test/agendor.cacheInvalidation.test.js | grep -E "^[+-][^+-]" | grep -vE "^[+-][[:space:]]*//" | wc -l` imprime `0`
    - `git diff backend/src/agendor.js` não altera `getOrgCategory`, `EXCLUDED_CATEGORIES`, `getDealById`, `getDealsWithFutureTasks` nem o bloco `module.exports`
  </acceptance_criteria>
  <done>CR-01 fechado: a decisão de exclusão por categoria não depende mais de estado de módulo compartilhado, e os comentários da fase pararam de garantir uma propriedade que o código não tinha.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint C7 — revisar a correção da condição de corrida</name>
  <files>backend/src/agendor.js, backend/test/agendor.cacheConcurrency.test.js</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` §CR-01 (o defeito e a evidência)
    - `backend/src/agendor.js` (estado final)
    - `backend/test/agendor.cacheConcurrency.test.js` (arquivo final)
  </read_first>
  <action>
    PAUSAR a execução e apresentar ao humano o checkpoint C7. O usuário pediu explicitamente por este gate
    (2026-08-04) para revisar a correção da condição de corrida antes de avançar. Reunir e exibir: o
    `git diff backend/src/agendor.js` completo, a saída literal do RED (a corrida reproduzida com
    `[101, 103, 105]`) e a saída do GREEN, e a suíte completa. Não prosseguir para o 04-09 sem o sinal de
    retomada — `auto_advance` está desligado por decisão Q4.
  </action>
  <what-built>
    O laço de enriquecimento de `getStaleDeals` passou a consumir um mapa local à execução, construído a
    partir do retorno do `Promise.all` das organizações, em vez de reler o dicionário de módulo
    `orgCategoryCache`. A limpeza por execução de REL-04 e a assinatura de `getOrgCategory` ficam intactas.
  </what-built>
  <how-to-verify>
    1. Rodar `cd backend && export PATH="$HOME/bin:$PATH" && npm run test:coverage` — deve sair 0.
    2. Ler o `git diff backend/src/agendor.js` e confirmar que a leitura de estado de módulo no laço de
       enriquecimento (a antiga `:219`) foi REMOVIDA, e que nenhuma outra leitura compartilhada a
       substituiu. Se o laço ainda tocar `orgCategoryCache`, REPROVAR.
    3. Confirmar que o RED foi medido, não afirmado: a saída literal com `[101, 103, 105]` deve estar no
       SUMMARY. Um teste que nunca ficou vermelho não prova nada.
    4. Confirmar que o determinismo do interleaving vem dos dois pontos de suspensão, e não de `sleep` ou
       timing — um teste concorrente intermitente é pior que nenhum, porque dá falsa confiança.
    5. Confirmar que o golden `[101, 103]` de `agendor.getStaleDeals.test.js` não foi editado
       (`git diff --name-only` desse arquivo deve estar vazio).
    6. Confirmar que a limpeza de REL-04 continua existindo — a correção da corrida não pode desfazer o
       requisito que o 04-07 entregou.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirmou por escrito que o laço de enriquecimento não lê mais estado de módulo compartilhado
    - Humano confirmou que o RED foi reproduzido de fato, com a saída literal registrada
    - Humano confirmou que o golden `[101, 103]` não foi editado e que a limpeza de REL-04 sobreviveu
    - Humano autorizou explicitamente a entrada no plano 04-09
  </acceptance_criteria>
  <verify>
    <human-check>Humano confirma a ausência de cache compartilhado no laço, o RED medido, o golden intacto, e autoriza o 04-09.</human-check>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; npm run test:coverage</automated>
  </verify>
  <done>C7 aprovado por escrito; entrada no 04-09 autorizada.</done>
  <resume-signal>Digite "aprovado" para liberar o 04-09, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Agendor → `orgCategoryCache` | Dado externo guardado em estado de módulo e usado para decidir quem NÃO recebe notificação |
| Execução concorrente → estado de módulo | Cron (8h) e rotas HTTP compartilham o mesmo processo Node single-instance, sem lock comum |
| `categoriaPorOrg` (local) → regra de exclusão | Fronteira nova: a decisão passa a depender apenas de dado capturado dentro da própria execução |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering (integridade da regra de negócio) | `getStaleDeals` lendo `orgCategoryCache` no laço de enriquecimento enquanto outra execução apaga as chaves | mitigate | Mapa local à execução construído com o retorno do `Promise.all`; `agendor.cacheConcurrency.test.js` prova o interleaving com pré-condições explícitas |
| T-04-08-02 | Information Disclosure | Organização `'Parceiro'`/`'Fornecedor'`/`'Inativo'` recebendo e-mail de cobrança de negócio parado por causa da corrida | mitigate | Golden `[101, 103]` asserido nas DUAS execuções sobrepostas, não só na sequencial |
| T-04-08-03 | Tampering | Regressão da limpeza por execução (REL-04) ao mexer no mesmo trecho — categoria obsoleta voltando a valer | mitigate | `agendor.cacheInvalidation.test.js` roda sem edição de asserção; critério de aceite exige `delete orgCategoryCache[` presente |
| T-04-08-04 | Denial of Service (eficiência) | Trocar a leitura por um `Map` multiplicando consultas a `/organizations/:id` | mitigate | O cenário (2) de `agendor.cacheInvalidation.test.js` conta 6 urls para 6 organizações únicas e continua verde |
| T-04-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote neste plano; `backend/package.json` e o lockfile não são tocados |
</threat_model>

<verification>
- `node --test test/agendor.cacheConcurrency.test.js` sai 0
- `node --test test/agendor.cacheInvalidation.test.js` e `node --test test/agendor.getStaleDeals.test.js` saem 0, o segundo sem nenhuma edição
- `npm run test:coverage` e `npm run lint` saem 0
- Diff de produção restrito a duas linhas efetivas + comentário dentro de `getStaleDeals`
- `git diff --name-only backend/package.json backend/package-lock.json` vazio
</verification>

<success_criteria>
- Duas execuções sobrepostas de `getStaleDeals` devolvem, ambas, `[101, 103]`
- O laço de enriquecimento não referencia mais `orgCategoryCache`
- A limpeza por execução (REL-04) e a eficiência dentro da rodada permanecem intactas
- Nenhum dos 7 arquivos de teste das ondas 1-7 teve asserção alterada
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-08-SUMMARY.md` when done.
Commit: `fix(04-08): categoria por organização em mapa local à execução — corrida do orgCategoryCache (CR-01/WR-06)`
Rollback: revert do commit (volta ao estado com a corrida).
</output>
