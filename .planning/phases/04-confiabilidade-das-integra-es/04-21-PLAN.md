---
phase: 04-confiabilidade-das-integra-es
plan: 21
type: execute
wave: 14
depends_on: ["04-20"]
files_modified:
  - backend/src/emailer.js
  - backend/test/emailer.resumoIndecidivel.test.js
autonomous: true
gap_closure: true
requirements: [REL-02, REL-06]

must_haves:
  truths:
    - "O relatório semanal individual do comercial não lista negócio de categoria indecidível"
    - "Quando TODOS os negócios de um responsável são indecidíveis, nenhum e-mail é enviado a ele — não um e-mail vazio"
    - "O resumo consolidado do admin CONTINUA listando o negócio indecidível: ele é a superfície de observação, não de notificação"
    - "O filtro de funil (Beefor) continua funcionando e compõe com o novo, sem que um anule o outro"
  artifacts:
    - path: "backend/src/emailer.js"
      provides: "sendOwnerWeeklySummary exclui negócio indecidível do e-mail individual"
      contains: "categoriaIndecidivel"
    - path: "backend/test/emailer.resumoIndecidivel.test.js"
      provides: "4 cenários: exclusão parcial, exclusão total (simétrico), resumo admin preservado e composição com o filtro de funil"
      min_lines: 120
  key_links:
    - from: "backend/src/agendor.js getStaleDeals"
      to: "backend/src/emailer.js sendOwnerWeeklySummary"
      via: "campo categoriaIndecidivel, filtrado ao lado de shouldNotifyOwner"
      pattern: "categoriaIndecidivel"
---

<objective>
Fechar o **caminho vizinho de CR3-01**: o resumo semanal individual do comercial também é e-mail, também é
alimentado por `getStaleDeals`, e hoje listaria um negócio de organização `'Parceiro'` cuja categoria não pôde
ser consultada. O 04-20 fechou a notificação diária; sem este plano, o mesmo negócio volta pela sexta-feira.

Purpose:

É exatamente o padrão que se repetiu três vezes nesta fase: o conserto fecha o cenário do achado, o teste novo
confirma esse mesmo cenário, e o caminho **imediatamente ao lado** fica aberto — e é ele que a rodada seguinte
de review encontra. `runCheck` e `sendOwnerWeeklySummary` são os dois únicos produtores de e-mail dirigido ao
responsável, e os dois leem a mesma lista.

A separação entre as duas superfícies não é arbitrária, e o próprio `emailer.js` já tem o precedente: o filtro
de funil existente exclui os cards do funil Beefor do e-mail individual e os **mantém** no relatório admin,
com o comentário dizendo isso em duas linhas. O negócio indecidível recebe o mesmo tratamento, pela mesma
razão: o admin é quem precisa ver a anomalia; o comercial é quem não pode ser notificado sobre um negócio que
talvez nem devesse estar na lista.

Output: um segundo predicado no filtro de `sendOwnerWeeklySummary`, com contagem própria no log, e um arquivo
de teste com 4 cenários.

## O que NÃO muda

- `sendWeeklySummary` (consolidado do admin), `weeklySummaryHtml`, `buildOwnerBlocks`, `ownerWeeklyHtml`: nada.
  O negócio indecidível continua aparecendo lá — é isso que o cenário (3) pina.
- `saveWeeklySnapshot` em `runWeeklySummary` e os agrupamentos `byOwner`/`byCategory`/`byFunnel`: nada. O
  histórico de relatório continua contando o negócio.
- `sendStaleNotification`, `sendMailWithRetry`, `createTransporter`, `dealEmailHtml`, `verifySmtp`: intocados.
- As rotas `POST /api/notifications/test-owner-summary` e `/send-owner-summaries` chamam a mesma função e
  herdam o filtro de graça — nenhuma delas é editada.
</objective>

<baseline>
## Fatos medidos (2026-08-05)

| Item | Valor medido | Como |
|---|---|---|
| Filtro atual do e-mail individual | `const notifiable = deals.filter(shouldNotifyOwner);` seguido de `const skippedByFunnel = deals.length - notifiable.length;` e um `console.log` com a contagem | leitura de `sendOwnerWeeklySummary` |
| Comentário existente | declara que os cards excluídos por funil "ainda aparecem no relatório admin, mas não no e-mail individual do comercial" — o precedente de forma e de política | leitura de `sendOwnerWeeklySummary` |
| Saída antecipada | `if (!notifiable.length) return [];` **antes** de criar o transporte — nenhum e-mail é enviado quando não sobra nada | leitura de `sendOwnerWeeklySummary` |
| Agrupamento | `byOwner[owner.email]` a partir de `users[d.ownerId]`, com `if (!owner?.email) continue;` | leitura de `sendOwnerWeeklySummary` |
| Consumidores da função | `runWeeklySummary` (scheduler) e duas rotas de `routes/notifications.js` | `grep -n "sendOwnerWeeklySummary" src/` |
| Logs do módulo | `emailer.js` usa `console.log`/`console.warn`/`console.error` (legado; LOG-01 é da Fase 5) e **não** requer `./logger` hoje | leitura de `emailer.js` |
| Relatório admin | `buildOwnerBlocks` monta uma linha de tabela por negócio, com `${d.title}` — o título do negócio aparece literalmente no HTML | leitura de `buildOwnerBlocks` |

## Decisões travadas (não reabrir)

- **D-CR3-01-l:** o e-mail **individual** do comercial exclui negócio indecidível; o **consolidado do admin**
  e o snapshot semanal **mantêm**. Mesma política do filtro de funil já existente no mesmo bloco.
- **D-CR3-01-m:** o filtro novo é um segundo passo, com contagem própria, e **não** é embutido no
  `deals.filter(shouldNotifyOwner)` existente: `skippedByFunnel` continua significando exatamente o que o seu
  nome diz. Um contador cujo nome mente é o defeito que WR2-03 e IN3-07 nomearam nesta fase.
- **D-CR3-01-n:** o aviso do filtro novo usa `logger.warn` com a tag `[Emailer]`, e o `require('./logger')`
  entra em `emailer.js`. Motivo: o CLAUDE.md manda usar o logger em **código novo** e trata o `console.*`
  daquele arquivo como legado a migrar (LOG-01, Fase 5). As linhas de `console.*` existentes **não** são
  tocadas — migrá-las é a Fase 5, não este plano.
- **D-CR3-01-o:** quando nada sobra depois dos dois filtros, a saída antecipada existente já garante que
  nenhum e-mail é enviado. Não escrever um caminho novo para isso.
</baseline>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-20-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-05. Não explorar o codebase. -->

`backend/src/emailer.js`, `async function sendOwnerWeeklySummary({ deals, users })` — sequência atual:
1. `const notifiable = deals.filter(shouldNotifyOwner);`
2. `const skippedByFunnel = deals.length - notifiable.length;` e, se maior que zero, um `console.log`
   com a contagem;
3. `if (!notifiable.length) return [];`
4. `const transporter = createTransporter();`, `from`, `staleDays`, `weekLabel`, `const results = [];`
5. agrupamento por dono: `for (const d of notifiable) { const owner = users[d.ownerId]; if (!owner?.email) continue; ... byOwner[owner.email].deals.push(d); }`
6. um `transporter.sendMail({ from, to: email, subject, html })` por dono, com `results.push({ to, name, count, success })`
   no sucesso e `{ to, name, success: false, error }` no `catch`.

`backend/src/emailer.js`, `async function sendWeeklySummary({ deals, adminEmails })` — o consolidado do admin,
que usa `weeklySummaryHtml` → `buildOwnerBlocks`, onde cada negócio vira uma linha `<td>${d.title}</td>`.

`backend/src/emailer.js`, `function shouldNotifyOwner(deal)` vem de `agendor.js` (funis
`NO_OWNER_NOTIFY_FUNNELS = ['beefor']`, comparação por `funnel` normalizado).

`backend/test/emailer.timeout.test.js` — o molde de teste de `emailer.js` sem banco real: `require('./setup')`,
`setConfig` para host/porta/usuário/remetente, e `mock.method(nodemailer, 'createTransport', ...)` devolvendo
`{ verify, sendMail }`. **PC-13:** o objeto de opções não pode ser impresso nem comparado inteiro — asserções
apenas sobre campos individuais (`mailOptions.to`, `mailOptions.html`).

Objeto de negócio devolvido por `getStaleDeals` após o 04-19: além dos campos de sempre (`id`, `title`,
`ownerId`, `ownerName`, `organization`, `orgCategory`, `dealType`, `funnel`, `daysSinceUpdate`, `webUrl`),
traz `categoriaIndecidivel: true|false`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — o resumo semanal individual lista o negócio que o agendador se recusa a notificar</name>
  <files>backend/test/emailer.resumoIndecidivel.test.js</files>
  <behavior>
    - Cenário (1): um responsável com dois negócios, um indecidível → o e-mail sai com **um** card, e o título do indecidível não aparece no HTML
    - Cenário (2) SIMÉTRICO: um responsável cujos negócios são **todos** indecidíveis → nenhum `sendMail` para ele, e ele não aparece em `results`
    - Cenário (3): o consolidado do admin (`sendWeeklySummary`) CONTINUA listando o negócio indecidível
    - Cenário (4): um negócio do funil Beefor mais um indecidível mais um normal → só o normal sai no e-mail individual; os dois filtros compõem
  </behavior>
  <read_first>
    - `backend/test/emailer.timeout.test.js` (o molde de bootstrap de teste de `emailer.js` e a regra PC-13)
    - `backend/src/emailer.js` — `sendOwnerWeeklySummary`, `sendWeeklySummary`, `buildOwnerBlocks` e `ownerWeeklyHtml`
    - `backend/src/agendor.js` — `shouldNotifyOwner` e `NO_OWNER_NOTIFY_FUNNELS`, além do campo criado no 04-19
    - `.planning/phases/04-confiabilidade-das-integra-es/04-20-SUMMARY.md`
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção CR3-01
  </read_first>
  <action>
    Criar `backend/test/emailer.resumoIndecidivel.test.js`.

    Cabeçalho em PT-BR nomeando **CR3-01 (caminho vizinho)** e explicando em 6 a 10 linhas: `runCheck` e
    `sendOwnerWeeklySummary` são os dois únicos produtores de e-mail dirigido ao responsável e leem a mesma
    lista de `getStaleDeals`; fechar só o primeiro deixa o negócio indecidível voltar pela sexta-feira.
    Declarar a política das duas superfícies (individual exclui, admin mantém) e apontar o precedente do
    filtro de funil, que já faz exatamente isso no mesmo bloco. Referências por âncora nomeada, nunca por
    número de linha (WR2-06).

    **Bootstrap.** Copiar de `emailer.timeout.test.js`: `require('./setup')`, `setConfig` de
    `smtp_host`/`smtp_port`/`smtp_user`/`smtp_from`, e um único `mock.method(nodemailer, 'createTransport')`
    para o arquivo inteiro, devolvendo `{ verify, sendMail }`. O `sendMail` do stub registra, num array de
    módulo `enviosCapturados`, apenas `{ to: mailOptions.to, html: mailOptions.html }` — nunca o objeto de
    opções inteiro (PC-13). `beforeEach` zera `enviosCapturados` e os contadores. Não há banco real nem
    axios aqui: as funções são chamadas **diretamente** com negócios sintéticos, o que mantém o arquivo
    barato e o oráculo preciso.

    **Negócios sintéticos.** Uma fábrica local `negocio({ id, title, ownerId, funnel, categoriaIndecidivel })`
    que devolve o objeto com os campos que `ownerWeeklyHtml` e `buildOwnerBlocks` leem (`title`,
    `organization`, `funnel`, `dealType`, `daysSinceUpdate`, `webUrl`, `ownerName`, `ownerId`). Títulos
    distintos e reconhecíveis por `includes` no HTML (por exemplo `'NEGOCIO-NORMAL'`,
    `'NEGOCIO-INDECIDIVEL'`, `'NEGOCIO-BEEFOR'`). `users` é um mapa mínimo `{ [ownerId]: { email, name } }`.

    **Cenário (1) — exclusão parcial.** Dois negócios do mesmo responsável, um com
    `categoriaIndecidivel: true`. Asserir: exatamente **um** envio; `results[0].count === 1`; o HTML enviado
    **contém** `'NEGOCIO-NORMAL'` e **não contém** `'NEGOCIO-INDECIDIVEL'`. Comentar que a asserção sobre o
    HTML é o que impede um conserto que apenas conte errado sem deixar de listar.

    **Cenário (2) — SIMÉTRICO: exclusão total.** Um responsável cujos dois negócios são indecidíveis. Asserir
    `enviosCapturados.length === 0` e que `results` não contém aquele endereço (idealmente
    `assert.deepStrictEqual(results, [])`). Comentar em 2 a 3 linhas por que este é o simétrico exigido: o
    cenário (1) prova que o filtro tira o negócio errado da lista; este prova que o filtro não produz um
    **e-mail vazio** — "Seus 0 cards parados" seria uma notificação indevida de outro tipo, e a saída
    antecipada existente é quem impede isso.

    **Cenário (3) — o consolidado do admin mantém.** Chamar `sendWeeklySummary({ deals, adminEmails })` com
    a mesma lista do cenário (1). Asserir um envio para o endereço de admin e que o HTML **contém**
    `'NEGOCIO-INDECIDIVEL'`. É a metade "permanece nos relatórios" da decisão do usuário, medida e não
    presumida.

    **Cenário (4) — composição com o filtro de funil.** Três negócios do mesmo responsável: um normal, um do
    funil `'Beefor'` e um indecidível. Asserir um envio, `results[0].count === 1`, e que o HTML contém
    `'NEGOCIO-NORMAL'` e não contém nem `'NEGOCIO-BEEFOR'` nem `'NEGOCIO-INDECIDIVEL'`. Este caso impede que
    o filtro novo substitua o antigo em vez de somar-se a ele.

    Rodar e registrar o RED com a **saída literal**. A previsão é que (1), (2) e (4) fiquem vermelhos e (3)
    já esteja verde. Se (1) já estiver verde, PARAR e reportar.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/emailer.resumoIndecidivel.test.js; test $? -ne 0 &amp;&amp; echo "RED confirmado"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/emailer.resumoIndecidivel.test.js` sai com código diferente de 0, com (1), (2) e (4) vermelhos e (3) verde
    - A saída literal do RED vai para o SUMMARY
    - `grep -c "^test(" backend/test/emailer.resumoIndecidivel.test.js` retorna 4
    - `grep -ci "simétrico" backend/test/emailer.resumoIndecidivel.test.js` retorna pelo menos 1
    - O HTML é asserido, não só a contagem: `grep -c "includes('NEGOCIO-INDECIDIVEL')" backend/test/emailer.resumoIndecidivel.test.js` retorna pelo menos 2
    - PC-13 respeitado: `grep -c "deepStrictEqual(mailOptions" backend/test/emailer.resumoIndecidivel.test.js` retorna 0 e nenhuma asserção usa o objeto de opções inteiro
    - `git diff --name-only backend/src/` está vazio nesta task
  </acceptance_criteria>
  <done>Existe prova executável de que o relatório semanal individual entrega ao comercial o negócio que o agendador se recusa a notificar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — segundo predicado no filtro do e-mail individual</name>
  <files>backend/src/emailer.js</files>
  <behavior>
    - Os cenários (1), (2) e (4) ficam verdes e o (3) continua verde
    - `emailer.timeout.test.js`, `emailer.transporteVivo.test.js`, `emailer.smtpPass.test.js`, `notificationStatus.*` e `scheduler.*` continuam verdes SEM edição
  </behavior>
  <read_first>
    - `backend/src/emailer.js` — `sendOwnerWeeklySummary` inteira e o topo do arquivo (a lista de `require`)
    - `backend/test/emailer.resumoIndecidivel.test.js` (criado na Task 1)
    - `backend/src/logger.js` — a assinatura de `logger.warn` e o formato de saída
    - `./CLAUDE.md` seção "Logging" (a regra de código novo contra o `console.*` legado)
  </read_first>
  <action>
    Editar `backend/src/emailer.js`. Duas mudanças, nenhuma além destas.

    **(a) O filtro.** Em `sendOwnerWeeklySummary`, manter `deals.filter(shouldNotifyOwner)` como está — o
    resultado dele passa a ser um passo intermediário, com nome próprio — e acrescentar um segundo filtro que
    remove os negócios com `categoriaIndecidivel` verdadeiro, produzindo o `notifiable` final. Manter
    `skippedByFunnel` calculado sobre o **primeiro** passo (o nome continua verdadeiro) e acrescentar uma
    contagem própria para o segundo. Quando essa segunda contagem for maior que zero, emitir um
    `logger.warn` com a tag `[Emailer]` dizendo quantos cards ficaram fora do relatório individual porque a
    categoria da organização não pôde ser consultada. Acrescentar `const logger = require('./logger');` ao
    topo do arquivo. A saída antecipada `if (!notifiable.length) return [];` e todo o resto da função ficam
    byte a byte.

    **(b) O comentário.** Estender o bloco que hoje explica o filtro de funil (PT-BR, mais 4 a 6 linhas, sem
    citar números de linha): dizer que o mesmo princípio vale para o negócio de categoria indecidível — ele
    aparece no relatório do admin e no painel, mas não no e-mail individual do comercial —, e por quê: não
    saber a categoria é indistinguível de "pode ser uma categoria excluída", e este é o segundo dos dois
    únicos produtores de e-mail dirigido ao responsável (o outro é `runCheck`, fechado no 04-20). Citar
    `emailer.resumoIndecidivel.test.js` como o oráculo.

    **Nada mais pode mudar em `emailer.js`:** as linhas de `console.log`/`console.warn`/`console.error`
    existentes não são migradas (isso é LOG-01, Fase 5); `sendWeeklySummary`, `weeklySummaryHtml`,
    `buildOwnerBlocks`, `ownerWeeklyHtml`, `sendStaleNotification`, `sendMailWithRetry`, `createTransporter`,
    `dealEmailHtml`, `verifySmtp` e `sendResetPasswordEmail` ficam intocados.

    Ao final: rodar o arquivo novo, os três arquivos `emailer.*` existentes, os arquivos de
    `notificationStatus.*` e de `scheduler.*`, a suíte com cobertura e o lint.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/emailer.resumoIndecidivel.test.js &amp;&amp; node --test test/emailer.timeout.test.js &amp;&amp; node --test test/emailer.transporteVivo.test.js &amp;&amp; node --test test/emailer.smtpPass.test.js &amp;&amp; node --test test/scheduler.categoriaIndecidivel.test.js &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/emailer.resumoIndecidivel.test.js` sai 0 com os 4 cenários verdes
    - `grep -v "^\s*//" backend/src/emailer.js | grep -c "categoriaIndecidivel"` retorna 1 (só o predicado do filtro)
    - `grep -v "^\s*//" backend/src/emailer.js | grep -c "skippedByFunnel"` retorna 3 (declaração, condição e interpolação — o executor mede o valor ANTES de editar e confirma que não mudou)
    - `grep -c "require('./logger')" backend/src/emailer.js` retorna 1 e `grep -v "^\s*//" backend/src/emailer.js | grep -c "logger.warn"` retorna 1
    - As linhas legadas de console não foram migradas: `grep -v "^\s*//" backend/src/emailer.js | grep -cE "console\.(log|warn|error)"` devolve o mesmo número medido ANTES da edição (os dois valores vão para o SUMMARY)
    - `sendWeeklySummary` intocada: `git diff backend/src/emailer.js | grep -c "weeklySummaryHtml"` retorna 0
    - `emailer.timeout.test.js`, `emailer.transporteVivo.test.js` e `emailer.smtpPass.test.js` saem 0 e `git diff --name-only` deles está vazio
    - `npm run test:coverage` sai 0; o total medido (baseline de entrada mais 4) vai literal para o SUMMARY
    - `npm run lint` sai 0
    - `git diff --name-only backend/src/` lista apenas `backend/src/emailer.js`
  </acceptance_criteria>
  <done>Os dois únicos caminhos de e-mail dirigido ao responsável passam a respeitar a mesma regra, e o admin continua vendo o que foi suprimido.</done>
</task>

</tasks>

<rastreabilidade>
| Requisito | Achado | Tarefa | Teste | Evidência |
|---|---|---|---|---|
| REL-06 (mesma regra da Decisão Q2) | CR3-01 (caminho vizinho) | Task 2 (a) | cenários (1), (2), (4) | HTML sem o card indecidível; zero e-mails quando tudo é indecidível |
| REL-02 (não-regressão do envio) | — | Task 2 | `emailer.timeout`, `emailer.transporteVivo` sem edição | timeouts e transporte vivo intactos |
| REL-06 (superfície de observação) | CR3-01 | Task 1 | cenário (3) | consolidado do admin ainda lista o negócio |
</rastreabilidade>

<matriz_de_teste>
| Cenário | Tipo | Arquivo | Setup → Ação → Esperado | Regressão protegida |
|---|---|---|---|---|
| Exclusão parcial | novo (RED→GREEN) | `resumoIndecidivel` (1) | 2 negócios, 1 indecidível → 1 card no HTML | card indecidível entregue ao comercial |
| Exclusão total | novo — SIMÉTRICO | `resumoIndecidivel` (2) | todos indecidíveis → 0 e-mails | e-mail "Seus 0 cards parados" |
| Consolidado do admin | novo (não-regressão) | `resumoIndecidivel` (3) | mesma lista → HTML do admin contém o card | filtro largo demais escondendo a anomalia de quem deve vê-la |
| Composição com o funil | novo | `resumoIndecidivel` (4) | normal + Beefor + indecidível → só o normal | filtro novo substituindo o antigo |
| Timeouts e retry SMTP | regressão | `emailer.timeout`, `emailer.transporteVivo` | inalterados | tocar o caminho de envio ao editar o módulo |
</matriz_de_teste>

<matriz_de_risco>
| # | Risco | Prob. | Impacto | Mitigação | Sinal | Rollback |
|---|---|---|---|---|---|---|
| R3-10 | Embutir o predicado novo dentro do `filter(shouldNotifyOwner)` e fazer `skippedByFunnel` mentir | média | médio | D-CR3-01-m proíbe; critério mede `skippedByFunnel` antes e depois | contagem de `skippedByFunnel` alterada | revert do commit |
| R3-11 | Filtrar também o consolidado do admin, escondendo a anomalia de quem precisa vê-la | média | alto | Cenário (3) assere o HTML do admin; critério proíbe diff em `weeklySummaryHtml` | cenário (3) vermelho | revert do commit |
| R3-12 | Migrar o `console.*` legado do arquivo junto, misturando LOG-01 (Fase 5) com esta correção | média | médio | Critério compara a contagem de `console.*` antes e depois | contagem menor depois | revert do commit |
| R3-13 | Enviar e-mail vazio quando tudo é indecidível | baixa | médio | A saída antecipada existente cobre; cenário (2) assere zero envios | cenário (2) vermelho | ajustar a ordem dos passos do filtro |
</matriz_de_risco>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lista de `getStaleDeals` → e-mail individual do comercial | Segundo produtor de e-mail dirigido ao responsável |
| Lista de `getStaleDeals` → consolidado do admin | Superfície de observação, deliberadamente não filtrada |
| `emailer.js` → SMTP | Envio irreversível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21-01 | Spoofing (elegibilidade presumida) | `sendOwnerWeeklySummary` listando negócio de categoria não consultada | mitigate | Segundo predicado no filtro; cenários (1), (2) e (4) com asserção sobre o HTML |
| T-04-21-02 | Information Disclosure | Card de organização `'Parceiro'` chegando ao comercial pelo relatório semanal | mitigate | Mesmo filtro; o card permanece apenas nas superfícies internas (painel e consolidado do admin) |
| T-04-21-03 | Repudiation | Supressão silenciosa, sem contagem | mitigate | `logger.warn` com tag `[Emailer]` e contagem própria, separada de `skippedByFunnel` |
| T-04-21-04 | Information Disclosure | `logger.warn` novo despejando objeto de erro ou credencial SMTP | mitigate | O aviso carrega apenas uma contagem inteira; PC-13 vale no teste e no código |
| T-04-21-05 | Denial of Service (relatório incompleto) | Comercial deixando de ver um card legítimo no relatório da semana da falha | accept | Decisão do usuário de 2026-08-05; o card continua no painel e no consolidado do admin, e a semana seguinte reconsulta |
| T-04-21-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote; `package.json` e lockfile não são tocados |
</threat_model>

<dod>
- [ ] Os 4 cenários verdes, com o RED de (1), (2) e (4) registrado por saída literal
- [ ] O cenário SIMÉTRICO (2) existe e está nomeado como tal
- [ ] O consolidado do admin continua listando o negócio indecidível (cenário 3)
- [ ] `skippedByFunnel` continua significando o que o nome diz, com contagem inalterada
- [ ] Nenhuma linha de `console.*` legada foi migrada
- [ ] Suíte completa verde, cobertura acima dos pisos, `npm run lint` exit 0
</dod>

<verification>
- `node --test test/emailer.resumoIndecidivel.test.js` sai 0
- Os três arquivos `emailer.*` existentes saem 0 sem edição
- `npm run test:coverage` sai 0 e `npm run lint` sai 0
- `git diff --name-only backend/src/` lista apenas `backend/src/emailer.js`
</verification>

<success_criteria>
- Os dois produtores de e-mail dirigido ao responsável aplicam a mesma regra de elegibilidade
- O admin continua enxergando o negócio que foi suprimido do e-mail individual
- O filtro de funil e o filtro novo compõem, sem que um anule o outro
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-21-SUMMARY.md` when done.
Commit: `fix(04-21): o resumo semanal individual não lista negócio de categoria indecidível (CR3-01, vizinho)`
Rollback: revert do commit. Independente do 04-20 — reverter aqui não reabre o caminho da notificação diária.
</output>
