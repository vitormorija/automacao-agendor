---
phase: 04-confiabilidade-das-integra-es
plan: 34
type: execute
wave: 27
depends_on: ["04-33"]
files_modified:
  - .planning/todos/pending/in4-02-justificativa-do-retry-descreve-mecanismo-inexistente.md
  - .planning/todos/pending/in4-03-assercao-decorativa-em-categoriaindecidivel.md
  - .planning/todos/pending/in4-04-leitura-de-success-divergente-entre-ramos.md
  - .planning/todos/pending/in4-05-literal-100-em-tasks-sem-constante.md
  - .planning/todos/pending/in4-06-teto-de-tasks-sem-medicao-de-volume.md
  - .planning/todos/pending/cr4-01b-limiar-de-supressao-total.md
  - .planning/todos/pending/cr4-01c-skipreason-invisivel-na-ui.md
  - .planning/todos/pending/wr4-03b-referencias-por-linha-nos-demais-arquivos-de-teste.md
  - .planning/todos/pending/wr4-04b-fanout-proporcional-em-resolved.md
  - .planning/todos/pending/wr4-07b-saudacao-com-ownername-nulo.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [REL-03, REL-06]

must_haves:
  truths:
    - "Nenhum achado da rodada 4 termina a fase sem dono: os Info viram todos pendentes e os residuais do inventário de irmãos também"
    - "IN4-01 é registrado como FECHADO com a medição que o comprova, em vez de virar um todo que já não tem objeto"
    - "O ROADMAP declara, como comportamento garantido e sem nomear identificador, que o apagão de categoria deixou de ser silencioso"
    - "Nenhum todo com prioridade decidida pelo usuário é editado"
  artifacts:
    - path: ".planning/todos/pending/cr4-01b-limiar-de-supressao-total.md"
      provides: "O residual medido do limiar de supressão total"
      min_lines: 40
    - path: ".planning/todos/pending/wr4-03b-referencias-por-linha-nos-demais-arquivos-de-teste.md"
      provides: "As 39 linhas de referência por número nos 9 arquivos que o in3-06 não cobre"
      min_lines: 40
    - path: ".planning/ROADMAP.md"
      provides: "Success criterion 8 da Fase 4 e a lista de planos da gap closure r4"
      contains: "gap closure r4"
  key_links:
    - from: ".planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md"
      to: ".planning/todos/pending/"
      via: "cada achado Info e cada residual declarado no inventário de irmãos tem arquivo próprio"
      pattern: "in4-|cr4-01|wr4-0"
---

<objective>
Fechar o **RESÍDUO DOCUMENTAL da rodada 4**. Este não é um plano de conserto: é o que impede a
rodada de terminar deixando achado sem dono.

Escopo travado pelo usuário: os 6 achados Info (IN4-01 a IN4-06) viram **todos pendentes, não
planos** — mesmo precedente de IN2-01..IN2-04 (plano 04-18) e IN3-01..IN3-08 (plano 04-27). A esses
somam-se os **residuais declarados nos inventários de irmãos** dos planos 04-28 a 04-33: cada item
classificado como "fora-de-escopo-com-medição" no mandato desta rodada exige um dono, e é aqui que
o dono passa a existir.

Purpose:

Foi exatamente esse precedente que permitiu ao 04-26 FECHAR o `in2-02` com evidência: achado sem
arquivo não sobrevive à fase. E é o inventário de irmãos que produziu WR4-01, WR4-02 e a maior parte
do que esta rodada consertou — os itens que ele deixou de fora só continuam sendo trabalho
rastreável se virarem arquivo.

Output: 10 todos pendentes, 1 achado Info registrado como FECHADO com medição, e o ROADMAP com o
critério novo e a lista dos 7 planos da rodada.

## O que NÃO muda

- **Nenhum arquivo de `backend/` ou `frontend/`.** Diff de código zero, medido em cada task. Este
  plano é registro, não conserto.
- **Nenhum todo com prioridade decidida pelo usuário.** Ficam byte a byte: `in-01`, `rel-02b`,
  `sec-01`, `wr3-07b` e `in3-01` a `in3-08`. Nenhum deles aparece no diff.
- **`.planning/REQUIREMENTS.md`:** nenhuma linha — ver D-IN4-e.
- **SEC-01 permanece ABERTO** (decisão C8) e o valor do `AGENDOR_TOKEN` não aparece em nenhum
  artefato deste plano.
</objective>

<baseline>
## Fatos medidos (2026-08-05)

| Item | Valor medido | Como |
|---|---|---|
| Todos pendentes na entrada | **23** arquivos | `ls .planning/todos/pending/` |
| Todos de rodadas anteriores desta fase | `in-01` a `in-04`, `in2-01`, `in2-03`, `in2-04`, `in3-01` a `in3-08`, `wr3-07b` | idem |
| Achados Info da rodada 4 | **6** (IN4-01 a IN4-06) | `04-REVIEW.md`, seção Info |
| Achados Info que este plano converte em todo | **5** (IN4-02 a IN4-06) — IN4-01 é fechado pelo 04-30 | ver D-IN4-a |
| Residuais declarados nos inventários de irmãos | **5**: `cr4-01b`, `cr4-01c` (04-28), `wr4-04b` (04-30), `wr4-03b` (04-33), `wr4-07b` (04-32) | leitura dos planos da rodada |
| Residuais que apontam para dono JÁ EXISTENTE | **2**: `in3-06` (04-33) e `in3-08` (04-31) — nenhum arquivo novo, nenhuma edição | idem |
| Molde de todo desta fase | frontmatter com `id`, `type`, `status`, `priority`, `created`, `source`, `resolves_phase`, `tags`; corpo com Onde / O que acontece / Por que isso importa / Correção proposta | `.planning/todos/pending/in3-06-*.md` |
| Convenção aplicada aos próprios artefatos (D-IN3-a) | `grep -cE "\.js:[0-9]\|linhas? [0-9]"` = 0 nos oito todos do 04-27 | 04-27-SUMMARY.md |
| Success criteria da Fase 4 no ROADMAP | **7** itens; o item 7 foi acrescentado pelo 04-27 | leitura de `.planning/ROADMAP.md` |
| Armadilha de medição registrada pelo 04-27 | `grep -c "indecidível"` no ROADMAP devolve 3, não 1: as outras 2 ocorrências são as descrições dos planos 04-20 e 04-21 na lista da r3 | 04-27-SUMMARY.md |

## Decisões travadas (não reabrir)

- **D-IN4-a — IN4-01 NÃO VIRA TODO: É REGISTRADO COMO FECHADO.** O comentário do laço de páginas que
  dizia "batches de 10" sobre um `batchSize` de 5 foi corrigido pelo plano 04-30, e isso está
  declarado lá (D-WR4-04-e) com o motivo: aquele plano introduziu uma segunda constante de lote cujo
  valor É 10, e deixar a frase errada ao lado dela tornaria os dois lotes indistinguíveis. Fechar um
  Info como efeito de um conserto de warning NÃO é transformá-lo em plano, e tem precedente na fase:
  o 04-26 fechou o `in2-02` do mesmo modo. Este plano registra o desfecho com a medição, e não cria
  arquivo para um achado que já não tem objeto.
- **D-IN4-b — PRIORIDADES.** `in4-02` baixa (comentário com justificativa incorreta, sem efeito de
  execução); `in4-03` média (asserção que não pode ficar vermelha — um oráculo que não mede nada é
  pior que oráculo ausente, porque dá falsa confiança); `in4-04` média (a mesma variável decide
  `'sent'` contra `'error'` e é lida de dois jeitos, no caminho do Core Value); `in4-05` baixa;
  `in4-06` média (limiar fail-closed sobre volume que cresce com o uso, sem medição que o
  sustente). Cada arquivo escreve o motivo da prioridade.
- **D-IN4-c — PRIORIDADES DOS RESIDUAIS.** `cr4-01b` média (o alarme do blocker falha aberto numa
  rodada mista); `cr4-01c` média (par de `in-01`: o dado existir e aparecer para quem opera);
  `wr4-03b` média (a convenção que já reprovou duas rodadas, agora medida por inteiro);
  `wr4-04b` média (fan-out sem teto sobre uma consulta sem `LIMIT`); `wr4-07b` baixa (saudação feia,
  sem perda de e-mail).
- **D-IN4-d — PARES DECLARADOS.** Cada arquivo que tem um par o nomeia, para que não seja fechado
  pela metade: `cr4-01c` com `in-01` e `in2-04`; `wr4-03b` com `in3-06` e `in3-04`; `wr4-04b` com
  `in3-02` (as políticas de retry) e com RETN-01 (v2, o crescimento do `notification_log`);
  `in4-04` com `in3-01`; `in4-06` com `in4-05` (o mesmo laço de tarefas futuras).
- **D-IN4-e — REQUIREMENTS.md NÃO É TOCADO.** O texto de REL-06 já cobre o COMPORTAMENTO ("resultado
  completo ou falha explícita", com a extensão de CR3-01 registrada pelo 04-27). O que a rodada 4
  acrescentou não é uma regra nova: é a OBSERVABILIDADE dela. Observabilidade é critério de sucesso
  da fase, não requisito novo, e criar um REL-07 fragmentaria a rastreabilidade de uma regra única.
- **D-IN4-f — O CRITÉRIO NOVO DO ROADMAP É ESCRITO COMO COMPORTAMENTO GARANTIDO.** Sem nomear
  identificador — nem o contador, nem o campo de erro, nem o nome da função. É a lição de C9,
  aplicada preventivamente pelo 04-27 no item 7 e mantida aqui: um critério que nomeia mecanismo faz
  o verificador procurar por um identificador que o refactor seguinte apaga.
- **D-IN4-g — A CONVENÇÃO DE WR2-06 VALE PARA OS PRÓPRIOS ARTEFATOS.** Nenhum dos 10 todos pode
  citar número de linha, inclusive o `wr4-03b`, que fala SOBRE esse padrão. É a mesma regra que o
  04-27 aplicou ao `in3-06`.
</baseline>

<inventario_de_irmaos>
## Inventário de irmãos — construções gêmeas de "achado registrado com dono"

O conserto deste plano é documental, e a construção gêmea de um todo é **outro lugar onde o mesmo
achado poderia ficar registrado sem dono**.

| Construção | Classificação | Medição / motivo |
|---|---|---|
| Os 6 Info de `04-REVIEW.md` | **corrigidos (5) e fechado (1)** | IN4-02 a IN4-06 viram arquivo; IN4-01 é registrado como fechado com a medição do 04-30 (D-IN4-a). |
| Os 5 residuais dos inventários de irmãos dos planos 04-28 a 04-33 | **corrigidos** | Cada um vira arquivo. Sem isso, o mandato desta rodada produziria classificações "fora-de-escopo-com-medição" sem dono — que é precisamente o defeito que ele existe para impedir. |
| Os 2 residuais com dono já existente (`in3-06` no 04-33, `in3-08` no 04-31) | **verificados-e-sãos** | Medido: os dois arquivos existem em `.planning/todos/pending/` e descrevem o achado. Por decisão do usuário não podem ser editados; os planos que os alcançaram os nomeiam e param aí. |
| A exclusão sem todo declarada pelo 04-29 (o `04-25-SUMMARY.md` que registra D-WR3-06-e) | **verificada-e-sã** | SUMMARY é registro datado de uma execução, não instrução viva. Nenhum arquivo é criado, e a exclusão está escrita no inventário daquele plano. |
| Os SUMMARY dos 7 planos da rodada | **verificados-e-sãos** | Cada um registra o seu inventário e os residuais que declarou; nenhum deles é o único lugar onde um achado vive, porque este plano cria o arquivo. |
| O ROADMAP e o REQUIREMENTS | **corrigido (ROADMAP) e verificada-e-sã (REQUIREMENTS)** | O critério novo entra no ROADMAP; REQUIREMENTS não muda, com o motivo em D-IN4-e. |
| `.planning/STATE.md` | **fora-de-escopo-com-medição, sem todo** | É escrito pelo fluxo de execução ao fim de cada plano, não por uma task de plano. Nenhuma task aqui o edita. |
</inventario_de_irmaos>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-27-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-05. Não explorar o codebase. -->

Molde de todo desta fase, medido em `.planning/todos/pending/in3-06-referencia-por-linha-em-mensagem-de-assercao.md`:
frontmatter YAML com `id` (igual ao nome do arquivo sem extensão), `type: todo`, `status: pending`,
`priority`, `created`, `source` (a fase, o arquivo de review e a seção), `resolves_phase: null` e
`tags` (lista). Corpo com título de nível 1 nomeando o achado, e seções em PT-BR: onde acontece, o
que acontece, por que importa, e a correção proposta. Rodapé apontando o achado original.

`.planning/ROADMAP.md`, seção "Phase 4: Confiabilidade das Integrações": linha `**Goal**`,
`**Depends on**`, `**Requirements**`, um bloco `**Success Criteria** (what must be TRUE)` com 7
itens numerados, `**Contrato de entrega**`, uma linha `**Plans**` com a soma dos planos, a lista dos
7 planos originais, e três blocos de gap closure (r1, r2, r3), cada um com um parágrafo de
introdução citando a fonte e a contagem de achados, seguido da lista de planos com caixas marcadas.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: os cinco achados Info da rodada 4 viram todos pendentes</name>
  <files>.planning/todos/pending/in4-02-justificativa-do-retry-descreve-mecanismo-inexistente.md, .planning/todos/pending/in4-03-assercao-decorativa-em-categoriaindecidivel.md, .planning/todos/pending/in4-04-leitura-de-success-divergente-entre-ramos.md, .planning/todos/pending/in4-05-literal-100-em-tasks-sem-constante.md, .planning/todos/pending/in4-06-teto-de-tasks-sem-medicao-de-volume.md</files>
  <behavior>
    - Cinco arquivos criados, cada um com o molde de frontmatter da fase e o corpo em PT-BR
    - IN4-01 NÃO vira arquivo: é registrado no SUMMARY como fechado, com a medição
    - Nenhum arquivo de `backend/` ou `frontend/` é tocado
  </behavior>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` — a seção Info inteira, IN4-01 a IN4-06
    - `.planning/todos/pending/in3-06-referencia-por-linha-em-mensagem-de-assercao.md` — o molde exato
    - `.planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md` — o molde de um todo que faz uma pergunta de DIREÇÃO
    - `backend/src/agendor.js` — os trechos de IN4-02, IN4-05 e IN4-06, para confirmar que o achado continua válido no estado deixado pelos planos 04-29 e 04-30
    - `backend/src/scheduler.js` — os dois ramos de leitura de `success` de IN4-04, no estado deixado pelos planos 04-28 e 04-31
    - `backend/test/agendor.categoriaIndecidivel.test.js` — a asserção decorativa de IN4-03
  </read_first>
  <action>
    Criar cinco arquivos em `.planning/todos/pending/`, cada um entre 45 e 90 linhas, seguindo o
    molde medido. Antes de escrever cada um, CONFERIR no arquivo real que o achado continua válido
    depois dos consertos desta rodada — quatro dos cinco vivem em arquivos que os planos 04-28 a
    04-31 editaram. Se algum já não tiver objeto, registrar isso no arquivo e no SUMMARY em vez de
    descrever um defeito que não existe mais.

    **`in4-02`, prioridade baixa.** A justificativa de manter a guarda de id FORA do callback do
    retry afirma que movê-la para dentro faria um id hostil sair três vezes pela instância
    compartilhada. Não faria: o helper invoca o callback dentro do `try`, um lançamento síncrono ali
    não traz resposta HTTP, logo não entra no ramo de rate limit e é relançado na primeira iteração;
    e a guarda lançaria ANTES da requisição, emitindo zero. A asserção de zero requisições continua
    correta e útil — só o porquê está errado, e um leitor que o confira conclui que o retry tem um
    comportamento que ele não tem. O texto se repete em `agendor.retry429.test.js`, e os dois
    precisam ser corrigidos juntos. Correção proposta: reescrever para o motivo verdadeiro (validar
    antes de entrar na política de retry mantém a guarda independente de qualquer mudança futura no
    helper) ou remover a quantificação.

    **`in4-03`, prioridade média.** Em `agendor.categoriaIndecidivel.test.js`, a asserção que
    pretende provar que nenhuma requisição saiu para uma organização indefinida é verdadeira POR
    CONSTRUÇÃO do sistema sob teste: `getStaleDeals` monta a lista de organizações únicas filtrando
    valores falsos, então uma organização ausente nunca chega à consulta de categoria. A asserção
    permaneceria verde mesmo se a guarda de id fosse removida. Escrever por que isso é pior que uma
    asserção ausente: um oráculo que não pode ficar vermelho dá falsa confiança justamente na rede
    de testes cujo valor é ser confiável. Correção proposta: chamar a função com o caminho realmente
    sob teste, ou trocar por uma asserção sobre o total de consultas do caso.

    **`in4-04`, prioridade média.** O mesmo campo `success` é lido com truthiness num ramo e com
    igualdade estrita no outro, dentro da mesma função. O plano 04-24 endureceu a leitura do canal
    parcial e escreveu por extenso por que a comparação é estrita; o ramo de retorno, que decide
    exatamente o mesmo, continua com a leitura frouxa. Hoje é inofensivo — o produtor grava booleano
    —, mas as duas leituras divergem se alguém mudar o produtor, e é a mesma variável que decide
    `'sent'` contra `'error'`, no caminho do Core Value. Correção proposta: uniformizar o ramo de
    retorno e apontar, no comentário do 04-24, que a regra vale nos dois ramos. Declarar o par com
    `in3-01`.

    **`in4-05`, prioridade baixa.** O literal `100` aparece duas vezes no laço de tarefas futuras:
    uma como tamanho de página e outra como critério de parada. Trocar o tamanho da página sem mexer
    no critério transforma toda página cheia em "última página" e devolve um conjunto PARCIAL — que
    é exatamente o desfecho que a Decisão Q2 recusa, e silenciosamente, porque o contrato "completo
    ou falha" não seria violado por exceção nenhuma. Correção proposta: uma constante única usada
    nos dois pontos. Declarar o par com `in4-06`.

    **`in4-06`, prioridade média.** O teto de páginas da consulta de tarefas futuras é um limiar
    fail-closed sobre um volume que cresce com o uso, e nenhum plano da fase mediu o volume atual. O
    comentário afirma que o teto está ordens de magnitude acima do uso real, mas o número que
    sustenta a afirmação não existe em nenhum SUMMARY. Se o volume alcançar o teto, ele passa a
    ABORTAR a rodada diária — zero notificações — e o sintoma é indistinguível, para o operador, de
    uma falha de borda. O teto é a decisão certa; o que falta é a medição. Correção proposta:
    registrar a medição atual (a função já loga o tamanho do conjunto) e, se a margem for menor que
    uma ordem de magnitude, avisar em nível de alerta ao ultrapassar metade do teto, para que o
    limite avise antes de doer. Declarar o par com `in4-05`.

    Em cada arquivo: escrever o motivo da prioridade; citar função, identificador, arquivo ou nome de
    caso de teste, NUNCA número de linha (D-IN4-g); e apontar no rodapé a seção do `04-REVIEW.md`.

    **IN4-01 não vira arquivo.** Confirmar por medição que o comentário do laço de páginas deixou de
    dizer "batches de 10" e registrar o resultado no SUMMARY, com a citação de D-WR4-04-e como
    justificativa do fechamento.
  </action>
  <verify>
    <automated>ls .planning/todos/pending/in4-*.md | wc -l | grep -q "^ *5$" &amp;&amp; grep -LE "\.js:[0-9]|linhas? [0-9]" .planning/todos/pending/in4-*.md | wc -l | grep -q "^ *5$" &amp;&amp; echo "5 todos criados e sem referência por número"</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/todos/pending/in4-*.md | wc -l` retorna **5**
    - Cada arquivo tem entre 45 e 90 linhas: `wc -l .planning/todos/pending/in4-*.md` — os cinco valores vão literais para o SUMMARY
    - Frontmatter válido nos cinco: `grep -c "^status: pending$" .planning/todos/pending/in4-*.md` retorna 1 em cada
    - Prioridades conforme D-IN4-b: `grep -h "^priority:" .planning/todos/pending/in4-*.md | sort | uniq -c` retorna **2 baixa** e **3 média**
    - Convenção de WR2-06 aplicada aos próprios artefatos: `grep -cE "\.js:[0-9]|linhas? [0-9]" .planning/todos/pending/in4-*.md` retorna **0** em cada um dos cinco
    - `in4-01` NÃO existe como arquivo: `ls .planning/todos/pending/in4-01* 2>/dev/null | wc -l` retorna **0**
    - A medição que fecha IN4-01 vai literal para o SUMMARY: `grep -c "batches de 10" backend/src/agendor.js` retorna **0**
    - `git status --porcelain backend/ frontend/` está vazio
  </acceptance_criteria>
  <done>Nenhum achado Info da rodada 4 termina a fase sem arquivo ou sem desfecho registrado.</done>
</task>

<task type="auto">
  <name>Task 2: os cinco residuais dos inventários de irmãos viram todos pendentes</name>
  <files>.planning/todos/pending/cr4-01b-limiar-de-supressao-total.md, .planning/todos/pending/cr4-01c-skipreason-invisivel-na-ui.md, .planning/todos/pending/wr4-03b-referencias-por-linha-nos-demais-arquivos-de-teste.md, .planning/todos/pending/wr4-04b-fanout-proporcional-em-resolved.md, .planning/todos/pending/wr4-07b-saudacao-com-ownername-nulo.md</files>
  <behavior>
    - Cinco arquivos criados, cada um carregando a MEDIÇÃO que justificou a exclusão do escopo
    - Nenhum todo com prioridade decidida pelo usuário é editado
    - Nenhum arquivo de `backend/` ou `frontend/` é tocado
  </behavior>
  <read_first>
    - Os inventários de irmãos dos planos `04-28-PLAN.md`, `04-30-PLAN.md`, `04-31-PLAN.md`, `04-32-PLAN.md` e `04-33-PLAN.md` — cada residual está classificado ali com a sua medição
    - Os SUMMARY dos mesmos planos, para colher o valor MEDIDO durante a execução, que pode divergir do previsto
    - `.planning/todos/pending/in3-06-*.md` — o molde
    - `.planning/todos/pending/in-01-status-pending-na-ui.md` e `.planning/todos/pending/in2-04-parcial-sent-invisivel.md` — os pares declarados de `cr4-01c`
    - `.planning/todos/pending/in3-04-convencao-de-comentario-sem-casa-e-sem-gate.md` — o par declarado de `wr4-03b`
  </read_first>
  <action>
    Criar cinco arquivos em `.planning/todos/pending/`, cada um entre 40 e 80 linhas, no mesmo molde.
    Cada um DEVE carregar a medição que justificou a exclusão — é ela que distingue este registro de
    uma nota vaga, e é o que o mandato desta rodada exige de todo item "fora-de-escopo-com-medição".

    **`cr4-01b`, prioridade média — o limiar não cobre a rodada mista.** O alarme de supressão total
    dispara quando TODOS os negócios parados do dia foram suprimidos por categoria indecidível.
    Medição: um negócio sem organização não passa pela consulta de categoria e sai da lista sem a
    marca de indecidível, então uma rodada em que a borda inteira está fora mas existe um negócio
    sem organização fica abaixo do limiar e NÃO dispara o alarme, apesar do apagão. Registrar por que
    o denominador derivado (contar só os negócios com organização) foi avaliado e rejeitado: na lista
    enriquecida o campo de organização carrega o NOME e não o identificador, então uma organização
    com identificador e sem nome faria o denominador divergir do numerador e o alarme falharia
    ABERTO por um caminho novo. Correção proposta: fazer o numerador e o denominador virem da mesma
    fonte, contando na borda quantos negócios tinham organização a consultar.

    **`cr4-01c`, prioridade média — o motivo por negócio continua invisível na interface.** Medição:
    o motivo escrito por negócio não é lido por nenhum componente do frontend, e o campo de erro da
    rodada também não; o único bloco que a interface renderiza é a lista de erros da última execução.
    Depois da rodada 4 o apagão total é visível por ali, mas a supressão de UM negócio continua sem
    superfície. Declarar os pares `in-01` e `in2-04`: os três dizem a mesma coisa em contextos
    diferentes — o dado existe no backend e não chega a quem opera. Correção proposta: exibir o
    motivo na lista de negócios do resultado da verificação e no histórico.

    **`wr4-03b`, prioridade média — as referências por número nos demais arquivos de teste.**
    Medição: a varredura da rodada 4 encontrou 45 linhas com referência por número em 11 arquivos de
    teste; 4 delas foram corrigidas pelo plano 04-33 e 1 tem dono no `in3-06`, restando 39 linhas em
    9 arquivos sem dono nenhum. Listar os 9 arquivos com a contagem de cada um, sem citar número de
    linha (D-IN4-g). Registrar por que o conserto é mais caro do que parece: cada referência precisa
    ser CONFERIDA contra o arquivo apontado antes de virar âncora, porque o próprio achado nasceu de
    referências que estavam erradas — trocar número por nome sem conferir repete o defeito com outra
    sintaxe. Declarar os pares `in3-06` e `in3-04`: este é o volume que o gate de CI daquele todo
    apanharia, e serve de caso de aceitação para ele.

    **`wr4-04b`, prioridade média — o fan-out gêmeo na rota de negócios resolvidos.** Medição: o
    handler que verifica quais negócios notificados foram atualizados dispara uma requisição por
    negócio, todas simultâneas, cada uma pela política de retry — o mesmo perfil que o plano 04-30
    consertou na consulta de categoria. A consulta que alimenta a lista não tem limite: seleciona
    todas as linhas com status de enviado agrupadas por negócio, então o número cresce com a base
    histórica. Registrar o que torna o perfil de risco DIFERENTE, e por isso adiável: o handler tem
    tratamento de erro por item, então nenhuma falha aborta a rota, e o caminho é disparado por
    clique e não pela rodada diária. Correção proposta: aplicar o mesmo lote de concorrência do
    plano 04-30 e avaliar um limite na consulta. Declarar os pares `in3-02` e RETN-01 (v2).

    **`wr4-07b`, prioridade baixa — a saudação do e-mail diário com nome nulo.** Medição: o template
    do e-mail de negócio parado INTERPOLA o nome do responsável em duas saudações, sem guarda; com
    nome ausente a interpolação não lança, e o custo é a saudação sair com a palavra `null` para o
    destinatário. Registrar por que ficou fora: interpolação não é desreferência, nenhum e-mail é
    perdido, e o ponto de chamada é outro — o do envio diário, no caminho do Core Value, onde mudar
    o texto pede um caso próprio. Correção proposta: aplicar o mesmo encadeamento de fallbacks do
    relatório individual, preferindo o nome do usuário resolvido, com um caso que assere a ausência
    das palavras `null` e `undefined` no corpo enviado.

    **Nenhum todo existente pode ser editado.** Os arquivos de `in-01`, `rel-02b`, `sec-01`,
    `wr3-07b` e `in3-01` a `in3-08` ficam byte a byte; os pares são declarados NOS ARQUIVOS NOVOS,
    por referência, nunca por edição do arquivo apontado. Nenhum valor de segredo aparece.
  </action>
  <verify>
    <automated>ls .planning/todos/pending/cr4-01b*.md .planning/todos/pending/cr4-01c*.md .planning/todos/pending/wr4-03b*.md .planning/todos/pending/wr4-04b*.md .planning/todos/pending/wr4-07b*.md | wc -l | grep -q "^ *5$" &amp;&amp; git status --porcelain backend/ frontend/ | wc -l | grep -q "^ *0$" &amp;&amp; echo "5 residuais criados, diff de codigo vazio"</automated>
  </verify>
  <acceptance_criteria>
    - Os cinco arquivos existem e cada um tem entre 40 e 80 linhas; os cinco valores vão literais para o SUMMARY
    - Prioridades conforme D-IN4-c: quatro `média` e uma `baixa`
    - Convenção de WR2-06: `grep -cE "\.js:[0-9]|linhas? [0-9]"` retorna **0** em cada um dos cinco, inclusive no `wr4-03b`, que fala sobre esse padrão
    - Os pares estão declarados: `grep -c "in-01" .planning/todos/pending/cr4-01c-*.md` retorna pelo menos 1; `grep -c "in3-04" .planning/todos/pending/wr4-03b-*.md` retorna pelo menos 1; `grep -c "in3-02" .planning/todos/pending/wr4-04b-*.md` retorna pelo menos 1
    - Nenhum todo existente foi tocado: `git status --porcelain .planning/todos/` lista apenas os 10 arquivos criados nas Tasks 1 e 2, todos como não rastreados
    - Total de pendentes: `ls .planning/todos/pending/ | wc -l` retorna **33** (23 de entrada mais 10)
    - Nenhum segredo: `grep -ric "AGENDOR_TOKEN\|smtp_pass" .planning/todos/pending/in4-*.md .planning/todos/pending/cr4-*.md .planning/todos/pending/wr4-*.md` retorna 0 em todos
    - `git status --porcelain backend/ frontend/` está vazio
  </acceptance_criteria>
  <done>Todo item classificado como "fora-de-escopo-com-medição" nos inventários de irmãos da rodada tem dono e carrega a medição que o justificou.</done>
</task>

<task type="auto">
  <name>Task 3: o ROADMAP ganha o critério de que o apagão de categoria deixou de ser silencioso</name>
  <files>.planning/ROADMAP.md</files>
  <behavior>
    - A Fase 4 ganha um oitavo success criterion, escrito como comportamento garantido e sem nomear identificador
    - Os sete critérios existentes ficam byte a byte
    - O bloco de gap closure r4, a linha de contagem de planos e a lista dos sete planos JÁ EXISTEM (escritos no planejamento) e não são reescritos
  </behavior>
  <read_first>
    - `.planning/ROADMAP.md` — a seção "Phase 4: Confiabilidade das Integrações" inteira, incluindo o bloco de gap closure r4 já presente
    - `.planning/phases/04-confiabilidade-das-integra-es/04-27-SUMMARY.md` — como o item 7 foi escrito, a decisão C9 e a armadilha de medição do grep
    - `.planning/REQUIREMENTS.md` — REL-06, para confirmar que o comportamento já está coberto e que nada precisa mudar lá
    - Os SUMMARY dos planos 04-28 a 04-31, para confirmar que o comportamento entregue é o que o critério vai declarar
  </read_first>
  <action>
    Editar `.planning/ROADMAP.md`, apenas dentro do bloco de Success Criteria da Fase 4.

    Acrescentar um oitavo item, escrito como COMPORTAMENTO GARANTIDO e sem nomear nenhum
    identificador (D-IN4-f): uma rodada em que NENHUM negócio parado pôde ser notificado porque a
    categoria da organização não pôde ser consultada deixa de ser indistinguível de um dia calmo —
    ela registra quantos negócios foram suprimidos por essa causa, marca a execução como tendo erro
    numa superfície que o painel de fato exibe, e emite uma linha de log em nível de erro; uma
    supressão PARCIAL continua sem alarme e o comportamento por negócio aprovado em CR3-01 não muda;
    uma supressão total causada por outro filtro não dispara o alarme; e a prévia do envio marca,
    negócio a negócio, quem será notificado, de modo que o número exibido antes do disparo não
    prometa mais do que o envio entrega.

    Fechar com a mesma nota em itálico dos itens 4 e 7, apontando a decisão do usuário de 2026-08-05
    e registrando o motivo de o critério não nomear mecanismo: um critério que nomeia identificador
    faz o verificador procurar por um símbolo que o refactor seguinte apaga (lição de C9).

    **Nada mais pode mudar.** Os sete critérios existentes, inclusive a redação aprovada em C9 do
    item 4 e o item 7 do 04-27, ficam byte a byte. O bloco de gap closure r4, a linha de contagem de
    planos e as quatro listas de planos ficam byte a byte — foram escritos no planejamento desta
    rodada, e as caixas são marcadas pelo fluxo de execução, não por esta task. Nenhuma outra fase do
    ROADMAP é tocada. `.planning/REQUIREMENTS.md` não é tocado (D-IN4-e) — registrar essa decisão e o
    seu motivo no SUMMARY.

    **Armadilha de medição, a mesma que o 04-27 documentou:** um `grep -c` por qualquer palavra do
    critério novo vai contar também as ocorrências nas descrições dos planos das quatro listas.
    Medir o valor real, registrar os dois números no SUMMARY, e não forçar o número previsto.
  </action>
  <verify>
    <automated>git diff --numstat .planning/ROADMAP.md &amp;&amp; git status --porcelain backend/ frontend/ .planning/REQUIREMENTS.md | wc -l | grep -q "^ *0$" &amp;&amp; echo "ROADMAP atualizado, codigo e REQUIREMENTS intactos"</automated>
  </verify>
  <acceptance_criteria>
    - O bloco de Success Criteria da Fase 4 passa a ter **8** itens numerados; a contagem medida vai literal para o SUMMARY
    - O critério novo não nomeia identificador: `grep -c "skippedCategoriaIndecidivel\|seraNotificado\|results.error" .planning/ROADMAP.md` retorna **0**
    - Somente adições: `git diff --numstat .planning/ROADMAP.md` mostra **0** remoções; o valor medido vai literal para o SUMMARY
    - As listas de planos não foram tocadas: `git diff .planning/ROADMAP.md | grep -c "PLAN.md"` retorna **0**
    - A tabela de rastreabilidade não foi tocada: `git diff .planning/ROADMAP.md | grep -c "| Phase"` retorna **0**
    - `git status --porcelain .planning/REQUIREMENTS.md` está vazio
    - `git status --porcelain backend/ frontend/` está vazio
    - Nenhum segredo: `git diff .planning/ROADMAP.md | grep -ci "AGENDOR_TOKEN"` retorna **0**
  </acceptance_criteria>
  <done>O ROADMAP declara, como comportamento garantido, que o apagão de categoria deixou de ser silencioso.</done>
</task>

</tasks>

<rastreabilidade>
| Requisito | Achado | Tarefa | Evidência |
|---|---|---|---|
| REL-03 | CR4-01 (critério) | Task 3 (a) | o critério 8 declara que a supressão total deixou de ser silenciosa |
| REL-06 | IN4-05, IN4-06 | Task 1 | o contrato "completo ou falha explícita" ganha dois residuais rastreáveis |
| REL-03 e REL-06 (rastreabilidade) | IN4-02 a IN4-06 e os 5 residuais | Tasks 1 e 2 | 10 arquivos em `.planning/todos/pending/` |
| — | IN4-01 | Task 1 | fechado pelo 04-30, com a medição registrada em vez de arquivo criado |
</rastreabilidade>

<matriz_de_teste>
| Cenário | Tipo | Arquivo | Setup → Ação → Esperado | Regressão protegida |
|---|---|---|---|---|
| Todos criados | gate documental | `.planning/todos/pending/` | 10 arquivos novos, 33 no total | achado terminando a fase sem dono |
| Convenção de comentário nos artefatos | gate documental | os 10 arquivos | zero referências por número | reproduzir o defeito no texto que o denuncia |
| Todos com prioridade do usuário | regressão | `.planning/todos/` | nenhum arquivo existente no diff | reabrir decisão do usuário por edição acidental |
| Código intacto | gate | `backend/`, `frontend/` | `git status --porcelain` vazio nas três tasks | mudança de código se disfarçando de documentação |
| REQUIREMENTS intacto | regressão | `.planning/REQUIREMENTS.md` | `git status --porcelain` vazio | fragmentar a rastreabilidade de uma regra única |
</matriz_de_teste>

<matriz_de_risco>
| # | Risco | Prob. | Impacto | Mitigação | Sinal | Rollback |
|---|---|---|---|---|---|---|
| R4-37 | Descrever um achado Info que os consertos da rodada já eliminaram | alta | médio | A ação exige conferir cada achado no arquivo real antes de escrever, e registrar o desfecho se já não houver objeto | todo descrevendo defeito inexistente | corrigir ou fechar o arquivo |
| R4-38 | Criar arquivo para IN4-01, que já não tem objeto | média | baixo | D-IN4-a; critério de zero arquivos com esse prefixo | arquivo presente | remover e registrar o fechamento |
| R4-39 | Editar um todo com prioridade decidida pelo usuário ao declarar um par | alta | alto | D-IN4-d exige declarar o par NO ARQUIVO NOVO; critério de `git status` listando apenas arquivos não rastreados | todo existente no diff | revert do arquivo |
| R4-40 | Escrever o critério do ROADMAP nomeando identificador e criar um critério que o refactor seguinte apaga | média | médio | D-IN4-f; critério de zero ocorrências dos identificadores | grep maior que 0 | reescrever o item |
| R4-41 | Forçar o número previsto num grep de contagem que também apanha as descrições dos planos | alta | baixo | A ação exige medir e registrar os dois números; precedente do 04-27 | número do plano repetido sem medição | registrar o valor medido |
| R4-42 | Registrar o residual só no SUMMARY e não como arquivo | média | alto | Tasks 1 e 2 criam os arquivos; o inventário deste plano trata o SUMMARY como registro, não como dono | arquivo ausente | criar o arquivo |
</matriz_de_risco>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artefato de planejamento → decisão do usuário | Um todo editado reabre, em silêncio, uma prioridade que o usuário decidiu |
| Documentação → código | Uma task documental pode arrastar mudança de comportamento |
| Artefato de planejamento → segredos | Um todo que descreve a borda pode acabar citando token ou senha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-34-01 | Repudiation | Achado da rodada 4 terminar a fase sem dono e sem arquivo | mitigate | 10 todos criados; IN4-01 fechado com medição; critérios de contagem |
| T-04-34-02 | Tampering | Mudança de código se disfarçando de documentação | mitigate | `git status --porcelain backend/ frontend/` vazio como critério nas três tasks |
| T-04-34-03 | Tampering (decisão do usuário) | Edição acidental de todo com prioridade decidida | mitigate | D-IN4-d; critério de `git status .planning/todos/` listando apenas arquivos não rastreados |
| T-04-34-04 | Information Disclosure | Todo ou entrada de ROADMAP citando `AGENDOR_TOKEN` ou senha de SMTP | mitigate | Critérios de grep em todos os arquivos criados e no diff do ROADMAP; SEC-01 permanece aberto e não é declarado resolvido |
| T-04-34-05 | Repudiation | Critério de ROADMAP que nomeia mecanismo e deixa de valer quando o mecanismo muda | mitigate | D-IN4-f; critério de zero identificadores no arquivo |
| T-04-34-06 | Repudiation | O `04-25-SUMMARY.md` continuar registrando uma justificativa que WR4-01 desmentiu | accept | Registro datado de execução, não instrução viva; exclusão declarada no inventário do 04-29 |
| T-04-34-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote; nenhum arquivo de código é tocado |
</threat_model>

<dod>
- [ ] 10 todos criados: 5 dos Info e 5 dos residuais dos inventários de irmãos
- [ ] IN4-01 registrado como FECHADO com a medição, sem arquivo
- [ ] Prioridades conforme D-IN4-b e D-IN4-c, com o motivo escrito em cada arquivo
- [ ] Pares declarados nos arquivos NOVOS, sem editar nenhum arquivo existente
- [ ] Convenção de WR2-06 aplicada aos próprios artefatos: zero referências por número nos 10
- [ ] ROADMAP com o critério 8, a contagem de planos e a lista da r4
- [ ] `.planning/REQUIREMENTS.md`, `backend/` e `frontend/` intactos
- [ ] SEC-01 não é declarado resolvido e nenhum valor de segredo aparece
</dod>

<verification>
- `ls .planning/todos/pending/ | wc -l` retorna 33
- `grep -cE "\.js:[0-9]|linhas? [0-9]"` retorna 0 nos 10 arquivos criados
- `git status --porcelain backend/ frontend/ .planning/REQUIREMENTS.md` está vazio
- `git status --porcelain .planning/todos/` lista apenas os 10 arquivos novos
- `git diff .planning/ROADMAP.md | grep -c "| Phase"` retorna 0
</verification>

<success_criteria>
- Nenhum achado da rodada 4 pode terminar a fase sem dono
- Nenhum item classificado como fora de escopo nos inventários de irmãos pode ficar sem a medição que o justificou
- Nenhuma decisão de prioridade do usuário pode ser alterada
- Nenhuma linha de código pode mudar
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-34-SUMMARY.md` when done.
Commit: `docs(04-34): IN4-02..IN4-06 e os residuais do inventário de irmãos como todos pendentes`
Rollback: revert do commit. Último plano da gap closure r4; depende dos SUMMARY dos planos 04-28 a 04-33 para colher os valores medidos.
</output>
