---
phase: 04-confiabilidade-das-integra-es
plan: 27
type: execute
wave: 20
depends_on: ["04-26"]
files_modified:
  - .planning/todos/pending/in3-01-coluna-error-perde-destinatario.md
  - .planning/todos/pending/in3-02-sendmailwithretry-sem-tentativa.md
  - .planning/todos/pending/in3-03-cache-somente-escrita.md
  - .planning/todos/pending/in3-04-convencao-de-comentario-sem-casa-e-sem-gate.md
  - .planning/todos/pending/in3-05-stoptasks-duplicado.md
  - .planning/todos/pending/in3-06-referencia-por-linha-em-mensagem-de-assercao.md
  - .planning/todos/pending/in3-07-transporte-por-deal.md
  - .planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05, REL-06]

must_haves:
  truths:
    - "Os 8 achados Info da rodada 3 existem como todos pendentes rastreáveis, com origem, evidência e correção proposta"
    - "Nenhum achado Info virou plano — a decisão do usuário sobre o escopo está respeitada"
    - "O ROADMAP descreve o COMPORTAMENTO GARANTIDO da exclusão por categoria, não o mecanismo, seguindo o precedente da decisão C9"
    - "REQUIREMENTS registra o contrato fail-safe da categoria dentro de REL-06"
  artifacts:
    - path: ".planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md"
      provides: "Registro do segundo filtro de elegibilidade que falha aberto, com a pergunta de direção para a categoria inteira"
      min_lines: 40
    - path: ".planning/ROADMAP.md"
      provides: "Success Criteria da Fase 4 com o critério do fail-safe de categoria"
      contains: "indecidível"
  key_links:
    - from: ".planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md"
      to: ".planning/todos/pending/"
      via: "cada achado Info da rodada 3 vira um arquivo rastreável"
      pattern: "in3-0"
---

<objective>
Fechar o **resíduo documental da rodada 3**: registrar os 8 achados Info como todos pendentes e atualizar os
dois artefatos de requisito para descrever o comportamento que esta rodada passou a garantir.

Purpose:

Duas lições desta fase justificam este plano existir em vez de virar um parágrafo de SUMMARY:

1. **Achado sem arquivo não sobrevive à fase.** O precedente é o 04-09 e o 04-18, que registraram IN-01..IN-04
   e IN2-01..IN2-04 como todos — e foi por isso que `in2-02` pôde ser fechado no 04-26, com evidência.
2. **Critério que nomeia mecanismo falha da pior forma.** É a decisão **C9**, tomada pelo usuário nesta
   fase: um Success Criteria que descrevia "`orgCategoryCache` é invalidado a cada execução" sobreviveu ao
   mecanismo que descrevia, e o verificador procuraria por um identificador inexistente. A rodada 3 acabou de
   introduzir um comportamento novo (categoria indecidível) e o ROADMAP ainda não o menciona.

Output: 8 arquivos de todo, o Success Criteria da Fase 4 com um item novo, e uma nota em REQUIREMENTS.

## Escopo travado pelo usuário

**Os achados Info viram todos pendentes, NÃO planos.** Nenhuma linha de `backend/` muda neste plano — é
critério de aceite. Também não se editam os todos com prioridade já decidida pelo usuário: `in-01` (média,
C10), `rel-02b` (alta / pré-go-live, C11) e `sec-01` (aberto, C8) ficam exatamente como estão.
</objective>

<baseline>
## Fatos medidos (2026-08-05)

| Item | Valor medido | Como |
|---|---|---|
| Achados Info da rodada 3 | 8 (IN3-01 a IN3-08) | `04-REVIEW.md` seção Info |
| Todos pendentes | 15 arquivos antes do 04-26, que fecha o `in2-02` | `ls .planning/todos/pending/` |
| Molde de todo | frontmatter com `id`, `type`, `status`, `priority`, `created`, `source`, `resolves_phase`, `tags`; corpo com **Onde**, **O que acontece**, **Por que isso importa**, **Correção proposta** | `.planning/todos/pending/in2-02-relogio-falso-em-before.md` (74 a 82 linhas) |
| Success Criteria da Fase 4 | 6 itens; o item 4 traz a redação de C9 e a explicação de por que foi reescrito | `.planning/ROADMAP.md` |
| REL-06 em REQUIREMENTS | fala apenas de tarefas futuras ("resultado completo ou falha explícita, nunca proteção parcial silenciosa") | `.planning/REQUIREMENTS.md` |

## Decisões travadas (não reabrir)

- **D-IN3-a:** um arquivo por achado, no molde do `in2-02`, entre 40 e 90 linhas. Cada um cita a âncora
  nomeada (função, identificador, arquivo, caso de teste) e **nunca** número de linha — a convenção fechada
  no 04-18 vale também para os artefatos de planejamento.
- **D-IN3-b:** prioridades atribuídas nesta rodada, com o motivo escrito em cada arquivo:
  `in3-01` média · `in3-02` baixa · `in3-03` baixa · `in3-04` média · `in3-05` baixa · `in3-06` baixa ·
  `in3-07` média · `in3-08` **alta, candidato a promoção** (é o segundo filtro de elegibilidade que falha
  aberto, e o primeiro foi o bloqueante desta rodada).
- **D-IN3-c:** `in3-02` declara explicitamente que fecha junto com `in2-01` — mesma causa, mesmo conserto,
  em duas funções irmãs. Não duplicar o conteúdo daquele arquivo: referenciá-lo.
- **D-IN3-d:** o ROADMAP ganha um Success Criteria **novo**, e o item 4 (C9) **não** é reescrito. Substituir
  uma redação aprovada pelo usuário sem que ele peça é o oposto do que C9 estabeleceu.
- **D-IN3-e:** REQUIREMENTS recebe uma **nota** em REL-06, não um requisito novo: a regra é a mesma
  ("completo ou falha explícita"), aplicada a uma segunda consulta. Criar um REL-07 aqui exigiria remapear a
  tabela de rastreabilidade sem ganho.
</baseline>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/todos/pending/in2-04-parcial-sent-invisivel.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-05. Não explorar o codebase. -->

Molde de todo (`.planning/todos/pending/in2-02-relogio-falso-em-before.md`): frontmatter YAML com
`id` (igual ao nome do arquivo sem extensão), `type: todo`, `status: pending`, `priority`, `created`,
`source` (fase e seção do review), `resolves_phase: null`, `tags` (lista); corpo com título `# IN3-0X — …`
e as seções **Onde**, **O que acontece**, **Por que isso importa**, **Correção proposta**.

`.planning/ROADMAP.md`, `### Phase 4: Confiabilidade das Integrações`: linha `**Goal**`, `**Depends on**`,
`**Requirements**`, bloco `**Success Criteria** (what must be TRUE)` numerado de 1 a 6, `**Contrato de
entrega**`, `**Plans**` e as listas de planos por rodada de gap closure.

`.planning/REQUIREMENTS.md`, seção `### Confiabilidade das Integrações`: REL-01 a REL-06, cada um numa linha
de checklist, com as notas de decisão entre parênteses ao fim do texto — o padrão que REL-04 e REL-05 já usam.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: os 4 achados Info que tocam comportamento viram todos</name>
  <files>.planning/todos/pending/in3-01-coluna-error-perde-destinatario.md, .planning/todos/pending/in3-02-sendmailwithretry-sem-tentativa.md, .planning/todos/pending/in3-07-transporte-por-deal.md, .planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seções IN3-01, IN3-02, IN3-07 e IN3-08
    - `.planning/todos/pending/in2-04-parcial-sent-invisivel.md` (o molde, incluindo como um todo declara "candidato a promoção")
    - `.planning/todos/pending/in2-01-fetchwithretry-sem-tentativa.md` (o irmão de IN3-02, que deve ser referenciado e não duplicado)
    - `backend/src/scheduler.js`, `backend/src/emailer.js`, `backend/src/agendor.js` — os trechos citados pelos quatro achados
  </read_first>
  <action>
    Criar quatro arquivos em `.planning/todos/pending/`, no molde do `in2-02`, em PT-BR, com âncoras nomeadas
    e sem números de linha.

    **`in3-01-coluna-error-perde-destinatario.md` (prioridade média).** O ramo de EXCEÇÃO de `runCheck`
    grava na coluna `error` apenas `err.message`, enquanto o ramo de retorno agrega os erros por
    destinatário — e os parciais, já lidos e validados duas instruções acima, carregam `{ to, error }` de
    quem falhou. Quando o parcial confirma e a linha vira `'sent'`, essa coluna é o **único** vestígio de
    quem não recebeu (é a premissa do `in2-04`), e ela perde justamente a identificação do destinatário.
    Correção proposta: compor as duas fontes. Registrar que deve ser tratado junto de `in2-04`.

    **`in3-02-sendmailwithretry-sem-tentativa.md` (prioridade baixa).** Com `retries = 0` o `for` de
    `sendMailWithRetry` não itera e a função cai no fim **sem `return`**; os dois call-sites desestruturam o
    resultado, então o sintoma seria um `TypeError` de desestruturação que não aponta para a causa. Não
    alcançável hoje (nenhum chamador passa o parâmetro), mas ele é público na assinatura. Declarar que é o
    irmão exato de `in2-01` (que nomeia `fetchWithRetry`), com a mesma causa e o mesmo conserto — guarda no
    topo ou `do/while` — e que os dois devem fechar juntos, **sem** duplicar aqui o conteúdo daquele arquivo.

    **`in3-07-transporte-por-deal.md` (prioridade média).** O ganho de WR2-05 não atravessa negócios:
    `sendStaleNotification` cria um transporte na entrada e é chamada **uma vez por negócio**, então numa
    rodada com N negócios e a sessão SMTP morta o sistema volta a pagar o ciclo de 3 s + 6 s por negócio. E o
    caso (3) de `emailer.transporteVivo.test.js` chama de "rodada" o que é um **negócio** — a mesma palavra
    com dois significados nos dois lados da fronteira, sendo que em `scheduler.js` "rodada" designa a
    execução inteira de `runCheck`. Duas ações propostas: renomear o caso para falar em negócio, e decidir se
    vale reusar o transporte entre negócios da mesma rodada, com o `socketTimeout` de 30 s (D-02) como teto
    de vida. Registrar que a asserção em si está correta — só o vocabulário engana.

    **`in3-08-filtros-de-elegibilidade-fail-open.md` (prioridade ALTA, candidato a promoção).**
    `shouldNotifyOwner` transforma funil ausente em string vazia, que não está em `NO_OWNER_NOTIFY_FUNNELS`
    → **notifica**. `getStaleDeals` monta esse campo a partir do funil do estágio do negócio, então qualquer
    payload sem funil reabilita a notificação que a regra de negócio manda suprimir; o mesmo vale para um
    rename do funil. Os dois comportamentos **estão** pinados como quirks conhecidos em
    `agendor.funnel.test.js`, e é por isso que o review classificou como Info. O que falta é a avaliação de
    risco: **este é o segundo filtro de elegibilidade que falha aberto**, e o primeiro foi o bloqueante desta
    rodada (CR3-01). Registrar a pergunta de direção como o item central — fail-open ou fail-safe **como
    categoria**, para todos os filtros de elegibilidade —, com a rota "indecidível" aprovada pelo usuário em
    2026-08-05 como precedente disponível. Declarar explicitamente que este todo é candidato a virar
    requisito da fase seguinte.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor &amp;&amp; ls .planning/todos/pending/in3-01-coluna-error-perde-destinatario.md .planning/todos/pending/in3-02-sendmailwithretry-sem-tentativa.md .planning/todos/pending/in3-07-transporte-por-deal.md .planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos existem e cada um tem entre 40 e 90 linhas (`wc -l`)
    - Cada um tem frontmatter com `id`, `status: pending`, `priority`, `source` citando a rodada 3 do review, e `tags` incluindo `phase-4-carryover`
    - As prioridades seguem D-IN3-b: `média` em `in3-01`, `baixa` em `in3-02`, `média` em `in3-07` e `alta` em `in3-08`
    - `in3-08` declara a candidatura: `grep -ci "candidato a promo" .planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md` retorna pelo menos 1
    - `in3-02` referencia o irmão: `grep -c "in2-01" .planning/todos/pending/in3-02-sendmailwithretry-sem-tentativa.md` retorna pelo menos 1
    - Nenhuma referência por número de linha: `grep -cE "\.js:[0-9]|linhas? [0-9]"` retorna 0 nos quatro arquivos
    - `git status --porcelain backend/` está vazio
  </acceptance_criteria>
  <done>Os quatro achados Info que tocam comportamento existem como registro rastreável, com prioridade e correção proposta.</done>
</task>

<task type="auto">
  <name>Task 2: os 4 achados Info de convenção e higiene viram todos</name>
  <files>.planning/todos/pending/in3-03-cache-somente-escrita.md, .planning/todos/pending/in3-04-convencao-de-comentario-sem-casa-e-sem-gate.md, .planning/todos/pending/in3-05-stoptasks-duplicado.md, .planning/todos/pending/in3-06-referencia-por-linha-em-mensagem-de-assercao.md</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seções IN3-03 a IN3-06, mais o item 4 da seção "Avaliação dos desvios deliberados"
    - `.planning/todos/pending/in2-02-relogio-falso-em-before.md` (o molde)
    - `backend/src/agendor.js` (o topo do arquivo e a fase de consulta de categorias) e `backend/src/scheduler.js` (`scheduleTask` e `stopTasks`)
    - `./CLAUDE.md` seção "Comments"
    - `.github/workflows/ci.yml`
  </read_first>
  <action>
    Criar quatro arquivos em `.planning/todos/pending/`, no mesmo molde, em PT-BR, com âncoras nomeadas.

    **`in3-03-cache-somente-escrita.md` (baixa).** O `Map` por execução é somente-escrita: `uniqueOrgIds` já
    é o resultado de um `Set`, então cada id é passado a `getOrgCategory` exatamente uma vez e a consulta ao
    cache nunca acerta. O `Map` cumpre bem o seu propósito real — ser o seam que eliminou o estado de módulo
    (CR2-01) — mas o comentário descreve um mecanismo de dedup que na verdade mora no `Set`. Risco: alguém
    "otimizar" o `Set` fora, confiando num cache que nunca acerta. Correção proposta: ajustar o comentário
    para dizer que o `Map` existe para **escopo**, não para dedup, e que a dedup mora no `Set`.

    **`in3-04-convencao-de-comentario-sem-casa-e-sem-gate.md` (média).** A convenção repo-wide fechada no
    WR2-06 (referência por âncora nomeada, nunca por número de linha) mora nas duas primeiras linhas de um
    módulo de cliente HTTP — que não é onde alguém procura uma regra de estilo do repositório. A seção
    "Comments" do `CLAUDE.md`, o lugar canônico e que o próprio agente lê antes de editar, não foi
    atualizada; e o `grep` detector descrito no `04-18-SUMMARY.md` continua sendo um comando de SUMMARY, não
    um step do workflow de CI. Correção proposta: mover as duas linhas para o `CLAUDE.md` e acrescentar o
    `grep` como step do CI (roda em menos de um segundo). Registrar que uma convenção sem casa e sem gate
    volta a se degradar no próximo commit longo.

    **`in3-05-stoptasks-duplicado.md` (baixa).** `stopTasks()` duplica o bloco de parada do topo de
    `scheduleTask()` — oito linhas idênticas em dois lugares. Se um terceiro temporizador for acrescentado, é
    o tipo de duplicação que fica meio-atualizada e deixa um `cron` órfão rodando depois do graceful
    shutdown. Correção proposta: `scheduleTask` passar a chamar `stopTasks` no início.

    **`in3-06-referencia-por-linha-em-mensagem-de-assercao.md` (baixa).** As duas referências residuais em
    `scheduler.resilience.test.js` apontam para uma linha errada desde que nasceram — o guard está uma linha
    adiante. A decisão de **não** renomear o **caso** está registrada e é mantida: o nome é citado pelo
    `04-RESEARCH.md`, e renomear mexe num identificador de oráculo. O que muda é o veredito sobre a
    **mensagem de asserção**: o review discordou em parte e tem razão — mensagem de `assert` não é oráculo,
    não é citada por nenhum artefato, e convertê-la para âncora nomeada não altera o que o teste prova.
    Correção proposta: converter apenas a mensagem, deixando o nome do caso como está, com o motivo
    registrado.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor &amp;&amp; ls .planning/todos/pending/in3-0*.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos existem e cada um tem entre 40 e 90 linhas
    - As prioridades seguem D-IN3-b: `baixa` em `in3-03`, `média` em `in3-04`, `baixa` em `in3-05` e `baixa` em `in3-06`
    - `in3-06` registra a decisão preservada: `grep -ci "nome do caso" .planning/todos/pending/in3-06-referencia-por-linha-em-mensagem-de-assercao.md` retorna pelo menos 1
    - Nenhuma referência por número de linha nos quatro arquivos (`grep -cE "\.js:[0-9]|linhas? [0-9]"` retorna 0), inclusive no `in3-06`, que fala **sobre** esse padrão sem reproduzi-lo
    - O total de achados Info registrados é 8: `ls .planning/todos/pending/in3-0*.md | wc -l` imprime `8`
    - `git status --porcelain backend/` está vazio
  </acceptance_criteria>
  <done>Os oito achados Info da rodada 3 existem como registro rastreável, e nenhum deles virou plano.</done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP e REQUIREMENTS descrevem o comportamento garantido pela rodada 3</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — a seção inteira da Fase 4, incluindo o item 4 dos Success Criteria (redação de C9) e as listas de planos por rodada
    - `.planning/REQUIREMENTS.md` — a seção "Confiabilidade das Integrações" e a tabela de rastreabilidade
    - `.planning/phases/04-confiabilidade-das-integra-es/04-19-SUMMARY.md` e `04-20-SUMMARY.md` (o comportamento efetivamente entregue, medido)
    - `.planning/STATE.md` — a decisão do usuário sobre CR3-01, para copiar a formulação sem reinterpretá-la
  </read_first>
  <action>
    **(a) `.planning/ROADMAP.md` — Success Criteria da Fase 4.** Acrescentar um item **7**, escrito como
    comportamento garantido e não como mecanismo (o precedente é C9): quando a consulta de categoria da
    organização falha mesmo depois do retry da borda, o negócio é marcado **indecidível** e fica **fora de
    todo envio de e-mail** — notificação diária e relatório individual do responsável —, permanecendo visível
    no painel, no relatório consolidado do admin e no snapshot semanal; e a rodada **não** é abortada por
    causa disso. Citar `CR3-01` e a decisão do usuário de 2026-08-05. **Não** reescrever os itens 1 a 6.

    Atualizar também a linha `**Plans**` da Fase 4 para refletir o total após a rodada 3 e marcar como
    concluídos os planos desta rodada que já tiverem SUMMARY, no mesmo formato das listas das rodadas
    anteriores.

    **(b) `.planning/REQUIREMENTS.md` — REL-06.** Acrescentar ao texto de REL-06 uma nota, no padrão de
    parênteses que REL-04 e REL-05 já usam, dizendo que a mesma regra — resultado completo ou falha
    explícita, nunca proteção parcial silenciosa — passou a valer para a **consulta de categoria da
    organização** a partir de CR3-01 (2026-08-05), com a rota aprovada pelo usuário: retry na borda e, se
    ainda assim falhar, negócio indecidível, fora do envio e dentro do painel. Não criar requisito novo e não
    mexer na tabela de rastreabilidade.

    **Nada de código.** Nenhum arquivo de `backend/` pode aparecer no diff deste plano.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor &amp;&amp; grep -c "indecidível" .planning/ROADMAP.md &amp;&amp; grep -c "CR3-01" .planning/REQUIREMENTS.md &amp;&amp; test -z "$(git status --porcelain backend/)" &amp;&amp; echo "sem diff em backend/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indecidível" .planning/ROADMAP.md` retorna pelo menos 1, e a ocorrência está dentro do bloco de Success Criteria da Fase 4, como item 7
    - Os itens 1 a 6 dos Success Criteria da Fase 4 não mudaram: `git diff .planning/ROADMAP.md | grep -E "^-[^-]" | grep -c "orgCategoryCache"` retorna 0
    - `grep -c "CR3-01" .planning/REQUIREMENTS.md` retorna pelo menos 1, na linha de REL-06
    - A tabela de rastreabilidade de REQUIREMENTS não mudou: `git diff .planning/REQUIREMENTS.md | grep -E "^[+-][^+-]" | grep -c "| Phase"` retorna 0
    - `git status --porcelain backend/` está vazio
    - `cd backend && npm run lint` sai 0 e `node --test` sai 0 (nenhuma regressão pôde ser introduzida — este plano não toca código, e a execução é a prova)
  </acceptance_criteria>
  <done>Quem for verificar a Fase 4 encontra o comportamento novo descrito no ROADMAP e em REQUIREMENTS, sem precisar abrir um SUMMARY.</done>
</task>

</tasks>

<rastreabilidade>
| Requisito | Achado | Tarefa | Evidência |
|---|---|---|---|
| REL-05 | IN3-01 | Task 1 | todo com a composição das duas fontes de erro |
| REL-02 | IN3-02, IN3-07 | Task 1 | todos com a causa comum a `in2-01` e a pergunta do reuso de transporte |
| REL-06 | IN3-08 | Task 1 e Task 3 | todo de prioridade alta e nota em REQUIREMENTS |
| REL-04 | IN3-03 | Task 2 | todo sobre o comentário do cache por execução |
| REL-01, REL-03 | IN3-04, IN3-05, IN3-06 | Task 2 | todos de convenção, duplicação e mensagem de asserção |
| REL-01 a REL-06 | CR3-01 | Task 3 | Success Criteria 7 descrevendo comportamento garantido |
</rastreabilidade>

<matriz_de_teste>
| Cenário | Tipo | Arquivo | Setup → Ação → Esperado | Regressão protegida |
|---|---|---|---|---|
| Registro dos 8 Info | documental | `.planning/todos/pending/in3-0*.md` | 8 arquivos, prioridades de D-IN3-b | achado perdido ao fim da fase |
| Redação do ROADMAP | documental | `.planning/ROADMAP.md` | item 7 descrevendo comportamento | critério que nomeia mecanismo (a lição de C9) |
| Suíte intacta | regressão | toda | `node --test` verde | mudança de código disfarçada de documentação |
</matriz_de_teste>

<matriz_de_risco>
| # | Risco | Prob. | Impacto | Mitigação | Sinal | Rollback |
|---|---|---|---|---|---|---|
| R3-36 | Transformar um achado Info em conserto de código, contrariando o escopo travado | média | médio | Critério exige `git status --porcelain backend/` vazio nas três tasks | qualquer arquivo de `backend/` no diff | revert do commit |
| R3-37 | Reescrever o Success Criteria 4 (redação aprovada em C9) em vez de acrescentar o item 7 | média | alto | D-IN3-d; critério verifica que nenhuma linha com `orgCategoryCache` foi removida | linha removida no diff do ROADMAP | revert do commit |
| R3-38 | Editar `in-01`, `rel-02b` ou `sec-01`, mexendo em prioridade decidida pelo usuário | baixa | alto | Escopo declarado no objetivo; os arquivos não estão em `files_modified` | qualquer um deles no diff | revert do commit |
| R3-39 | Todos genéricos demais, sem evidência, que a próxima fase não consegue usar | média | médio | Molde obrigatório com as quatro seções e limite de 40 a 90 linhas | arquivo com menos de 40 linhas | reescrever o arquivo |
</matriz_de_risco>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Review → planejamento | Achado que não vira arquivo desaparece com a fase |
| ROADMAP → verificador da fase | Critério que descreve mecanismo inexistente reprova por engano |
| Todos → fases seguintes | Prioridade errada esconde um risco de elegibilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27-01 | Repudiation | Achado Info sem registro rastreável | mitigate | Oito arquivos com origem, evidência e correção proposta |
| T-04-27-02 | Tampering (do critério) | Success Criteria descrevendo mecanismo em vez de comportamento | mitigate | Item 7 escrito como comportamento garantido; itens 1 a 6 preservados |
| T-04-27-03 | Elevation of Privilege (de escopo) | Achado Info virando conserto de código sem aprovação | mitigate | `git status --porcelain backend/` vazio como critério nas três tasks |
| T-04-27-04 | Information Disclosure | Artefato exibindo o valor do `AGENDOR_TOKEN` ao citar `sec-01` | mitigate | Nenhum todo desta rodada cita `sec-01`; o valor do token não aparece em lugar nenhum |
| T-04-27-05 | Repudiation | Segundo filtro de elegibilidade fail-open ficando sem dono | accept com registro | `in3-08` com prioridade alta e declarado candidato a promoção na fase seguinte |
| T-04-27-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote; nenhum arquivo de `backend/` tocado |
</threat_model>

<dod>
- [ ] 8 arquivos `in3-*.md` em `.planning/todos/pending/`, entre 40 e 90 linhas cada
- [ ] Prioridades conforme D-IN3-b, com o motivo escrito em cada arquivo
- [ ] Nenhuma referência por número de linha em nenhum dos 8
- [ ] `in-01`, `rel-02b` e `sec-01` não editados
- [ ] Success Criteria 7 acrescentado ao ROADMAP; itens 1 a 6 intactos
- [ ] Nota de CR3-01 em REL-06, sem requisito novo e sem mexer na rastreabilidade
- [ ] `git status --porcelain backend/` vazio; suíte verde e lint exit 0
- [ ] Nenhum artefato exibe o valor do `AGENDOR_TOKEN` nem declara `sec-01` resolvido
</dod>

<verification>
- `ls .planning/todos/pending/in3-0*.md | wc -l` imprime `8`
- `grep -c "indecidível" .planning/ROADMAP.md` é maior que 0
- `grep -c "CR3-01" .planning/REQUIREMENTS.md` é maior que 0
- `git status --porcelain backend/` está vazio
- `cd backend && node --test` e `npm run lint` saem 0
</verification>

<success_criteria>
- Nenhum achado da rodada 3 fica sem destino: blocker e warnings viraram planos, Info viraram todos
- O ROADMAP e REQUIREMENTS descrevem o comportamento garantido, não o mecanismo
- A fase 4 pode ser verificada sem depender da leitura de SUMMARYs
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-27-SUMMARY.md` when done.
Commit: `docs(04-27): achados Info da rodada 3 como todos e critério do fail-safe de categoria no ROADMAP`
Rollback: revert do commit. Nenhum comportamento de produção depende deste plano.
</output>
