---
phase: 04-confiabilidade-das-integra-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/scheduler.js
  - backend/test/scheduler.resilience.test.js
autonomous: false
requirements: [REL-03]

must_haves:
  truths:
    - "Uma falha na borda HTTP durante runCheck é registrada em results.error e NÃO relançada (D-04: somente caracterização, nenhuma mudança de comportamento)"
    - "O lock isRunning é liberado mesmo quando a execução falha"
    - "A execução seguinte a uma falha roda normalmente (o guard de scheduler.js:27 não recusa)"
    - "Uma chamada concorrente a runCheck devolve { skipped: true } sem executar"
    - "runWeeklySummary resolve sem lançar quando a borda falha"
  artifacts:
    - path: "backend/test/scheduler.resilience.test.js"
      provides: "Caracterização dos 5 invariantes de resiliência do scheduler (REL-03)"
      contains: "runCheck"
      min_lines: 120
    - path: "backend/src/scheduler.js"
      provides: "Seam aditivo de teste para runWeeklySummary (não exportada hoje)"
      contains: "module.exports.runWeeklySummary"
  key_links:
    - from: "backend/test/scheduler.resilience.test.js"
      to: "backend/src/scheduler.js"
      via: "require('../src/scheduler') após installFakeAxios"
      pattern: "require\\('\\.\\./src/scheduler'\\)"
    - from: "backend/test/scheduler.resilience.test.js"
      to: "backend/test/helpers/fakeAxios.js"
      via: "installFakeAxios com routeHandler que ramifica por flag mutável"
      pattern: "installFakeAxios"
---

<objective>
Fixar por teste o comportamento **atual** de resiliência do agendador (REL-03, D-04): falha registrada e
não relançada, lock liberado, execução seguinte permitida, concorrência recusada, resumo semanal que não
propaga exceção.

Purpose: `scheduler.js` tem **10,65% de cobertura** e é o arquivo que os dois planos comportamentais desta
fase (04-02 e 04-06) vão alterar. Esta caracterização é a rede que torna aqueles planos seguros — sem ela,
uma regressão no `finally` de `scheduler.js:174` faria o sistema **parar de notificar em silêncio**, que é
exatamente a classe de falha que o Core Value do milestone existe para impedir.

Output: `backend/test/scheduler.resilience.test.js` com 5 cenários verdes + um seam aditivo de export em
`scheduler.js`. **Zero mudança de lógica de produção.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-CONTEXT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-VALIDATION.md
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do repositório em 2026-08-04. Não explorar o codebase. -->

`backend/src/scheduler.js` (exports atuais, linha 319):
`module.exports = { scheduleTask, runCheck, runCheckOnly, getStatus, stopTasks };`
— `runWeeklySummary` (definida em `:182`) **não é exportada**: por isso o seam aditivo desta task.

`runCheck()` → `Promise<results>` onde `results = { checked, stale, notified, skipped, errors[], deals[],
duration?, ranAt?, error? }`. Guard em `:27` devolve `{ skipped: true, reason: 'Verificação já em andamento' }`.
`getStatus()` → `{ isRunning, lastRunResult, schedule, notificationsEnabled, nextRun }`.

`backend/test/helpers/fakeAxios.js`:
`installFakeAxios(routeHandler) -> { get: mock.fn(async (url, config) => routeHandler(url, config)) }`
— instala `mock.method(axios, 'create', () => fakeInstance)`.

`backend/test/helpers/tmpDb.js`: `makeTmpDbPath() -> { path, cleanup }` · `openRaw(dbPath) -> Database`.

Bordas atravessadas por `runCheck`: `api.get('/deals')`, `api.get('/users')`, `api.get('/tasks')`,
`api.get('/organizations/:id')` (via `getOrgCategory`), e `nodemailer.createTransport()`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seam aditivo + bootstrap do teste + cenários 1-3 (falha registrada, lock liberado, rodada seguinte)</name>
  <files>backend/src/scheduler.js, backend/test/scheduler.resilience.test.js</files>
  <read_first>
    - `backend/src/scheduler.js` (integral — o alvo da caracterização; atenção a `:26-29`, `:54-58`, `:171-180`, `:182-245`, `:319`)
    - `backend/src/routes/auth.js:354-369` — padrão exato do seam aditivo com comentário box-drawing
    - `backend/test/db.dedup.test.js:1-40` — ordem canônica de bootstrap (tmpDb → DB_PATH → setup → require)
    - `backend/test/agendor.futureTasks.test.js:1-90` — `installFakeAxios` antes do require, ramificação dentro do routeHandler, `beforeEach` com `resetCalls`
    - `backend/test/agendor.getStaleDeals.test.js:16-79` — `FIXED_NOW`, dicionário `ORG_CATEGORY`, `mock.timers.enable({ apis: ['Date'], now: FIXED_NOW })`
    - `backend/test/emailer.smtpPass.test.js:35-49` — stub de `nodemailer.createTransport` (pode vir depois do require)
    - `backend/test/setup.js` (integral) — quais variáveis são guardadas e quais são sobrescritas sem guarda
    - `backend/src/agendor.js:119-197` — o que `getStaleDeals` exige do fake para produzir o golden `[101, 103]`
    - `backend/test/fixtures/synthetic/deals-page.json` — fixture a REUSAR, não recriar
  </read_first>
  <action>
    Parte A — seam aditivo em `backend/src/scheduler.js`. Após a linha `module.exports = { scheduleTask,
    runCheck, runCheckOnly, getStatus, stopTasks };`, acrescentar um bloco com comentário de seção
    box-drawing em PT-BR no padrão literal de `routes/auth.js:361-369` (ex.: `// ── Seam de teste (não
    altera o agendamento) ─────────`), explicando que `runWeeklySummary` é exposta apenas para
    caracterização do REL-03 e que nenhum consumidor de produção a importa, seguido de
    `module.exports.runWeeklySummary = runWeeklySummary;`. **Nenhuma outra linha de `scheduler.js` pode
    mudar** — sem reordenar, sem reformatar, sem tocar `runCheck`.

    Parte B — criar `backend/test/scheduler.resilience.test.js` com cabeçalho de comentário em PT-BR no
    modelo dos 18 testes existentes: declarar que é caracterização (golden) do comportamento ATUAL, nomear
    REL-03 e listar os invariantes pinados. Bootstrap na ordem canônica obrigatória: (1) `makeTmpDbPath()`
    e `process.env.DB_PATH`; (2) `require('./setup')`; (3) `installFakeAxios(...)`; (4) stub de
    `nodemailer.createTransport`; (5) `require('../src/db')` e `require('../src/scheduler')`.

    O `routeHandler` do fake deve ramificar por **variáveis mutáveis do arquivo** (nunca reinstalar o
    stub): usar `let usuariosDevemFalhar = false` e um deferred opcional. Rotas a atender:
    `/deals` → `{ data: { data: dealsPage, meta: { totalCount: dealsPage.length }, links: {} } }` a partir
    da fixture `./fixtures/synthetic/deals-page.json`; `/organizations/:id` → categoria por dicionário
    mutável com default `'Lead'` e `205: 'Parceiro'`; `/tasks` → `{ data: { data: [] } }`;
    `/users` → mapa com os ids `11` e `13` contendo `contact.email` e `links: {}` (sem `next`), **ou**
    rejeitar com `Object.assign(new Error('timeout of 15000ms exceeded'), { code: 'ECONNABORTED' })` quando
    `usuariosDevemFalhar` for `true`.

    **A borda que falha nesta caracterização é obrigatoriamente `/users`** (e não `/tasks`): hoje
    `getDealsWithFutureTasks` engole erros, e o plano 04-02 muda exatamente esse caminho — usar `/users`
    mantém estes 5 cenários válidos e verdes durante toda a fase.

    Fixar o tempo com `mock.timers.enable({ apis: ['Date'], now: FIXED_NOW })` usando
    `FIXED_NOW = new Date('2026-06-01T00:00:00.000Z').getTime()` (mesmo valor de
    `agendor.getStaleDeals.test.js`), e `mock.timers.reset()` no `after`. **Não habilitar `setTimeout`**
    neste arquivo: com `totalCount = 10` não há segunda página, mas habilitar `setTimeout` congelaria a
    espera de paginação de `agendor.js:143`.

    Cenários desta task:
    (1) `usuariosDevemFalhar = true` → `const r = await runCheck()`; a promessa **resolve** (não rejeita) e
        `r.error` é uma string não vazia contendo a mensagem do erro injetado.
    (2) Após o mesmo cenário, `getStatus().isRunning === false` — pina o `finally` de `scheduler.js:174-177`.
    (3) `usuariosDevemFalhar = false` → segunda `runCheck()` devolve `r.skipped === undefined` e
        `r.error === undefined`, provando que o guard de `:27` não ficou preso.

    `after` deve fazer `mock.restoreAll()`, `db.closeDb()`, `cleanup()` e `mock.timers.reset()`.
    Comentários e mensagens de asserção em PT-BR (PC-9). Nenhuma asserção pode imprimir objeto de
    configuração de transporte (PC-13).
  </action>
  <verify>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && node --test test/scheduler.resilience.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/scheduler.resilience.test.js` sai com código 0 e reporta ≥ 3 testes passando
    - `git diff backend/src/scheduler.js` contém **apenas linhas adicionadas** (nenhuma linha removida ou modificada), e as adições são o comentário de seção + `module.exports.runWeeklySummary = runWeeklySummary;`
    - `grep -c "module.exports.runWeeklySummary" backend/src/scheduler.js` retorna 1
    - O arquivo de teste requer `./setup` **depois** de atribuir `process.env.DB_PATH` e chama `installFakeAxios` **antes** de `require('../src/scheduler')`
    - `git status --porcelain backend/agendor.db` não retorna nada (o banco real não foi tocado)
    - Nenhuma asserção do arquivo imprime o objeto de opções do transporte SMTP nem `process.env.SMTP_PASS`
  </acceptance_criteria>
  <done>Os 3 primeiros invariantes de REL-03 estão pinados por teste verde e o seam de export existe sem alterar lógica.</done>
</task>

<task type="auto">
  <name>Task 2: Cenários 4-5 (concorrência recusada, resumo semanal não relança) + gate de fase</name>
  <files>backend/test/scheduler.resilience.test.js</files>
  <read_first>
    - `backend/test/scheduler.resilience.test.js` (estado após a Task 1)
    - `backend/src/scheduler.js:26-29` — o guard `if (isRunning) return { skipped: true, reason: 'Verificação já em andamento' }`
    - `backend/src/scheduler.js:182-245` — `runWeeklySummary`: catch em `:242-244` que registra e não relança, sem lock
    - `backend/.c8rc.json` — pisos ativos (lines/statements/functions 20, branches 60; `per-file: false`)
  </read_first>
  <action>
    Acrescentar ao mesmo arquivo os dois cenários restantes.

    (4) Concorrência: usar um **deferred** controlado pelo arquivo — o `routeHandler` de `/users` devolve
    uma promise que só resolve quando o teste chamar `liberar()`. Iniciar `const emAndamento = runCheck()`
    **sem `await`**; chamar `const concorrente = await runCheck()` e asserir
    `concorrente.skipped === true` e `concorrente.reason === 'Verificação já em andamento'`; então chamar
    `liberar()` e `await emAndamento` para não deixar promessa pendurada ao fim do arquivo. Ao final,
    asserir `getStatus().isRunning === false`.

    (5) Resumo semanal: com `usuariosDevemFalhar = true`, asserir
    `await assert.doesNotReject(() => runWeeklySummary())` usando o seam
    `require('../src/scheduler').runWeeklySummary` criado na Task 1 — pina o catch de `scheduler.js:242-244`.
    Comentar em PT-BR que `runWeeklySummary` **não tem lock** (é o comportamento atual, deliberadamente
    pinado como está).

    Como `node --test` isola por arquivo mas não por `test()`, garantir que cada cenário deixe as flags no
    estado neutro que o próximo espera (reatribuir `usuariosDevemFalhar` no início de cada teste, e usar
    `beforeEach(() => fake.get.mock.resetCalls())` no padrão de `agendor.futureTasks.test.js:79-83`).

    Ao final, rodar o gate completo do plano: suíte inteira com cobertura e lint.
  </action>
  <verify>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && node --test test/scheduler.resilience.test.js && npm run test:coverage && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/scheduler.resilience.test.js` sai 0 com **5 ou mais** testes passando
    - `npm run test:coverage` sai 0: total de testes ≥ 83 (baseline 78 + os novos) e nenhum piso de cobertura violado (lines/statements/functions ≥ 20, branches ≥ 60)
    - `npm run lint` sai 0
    - Nenhum teste do repositório pré-existente foi editado: `git diff --name-only backend/test/` lista **somente** `backend/test/scheduler.resilience.test.js`
    - O cenário 4 assere literalmente `skipped === true` e `reason === 'Verificação já em andamento'`
    - Ao fim do arquivo não resta promessa pendurada (o deferred foi liberado e aguardado)
  </acceptance_criteria>
  <done>Os 5 invariantes de REL-03 estão verdes; suíte completa, cobertura e lint verdes; diff de produção limitado ao seam aditivo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint C2 — autorizar a entrada no primeiro plano comportamental</name>
  <files>backend/src/scheduler.js, backend/test/scheduler.resilience.test.js</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md` §6 (escopo do 04-01) e §21 (definição de C2)
    - `backend/test/scheduler.resilience.test.js` (arquivo final)
  </read_first>
  <action>
    PAUSAR a execução e apresentar ao humano o checkpoint C2 do contrato §21. Reunir e exibir: a saída de
    `npm run test:coverage`, o `git diff backend/src/scheduler.js` completo, e a lista dos 5 cenários com o
    invariante que cada um pina. Não prosseguir para o plano 04-02 sem o sinal de retomada — o `auto_advance`
    está desligado por decisão Q4 e este checkpoint é obrigatório.
  </action>
  <what-built>
    5 cenários de caracterização de REL-03 em `backend/test/scheduler.resilience.test.js`, mais um seam
    aditivo de export (`module.exports.runWeeklySummary`) em `backend/src/scheduler.js`. Nenhuma linha de
    lógica de produção foi alterada.
  </what-built>
  <how-to-verify>
    1. Rodar `cd backend && export PATH="$HOME/bin:$PATH" && npm run test:coverage` — deve sair 0.
    2. Rodar `git diff backend/src/scheduler.js` — confirmar que o diff é **puramente aditivo** (só o
       comentário de seção e a linha do seam). Se houver qualquer linha removida/modificada, REPROVAR.
    3. Ler os 5 cenários e confirmar que cada um pina o comportamento **atual** (não o ideal):
       falha registrada e não relançada · `isRunning === false` após falha · 2ª execução roda ·
       concorrente recebe `{ skipped: true }` · `runWeeklySummary` resolve sem lançar.
    4. Confirmar que a borda que falha nos cenários é `/users` (e não `/tasks`) — decisão deliberada para
       manter estes testes válidos depois do fail-safe do 04-02.
    5. Se algum defeito objetivo tiver sido descoberto durante a caracterização, ele deve estar registrado
       no SUMMARY — a cláusula do contrato §6 é "registrar e parar".
    6. **Autorizar (ou não) a entrada no 04-02**, que é o primeiro plano que altera comportamento de
       produção nesta fase.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirmou por escrito que o diff de `scheduler.js` é puramente aditivo
    - Humano confirmou que os 5 cenários refletem o comportamento atual esperado
    - Humano autorizou explicitamente a entrada no plano 04-02 (mudança comportamental)
  </acceptance_criteria>
  <verify>
    <human-check>Humano confirma diff aditivo em `scheduler.js`, os 5 cenários e autoriza o 04-02.</human-check>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && npm run test:coverage</automated>
  </verify>
  <done>C2 aprovado por escrito; entrada no plano comportamental 04-02 autorizada.</done>
  <resume-signal>Digite "aprovado" para liberar o 04-02, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → API Agendor (HTTPS) | Borda de saída substituída por `installFakeAxios` nos testes; nenhuma chamada real |
| backend → servidor SMTP | Borda de saída substituída por `mock.method(nodemailer, 'createTransport')`; nenhum envio real |
| backend → SQLite | `DB_PATH` apontado para arquivo temporário; `backend/agendor.db` real nunca é tocado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Repudiation | `scheduler.js` `finally` de `:174-177` (lock `isRunning`) | mitigate | Cenários 2 e 3 provam liberação do lock e execução subsequente; qualquer regressão futura fica vermelha no CI |
| T-04-01-02 | Information Disclosure | Asserções e logs do arquivo de teste | mitigate | Nunca imprimir o objeto de opções do transporte (contém `auth.pass`) nem `AGENDOR_TOKEN`; `setup.js` já força `SMTP_PASS=''` (PC-13) |
| T-04-01-03 | Tampering | `backend/agendor.db` de desenvolvimento | mitigate | `makeTmpDbPath()` + `process.env.DB_PATH` **antes** de qualquer `require('../src/db')`; `cleanup()` no `after` |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote neste plano; `package.json` e `package-lock.json` fora de `files_modified` |
</threat_model>

<verification>
- `cd backend && export PATH="$HOME/bin:$PATH" && npm run test:coverage` sai 0
- `cd backend && npm run lint` sai 0
- `git diff --stat backend/src/` mostra apenas `scheduler.js` com adições e **zero** remoções
- Checkpoint C2 aprovado pelo humano
</verification>

<success_criteria>
- 5 cenários de REL-03 verdes em `backend/test/scheduler.resilience.test.js`
- Total da suíte ≥ 83 testes; pisos de cobertura (20/20/20/60) respeitados; lint exit 0
- Diff de produção restrito a export aditivo em `scheduler.js`
- C2 aprovado, autorizando o 04-02
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-01-SUMMARY.md` when done.
Commit: `test(04-01): caracteriza resiliência do scheduler (REL-03)`
</output>
