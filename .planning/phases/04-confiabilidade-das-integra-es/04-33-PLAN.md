---
phase: 04-confiabilidade-das-integra-es
plan: 33
type: execute
wave: 26
depends_on: ["04-32"]
files_modified:
  - backend/test/agendor.retry429.test.js
  - backend/test/agendor.cacheConcurrency.test.js
  - backend/test/emailer.timeout.test.js
autonomous: true
gap_closure: true
requirements: [REL-01, REL-02]

must_haves:
  truths:
    - "Nenhum comentário vivo da suíte afirma que existe uma cópia do helper de relógio que foi removida"
    - "O oráculo de REL-02 deixa de mandar o leitor para linhas que não têm relação com o que o comentário afirma"
    - "Nenhuma linha de produção muda: o diff de `backend/src/` é vazio"
  artifacts:
    - path: "backend/test/agendor.retry429.test.js"
      provides: "Comentário alinhado com o helper único, sem afirmar duplicação inexistente"
      contains: "avancarRelogioAte"
    - path: "backend/test/emailer.timeout.test.js"
      provides: "Âncoras nomeadas no lugar das quatro referências por número de linha a emailer.js"
      contains: "sendMailWithRetry"
  key_links:
    - from: "backend/test/helpers/fakeTimers.js"
      to: "backend/test/agendor.retry429.test.js e backend/test/agendor.cacheConcurrency.test.js"
      via: "os comentários que descrevem o instrumento passam a concordar com a fonte da verdade"
      pattern: "avancarRelogioAte"
---

<objective>
Fechar **WR4-02** e **WR4-03**, os dois achados sobre o INSTRUMENTO — não sobre o produto. Diff de
produção **zero**.

**WR4-02** — a remoção da cópia do helper de relógio (WR3-05, plano 04-26) atualizou 2 dos 4
comentários que a declaravam viva. `helpers/fakeTimers.js` afirma, corretamente, que não existe
mais nenhuma cópia; e `agendor.retry429.test.js` afirma que restam duas variantes e que a cópia
local "continua de propósito". Os dois arquivos hoje se contradizem. `agendor.cacheConcurrency.test.js`
aponta para um "helper homônimo" num arquivo onde o símbolo não existe mais.

**WR4-03** — `emailer.timeout.test.js` mantém 4 referências por número de linha a `emailer.js`, e
as 4 apontam hoje para o lugar errado, porque `emailer.js` foi reescrito por 04-17, 04-21 e 04-24
depois de o teste ter sido escrito. É literalmente o defeito que a convenção de WR2-06 descreve, e
o alvo agrava: este arquivo é o oráculo de REL-02.

Purpose:

Numa suíte cujo valor é ser oráculo confiável de quem é notificado, comentário que mente sobre o
instrumento é defeito do instrumento. Quem for auditar lê que existe uma cópia sabidamente
defeituosa na rede de testes do Core Value, vai procurá-la, não acha, e fica sem saber se o
problema é o comentário ou a própria auditoria. E quem seguir uma âncora por número para entender
por que dono igual a autor rende um só e-mail cai numa linha sem relação nenhuma.

Output: cinco edições de comentário, em três arquivos de teste, sem uma única asserção alterada e
sem uma única linha de produção.

## O que NÃO muda

- Nenhuma asserção, nenhum nome de caso, nenhuma armação, nenhum `beforeEach`, nenhum stub.
- `backend/src/`: nenhuma linha, medido por diff vazio ao fim de cada task.
- `backend/test/helpers/fakeTimers.js`: byte a byte. Ele é a FONTE DA VERDADE deste conserto — os
  outros arquivos passam a concordar com ele, não o contrário.
- O parágrafo de `agendor.retry429.test.js` que explica por que a guarda de id fica fora do callback
  do retry: byte a byte. É o achado IN4-02, com todo próprio.
</objective>

<baseline>
## Fatos medidos (2026-08-05, no arquivo real)

| Item | Valor medido | Como |
|---|---|---|
| Implementações de `avancarRelogioAte` na suíte | **1**, em `backend/test/helpers/fakeTimers.js` | `grep -rn "function avancarRelogioAte\|const avancarRelogioAte" backend/test` |
| Comentários que citam a cópia | **4 linhas em 4 arquivos**: `agendor.cacheConcurrency.test.js` ("o helper homônimo de emailer.timeout.test.js"), `agendor.retry429.test.js` ("Restam duas variantes... a cópia local de"), `emailer.timeout.test.js` ("Este arquivo mantinha uma cópia local"), `helpers/fakeTimers.js` ("a cópia local de ... desapareceu no 04-26") | `grep -rn "cópia local\|helper homônimo" backend/test` |
| Linhas com referência por número em `backend/test` | **45 linhas em 11 arquivos** | contagem por arquivo com `grep -cE "\.js:[0-9]+"` |
| Distribuição medida | `scheduler.failsafe.test.js` 12, `notificationStatus.test.js` 11, `agendor.timeout.test.js` 8, `emailer.timeout.test.js` 5, `notifications.resolved.test.js` 3, e 1 em cada um de `config.route.smtpPass.test.js`, `db.smtpPassMigration.clear.test.js`, `db.smtpPassMigration.keep.test.js`, `emailer.smtpPass.test.js`, `scheduler.resilience.test.js` e `setup.js` | idem |
| Referências a `emailer.js` por número dentro de `emailer.timeout.test.js` | **4** (a quinta linha do arquivo com o padrão aponta para o fonte do nodemailer, não para o projeto) | `grep -cE "emailer\.js:[0-9]+" backend/test/emailer.timeout.test.js` |
| O que as 4 afirmam versus o que está lá | `emailer.js:178-203` diz `sendMailWithRetry`, hoje é miolo de template; `:188` diz o ramo pela mensagem, hoje é fim de template; `:197` diz a recriação do transporte, hoje é linha de comentário; `:229` diz a guarda de autor diferente de dono, hoje é uma atribuição dentro do retorno de falha | conferido pelo revisor linha a linha contra o arquivo atual |
| Casos por arquivo | `agendor.retry429.test.js` 8, `emailer.timeout.test.js` 9, `agendor.cacheConcurrency.test.js` 3 | `grep -c "^test(" ...` |

## Decisões travadas (não reabrir)

- **D-WR4-02-a:** a fonte da verdade é `helpers/fakeTimers.js`. Os outros dois arquivos são
  alinhados a ele. Nenhuma linha do helper muda.
- **D-WR4-02-b:** o parágrafo de `agendor.retry429.test.js` é REESCRITO, não apagado. Ele explica
  também por que o envelope local daquele arquivo desapareceu no 04-13, e essa parte continua
  verdadeira e útil. O que sai é a afirmação de que restam duas variantes e o motivo já expirado.
- **D-WR4-03-a:** as 4 âncoras viram âncoras NOMEADAS, na forma que a convenção de WR2-06 já usa em
  todo o resto do repositório: o nome da função, do identificador ou uma descrição do trecho
  (`sendMailWithRetry`; o ramo `isNetworkError`; a recriação do transporte dentro do `catch` de
  `sendMailWithRetry`; a guarda de autor diferente de dono em `sendStaleNotification`).
- **D-WR4-03-b:** a quinta referência do arquivo, que aponta para o fonte do `nodemailer`, FICA. Ela
  documenta uma medição feita numa versão pinada pelo lockfile, num arquivo que este repositório não
  versiona; a convenção de WR2-06 endereça o código do projeto, cujas linhas se deslocam a cada
  commit deste repositório. Esta exclusão é declarada por escrito no próprio arquivo, para que a
  assimetria não pareça esquecimento — mesma forma da decisão registrada em `in3-06` sobre o nome do
  caso.
- **D-WR4-03-c:** os outros 9 arquivos com referência por número NÃO entram neste plano. Medição e
  dono estão no inventário de irmãos.
- **D-WR4-02-c / D-WR4-03-d — DIFF DE PRODUÇÃO ZERO.** É critério de aceite em cada task, e é o que
  torna este plano revertível sem risco. Precedente: 04-26 e 04-27.
</baseline>

<inventario_de_irmaos>
## Inventário de irmãos — WR4-02: as frases que declaram a cópia do helper

Medido: `grep -rn "cópia local\|helper homônimo" backend/test` devolve **4 linhas em 4 arquivos** —
o critério que o próprio revisor apontou como suficiente para ter encontrado o achado em menos de um
segundo.

| Arquivo | Classificação | Medição / motivo |
|---|---|---|
| `agendor.retry429.test.js` | **corrigida** | Afirma que "restam duas variantes" e que a cópia "continua de propósito", com um motivo que o 04-26 extinguiu. |
| `agendor.cacheConcurrency.test.js` | **corrigida** | Aponta para "o helper homônimo de emailer.timeout.test.js", um símbolo que não existe mais naquele arquivo. |
| `emailer.timeout.test.js` | **verificada-e-sã** | Lida e conferida: a frase está no PASSADO ("Este arquivo mantinha uma cópia local") e descreve corretamente o que o 04-26 fez. Não é corrigida porque não está errada. |
| `helpers/fakeTimers.js` | **verificada-e-sã e é a FONTE DA VERDADE** | Afirma que não existe mais nenhuma cópia, o que a medição confirma (1 implementação na suíte). Fica byte a byte. |
| Uma quinta cópia do helper | **inexistente, medido** | `grep -rn "function avancarRelogioAte\|const avancarRelogioAte" backend/test` = 1. |

## Inventário de irmãos — WR4-03: as referências por número de linha

Medido: **45 linhas em 11 arquivos** de `backend/test`.

| Escopo | Classificação | Medição / motivo |
|---|---|---|
| As 4 referências a `emailer.js` em `emailer.timeout.test.js` | **corrigidas** | São o achado: as 4 apontam para o lugar errado, no oráculo de REL-02. |
| A 5ª linha com o padrão em `emailer.timeout.test.js`, apontando para o fonte do `nodemailer` | **verificada-e-sã** | Referencia arquivo de dependência, versionado pelo lockfile e não por este repositório; documenta uma medição de defaults feita naquela versão. A convenção de WR2-06 endereça código do projeto. Exclusão declarada no arquivo (D-WR4-03-b). |
| `scheduler.resilience.test.js` (1 linha) | **fora-de-escopo-com-medição, dono já existente** | É exatamente o residual que o todo **`in3-06`** descreve, com prioridade baixa e uma decisão registrada (corrigir a mensagem de asserção, preservar o nome do caso). Por decisão do usuário, `in3-01` a `in3-08` não podem ser editados. |
| Os outros 9 arquivos, **39 linhas**: `scheduler.failsafe.test.js` 12, `notificationStatus.test.js` 11, `agendor.timeout.test.js` 8, `notifications.resolved.test.js` 3, e 1 em cada um de `config.route.smtpPass.test.js`, `db.smtpPassMigration.clear.test.js`, `db.smtpPassMigration.keep.test.js`, `emailer.smtpPass.test.js` e `setup.js` | **fora-de-escopo-com-medição** | O `in3-06` nomeia APENAS `scheduler.resilience.test.js`; esta é a primeira varredura da fase a medir o conjunto inteiro, e o número (39 linhas em 9 arquivos) não tem dono nenhum hoje. Fora do escopo aprovado pelo usuário (blocker mais os 7 warnings) e caro de fazer bem: cada referência precisa ser conferida contra o arquivo apontado antes de virar âncora, e o próprio WR4-03 nasceu de referências que estavam erradas. Dono: todo **`wr4-03b`**, criado no plano 04-34, com a indicação de fechar junto do gate de CI proposto em `in3-04`. |
| Referências por número nos arquivos criados nesta rodada (`agendor.loteDeOrganizacoes.test.js`) | **verificada-e-sã** | O plano 04-30 tem critério de aceite exigindo zero ocorrências no arquivo novo. |
| Referências por número em `backend/src` | **verificada-e-sã** | O 04-18 zerou os cinco arquivos de produção e a convenção está declarada no topo de `agendor.js`. |
</inventario_de_irmaos>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-26-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-05. Não explorar o codebase. -->

`backend/test/helpers/fakeTimers.js` — exporta `avancarRelogioAte(promessa)`; a nota de topo
enumera as três implementações que convergiram para lá e afirma que não resta nenhuma cópia. É a
fonte da verdade e não é editada por este plano.

`backend/src/emailer.js` no estado atual — os quatro alvos das âncoras: `sendMailWithRetry`
(a função de retry com 3 tentativas); o ramo que detecta erro de rede pela MENSAGEM dentro dela; a
recriação do transporte dentro do `catch` de `sendMailWithRetry`; e a guarda
`authorEmail !== ownerEmail` em `sendStaleNotification`, que evita o segundo e-mail quando dono e
autor são a mesma pessoa.

`backend/test/emailer.timeout.test.js` — 9 casos; as quatro referências alvo estão no cabeçalho do
arquivo, no bloco de caracterização do caminho de envio, num comentário dentro do caso de falha de
rede seguida de sucesso, e num comentário dentro do caso de autor igual a dono.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WR4-02 — os dois comentários que contradizem a fonte da verdade</name>
  <files>backend/test/agendor.retry429.test.js, backend/test/agendor.cacheConcurrency.test.js</files>
  <behavior>
    - `agendor.retry429.test.js` deixa de afirmar que restam duas variantes do helper e que a cópia local continua de propósito
    - `agendor.cacheConcurrency.test.js` deixa de apontar para um "helper homônimo" num arquivo onde o símbolo não existe
    - Os dois arquivos continuam verdes, com o mesmo número de casos e sem nenhuma asserção alterada
  </behavior>
  <read_first>
    - `backend/test/helpers/fakeTimers.js` — a nota de topo, que é a fonte da verdade deste conserto
    - `backend/test/agendor.retry429.test.js` — o bloco que fala do envelope local e das variantes do helper
    - `backend/test/agendor.cacheConcurrency.test.js` — o comentário acima do helper de espera por condição
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção WR4-02
    - `.planning/phases/04-confiabilidade-das-integra-es/04-26-SUMMARY.md` — o que o 04-26 removeu e o que ele atualizou
  </read_first>
  <action>
    Editar apenas comentário, nos dois arquivos.

    **Em `agendor.retry429.test.js`.** Substituir o parágrafo que afirma restarem duas variantes do
    helper. A redação nova, em 3 a 5 linhas PT-BR: não resta nenhuma cópia; `backend/test/helpers/fakeTimers.js`
    é a ÚNICA implementação de `avancarRelogioAte` desde o 04-26 (WR3-05); quem precisar avançar
    relógio falso importa de lá. Preservar o parágrafo imediatamente anterior, que explica por que o
    envelope local daquele arquivo desapareceu no 04-13 — ele continua verdadeiro e é o que dá
    contexto à frase nova.

    **Em `agendor.cacheConcurrency.test.js`.** Trocar a referência ao "helper homônimo de
    emailer.timeout.test.js" pela referência ao helper compartilhado, nomeando o arquivo
    `helpers/fakeTimers.js`. Uma linha; o resto do comentário fica.

    **Em nenhum dos dois:** alterar asserção, nome de caso, armação, hook, stub ou importação. O
    parágrafo de `agendor.retry429.test.js` sobre a guarda de id ficar fora do callback do retry fica
    byte a byte — é IN4-02, com todo próprio.

    Rodar os dois arquivos e o helper.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/agendor.retry429.test.js &amp;&amp; node --test test/agendor.cacheConcurrency.test.js &amp;&amp; node --test test/fakeTimers.helper.test.js &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os três arquivos saem 0
    - `grep -rc "cópia local\|helper homônimo" backend/test/agendor.retry429.test.js backend/test/agendor.cacheConcurrency.test.js` retorna **0** nos dois
    - As mesmas expressões continuam existindo nos dois arquivos corretos: `grep -rn "cópia local" backend/test/emailer.timeout.test.js backend/test/helpers/fakeTimers.js` devolve **2 linhas**
    - Nenhuma asserção alterada: `git diff -U0 backend/test/agendor.retry429.test.js backend/test/agendor.cacheConcurrency.test.js | grep "^[-+][^-+]" | grep -c "assert"` retorna **0**
    - Mesmo número de casos: `grep -c "^test(" backend/test/agendor.retry429.test.js` retorna **8** e `grep -c "^test(" backend/test/agendor.cacheConcurrency.test.js` retorna **3**
    - IN4-02 intocado: `git diff backend/test/agendor.retry429.test.js | grep -c "três vezes"` retorna **0**
    - `git diff --name-only backend/src/` está vazio
    - `git diff --name-only backend/test/helpers/` está vazio
  </acceptance_criteria>
  <done>WR4-02 fechado: nenhum comentário vivo da suíte afirma que existe uma cópia do helper que foi removida.</done>
</task>

<task type="auto">
  <name>Task 2: WR4-03 — as quatro âncoras por número no oráculo de REL-02</name>
  <files>backend/test/emailer.timeout.test.js</files>
  <behavior>
    - As 4 referências a `emailer.js` por número viram âncoras nomeadas
    - A referência ao fonte do `nodemailer` permanece, com a exclusão declarada por escrito
    - O arquivo continua verde, com os mesmos 9 casos e sem nenhuma asserção alterada
  </behavior>
  <read_first>
    - `backend/test/emailer.timeout.test.js` — arquivo inteiro, localizando as cinco linhas com o padrão de referência por número
    - `backend/src/emailer.js` — `sendMailWithRetry` inteira, o ramo que detecta erro de rede pela mensagem, a recriação do transporte no `catch`, e a guarda de autor diferente de dono em `sendStaleNotification`
    - `backend/src/agendor.js` — o topo do arquivo, onde a convenção de WR2-06 está declarada, para copiar a forma
    - `.planning/todos/pending/in3-06-referencia-por-linha-em-mensagem-de-assercao.md` — a decisão sobre o que NÃO renomear e por quê
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção WR4-03
  </read_first>
  <action>
    Editar apenas comentário, em `backend/test/emailer.timeout.test.js`.

    Antes de escrever cada âncora, LOCALIZAR no `emailer.js` atual o trecho que o comentário afirma
    descrever, e confirmar que a afirmação continua verdadeira. As quatro referências existentes
    nasceram de uma versão antiga do módulo; trocar número por nome sem conferir repetiria o defeito
    com outra sintaxe.

    Substituir as quatro por âncoras nomeadas: a função `sendMailWithRetry`; o ramo que reconhece
    erro de rede pela MENSAGEM dentro dela; a recriação do transporte dentro do `catch` de
    `sendMailWithRetry`; e a guarda de autor diferente de dono em `sendStaleNotification`. Manter o
    sentido de cada comentário — o que muda é como o trecho é apontado, não o que ele explica.

    Acrescentar ao cabeçalho do arquivo 3 a 5 linhas PT-BR declarando duas coisas: (i) que este
    arquivo passa a seguir a convenção de WR2-06 — referência por âncora nomeada, nunca por número de
    linha, que se desloca no próprio commit que o escreve —, e que ele ficou de fora da limpeza do
    04-18 porque foi editado depois; (ii) que a única referência por número que PERMANECE aponta para
    o fonte do `nodemailer`, arquivo de dependência versionado pelo lockfile e não por este
    repositório, e que a exclusão é deliberada.

    **Nada mais pode mudar:** nenhuma asserção, nenhum nome de caso, nenhuma armação, nenhum stub,
    nenhuma importação, nenhum hook.

    Rodar o arquivo, os demais arquivos de `emailer.*` e o lint.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/emailer.timeout.test.js &amp;&amp; node --test test/emailer.resumoIndecidivel.test.js &amp;&amp; node --test test/emailer.transporteVivo.test.js &amp;&amp; node --test test/emailer.smtpPass.test.js &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os quatro arquivos de `emailer.*` saem 0
    - `grep -cE "emailer\.js:[0-9]+" backend/test/emailer.timeout.test.js` retorna **0** (era 4)
    - `grep -cE "\.js:[0-9]+" backend/test/emailer.timeout.test.js` retorna **1** — a referência ao fonte do `nodemailer`, que permanece por decisão declarada
    - As âncoras nomeadas existem: `grep -c "sendMailWithRetry" backend/test/emailer.timeout.test.js` retorna pelo menos **3** e `grep -c "sendStaleNotification" backend/test/emailer.timeout.test.js` retorna pelo menos **1**
    - Nenhuma asserção alterada: `git diff -U0 backend/test/emailer.timeout.test.js | grep "^[-+][^-+]" | grep -c "assert"` retorna **0**
    - Mesmo número de casos: `grep -c "^test(" backend/test/emailer.timeout.test.js` retorna **9**
    - O total da suíte não muda: `npm run test:coverage` sai 0 com o mesmo número de testes da entrada deste plano, registrado literal no SUMMARY
    - `npm run lint` sai 0
    - `git diff --name-only backend/src/` está vazio
  </acceptance_criteria>
  <done>WR4-03 fechado: o oráculo de REL-02 deixa de mandar o leitor para linhas sem relação com o que o comentário afirma.</done>
</task>

</tasks>

<rastreabilidade>
| Requisito | Achado | Tarefa | Teste | Evidência |
|---|---|---|---|---|
| REL-01 | WR4-02 | Task 1 | `agendor.retry429`, `agendor.cacheConcurrency` verdes | o instrumento do oráculo da borda deixa de mentir sobre si mesmo |
| REL-02 | WR4-03 | Task 2 | `emailer.timeout` verde com 9 casos | as âncoras do oráculo de REL-02 apontam para os trechos que descrevem |
| Todos (integridade da rede) | WR4-02 e WR4-03 | Tasks 1 e 2 | suíte completa | diff de produção zero: nenhuma mudança de comportamento se disfarçou de documentação |
</rastreabilidade>

<matriz_de_teste>
| Cenário | Tipo | Arquivo | Setup → Ação → Esperado | Regressão protegida |
|---|---|---|---|---|
| Retry de 429 nas cinco bordas | regressão | `agendor.retry429` | inalterado, 8 casos | edição de comentário mexendo em asserção |
| Entrelaçamento de execuções | regressão | `agendor.cacheConcurrency` | inalterado, 3 casos | idem |
| Helper de relógio | regressão | `fakeTimers.helper` | inalterado | alterar a fonte da verdade em vez dos que a contradizem |
| Timeouts SMTP e retry | regressão | `emailer.timeout` | inalterado, 9 casos | edição de comentário mexendo em asserção |
| Suíte completa | gate | `npm run test:coverage` | mesmo número de testes da entrada | mudança de código disfarçada de documentação |
</matriz_de_teste>

<matriz_de_risco>
| # | Risco | Prob. | Impacto | Mitigação | Sinal | Rollback |
|---|---|---|---|---|---|---|
| R4-31 | Trocar número por nome sem conferir o trecho e repetir o defeito com outra sintaxe | alta | médio | A ação exige localizar cada trecho no `emailer.js` atual antes de escrever a âncora | âncora nomeando função que não faz o que o comentário diz | corrigir no mesmo commit |
| R4-32 | Editar a fonte da verdade (`helpers/fakeTimers.js`) em vez dos arquivos que a contradizem | média | médio | D-WR4-02-a; critério de diff vazio em `backend/test/helpers/` | helper no `git diff --name-only` | revert das linhas |
| R4-33 | Uma edição de comentário arrastar asserção junto | média | alto | Critérios de zero `assert` em linhas adicionadas ou removidas, e contagem de casos por arquivo | grep maior que 0 | revert do commit |
| R4-34 | Remover a referência ao fonte do `nodemailer` e perder a medição dos defaults | média | baixo | D-WR4-03-b; critério de exatamente 1 referência remanescente | contagem 0 | restaurar a linha |
| R4-35 | Estender o conserto aos outros 9 arquivos e estourar o escopo aprovado | média | baixo | D-WR4-03-c; `files_modified` do plano lista 3 arquivos; dono registrado em `wr4-03b` | arquivo extra no `git diff --name-only` | revert das linhas |
| R4-36 | Fechar IN4-02 de carona ao editar `agendor.retry429.test.js` | média | baixo | Critério explícito de ausência no diff | grep maior que 0 | reverter as linhas |
</matriz_de_risco>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Comentário do teste → auditor da rede de testes | O texto é a única descrição do instrumento que decide quem é notificado |
| Âncora do comentário → código de produção | Uma âncora errada leva o leitor a concluir coisa errada sobre o comportamento |
| Diff de teste → diff de produção | Uma mudança de comportamento pode se disfarçar de documentação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33-01 | Repudiation | Comentário afirmando que existe uma cópia defeituosa do instrumento na rede de testes do Core Value | mitigate | As duas frases alinhadas à fonte da verdade; medição de 1 implementação na suíte |
| T-04-33-02 | Repudiation | Âncoras do oráculo de REL-02 apontando para trechos sem relação com o que afirmam | mitigate | Âncoras nomeadas, conferidas contra o `emailer.js` atual antes de escritas |
| T-04-33-03 | Tampering | Mudança de comportamento se disfarçando de edição de comentário | mitigate | Diff de `backend/src/` vazio e zero `assert` no diff, como critérios de aceite em cada task |
| T-04-33-04 | Information Disclosure | Comentário novo citando valor de segredo ou credencial | mitigate | Nenhum valor é citado; PC-13 já declarado nos arquivos e preservado |
| T-04-33-05 | Repudiation | As 39 linhas de referência por número nos outros 9 arquivos continuarem sem dono | accept | Medidas e registradas no inventário; dono: todo `wr4-03b`, com fechamento previsto junto do gate de CI de `in3-04` |
| T-04-33-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote; `package.json` e lockfile não são tocados |
</threat_model>

<dod>
- [ ] Nenhum comentário vivo afirma que a cópia do helper existe, e os dois que falam dela no passado continuam corretos
- [ ] As 4 âncoras por número a `emailer.js` viraram âncoras nomeadas, conferidas contra o arquivo atual
- [ ] A referência ao fonte do `nodemailer` permanece, com a exclusão declarada no arquivo
- [ ] Inventário de irmãos com as 4 frases sobre a cópia e as 45 linhas de referência por número, distribuídas por arquivo
- [ ] Diff de `backend/src/` vazio nas duas tasks
- [ ] Zero asserções no diff; contagem de casos por arquivo inalterada
- [ ] Suíte completa verde com o mesmo número de testes, `npm run lint` exit 0
</dod>

<verification>
- `node --test` sai 0 nos cinco arquivos tocados ou vizinhos
- `npm run test:coverage` sai 0 com o mesmo número de testes da entrada
- `npm run lint` sai 0
- `git diff --name-only backend/src/` está vazio
- `git diff --name-only backend/test/` lista exatamente os três arquivos de `files_modified`
</verification>

<success_criteria>
- Nenhum comentário da suíte pode afirmar que existe um instrumento que foi removido
- Nenhuma âncora do oráculo de REL-02 pode apontar para um trecho que não descreve
- Nenhuma linha de produção pode mudar neste plano
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-33-SUMMARY.md` when done.
Commit: `docs(04-33): comentários da suíte alinhados ao instrumento único e âncoras nomeadas (WR4-02, WR4-03)`
Rollback: revert do commit. Independente de todos os demais planos da rodada.
</output>
