---
phase: 04-confiabilidade-das-integra-es
plan: 35
type: tdd
wave: 28
depends_on: [04-34]
gap_closure: true
autonomous: true
requirements: [REL-06, REL-03]
files_modified:
  - backend/test/agendor.funnel.test.js
  - backend/test/emailer.resumoIndecidivel.test.js
  - backend/test/scheduler.categoriaIndecidivel.test.js
  - backend/src/agendor.js
  - backend/src/scheduler.js
  - .planning/todos/pending/in3-08b-comparacao-exata-nos-demais-filtros.md

must_haves:
  truths:
    - "Um funil renomeado no CRM que contenha 'beefor' (ex.: 'Beefor Comercial') continua suprimindo a notificação ao responsável nos TRÊS consumidores: envio diário, prévia do painel e resumo semanal individual"
    - "Um funil que NÃO contém 'beefor' (ex.: 'Comercial') continua notificando o responsável — a supressão nova não é larga demais"
    - "Um negócio cujo payload não traz funil continua sendo notificado — fail-open preservado, e essa é a asserção que NÃO muda"
    - "Uma rodada em que NENHUM negócio parado trouxe funil registra quantos foram e marca a execução com erro na superfície que o painel de fato exibe; uma rodada em que todos trazem funil não-Beefor continua silenciosa"
  artifacts:
    - path: "backend/src/agendor.js"
      provides: "Comparação por substring em shouldNotifyOwner + campo funilAusente por negócio + linha agregada de aviso em getStaleDeals"
      contains: "NO_OWNER_NOTIFY_FUNNELS.some("
    - path: "backend/src/scheduler.js"
      provides: "Contador results.funilNaoAvaliado (nasce no literal, incrementa sempre) + alarme aditivo de supressão-de-forma total"
      contains: "funilNaoAvaliado"
    - path: "backend/test/agendor.funnel.test.js"
      provides: "Oráculo unitário do contrato NOVO: 8 casos, zero QUIRK, com testemunha de não-supressão"
    - path: "backend/test/scheduler.categoriaIndecidivel.test.js"
      provides: "Cenários H, I e J — os consumidores runCheck e runCheckOnly sob funil renomeado e sob funil ausente"
    - path: "backend/test/emailer.resumoIndecidivel.test.js"
      provides: "Caso (8) — o terceiro consumidor (sendOwnerWeeklySummary) sob funil renomeado"
  key_links:
    - from: "backend/src/scheduler.js"
      to: "shouldNotifyOwner"
      via: "runCheck (envio) e runCheckOnly (prévia) — os dois chamam a MESMA função exportada"
      pattern: "shouldNotifyOwner\\("
    - from: "backend/src/emailer.js"
      to: "shouldNotifyOwner"
      via: "deals.filter(shouldNotifyOwner) em sendOwnerWeeklySummary"
      pattern: "deals\\.filter\\(shouldNotifyOwner\\)"
    - from: "backend/src/agendor.js"
      to: "backend/src/scheduler.js"
      via: "campo funilAusente por negócio, consumido pelo contador results.funilNaoAvaliado"
      pattern: "funilAusente"
---

<objective>
Fechar o ÚLTIMO filtro de elegibilidade fail-open ainda aberto da Fase 04 (todo `in3-08`), promovido a
plano por decisão do usuário em 2026-08-05.

O todo descrevia DOIS modos de falha misturados num só achado. As medições separaram-nos e o usuário
decidiu cada um por si:

- **MODO 2 — comparação frágil: CORRIGIR.** `NO_OWNER_NOTIFY_FUNNELS = ['beefor']` compara por
  igualdade EXATA. Renomear o funil no CRM para "Beefor Comercial" ou "Beefor 2026" faz a comparação
  deixar de casar e **reabilita silenciosamente** a notificação que a regra de negócio manda suprimir.
  Isso não depende de falha nenhuma — basta alguém renomear no Agendor. Passa a comparar por
  **substring** sobre o nome normalizado.
- **MODO 1 — direção da falha com funil ausente: MANTER fail-open, COM SINAL.** `(deal?.funnel || '')`
  transforma funil ausente em string vazia, que nunca casa, e o negócio é notificado. **Continua
  notificando.** O que muda é só a OBSERVABILIDADE: o caso passa a ser contado e avisado, para que uma
  mudança de forma do payload deixe de ser invisível.

Purpose: um rename no CRM não pode mais desligar em silêncio uma regra de quem-recebe; e uma mudança
de forma do payload da Agendor não pode mais desligá-la sem deixar vestígio. Nenhuma das duas custa
uma notificação perdida — o Core Value do milestone segue intacto.

Output: comparação por substring em `shouldNotifyOwner`, campo `funilAusente` por negócio, linha
agregada de aviso em `getStaleDeals`, contador `results.funilNaoAvaliado` + alarme aditivo em
`runCheck`, e os DOIS casos QUIRK do oráculo unitário reescritos para o contrato novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/todos/pending/in3-08-filtros-de-elegibilidade-fail-open.md
@.planning/phases/04-confiabilidade-das-integra-es/04-28-SUMMARY.md

Convenções que valem aqui e não se negociam: comentários e mensagens em português; Biome
(`lineWidth` 80) é o único formatador — `npm run format` é quem decide a quebra de linha, não o
executor; `node:test` nativo, um processo por arquivo de teste; referência a outro trecho **por
âncora nomeada** (função, identificador, caso de teste), nunca por número de linha (WR2-06).
</context>

<interfaces>
O executor NÃO precisa explorar o repositório para descobrir os contratos abaixo — eles já estão
medidos. Todos os números foram conferidos no arquivo real em 2026-08-05, na entrada deste plano.

**`backend/src/agendor.js` — o ponto único de decisão (estado de ENTRADA)**

- `const NO_OWNER_NOTIFY_FUNNELS = ['beefor'];` — 2 ocorrências não-comentário no arquivo.
- `function shouldNotifyOwner(deal)` — normaliza com `(deal?.funnel || '').trim().toLowerCase()` e
  devolve `!NO_OWNER_NOTIFY_FUNNELS.includes(funnel)`. Exportada em `module.exports`.
- Em `getStaleDeals`, o objeto enriquecido recebe `funnel: deal.dealStage?.funnel?.name || null` —
  é dali que sai o campo que `shouldNotifyOwner` lê.
- `logger` já está importado no topo do módulo (`require('./logger')`); há 1 `logger.warn`
  não-comentário hoje (o de categoria indecidível, dentro do laço de enriquecimento).
- Cobertura atual do arquivo: **100% linhas / 91,72% branches**.

**Os TRÊS consumidores em produção (medidos, não presumidos)**

| # | Consumidor | Arquivo | Forma da chamada |
|---|---|---|---|
| 1 | `runCheck` (envio diário) | `backend/src/scheduler.js` | `if (!shouldNotifyOwner(deal)) { ... continue; }`, com `dealResult.skipReason` já escrito |
| 2 | `runCheckOnly` (prévia do painel) | `backend/src/scheduler.js` | um dos quatro predicados da conjunção de `seraNotificado`; o objeto devolvido é `{ ...deal, ... }`, então qualquer campo novo de `getStaleDeals` flui de graça — **verificar, não presumir** |
| 3 | `sendOwnerWeeklySummary` (resumo individual) | `backend/src/emailer.js` | `const doFunilNotificavel = deals.filter(shouldNotifyOwner);` com contador próprio `skippedByFunnel` |

Medição que sustenta a classificação: `grep -rin "beefor" backend/src/scheduler.js backend/src/emailer.js | grep -cv '//'`
= **0**. Não existe nenhuma segunda comparação de funil no backend — os três leem a mesma função.

**`backend/src/scheduler.js` — o padrão do 04-28 a espelhar (estado de ENTRADA)**

- `results` nasce com `skippedCategoriaIndecidivel: 0` no literal, **ao lado** de `skipped`.
- O incremento fica dentro da guarda `if (deal.categoriaIndecidivel)`, e incrementa **sempre**.
- O alarme é um bloco **ADITIVO** depois do `for` e antes do `logger.info` de conclusão, dentro do
  `try`: `if (results.stale > 0 && results.skippedCategoriaIndecidivel === results.stale)` preenche
  `results.error`, faz `results.errors.push(...)` e emite `logger.error` com tag `[Scheduler]`,
  carregando **apenas inteiros e texto fixo**.
- Contagens não-comentário na entrada: `skippedCategoriaIndecidivel` = **3**, `results.error = ` =
  **2**, `continue;` = **3**, `dealType` = **1** (só no payload do log).

**`backend/test/scheduler.categoriaIndecidivel.test.js` — a armação (760 linhas)**

- `servirDeals(idPrimeiro, idSegundo)` monta dois clones do molde 101 com donos/autores distintos.
- `servirDealsDoFunilBeefor(idPrimeiro, idSegundo, idsNoBeefor = null)` troca o `dealStage` dos ids
  escolhidos por `{ name: MOLDE.dealStage.name, funnel: { name: 'Beefor' } }` — **3 ocorrências** no
  arquivo (a declaração + as chamadas de E e G).
- `idsPrometidosPelaPrevia(previa)` e `idsNotificadosDeFato(resultado)` são os dois lados da
  igualdade prévia-x-envio dos cenários F e G. `envios(destinatario)`, `linhasDoDeal(dealId)`,
  `avancarRelogioAte(...)`, `orgsQueFalham`, `organizacaoDe(dealId)` completam a armação.
- O funil do molde 101 é **`'Comercial'`** (fixture `synthetic/deals-page.json`).
- 7 casos hoje (`^test(` = 7): A, B, C, D, E, F, G — descritos no cabeçalho do arquivo.

**`backend/test/emailer.resumoIndecidivel.test.js` — a armação (343 linhas)**

- `negocio({ id, title, ownerId, funnel, categoriaIndecidivel, ownerName })` monta um negócio no
  formato que `getStaleDeals` devolve; `funnel` cai no padrão `'Funil Padrão'` quando omitido.
- `enviosCapturados` guarda `{ to, html }` por envio; `USERS` tem os ids 11, 12 e 13.
- O caso **(4)** já pina que o funil `'Beefor'` (exato) sai do relatório individual. 7 casos hoje.

**Fixtures — a medição que sustenta a decisão do MODO 1**

`real-deals.sample.json` (5 negócios reais anonimizados) e `synthetic/deals-page.json` (10
sintéticos): **15 de 15 têm `dealStage.funnel.name`**. Funil ausente é caminho de exceção, não caso
comum. Nomes presentes: `Funil 1`, `Funil 2`, `Comercial`. Nenhum contém `beefor` — a mudança para
substring **não** atinge fixture nenhuma por acidente.

**Baseline da suíte:** 186/186 verdes; `npm run lint` exit 0 com 44 warnings.
</interfaces>

<decisoes_do_usuario>
Tomadas em 2026-08-05, depois de as duas falhas terem sido medidas e apresentadas separadamente.
**NÃO reabrir nenhuma delas.** Cada task abaixo cita a decisão que implementa.

**D-IN3-08-a — MODO 2: substring, e não word-boundary.** A comparação passa a ser por substring
sobre o nome normalizado. O usuário viu explicitamente que **`'beeforx'` passaria a ser suprimido** e
escolheu assim mesmo. Não propor word-boundary, não propor lista de exceções.

**D-IN3-08-b — os dois QUIRK INVERTEM, e isso é o sinal de que a decisão foi tomada.** Os casos
`'beefor vendas' NÃO é suprimido` e `'beeforx' NÃO é suprimido` pinam hoje o comportamento ATUAL e
passam a asserir o contrato NOVO. É mudança deliberada de comportamento **com teste cobrindo o novo
comportamento** — exatamente o que a constraint de processo do `PROJECT.md` exige. Um caso cujo nome
diz "comportamento ATUAL" não pode sobreviver a esta mudança.

**D-IN3-08-c — MODO 1: continua fail-open.** Funil ausente continua NOTIFICANDO. A razão medida, que
precisa ficar escrita no código: 15 de 15 negócios das fixtures têm a estrutura de funil, então funil
ausente é caminho de exceção. Um fail-safe uniforme faria "não sei o funil" virar o estado padrão de
qualquer negócio cujo payload mude de forma — reintroduzindo exatamente o **CR4-01** (supressão em
massa invisível) que o 04-28 acabou de fechar. A diferença essencial em relação ao CR3-01, que o todo
equipara mas **não** é igual: lá o indecidível só surgia depois de esgotado o retry de uma chamada
HTTP específica — evento raro e detectável; aqui seria o default de qualquer payload sem a estrutura.

**D-IN3-08-d — o que muda no MODO 1 é OBSERVABILIDADE, não quem recebe.** `logger.warn` + contagem.
A asserção principal do caso `funil null/ausente NÃO suprime` **não muda**.

**D-IN3-08-e — o aviso do MODO 1 é AGREGADO, não por negócio.** Uma linha por chamada de
`getStaleDeals`, e não uma por negócio como faz o aviso de categoria indecidível. O motivo é medido:
`getStaleDeals` é também o caminho de LEITURA do painel — 8 invocações fora do módulo, com
auto-refresh no `DealsList.jsx` (medição do 04-30). Uma linha por negócio numa mudança de forma do
payload inundaria o log a cada atualização de tela, e um log inundado é um log que não se lê.

**D-IN3-08-f — o contador incrementa ANTES da cadeia de guardas, e não dentro de uma delas.** O
contador do 04-28 mora dentro da guarda que ele conta; este mora no topo do laço, logo depois de
`results.checked++`. A razão é o residual **`cr4-01b`** já registrado nesta fase: um contador que só
incrementa depois de outras guardas terem feito `continue` não consegue alcançar o denominador
`results.stale`, e o alarme falha ABERTO numa rodada MISTA. Aqui o denominador e o numerador
percorrem exatamente o mesmo conjunto. Este plano **não** conserta o `cr4-01b`; apenas não o
reproduz.

**D-IN3-08-g — o alarme novo vem ANTES do alarme do 04-28 no arquivo.** Quando as duas condições
totais valem na mesma rodada, `results.error` fica com a mensagem do 04-28 (a mais grave: ninguém foi
notificado), e as DUAS mensagens ficam em `results.errors`, que é o array que o Dashboard renderiza.
Ordem de escrita = precedência do campo escalar; é decisão, não acaso.

**D-IN3-08-h — normalização sem remoção de acentos.** A normalização nova é `trim().toLowerCase()`
— a que já existe —, sem `normalize('NFD')`. Igualar a normalização à da irmã `isExcludedStage`
exigiria extrair um helper compartilhado, o que é **refatoração estrutural no caminho do Core
Value**, e a constraint de processo do `CLAUDE.md` proíbe misturá-la a uma mudança de comportamento.
O regex de marcas de combinação de `isExcludedStage` tem no código a instrução "copiado byte-a-byte
do trecho inline original — NÃO reescrever", e ela é respeitada: o arquivo dela não entra no diff.
A assimetria vai registrada no todo `in3-08b`, com a medição que a torna tolerável ('beefor' é ASCII
puro e nenhum nome de funil das fixtures tem acento).
</decisoes_do_usuario>

<inventario_de_irmaos>
Mandato estrutural da fase. Cada item nomeado carrega **trabalho verificável** — grep, caso de teste
ou medição já feita. Item nomeado sem gate é a lacuna que reabriu esta fase quatro vezes.

## Grupo 1 — Os TRÊS consumidores de `shouldNotifyOwner`

| # | Construção | Classificação | Trabalho verificável |
|---|---|---|---|
| 1 | `runCheck` — `backend/src/scheduler.js`, a guarda `if (!shouldNotifyOwner(deal))` do envio diário | **corrigida** (herda o contrato novo) **+ instrumentada** (contador e alarme do MODO 1) | Cenário **H** (Task 2): com o funil renomeado `'Beefor Comercial'`, o envio SUPRIME — hoje notifica. Cenário **I**: com funil ausente, o envio NOTIFICA os dois (fail-open) e `r.funilNaoAvaliado === 2`. Cenário **J**: com `'Comercial'`, `r.funilNaoAvaliado === 0` e campo de erro vazio |
| 2 | `runCheckOnly` — a prévia que escreve o rótulo do botão de disparo (04-31) | **verificada-e-sã por medição, não presumida** | Cenário **H** exige `previstoPrimeiro.seraNotificado === false` para o funil renomeado **e** a igualdade prévia-x-envio de `idsPrometidosPelaPrevia` / `idsNotificadosDeFato`. Cenário **I** exige `previstoPrimeiro.funilAusente === true` — prova que o campo novo atravessa o spread `{ ...deal }` — e `seraNotificado === true`, provando que a prévia não promete MENOS que o envio |
| 3 | `sendOwnerWeeklySummary` — `backend/src/emailer.js`, o segundo produtor de e-mail dirigido ao responsável | **verificada-e-sã por medição, não presumida** | Caso **(8)** (Task 1): o card do funil `'Beefor Comercial'` some do HTML do relatório individual — asserção sobre o CORPO enviado, não sobre contagem. Medição estrutural que a acompanha: `grep -rin "beefor" backend/src/scheduler.js backend/src/emailer.js | grep -cv '//'` = **0**, ou seja, não existe segunda comparação de funil para divergir |

## Grupo 2 — As constantes de filtro de elegibilidade irmãs, no MESMO módulo

| Construção | Forma da comparação | Classificação | Trabalho verificável |
|---|---|---|---|
| `EXCLUDED_STAGE_WORDS` + `isExcludedStage` | correspondência **PARCIAL** (`stageName.includes(w)`) sobre nome normalizado (lowercase + NFD + remoção de marcas de combinação) | **verificada-e-sã — e é o PRECEDENTE LITERAL da decisão do MODO 2** | Medido: `grep -c "stageName.includes(w)" backend/src/agendor.js` = **1**. O módulo já continha, escrito e comentado, o argumento de que um filtro de elegibilidade sobre texto livre do CRM precisa de correspondência parcial ("para cobrir variações de gênero e composições"). O funil era a exceção, não a regra. Gate de não-regressão na Task 3: `git diff -U0 backend/src/agendor.js \| grep -c "EXCLUDED_STAGE_WORDS\|isExcludedStage"` = **0** — o regex byte-a-byte fica intocado |
| `EXCLUDED_CATEGORIES` | igualdade **EXATA** (`EXCLUDED_CATEGORIES.includes(orgCategory)`) | **fora-de-escopo-com-medição** | Medido: 1 ocorrência não-comentário. Tem a MESMA direção de falha do MODO 2 — renomear 'Parceiro' para 'Parceiro Comercial' no CRM reabilita a notificação. **Não é corrigida aqui porque mudá-la muda QUEM RECEBE**, e a decisão do usuário de 2026-08-05 cobre exclusivamente o funil: com substring, uma categoria nova chamada "Ex-Parceiro" passaria a ser excluída sem ninguém ter pedido. Dono: todo `in3-08b`, criado na Task 3. Gate: ausente do diff |
| `EXCLUDED_OWNERS` | igualdade **EXATA** (`EXCLUDED_OWNERS.includes(deal.owner?.name)`) | **fora-de-escopo-com-medição** | Medido: 1 ocorrência não-comentário. Mesma forma e mesma direção de falha (a pessoa muda de sobrenome no cadastro e volta a ser notificada), mesmo motivo para ficar de fora. Dono: `in3-08b`. Gate: ausente do diff |
| `NEGOCIO_CATEGORIES` + `getDealType` | igualdade exata | **verificada-e-sã — NÃO é filtro de elegibilidade** | Medido: `grep -c "dealType" backend/src/scheduler.js` = **1**, e é `deal_type: deal.dealType` no payload do `notification_log`. Nenhum consumidor gateia notificação por `dealType` — ele decide rótulo de e-mail e agrupamento de relatório. Uma comparação exata aqui erra o RÓTULO, nunca o destinatário |

## Grupo 3 — As superfícies do sinal novo (MODO 1)

| Superfície | Classificação | Trabalho verificável |
|---|---|---|
| `results.funilNaoAvaliado` no payload da rodada + `results.errors` (o único bloco que o Dashboard renderiza) | **corrigida** | Cenários **I** (alarme dispara, `r.errors.length === 1`) e **J** (não dispara, `r.errors.length === 0`). O par I+J é o guarda-corpo de que o alarme discrimina a CAUSA e não a quantidade — mesmo papel do par D+E do 04-28 |
| Linha agregada de log em `getStaleDeals` | **corrigida** | Emitida só quando o contador é > 0, com apenas inteiros e texto fixo (T-04-35-02). Gate: ausência de objeto de erro e de nome de negócio/organização na mensagem |
| `sendOwnerWeeklySummary` **sem** contador agregado de funil ausente | **fora-de-escopo-com-medição** | Medido no arquivo: a função já tem contador próprio por causa (`skippedByFunnel`) com linha de log dedicada, separado do de categoria. Um contador de "funil ausente" ali não traria informação nova: a mesma mudança de forma do payload dispara no `runCheck` **diário**, cinco vezes mais cedo que no resumo de sexta-feira, e as duas funções leem a MESMA lista de `getStaleDeals`. Dono do registro: `in3-08b` |
| `funilAusente` invisível na UI | **fora-de-escopo-com-medição** | Medido: `grep -rn "funilAusente\|skipReason" frontend/src` = **0**. Medido também que o `ReportPanel.jsx` já agrupa `d.funnel \|\| 'Sem funil'` — existe um balde de gráfico chamado "Sem funil", mas ele não distingue "a regra não pôde ser avaliada" de um agrupamento legítimo. Dono: `in3-08b`, ao lado do `cr4-01c` já existente (que cobre `skipReason`) |
| Normalização sem remoção de acentos (assimetria com `isExcludedStage`) | **fora-de-escopo-com-medição** | Decisão D-IN3-08-h. Medido: `'beefor'` é ASCII puro e nenhum dos nomes de funil das fixtures (`Funil 1`, `Funil 2`, `Comercial`) tem acento. Dono: `in3-08b` |
| Alarme do 04-28 (`skippedCategoriaIndecidivel`) | **verificada-e-sã** | Gate na Task 3: `git diff -U0 backend/src/scheduler.js \| grep -c "skippedCategoriaIndecidivel"` = **0**. O bloco do 04-28 fica byte a byte, e os cenários A–G continuam verdes sem edição (`git diff --name-only -- backend/test/` vazio na Task 3) |
</inventario_de_irmaos>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — oráculo unitário do contrato novo (MODO 2) e o terceiro consumidor</name>

  <read_first>
    - `backend/test/agendor.funnel.test.js` — INTEIRO (44 linhas). É o oráculo a reescrever.
    - `backend/src/agendor.js` — só a região de `NO_OWNER_NOTIFY_FUNNELS` e `shouldNotifyOwner`
      (âncoras nomeadas; ~10 linhas). Não editar nada neste arquivo nesta task.
    - `backend/test/emailer.resumoIndecidivel.test.js` — o cabeçalho, o helper `negocio(...)`, as
      constantes `USERS`/`COMERCIAL` e o caso **(4)**, que é o molde do caso novo.
    - A seção `<decisoes_do_usuario>` deste plano, decisões **D-IN3-08-a** e **D-IN3-08-b**.
  </read_first>

  <files>backend/test/agendor.funnel.test.js, backend/test/emailer.resumoIndecidivel.test.js</files>

  <behavior>
    `backend/test/agendor.funnel.test.js` — 6 casos passam a 8. Ordem e papel de cada um:

    1. `'beefor'` exato → NÃO notifica. **Inalterado.**
    2. `'Beefor'` (case-insensitive) → NÃO notifica. **Inalterado.**
    3. `' beefor '` (trim) → NÃO notifica. **Inalterado.**
    4. `'beefor vendas'` → **NÃO notifica** (era `true`; INVERTE, D-IN3-08-b).
    5. `'beeforx'` → **NÃO notifica** (era `true`; INVERTE). O comentário deste caso registra que o
       usuário viu explicitamente esta consequência e a escolheu — é o caso que impede alguém de
       "consertar" a substring com word-boundary por achar que foi descuido.
    6. `'Beefor Comercial'` → **NÃO notifica**. **NOVO.** É a renomeação real que motivou a
       correção — o modo de falha que não depende de erro nenhum.
    7. `'Comercial'` → **NOTIFICA**. **NOVO, e obrigatório.** É a TESTEMUNHA de não-supressão: com
       os dois QUIRK invertidos, os únicos `true` restantes seriam os de funil ausente, e uma
       implementação que devolvesse `false` sempre passaria em 7 dos 8 casos. `'Comercial'` não é
       um valor inventado — é o funil do molde 101 e de 10 das 15 fixtures.
    8. `funnel: null` e `{}` → **NOTIFICA**. A asserção principal **não muda** (D-IN3-08-d). O
       comentário passa a registrar que isto é fail-open DELIBERADO, com a razão medida (15/15 das
       fixtures têm a estrutura) e o risco que um fail-safe uniforme reintroduziria (CR4-01).

    O cabeçalho do arquivo é reescrito: hoje ele afirma que a supressão é "um match EXATO" e que
    "nomes próximos NÃO são suprimidos". Depois desta task essa afirmação seria falsa, e a fase já
    produziu dois achados (WR4-02, WR4-03) que eram exatamente comentários afirmando o contrário do
    código. O cabeçalho novo declara: a comparação é por substring sobre o nome normalizado; funil
    ausente continua notificando de propósito; e a palavra QUIRK não aparece mais no arquivo.

    `backend/test/emailer.resumoIndecidivel.test.js` — caso **(8)**, ao **FINAL do arquivo, depois
    do caso (7)**, e modelado no caso (4):
    três negócios do mesmo `ownerId: 11` — `NEGOCIO-NORMAL` (sem `funnel`, cai no padrão),
    `NEGOCIO-BEEFOR-RENOMEADO` com `funnel: 'Beefor Comercial'`, e `NEGOCIO-BEEFOR-EXATO` com
    `funnel: 'Beefor'`. Um único e-mail sai; o HTML contém `NEGOCIO-NORMAL` e **não** contém
    nenhum dos dois outros títulos. A asserção é sobre o CORPO enviado, não sobre `results.length`
    — um conserto que só contasse diferente e continuasse listando o card passaria por qualquer
    asserção de quantidade (lição do 04-32). O caso é **aditivo**: nenhuma linha dos casos (1) a (7)
    é removida ou alterada.
  </behavior>

  <action>
    Reescreva `backend/test/agendor.funnel.test.js` conforme o `<behavior>` acima, preservando as
    três primeiras asserções byte a byte e invertendo exclusivamente as duas dos ex-QUIRK. Acrescente
    os casos 6 e 7. Preserve a asserção do caso 8 e reescreva apenas o comentário dele. Cite as
    decisões pelos identificadores D-IN3-08-a, -b, -c e -d nos comentários, e refira-se a outros
    trechos por âncora nomeada (`shouldNotifyOwner`, `NO_OWNER_NOTIFY_FUNNELS`, `isExcludedStage`),
    nunca por número de linha.

    Acrescente o caso (8) ao **FINAL** de `backend/test/emailer.resumoIndecidivel.test.js`, depois do
    caso (7) — e **não** logo depois do (4), apesar de ser dele que o caso novo herda a forma. Os
    casos (5), (6) e (7) já ocupam fisicamente aquela posição, sob o cabeçalho de seção
    `// ── WR4-07: responsável sem nome ──`, e inserir um (8) antes deles quebraria a numeração
    sequencial que este repositório mantém com rigor. Dê ao caso novo o seu próprio cabeçalho de
    seção, no mesmo estilo de caixa do arquivo, nomeando o achado (`in3-08` / MODO 2) — é assim que
    o arquivo já separa o bloco do WR4-07 do bloco original.

    Reuse o helper `negocio(...)`, `USERS` e `enviosCapturados` que já existem — não crie armação
    nova, não toque no `beforeEach` nem no `after`.

    Rode `npm run format` antes de medir qualquer contagem: o Biome decide a quebra de linha, e
    contagens medidas antes dele divergem depois (lição registrada no 04-32).

    NÃO edite nada em `backend/src/` nesta task. O vermelho é o produto.

    Se algum dos casos 4, 5, 6 ou (8) ficar VERDE já neste estado, **PARE e reporte**: significa que
    a comparação por substring já existe por um caminho não medido, e o plano inteiro está apoiado
    numa premissa falsa.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && node --test test/agendor.funnel.test.js test/emailer.resumoIndecidivel.test.js; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | O RED existe e é dos casos certos | `cd backend && node --test test/agendor.funnel.test.js` | exit != 0, exatamente **3 fail** (casos 4, 5 e 6) e **5 pass** |
    | O RED do terceiro consumidor | `cd backend && node --test test/emailer.resumoIndecidivel.test.js` | exit != 0, exatamente **1 fail** (o caso 8) e **7 pass** |
    | Casos no oráculo unitário | `grep -c '^test(' backend/test/agendor.funnel.test.js` | **8** (era 6) |
    | A palavra que dizia "comportamento ATUAL" não sobrevive | `grep -c 'QUIRK' backend/test/agendor.funnel.test.js` | **0** (era 2) |
    | A testemunha de não-supressão existe | `grep -o "'Comercial'" backend/test/agendor.funnel.test.js \| wc -l` | **≥ 1** (era 0) |
    | A renomeação real está pinada | `grep -c 'Beefor Comercial' backend/test/agendor.funnel.test.js` | **≥ 1** |
    | Só as DUAS asserções dos ex-QUIRK saíram | `git diff -U0 backend/test/agendor.funnel.test.js \| grep -c '^-.*assert'` | **2** — nem uma a mais |
    | O caso do emailer é puramente aditivo | `git diff -U0 backend/test/emailer.resumoIndecidivel.test.js \| grep -c '^-[^-]'` | **0** |
    | Casos no oráculo do emailer | `grep -c '^test(' backend/test/emailer.resumoIndecidivel.test.js` | **8** (era 7) |
    | Nenhum código de produção mudou | `git diff --name-only -- backend/src/` | **vazio** |
    | Referência por número de linha não entrou | `git diff -U0 backend/test/ \| grep -c '\.js:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    Os 3 casos unitários do contrato novo e o caso (8) do emailer estão VERMELHOS, os demais verdes,
    `backend/src/` intocado, e o commit `test(04-35): RED` registra o vermelho.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — os consumidores runCheck e runCheckOnly sob funil renomeado e sob funil ausente</name>

  <read_first>
    - `backend/test/scheduler.categoriaIndecidivel.test.js` — o **cabeçalho** (a lista de cenários
      A–G e o porquê de cada par), a armação (`servirDeals`, `servirDealsDoFunilBeefor`,
      `organizacaoDe`, `USUARIOS`, o `installFakeAxios`, o `beforeEach`), os helpers
      `idsPrometidosPelaPrevia` / `idsNotificadosDeFato` / `envios` / `linhasDoDeal`, e os cenários
      **E**, **F** e **G** por inteiro — são os moldes dos três casos novos.
    - `backend/src/scheduler.js` — a cadeia de guardas de `runCheck` (da criação de `dealResult` até
      o `else` final), o bloco do alarme do 04-28 depois do laço, e `runCheckOnly` inteira.
    - A seção `<decisoes_do_usuario>`, decisões **D-IN3-08-c** a **D-IN3-08-g**, e a tabela do Grupo 3
      do `<inventario_de_irmaos>`.
  </read_first>

  <files>backend/test/scheduler.categoriaIndecidivel.test.js</files>

  <behavior>
    Um helper IRMÃO, aditivo: `servirDealsComFunil(idPrimeiro, idSegundo, nomeDoFunil, idsAlvo = null)`.
    Ele repete a forma de `servirDealsDoFunilBeefor` — que fica **intocado**, porque é a armação de E
    e G — com duas diferenças: o nome do funil vem por parâmetro, e `nomeDoFunil === null` significa
    `dealStage` **sem a chave `funnel`** (a forma que um payload de forma mudada teria). O `name` da
    etapa continua vindo do molde, pelo mesmo motivo já escrito no arquivo: uma etapa trocada por
    descuido excluiria o negócio ainda em `isExcludedStage`, e o caso mediria uma lista vazia.

    **Cenário H — RENOMEAÇÃO: o funil renomeado suprime no envio E na prévia.**
    `servirDealsComFunil(primeiro, segundo, 'Beefor Comercial', new Set([primeiro]))` — só o
    primeiro é renomeado; o segundo mantém o funil do molde (`'Comercial'`) e é a testemunha
    elegível. `orgsQueFalham` vazia. A prévia roda ANTES do envio (ordem obrigatória: `runCheckOnly`
    é somente leitura e `runCheck` grava — invertida, a linha 'sent' mudaria a resposta da dedup
    dentro da prévia). Asserções, nesta ordem:
    (a) pré-condição `previstoPrimeiro.funnel === 'Beefor Comercial'` — o negócio chegou com o funil
    que deve excluí-lo; (b) `previstoPrimeiro.seraNotificado === false` — **é aqui que o achado
    fica vermelho**; (c) `previstoSegundo.seraNotificado === true` — a supressão não é larga demais;
    (d) guarda de não-vacuidade: `idsPrometidosPelaPrevia(previa)` deepEqual `[segundo]`;
    (e) depois de `runCheck`: `idsNotificadosDeFato(r)` deepEqual os prometidos; (f) `envios(DONO_1)`
    e `envios(AUTOR_1)` = 0, `envios(DONO_2)` e `envios(AUTOR_2)` ≥ 1; (g) `r.funilNaoAvaliado === 0`
    — funil renomeado é funil PRESENTE, e um conserto que tratasse rename como ausência ficaria
    vermelho aqui.

    **Cenário I — FORMA DO PAYLOAD: funil ausente continua notificando, e a rodada AVISA.**
    `servirDealsComFunil(primeiro, segundo, null)` — os DOIS sem a chave `funnel`. `orgsQueFalham`
    vazia. Asserções, nesta ordem — **todas as de COMPORTAMENTO antes de todas as de INSTRUMENTO**,
    para que o vermelho do RED distinga "o fail-open regrediu" de "o instrumento ainda não existe":

    Comportamento (símbolos que JÁ existem hoje, e que portanto podem ficar verdes no RED):
    (a) `r.stale === 2`; (b) `r.notified === 2` e os quatro `envios(...) ≥ 1` — **o fail-open é a
    asserção que não pode mudar** (D-IN3-08-c); (c) `previstoPrimeiro.seraNotificado === true` e
    `previstoSegundo.seraNotificado === true` — a prévia não promete menos que o envio (o campo
    `seraNotificado` existe desde o 04-31).

    Instrumento (símbolos que a **Task 3** introduz — no RED os três são `undefined`, e o runner
    para no primeiro): (d) `r.funilNaoAvaliado === 2`; (e) `previstoPrimeiro.funilAusente === true`
    — prova que o campo novo atravessa o spread `{ ...deal }` de `runCheckOnly` (verificar, não
    presumir); (f) `r.errors.length === 1` e `typeof r.error === 'string'`, com
    `r.error.includes('funil')` verdadeiro — a mudança de forma deixou de ser invisível.

    A classificação de (e) como INSTRUMENTO, e não como comportamento, é o que mantém a previsão de
    RED coerente: `funilAusente` não existe em lugar nenhum do repositório hoje (medido: 0
    ocorrências em `backend/src` e `backend/test`), então uma asserção sobre ele **não pode** ficar
    verde antes da Task 3, e colocá-la entre as de comportamento faria a condição de PARAR abaixo
    disparar um falso positivo.

    **Cenário J — SIMÉTRICO DE CAUSA: funil presente e não-Beefor não dispara alarme nenhum.**
    `servirDealsComFunil(primeiro, segundo, 'Comercial')` — construção IDÊNTICA à de I, mudando
    exclusivamente o funil, que é o que faz o par medir a CAUSA e não outra coisa. Asserções, na
    ordem: (a) `r.stale === 2`, `r.notified === 2` e os quatro `envios(...) ≥ 1`; (b)
    `r.funilNaoAvaliado === 0`; (c) `r.error === undefined`; (d) `r.errors.length === 0`. Sem o J,
    qualquer implementação que ligasse o alarme em `notified === 0`, em `skipped === stale` ou
    simplesmente sempre passaria por I, e o operador receberia um erro por rodada.

    O cabeçalho do arquivo ganha as três linhas novas na lista de cenários (hoje A–G) e um parágrafo
    curto dizendo por que H, I e J existem: H fecha o MODO 2 pelos dois consumidores do scheduler;
    I e J são o par alarme/silêncio do MODO 1, com o mesmo papel que D e E têm para o 04-28.
  </behavior>

  <action>
    Acrescente `servirDealsComFunil` ao lado de `servirDealsDoFunilBeefor`, sem tocar neste último
    (E e G dependem dele exatamente como está), e acrescente os cenários H, I e J ao final do
    arquivo, depois de G. Atualize a lista de cenários do cabeçalho.

    Reuse a armação existente inteira — `avancarRelogioAte`, `envios`, `linhasDoDeal`,
    `idsPrometidosPelaPrevia`, `idsNotificadosDeFato`, `organizacaoDe`, o `beforeEach` — sem duplicar
    nada. Não altere `installFakeAxios`, `USUARIOS`, `MOLDE` nem qualquer asserção dos cenários A–G.

    Rode `npm run format` antes de medir contagens.

    NÃO edite `backend/src/` nesta task.

    Vermelho ESPERADO e o motivo de cada um, para conferência: **H** falha na asserção (b)
    (`seraNotificado` é `true` hoje, porque `'Beefor Comercial' !== 'beefor'`); **I** falha na
    asserção **(d)**, com a forma `undefined !== 2`, DEPOIS de passar por (a), (b) e (c) — e é esse
    trecho verde que prova que o fail-open já está correto hoje e continua correto; **J** falha na
    asserção (b), com a forma `undefined !== 0`, DEPOIS de (a). Em I, as asserções (e) e (f) também
    falhariam se fossem alcançadas — são do mesmo instrumento —, mas o runner para na primeira, e é
    por isso que a ordem dentro do bloco de instrumento importa: (d) é a que produz o diagnóstico
    mais legível. Nota de método herdada do 04-28: um cenário simétrico que assere um contador ainda
    inexistente **fica vermelho no RED e isso não é anomalia** — o que importa é ele falhar na
    asserção do contador e não antes dela.

    Se **H** ficar verde, PARE e reporte: a comparação já é por substring por caminho não medido.
    Se **I** falhar em (a), (b) ou (c) — e SOMENTE nessas três —, PARE e reporte: ou a armação de
    funil ausente não está produzindo o payload esperado, ou o fail-open regrediu, e nos dois casos a
    premissa do plano caiu. Falhar em (d) é o resultado ESPERADO e não interrompe nada.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && node --test test/scheduler.categoriaIndecidivel.test.js; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | O RED existe e A–G continuam verdes | `cd backend && node --test test/scheduler.categoriaIndecidivel.test.js` | exit != 0, **3 fail** (H, I, J) e **7 pass** |
    | I falha no instrumento, não no comportamento | saída do runner | a linha de falha de **I** é a asserção **(d)**, do contador (`undefined !== 2`) — nunca uma de `stale`/`notified`/`envios`/`seraNotificado` |
    | J falha no instrumento, não no comportamento | saída do runner | a linha de falha de **J** é a do contador (`undefined !== 0`) |
    | Cenários no arquivo | `grep -c '^test(' backend/test/scheduler.categoriaIndecidivel.test.js` | **10** (era 7) |
    | O helper de E e G ficou intocado | `grep -o 'servirDealsDoFunilBeefor' backend/test/scheduler.categoriaIndecidivel.test.js \| wc -l` | **3** (inalterado) |
    | O helper irmão existe | `grep -o 'servirDealsComFunil' backend/test/scheduler.categoriaIndecidivel.test.js \| wc -l` | **≥ 4** (declaração + 3 chamadas) |
    | O cabeçalho declara os cenários novos | `grep -cE '^//   (H\|I\|J) ' backend/test/scheduler.categoriaIndecidivel.test.js` | **3** |
    | A task é aditiva — nenhuma asserção de A–G saiu | `git diff -U0 backend/test/scheduler.categoriaIndecidivel.test.js \| grep -c '^-[^-]'` | **0** |
    | Nenhum código de produção mudou | `git diff --name-only -- backend/src/` | **vazio** |
    | Referência por número de linha não entrou | `git diff -U0 backend/test/ \| grep -c '\.js:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    H, I e J estão vermelhos pelos motivos previstos, A–G verdes, `backend/src/` intocado, e o commit
    `test(04-35): RED dos consumidores` registra o vermelho.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — substring em shouldNotifyOwner, sinal de funil não avaliado, e o residual com dono</name>

  <read_first>
    - `backend/src/agendor.js` — `NO_OWNER_NOTIFY_FUNNELS`, `shouldNotifyOwner`, o comentário de
      `EXCLUDED_STAGE_WORDS`/`isExcludedStage` (para NÃO tocar), e o laço de enriquecimento de
      `getStaleDeals` do `if (categoriaIndecidivel)` até o `return allDeals`.
    - `backend/src/scheduler.js` — o literal de `results`, o topo do laço (`results.checked++` e a
      criação de `dealResult`), a guarda de funil, e o bloco do alarme do 04-28 depois do laço.
    - `.planning/phases/04-confiabilidade-das-integra-es/04-28-SUMMARY.md`, seção "O enquadramento que
      ficou escrito no código" — é a redação a espelhar, e a formulação falsa que ela rejeita não
      pode reaparecer aqui.
    - A seção `<decisoes_do_usuario>` inteira e o `<inventario_de_irmaos>` deste plano.
  </read_first>

  <files>backend/src/agendor.js, backend/src/scheduler.js, .planning/todos/pending/in3-08b-comparacao-exata-nos-demais-filtros.md</files>

  <action>
    **(a) MODO 2 — `backend/src/agendor.js`, `shouldNotifyOwner`.** Mantenha a normalização
    existente (`(deal?.funnel || '').trim().toLowerCase()`) e troque a comparação: em vez de
    `NO_OWNER_NOTIFY_FUNNELS.includes(funnel)`, use `NO_OWNER_NOTIFY_FUNNELS.some(...)` verificando se
    o nome normalizado **contém** cada termo da lista. Sem `normalize('NFD')` (D-IN3-08-h) e sem
    word-boundary (D-IN3-08-a).

    O comentário acima da constante precisa registrar, além da regra de negócio que já está lá:
    (i) que a comparação é por substring, e que um funil renomeado no CRM para conter o termo
    continua suprimido — que é o modo de falha que não dependia de erro nenhum; (ii) que
    `'beeforx'` também passa a ser suprimido, que essa consequência foi vista e escolhida pelo
    usuário, e que quem quiser "consertar" isso com word-boundary está desfazendo uma decisão, não
    corrigindo um descuido; (iii) que a irmã `isExcludedStage`, no mesmo módulo, já usava
    correspondência parcial pelo mesmo motivo — o funil era a exceção; (iv) que a string vazia de um
    funil ausente **não** casa com nenhum termo, então o fail-open do MODO 1 é preservado **por
    construção** e não por acidente. Cite os casos do oráculo por nome (`agendor.funnel.test.js`),
    nunca por número de linha.

    **(b) MODO 1, o dado — `getStaleDeals`.** Acrescente `funilAusente: !deal.dealStage?.funnel?.name`
    ao objeto empurrado em `allDeals`, imediatamente ao lado de `funnel`. É a mesma forma de
    `categoriaIndecidivel`: um booleano derivado no ÚNICO lugar que conhece o payload cru, para que
    nenhum consumidor precise reimplementar o critério de "ausente".

    **(c) MODO 1, o aviso — `getStaleDeals`, depois do laço e antes do `return`.** Conte os negócios
    com `funilAusente` e, se houver algum, emita **uma** `logger.warn` com tag `[Agendor]` contendo
    apenas os dois inteiros (quantos e de quantos) e texto fixo: que a regra de supressão por funil
    não pôde ser avaliada neles, que todos seguem elegíveis para notificação por decisão deliberada,
    e que a forma do payload da Agendor pode ter mudado. **Nenhum** id, nome de negócio, nome de
    organização ou objeto de erro entra na mensagem (T-04-35-02). O comentário registra por que o
    aviso é AGREGADO e não por negócio (D-IN3-08-e): `getStaleDeals` é também o caminho de leitura do
    painel, com auto-refresh, e uma linha por negócio inundaria o log a cada atualização de tela.

    **(d) MODO 1, o contador — `backend/src/scheduler.js`.** `funilNaoAvaliado: 0` no literal de
    `results`, ao lado de `skippedCategoriaIndecidivel`, para que o campo exista SEMPRE no payload e
    nenhum consumidor precise distinguir `undefined` de zero. O incremento fica no **topo do laço**,
    logo depois de `results.checked++` e ANTES da guarda de dedup, condicionado a `deal.funilAusente`
    — e **não** faz `continue` nem toca em `dealResult.skipped`. O comentário registra D-IN3-08-f: a
    posição é decisão, porque um contador que só incrementa depois de outra guarda ter feito
    `continue` não alcança o denominador `results.stale` numa rodada mista, que é exatamente o
    residual `cr4-01b` desta fase; aqui numerador e denominador percorrem o mesmo conjunto. Registre
    também que o nome NÃO começa com `skipped` de propósito: nada foi suprimido — estes negócios
    **foram notificados**.

    **(e) MODO 1, o alarme — depois do laço, ANTES do bloco do 04-28 (D-IN3-08-g).** Quando
    `results.stale > 0 && results.funilNaoAvaliado === results.stale`, monte a mensagem com apenas
    inteiros e texto fixo, preencha `results.error`, empurre em `results.errors` e emita
    `logger.error` com tag `[Scheduler]`. A mensagem precisa dizer, por extenso, que a rodada
    CONCLUIU, que **a supressão por funil não impediu nenhuma notificação** e que este é um alarme de
    FORMA e não de supressão — um operador que leia só o campo de erro não pode concluir que perdeu
    envios por causa deste alarme.

    A afirmação é ESTREITA de propósito, e a redação larga é PROIBIDA: **não** escreva "ninguém
    deixou de ser notificado". Por D-IN3-08-f o contador incrementa no topo do laço, antes das
    guardas de dedup e de categoria indecidível, então a condição total pode valer numa rodada
    COMPOSTA em que alguns desses mesmos negócios foram pulados por OUTRA causa — e nessa rodada a
    frase larga seria factualmente falsa, minando justamente a ameaça que a mensagem existe para
    mitigar (T-04-35-05). O que o contador garante é só isto: a regra de funil não tirou ninguém do
    envio. Os cenários I e J isolam `funilAusente` como causa única e portanto **não** exercitam a
    rodada composta; a defesa aqui é a redação estreita, não um caso de teste, e o comentário diz
    isso para que a frase não seja "melhorada" depois.

    O comentário registra também: (i) que o bloco é **ADITIVO**, mora depois do laço, não condiciona
    nenhum `continue` e portanto **não decide quem recebe e-mail** — nenhuma escolha de limiar pode
    fazer isso; (ii) por que ele vem antes do bloco do 04-28 (precedência do campo escalar na colisão, com
    as duas mensagens no array que a UI renderiza); (iii) que o par que impede o alarme de virar
    ruído diário são os cenários I e J, e que J existe justamente para reprovar uma implementação que
    ligasse o alarme por quantidade em vez de por causa.

    **Não escreva** a formulação falsa que o 04-28 rejeitou por escrito ("um limiar menor mudaria o
    comportamento por negócio"). Ela ficaria disponível como precedente para justificar, sob
    enquadramento errado, uma mudança real de comportamento numa rodada futura.

    **(f) O residual com dono.** Crie
    `.planning/todos/pending/in3-08b-comparacao-exata-nos-demais-filtros.md`, no formato dos demais
    todos da fase (frontmatter com `id`, `type: todo`, `status: pending`, `priority`, `created`,
    `source`, `tags`), carregando **as medições que o excluíram do escopo**, item por item, conforme o
    `<inventario_de_irmaos>`: `EXCLUDED_CATEGORIES` e `EXCLUDED_OWNERS` (1 ocorrência não-comentário
    cada, mesma direção de falha, fora de escopo porque mudar a comparação muda QUEM RECEBE e a
    decisão do usuário cobre só o funil); a normalização sem remoção de acentos (D-IN3-08-h, com o
    argumento da constraint de processo); a ausência de contador agregado no resumo semanal (com o
    argumento do disparo diário chegar cinco vezes mais cedo); e a invisibilidade de `funilAusente`
    na UI (grep = 0 em `frontend/src`, ao lado do `cr4-01c` já existente). Cada item nomeia a
    medição, não só a intenção. **Nenhum todo existente é editado** — inclusive `in3-08`, que quem
    fechar a fase resolverá.

    Rode `npm run format` antes de medir contagens. Não instale nada: `package.json` e lockfiles
    ficam intocados (T-04-35-SC).
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && npm test && npm run test:coverage && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | Suíte inteira verde | `cd backend && npm test` | **192/192** (era 186; +2 unitários, +1 emailer, +3 scheduler) |
    | Cobertura | `cd backend && npm run test:coverage` | exit **0**; `agendor.js` com **100%** linhas e branches **≥ 91,72%** — registrar o medido no SUMMARY e, se divergir, registrar como divergência com o número, sem forçar |
    | Lint | `cd backend && npm run lint` | exit **0**, **44** warnings (baseline) |
    | A comparação virou substring | `grep -v '^\s*//' backend/src/agendor.js \| grep -c 'NO_OWNER_NOTIFY_FUNNELS.some('` | **1** |
    | E a igualdade exata sumiu | `grep -v '^\s*//' backend/src/agendor.js \| grep -c 'NO_OWNER_NOTIFY_FUNNELS.includes('` | **0** |
    | A constante não foi mexida | `grep -v '^\s*//' backend/src/agendor.js \| grep -o 'NO_OWNER_NOTIFY_FUNNELS' \| wc -l` | **2** (inalterado) |
    | O campo novo existe e é usado | `grep -v '^\s*//' backend/src/agendor.js \| grep -o 'funilAusente' \| wc -l` | **≥ 2** (o campo do objeto + a contagem do aviso agregado) — ocorrências, não linhas |
    | O aviso é AGREGADO, não por negócio | `grep -v '^\s*//' backend/src/agendor.js \| grep -c 'logger.warn'` | **2** (o de categoria, dentro do laço, + o novo, fora dele) |
    | O aviso não vaza dado sensível | inspeção do texto da `logger.warn` nova | só inteiros e texto fixo — sem id, sem nome de negócio/organização, sem objeto de erro |
    | O alarme não afirma mais do que garante | `grep -c 'ninguém deixou de ser notificado' backend/src/scheduler.js` | **0** — a redação larga é proibida (T-04-35-05); a mensagem afirma apenas que a supressão por funil não impediu notificação |
    | As irmãs de filtro não foram tocadas | `git diff -U0 backend/src/agendor.js \| grep -c 'EXCLUDED_STAGE_WORDS\|isExcludedStage\|EXCLUDED_CATEGORIES\|EXCLUDED_OWNERS\|NEGOCIO_CATEGORIES'` | **0** |
    | O alarme do 04-28 ficou byte a byte | `git diff -U0 backend/src/scheduler.js \| grep -c 'skippedCategoriaIndecidivel'` | **0** |
    | O contador novo existe nos três pontos | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'funilNaoAvaliado' \| wc -l` | **3** (literal, incremento, condição do alarme) |
    | O bloco novo não decide quem recebe | `grep -c 'continue;' backend/src/scheduler.js` | **3** (inalterado) |
    | A segunda superfície de erro foi preenchida | `grep -v '^\s*//' backend/src/scheduler.js \| grep -c 'results.error = '` | **3** (era 2) |
    | A prévia não foi editada à mão | `git diff -U0 backend/src/scheduler.js \| grep -c 'seraNotificado'` | **0** — o cenário I prova que o campo flui pelo spread, sem edição |
    | Nenhum teste foi editado para ficar verde | `git diff --name-only -- backend/test/` | **vazio** |
    | Nenhum pacote instalado | `git status --porcelain package.json package-lock.json backend/package.json backend/package-lock.json` | **vazio** |
    | O residual tem dono e medição | `ls .planning/todos/pending/in3-08b-comparacao-exata-nos-demais-filtros.md` | existe, com as 5 medições do Grupo 2/3 do inventário |
    | Nenhum todo existente foi editado | `git status --porcelain .planning/todos/` | apenas **1** arquivo, e não rastreado |
    | Referência por número de linha não entrou | `git diff -U0 backend/src/ \| grep -c '\.js:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    Os 7 casos vermelhos das Tasks 1 e 2 estão verdes sem que nenhum arquivo de teste tenha sido
    tocado, a suíte inteira passa em 192/192, cobertura e lint saem 0, as constantes irmãs estão fora
    do diff, o residual `in3-08b` existe com as medições, e o commit `fix(04-35): GREEN` registra a
    mudança.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| API Agendor → `getStaleDeals` | O **nome do funil** é texto livre editado por administradores no CRM e atravessa a fronteira sem validação. Ele decide um filtro de elegibilidade — quem recebe e-mail |
| API Agendor → forma do payload | A presença de `dealStage.funnel.name` é uma premissa de forma sobre um sistema de terceiro, e é justamente o que este plano deixa de assumir em silêncio |
| `logger` → `logs/error.log` e `logs/access.log` | Linhas de log são artefato persistido; um objeto de erro do axios carrega `config.headers` com o `AGENDOR_TOKEN` |
| `runCheck` → `results` → Dashboard | O payload da rodada é renderizado na UI autenticada; as mensagens de alarme aparecem para o operador |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-04-35-01 | Tampering | `shouldNotifyOwner` / `NO_OWNER_NOTIFY_FUNNELS` | **accept** | A substring amplia o conjunto suprimido: um funil de terceiro cujo nome contenha "beefor" (ex.: `'beeforx'`) deixa de notificar. Risco aceito **explicitamente pelo usuário** em 2026-08-05, com a consequência apresentada; o CRM é interno e nomes de funil são criados por administradores da Cadmus. Pinado pelo caso 5 do oráculo unitário, para que a escolha não seja desfeita por engano |
| T-04-35-02 | Information Disclosure | `logger.warn` de `getStaleDeals` e `logger.error` do alarme de `runCheck` | **mitigate** | As duas mensagens carregam **apenas inteiros e texto fixo** — nenhum objeto de erro, nenhum id, nenhum nome de negócio ou organização. Mesma regra do CR-02 (04-09) e do alarme do 04-28. Critério de aceite na Task 3 |
| T-04-35-03 | Denial of Service | `getStaleDeals` como caminho de leitura do painel | **mitigate** | O aviso é **agregado** (uma linha por chamada), não por negócio (D-IN3-08-e). Numa mudança de forma do payload com auto-refresh do `DealsList.jsx`, o aviso por negócio produziria N linhas por atualização de tela; um log inundado é um log que não se lê, e a mitigação viraria o defeito |
| T-04-35-04 | Repudiation | Supressão por funil no `runCheck` | **accept (verificada)** | O negócio suprimido por funil já registra `dealResult.skipReason` nomeando o funil, e a supressão continua sem linha em `notification_log` porque não houve evento de envio — contrato herdado, intocado. O diff não toca essa guarda |
| T-04-35-05 | Spoofing | Alarme de FORMA confundido com perda de notificação | **mitigate** | A mensagem do alarme afirma, por extenso, que a rodada CONCLUIU e que **a supressão por funil não impediu nenhuma notificação**. Sem isso, um operador que leia só `results.error` concluiria que perdeu envios e dispararia manualmente, gerando duplicatas. A afirmação é deliberadamente ESTREITA: a redação larga ("ninguém deixou de ser notificado") seria falsa numa rodada composta em que negócios também foram pulados por dedup ou por categoria indecidível — o contador incrementa antes dessas guardas (D-IN3-08-f) —, e uma mitigação que mente em parte dos casos é pior que nenhuma. Gate na Task 3 |
| T-04-35-SC | Tampering | Instalação de pacotes (npm) | **mitigate** | Este plano **não instala nada**. Nenhuma dependência nova, nenhum bump. Gate: `git status --porcelain` de `package.json` e dos lockfiles vazio na Task 3. Não havendo instalação, o portão humano de legitimidade de pacote não se aplica |
</threat_model>

<verification>
1. `cd backend && npm test` — **192/192** verdes (era 186).
2. `cd backend && npm run test:coverage` — exit 0; `agendor.js` em 100% linhas e branches ≥ 91,72%
   (medir e registrar; divergência é registrada com o número, nunca forçada).
3. `cd backend && npm run lint` — exit 0, 44 warnings (baseline inalterado).
4. Os TRÊS consumidores provados por caso de teste, não por leitura: cenário **H** (`runCheck` e
   `runCheckOnly`) e caso **(8)** (`sendOwnerWeeklySummary`).
5. O par **I + J** provado: a mudança de forma do payload dispara alarme; funil presente e
   não-Beefor não dispara nada.
6. Os cenários **A–G** e os casos **(1)–(7)** do emailer verdes **sem edição** —
   `git diff --name-only -- backend/test/` vazio na Task 3.
7. O inventário de irmãos: cada item classificado tem grep ou caso de teste, e os
   fora-de-escopo têm dono no todo `in3-08b`.
</verification>

<success_criteria>
- Um funil renomeado no CRM contendo "beefor" volta a ser suprimido nos três consumidores, e isso
  está pinado por teste — não por comentário.
- Os DOIS casos QUIRK foram reescritos para o contrato novo; a palavra QUIRK não sobrevive no
  arquivo, e existe uma testemunha de não-supressão impedindo um conserto largo demais.
- Funil ausente continua notificando — a asserção principal do caso de funil ausente não mudou —, e
  agora é contado e avisado, com o alarme discriminando CAUSA e não quantidade.
- Nenhum comportamento de quem-recebe mudou fora do que a decisão do usuário autoriza; a cadeia de
  guardas de `runCheck` continua com 3 `continue;` e o alarme do 04-28 está byte a byte.
- As constantes de filtro irmãs estão classificadas com medição, e as que ficaram de fora têm dono.
</success_criteria>

<output>
Crie `.planning/phases/04-confiabilidade-das-integra-es/04-35-SUMMARY.md` ao terminar.

Registre obrigatoriamente: o RED literal das Tasks 1 e 2 (saída do runner, com a asserção em que cada
caso falhou); todos os números medidos ao lado dos prescritos, **inclusive as divergências**, que
são registradas com o número medido e o motivo, nunca ajustadas para bater — esta fase acumulou dez
divergências de contagem e nenhuma foi forçada; a cobertura medida de `agendor.js`; o inventário de
irmãos com a classificação final de cada item; e o que este plano **não** fecha (`in3-08b`,
`cr4-01b`, `cr4-01c` e os demais todos pendentes).
</output>
