---
phase: 04-confiabilidade-das-integra-es
plan: 17
type: execute
wave: 10
depends_on: ["04-16"]
files_modified:
  - backend/src/agendor.js
  - backend/src/scheduler.js
  - backend/src/emailer.js
  - backend/src/routes/deals.js
  - backend/test/notificationStatus.partialFailure.test.js
  - .planning/todos/pending/in2-01-fetchwithretry-sem-tentativa.md
  - .planning/todos/pending/in2-02-relogio-falso-em-before.md
  - .planning/todos/pending/in2-03-mensagem-de-erro-interpola-id.md
  - .planning/todos/pending/in2-04-parcial-sent-invisivel.md
autonomous: true
gap_closure: true
requirements: [REL-01, REL-03, REL-04, REL-05, REL-06]

must_haves:
  truths:
    - "Nenhum comentário de backend/src referencia outro trecho por NÚMERO DE LINHA: as referências passam a ser por âncora estável (nome de função, identificador, nome de arquivo ou nome de caso de teste)"
    - "O mesmo vale para os arquivos de teste criados ou editados nas duas rodadas de gap closure"
    - "Os 4 achados Info da rodada 2 (IN2-01 a IN2-04) existem como arquivos de todo pendente, cada um com o achado na íntegra, o motivo de não ter sido tratado agora, a correção sugerida e o link de volta para 04-REVIEW.md"
    - "Nenhuma linha de código muda neste plano: o diff dos arquivos .js é exclusivamente de comentário"
  artifacts:
    - path: ".planning/todos/pending/in2-01-fetchwithretry-sem-tentativa.md"
      provides: "Registro de IN2-01 — fetchWithRetry devolve undefined quando retries <= 0"
      min_lines: 25
    - path: ".planning/todos/pending/in2-02-relogio-falso-em-before.md"
      provides: "Registro de IN2-02 — relógio falso habilitado em before, não em beforeEach"
      min_lines: 25
    - path: ".planning/todos/pending/in2-03-mensagem-de-erro-interpola-id.md"
      provides: "Registro de IN2-03 — mensagem de erro de getDealById interpola o valor recusado"
      min_lines: 25
    - path: ".planning/todos/pending/in2-04-parcial-sent-invisivel.md"
      provides: "Registro de IN2-04 — destinatário que faltou num parcial 'sent' nunca é retentado e não tem sinal"
      min_lines: 25
  key_links:
    - from: "comentários de backend/src"
      to: "o trecho referenciado"
      via: "nome de função ou identificador, em vez de número de linha"
      pattern: "^$"
---

<objective>
Fechar **WR2-06** e registrar os 4 achados Info da rodada 2 (**IN2-01 a IN2-04**) como todos pendentes.

Purpose:

**WR2-06 — ponteiros que nasceram errados.** Neste projeto o comentário É o artefato de racional: o
`CLAUDE.md` pede explicitamente "explicar o porquê", e os comentários desta fase têm 15 a 25 linhas cada.
Ponteiros errados nesse regime não são cosméticos — mandam o próximo leitor para o trecho errado justamente
quando ele está investigando um incidente. O review conferiu linha a linha:

| Comentário | Aponta para | Está em |
|---|---|---|
| `agendor.js:68` — "Diferente de getOrgCategory (`:35-47`)" | 35-47 | 49-61 |
| `agendor.js:149` — "a consulta de tarefas futuras (`:281`)" | 281 | 300-304 |
| `agendor.js:188` — "o Promise.all de `:211`" | 211 (um comentário) | 245 |
| `agendor.js:197` — "o mapa `categoriaPorOrg` local à execução (`:211`)" | 211 | 244 |
| `routes/deals.js:39` — "Mesma razão já registrada em `agendor.js:291-292`" | 291-292 | 325-326 / 333 |
| `scheduler.js:137` — "o `logger.info(...)` de `:187-189`" | 187-189 | 209-211 |
| `scheduler.js:366` (pré-existente) — "catch de `:242`" | 242 | 283-285 |

**Todos nasceram errados**: as linhas se deslocaram dentro do mesmo commit que escreveu o comentário — os
próprios blocos de 15 a 25 linhas empurraram o código para baixo. Não existe disciplina que salve uma
referência por número de linha; a única forma que sobrevive a edição é a âncora nomeada.

Os planos 04-12 a 04-16 já corrigiram por reescrita alguns desses pontos (os dois de `agendor.js` sobre o
cache, e os parágrafos de `scheduler.js` e do cabeçalho de `notificationStatus.partialFailure.test.js` que
foram reescritos). **Este plano é a varredura**: encontra o que restou, converte, e instala um critério
verificável de que não sobrou nenhum.

**IN2-01 a IN2-04 — registrar em vez de esquecer.** Os quatro achados Info são reais e ficam fora desta
rodada por decisão do usuário. Registrá-los como arquivo de todo — no mesmo formato dos todos criados pelo
04-09 — é o que impede que "fora de escopo" vire "esquecido": um achado que ninguém escreveu deixa de ser
procurado.

## Por que os dois trabalhos num plano só

Nenhum dos dois muda comportamento: um é comentário, o outro é documentação de planejamento. Não há risco de
rollback ambíguo, e a constraint "um comportamento por plano" não é violada porque não há comportamento
nenhum aqui. O plano é o último da rodada de propósito: os planos anteriores movem linhas, e varrer antes
seria varrer duas vezes.

## O que NÃO muda

- **Nenhuma linha de código.** O diff de todos os arquivos `.js` deste plano é exclusivamente de comentário,
  e isso é verificado por contagem, não por promessa.
- Nenhum teste é acrescentado, removido ou alterado; a contagem da suíte continua a mesma do 04-16 (148).
- Arquivos de teste das ondas 1-7 que **não** foram tocados pelas duas rodadas de gap closure ficam como
  estão: editá-los para embelezar comentário mexeria em oráculos estáveis sem nenhum ganho de comportamento.
  O que restar de referência por linha neles é **medido e registrado** no SUMMARY como residual conhecido.
- `sec-01` continua ABERTO como risco conscientemente aceito (decisão C8). Nenhum artefato deste plano pode
  marcá-lo como resolvido, e nenhum arquivo de todo novo pode sugerir que ele foi tratado.

Output: comentários convertidos para âncora estável nos arquivos de produção e nos arquivos de teste do gap
closure, mais 4 arquivos de todo novos.
</objective>

<baseline>
## Fatos medidos (2026-08-04)

| Item | Valor medido | Como |
|---|---|---|
| Suíte | 139/139 verdes (148 esperados ao entrar neste plano) | `node --test` no `backend/` |
| Referências erradas conferidas pelo review | 7 (tabela acima), todas nascidas erradas | `04-REVIEW.md` secao WR2-06 |
| Convenção do projeto | comentários em PT-BR, seções com `// ── Nome ──`, "explicar o porquê" | `CLAUDE.md` |
| Formato dos todos | frontmatter com `id`, `type`, `status`, `priority`, `created`, `source`, `resolves_phase`, `tags`; corpo com o achado, o motivo de estar fora de escopo e a correção sugerida | `.planning/todos/pending/in-01-status-pending-na-ui.md` e irmãos |

## Inferências

- Parte das 7 referências já terá desaparecido por reescrita nos planos 04-12 a 04-16. A varredura por grep
  é o que diz **quantas** restaram — o executor mede antes e depois, e registra os dois números.
- O padrão de busca pode produzir falso positivo (por exemplo um ternário formatado de modo incomum). Cada
  ocorrência é inspecionada individualmente; falsos positivos vão para o SUMMARY, não para o diff.

## Decisões travadas

- D-WR2-06-a: referência dentro do mesmo arquivo ou entre arquivos de produção usa **nome de função ou
  identificador**. Referência a arquivo de teste usa **nome do arquivo e/ou nome do caso**.
- D-WR2-06-b: a varredura é obrigatória em `backend/src/**` e nos arquivos de teste do gap closure; o resto
  é medido e registrado como residual.
- D-WR2-06-c: os 4 todos são criados neste plano, com o achado na íntegra (não um resumo) e link de volta
  para `04-REVIEW.md`.
</baseline>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md
@.planning/todos/pending/in-01-status-pending-na-ui.md
@.planning/todos/pending/in-04-escape-html-no-test-card.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-04. Não explorar o codebase. -->

**Padrão de busca das referências por linha** (ERE; as aspas simples protegem a crase no bash):
```
grep -rnE '(\.js:[0-9]|\(:[0-9]|`:[0-9]| :[0-9])' backend/src backend/test --include='*.js'
```
As quatro alternativas cobrem as formas usadas no repositório: `agendor.js:35-47`, `(:211)`,
`` `:187-189` `` e ` :242`.

**Arquivos de teste do gap closure** (as duas rodadas) — escopo obrigatório da varredura, além de
`backend/src/**`:
`backend/test/agendor.cacheConcurrency.test.js`, `backend/test/agendor.cacheInvalidation.test.js`,
`backend/test/agendor.retry429.test.js`, `backend/test/deals.errorLog.test.js`,
`backend/test/dealId.validation.test.js`, `backend/test/scheduler.resilience.test.js`,
`backend/test/notificationStatus.partialFailure.test.js`, `backend/test/notificationStatus.catchResiliente.test.js`,
`backend/test/fakeTimers.helper.test.js`, `backend/test/emailer.transporteVivo.test.js`,
`backend/test/helpers/fakeTimers.js`.

**Formato de arquivo de todo** — frontmatter YAML seguido de corpo em PT-BR
(molde: `.planning/todos/pending/in-01-status-pending-na-ui.md`):
```
---
id: <slug>
type: todo
status: pending
priority: baixa | média | alta
created: 2026-08-04
source: Fase 4, code review 04-REVIEW.md (rodada 2) §<ACHADO> — reconhecido e deliberadamente fora do gap closure r2
resolves_phase: null
tags: [...]
---
```

**Texto integral dos 4 achados:** `04-REVIEW.md`, secao Info (IN2-01, IN2-02, IN2-03, IN2-04).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: varredura — toda referência por número de linha vira âncora estável</name>
  <files>backend/src/agendor.js, backend/src/scheduler.js, backend/src/emailer.js, backend/src/routes/deals.js, backend/test/notificationStatus.partialFailure.test.js</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` secao WR2-06 (a tabela das 7 referências conferidas)
    - `backend/src/agendor.js` (arquivo inteiro — estado após o 04-12)
    - `backend/src/scheduler.js` (arquivo inteiro — estado após o 04-14 e o 04-15)
    - `backend/src/emailer.js` (arquivo inteiro — estado após o 04-15 e o 04-16)
    - `backend/src/routes/deals.js` (arquivo inteiro — o comentário do 04-09 que aponta para `agendor.js:291-292`)
    - `backend/test/notificationStatus.partialFailure.test.js` (cabeçalho — estado após o 04-14)
    - `./CLAUDE.md` secao Comments (a convenção de comentário do projeto)
  </read_first>
  <action>
    **(a) Medir antes.** Rodar o padrão de busca (secao `interfaces`) sobre `backend/src` e sobre a lista de
    arquivos de teste do gap closure, e registrar a contagem inicial e a lista de ocorrências no SUMMARY.
    A lista de arquivos efetivamente alterados nesta task sai da MEDIÇÃO, não desta lista de `files` — se um
    arquivo previsto já estiver limpo (porque um plano anterior o reescreveu), ele simplesmente não entra no
    diff, e isso é registrado.

    **(b) Converter, ocorrência por ocorrência.** Para cada referência, substituir o número de linha pela
    âncora que identifica o alvo de forma estável:
    - referência dentro do mesmo arquivo → nome da função ou do identificador. Exemplo do padrão desejado:
      em vez de "o `Promise.all` de `:211`", escrever "o `Promise.all` que popula `categoriaPorOrg`";
    - referência a outro arquivo de produção → nome do arquivo mais nome da função. Exemplo: em vez de
      "Mesma razão já registrada em `agendor.js:291-292`", escrever "Mesma razão já registrada no comentário
      de `getDealsWithFutureTasks`, em `agendor.js`";
    - referência a arquivo de teste → nome do arquivo e, quando útil, o nome do caso entre aspas;
    - referência a um trecho sem nome próprio (um bloco anônimo) → descrever o trecho pelo que ele faz, com
      um identificador citado. Se não houver identificador nenhum, é sinal de que a referência não agrega —
      remover a referência em vez de inventar uma âncora frágil.

    Pontos conhecidos a conferir explicitamente (podem já ter sido reescritos pelos planos anteriores):
    o comentário de `getDealById` que cita `getOrgCategory`; o comentário de `fetchWithRetry` que cita a
    consulta de tarefas futuras; o comentário de `routes/deals.js` que cita `agendor.js`; o parágrafo de
    `scheduler.js` que cita o `logger.info` de conclusão; o comentário do seam de teste no fim de
    `scheduler.js` que cita o `catch` de `runWeeklySummary`; e o cabeçalho de
    `notificationStatus.partialFailure.test.js`.

    **(c) Nenhuma linha de código pode mudar.** Se converter uma referência exigir mexer em código, a
    conversão está errada — a mudança é de texto de comentário, e só.

    **(d) Medir depois.** Rodar o mesmo padrão de busca e registrar: `0` ocorrências no escopo obrigatório
    (`backend/src/**` mais os arquivos de teste do gap closure). Para o restante de `backend/test`, registrar
    a contagem remanescente no SUMMARY como **residual conhecido e deliberado**, com a razão (são oráculos das
    ondas 1-7 e não são editados nesta rodada). Se alguma ocorrência do escopo obrigatório for falso positivo
    do padrão (por exemplo um ternário), documentá-la nominalmente no SUMMARY em vez de alterá-la.

    **(e) Regra para o futuro.** Acrescentar UMA linha à convenção de comentário já existente em
    `backend/src/agendor.js` (ou no arquivo onde o projeto concentra esse tipo de nota), dizendo que
    referências entre trechos usam âncora nomeada e nunca número de linha, porque as linhas se deslocam
    dentro do próprio commit que escreve o comentário. Uma linha, não um parágrafo.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero referências por linha no escopo obrigatório: `grep -rnE '(\.js:[0-9]|\(:[0-9]|`:[0-9]| :[0-9])' backend/src --include='*.js' | wc -l` imprime `0`
    - O mesmo para os arquivos de teste do gap closure: o comando com a lista de arquivos da secao `interfaces` imprime `0` (falsos positivos, se houver, documentados nominalmente no SUMMARY)
    - O diff de TODOS os arquivos `.js` é exclusivamente de comentário: `git diff -- 'backend/**/*.js' | grep -E "^[+-][^+-]" | grep -vE "^[+-][[:space:]]*//" | wc -l` imprime `0`
    - `npm run test:coverage` sai 0 com 148 testes (nenhum teste acrescentado, removido ou alterado) e `npm run lint` sai 0
    - O SUMMARY registra a contagem ANTES e DEPOIS, e a contagem residual do restante de `backend/test` com a razão
    - `git diff --name-only backend/package.json backend/package-lock.json frontend/` está vazio
  </acceptance_criteria>
  <done>Nenhum comentário do código de produção manda o próximo leitor para o trecho errado, e existe um critério executável que detecta a reincidência.</done>
</task>

<task type="auto">
  <name>Task 2: registrar IN2-01 a IN2-04 como todos pendentes</name>
  <files>.planning/todos/pending/in2-01-fetchwithretry-sem-tentativa.md, .planning/todos/pending/in2-02-relogio-falso-em-before.md, .planning/todos/pending/in2-03-mensagem-de-erro-interpola-id.md, .planning/todos/pending/in2-04-parcial-sent-invisivel.md</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` secao Info (o texto integral de IN2-01, IN2-02, IN2-03 e IN2-04)
    - `.planning/todos/pending/in-01-status-pending-na-ui.md` (o MOLDE: frontmatter, seções e tom)
    - `.planning/todos/pending/in-04-escape-html-no-test-card.md` (segundo exemplo do mesmo molde)
    - `backend/src/agendor.js` (estado final — para conferir onde `fetchWithRetry` e a mensagem de erro de `getDealById` estão hoje)
    - `backend/test/notificationStatus.partialFailure.test.js` (estado final — para conferir o `before` versus `beforeEach`)
  </read_first>
  <action>
    Criar quatro arquivos em `.planning/todos/pending/`, cada um seguindo o molde de
    `in-01-status-pending-na-ui.md`: frontmatter YAML, um título com o código do achado, o achado NA ÍNTEGRA
    (sintoma, arquivo e trecho, consequência), uma seção "Por que ficou fora da rodada 2", uma seção
    "Correção sugerida" (a do review) e uma linha final com o link de volta para
    `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md`.

    Nos quatro, as referências ao código usam **âncora nomeada** (nome de função e de arquivo). Citar o
    número de linha do estado atual é aceitável apenas entre parênteses, como pista de onde procurar, e
    sempre acompanhado da âncora — nunca sozinho (é o próprio WR2-06 aplicado à documentação).

    **`in2-01-fetchwithretry-sem-tentativa.md`** — `priority: baixa`,
    `tags: [backend, agendor, robustez, phase-4-carryover]`.
    Achado: `fetchWithRetry` (`backend/src/agendor.js`) devolve `undefined` quando `retries <= 0` — o `for`
    não executa nenhuma iteração e a função cai no fim sem `return`. Os dois call-sites desestruturam o
    retorno, então o sintoma seria um `TypeError: Cannot destructure property 'data' of 'undefined'`,
    mensagem que não aponta para a causa. Não é alcançável hoje (`fetchDealsPage` é interno e sempre passa o
    default 3), mas `retries` é parâmetro público e o helper existe justamente para ser reusado por um
    terceiro consumidor. Por que ficou fora: não é alcançável pelo código atual e a correção mudaria o
    contorno de uma função que o 04-11 acabou de extrair; entrar agora misturaria endurecimento preventivo
    com o fechamento dos achados alcançáveis. Correção sugerida: guarda no topo lançando
    `[Agendor] fetchWithRetry exige ao menos 1 tentativa`, ou trocar o `for` por `do/while`.

    **`in2-02-relogio-falso-em-before.md`** — `priority: média`,
    `tags: [backend, testes, contaminacao-de-ordem, phase-4-carryover]`.
    Achado: `notificationStatus.partialFailure.test.js` habilita o relógio falso em `before`, e não em
    `beforeEach`. Cada `mock.timers.tick(10000)` de `avancarRelogioAte` deixa o relógio adiantado para o caso
    seguinte — o cenário A avança cerca de 10s antes de B e C rodarem. É exatamente a contaminação de ordem
    que `agendor.retry429.test.js` descobriu e resolveu com `reset()` seguido de `enable()` no `beforeEach`
    (lá, um adiantamento de 30s movia o `cutoffDate` e trazia os deals de fronteira 102 e 104 para dentro do
    golden). O arquivo só continua verde porque serve um clone por caso, longe da fronteira de 15 dias.
    Fragilidade **latente** num arquivo que pertence à rede de testes do Core Value. Por que ficou fora:
    decisão do usuário para a rodada 2; o arquivo é editado pelo 04-14 apenas para acrescentar asserções, e
    trocar o regime de relógio no mesmo commit misturaria dois trabalhos. Correção sugerida: replicar o
    `beforeEach` de `agendor.retry429.test.js` (`mock.timers.reset()` seguido de
    `mock.timers.enable({ apis: ['Date','setTimeout'], now: FIXED_NOW })`).

    **`in2-03-mensagem-de-erro-interpola-id.md`** — `priority: média`,
    `tags: [backend, seguranca, logging, agendor, phase-4-carryover]`.
    Achado: a guarda de tipo de `getDealById` (`backend/src/agendor.js`) lança um erro cuja mensagem
    interpola o valor recusado, e esse valor tem origem externa (a coluna `notification_log.deal_id`, gravada
    a partir do corpo de uma requisição autenticada em `POST /api/notifications/test-card`). Hoje a rejeição
    é absorvida pelo `catch` por item de `resolvedHandler` e não chega a log nenhum — mas basta alguém
    acrescentar um `logger.warn` ali para virar injeção de linha no log, num logger que emite JSON por linha
    em produção. Por que ficou fora: o vetor está **reconhecido no threat model do 04-12** e não é explorável
    pelo caminho atual; fechá-lo exigiria mexer na função no mesmo commit que fecha CR2-01, contra a regra de
    um comportamento por plano. Correção sugerida: recortar e sanear o valor
    (`String(id).slice(0, 40).replace(/\s+/g, ' ')`) ou omitir o valor e registrar só o tipo. Registrar
    também a regra geral: qualquer `logger.*` novo nesse caminho precisa vir depois desta correção.

    **`in2-04-parcial-sent-invisivel.md`** — `priority: média`,
    `tags: [backend, frontend, ux, notificacoes, core-value, phase-4-carryover]`.
    Achado: a decisão de manter `'sent'` com ao menos um sucesso (D-03/Q1, agora estendida ao caminho de
    exceção) tem um outro lado que nenhum comentário registra: `alreadyNotifiedToday` bloqueia o deal pelo
    dia inteiro, e como o card continua parado ele voltará amanhã — mas o destinatário que falhou hoje
    simplesmente **não recebeu**, e o único vestígio é a coluna `error` de uma linha cujo `status` é
    `'sent'`. Na UI (`NotificationHistory.jsx`, que renderiza `status === 'sent'` como sucesso pleno) essa
    linha aparece como tudo certo. Dado que o Core Value do milestone trata notificação perdida em silêncio
    como a pior classe de falha, o trade-off merece estar visível. Por que ficou fora: mexe na UI, que está
    fora do escopo da Fase 4 por decisão registrada no `04-CONTEXT.md`. Correção sugerida: exibir no
    histórico um terceiro estado (`'sent'` com `error` não nulo → "parcial") e/ou contabilizar
    `results.parciais` na resposta de `POST /api/notifications/run`. Anotar a relação com
    `in-01-status-pending-na-ui` (mesmo arquivo de UI, mesma classe de problema: o backend passou a dizer a
    verdade e a UI ainda lê o mundo em dois estados) e sugerir que os dois sejam tratados juntos. Registrar
    que, por tocar o Core Value, este item é candidato a promoção de prioridade na triagem do próximo
    milestone.

    **Verificação de higiene, obrigatória:** nenhum dos quatro arquivos pode conter o valor do
    `AGENDOR_TOKEN`, senha SMTP ou qualquer segredo real, e nenhum deles pode afirmar que `sec-01` foi
    resolvido — ele permanece ABERTO como risco conscientemente aceito (decisão C8).

    **Conferência de arrasto dos checkpoints:** se o checkpoint C9 (04-12) decidiu ajustar a redação de
    REL-04 / Success Criteria 4 do ROADMAP para descrever o mecanismo novo, aplicar esse ajuste agora, em
    `.planning/REQUIREMENTS.md` e/ou `.planning/ROADMAP.md`, exatamente como decidido. Se a decisão foi
    manter a redação atual, registrar "mantida por decisão em C9" no SUMMARY e não tocar em nada.
  </action>
  <verify>
    <automated>ls .planning/todos/pending/in2-0*.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos existem: `ls .planning/todos/pending/in2-0*.md | wc -l` imprime `4`
    - Cada arquivo tem frontmatter válido com `id`, `type: todo`, `status: pending`, `priority`, `created`, `source`, `resolves_phase` e `tags`: `grep -l "status: pending" .planning/todos/pending/in2-0*.md | wc -l` imprime `4`
    - Cada arquivo aponta de volta para o review: `grep -c "04-REVIEW.md" .planning/todos/pending/in2-0*.md` retorna 1 ou mais para os 4 arquivos
    - Cada arquivo tem a seção do motivo e a da correção: `grep -c "Por que ficou fora" .planning/todos/pending/in2-0*.md` e `grep -c "Correção sugerida" .planning/todos/pending/in2-0*.md` retornam pelo menos 1 para os 4
    - Nenhum arquivo declara `sec-01` resolvido: `grep -ci "sec-01.*resolvid" .planning/todos/pending/in2-0*.md` retorna 0 para os 4
    - Nenhum segredo real nos arquivos novos: `node --test test/secrets.grep.test.js` (rodado a partir de `backend/`) continua saindo 0
    - Nenhum arquivo de código foi alterado nesta task: `git diff --name-only backend/ frontend/` está vazio
    - Se C9 decidiu ajustar a redação de REL-04, o ajuste está aplicado e citado no SUMMARY; se decidiu manter, o SUMMARY registra "mantida por decisão em C9"
  </acceptance_criteria>
  <done>Os 4 achados Info da rodada 2 existem como registro rastreável, com o achado na íntegra e o caminho de volta ao review — nenhum deles depende da memória de quem leu o relatório.</done>
</task>

</tasks>

<rastreabilidade>
| Requisito | Achado | Tarefa | Verificação | Evidência |
|---|---|---|---|---|
| REL-01, REL-04, REL-06 (comentários de `agendor.js`) | WR2-06 | Task 1 | grep de referências por linha = 0 em `backend/src` | contagem antes/depois no SUMMARY |
| REL-01 (comentário de `routes/deals.js`) | WR2-06 | Task 1 | mesmo grep | idem |
| REL-03, REL-05 (comentários de `scheduler.js`) | WR2-06 | Task 1 | mesmo grep | idem |
| REL-05 (cabeçalho de `notificationStatus.partialFailure.test.js`) | WR2-06 | Task 1 | grep sobre a lista de arquivos do gap closure | idem |
| — (registro de dívida) | IN2-01..IN2-04 | Task 2 | 4 arquivos em `.planning/todos/pending/` | `ls` + greps de seção |
</rastreabilidade>

<matriz_de_teste>
| Cenário | Tipo | Alvo | Setup → Ação → Esperado | Regressão protegida |
|---|---|---|---|---|
| Nenhuma referência por linha em produção | gate estático | `backend/src/**/*.js` | grep do padrão → 0 linhas | ponteiro que manda o leitor ao trecho errado num incidente |
| Nenhuma referência por linha nos testes do gap closure | gate estático | lista de arquivos da secao `interfaces` | grep do padrão → 0 linhas | mesma classe, no lado do teste |
| Diff exclusivamente de comentário | gate estático | todos os `.js` alterados | diff filtrado → 0 linhas de código | mudança de comportamento disfarçada de limpeza |
| Suíte inalterada | regressão | suíte completa | 148 testes, exit 0 | edição acidental de asserção |
| Todos criados e completos | gate estático | `.planning/todos/pending/in2-0*.md` | 4 arquivos com as seções exigidas | achado reconhecido virando achado esquecido |
| Sem segredo nos artefatos novos | segurança | `secrets.grep.test.js` | exit 0 | vazamento em documentação |
</matriz_de_teste>

<matriz_de_risco>
| # | Risco | Prob. | Impacto | Mitigação | Sinal | Rollback |
|---|---|---|---|---|---|---|
| R2-24 | A varredura tocar código junto com o comentário | baixa | alto | Critério de aceite com contagem 0 de linhas não-comentário no diff; suíte roda | teste vermelho ou diff com linha de código | revert do commit |
| R2-25 | O padrão de busca produzir falso positivo e o executor "corrigir" um ternário | média | baixo | Ação exige inspeção ocorrência por ocorrência e documentação nominal dos falsos positivos | lint ou teste vermelho | revert do trecho |
| R2-26 | Âncora inventada para um bloco anônimo, criando uma referência frágil de outro tipo | média | baixo | Regra explícita: sem identificador, remover a referência em vez de inventar âncora | revisão do diff | ajuste no mesmo commit |
| R2-27 | Todo escrito como resumo, perdendo a informação que o review levantou | média | médio | Ação exige o achado NA ÍNTEGRA e o link de volta; critérios verificam as seções | todo curto demais | reescrever antes do commit |
| R2-28 | Um todo novo sugerir que `sec-01` foi tratado | baixa | alto | Critério de aceite com grep explícito; regra repetida na ação | grep acusa | corrigir antes do commit |
</matriz_de_risco>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Comentário → decisão de quem investiga um incidente | O racional escrito é o que orienta a próxima pessoa a mexer no caminho crítico de notificação |
| Artefato de planejamento → repositório público | Todo arquivo em `.planning/` é versionado num repositório que precisa continuar público para o gate de CI existir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17-01 | Repudiation | Comentário apontando para o trecho errado durante a investigação de um incidente de notificação | mitigate | Conversão para âncora nomeada mais gate por grep que detecta reincidência |
| T-04-17-02 | Tampering | Mudança de código disfarçada de ajuste de comentário | mitigate | Critério de aceite com contagem 0 de linhas não-comentário no diff; suíte completa roda no verify |
| T-04-17-03 | Information Disclosure | Segredo real (token, senha SMTP) copiado do review para um arquivo de todo versionado em repositório público | mitigate | Regra explícita na ação e `secrets.grep.test.js` no critério de aceite |
| T-04-17-04 | Repudiation | Documento novo sugerindo que `sec-01` foi resolvido, apagando um risco aceito conscientemente | mitigate | Critério de aceite com grep dedicado; regra repetida na ação |
| T-04-17-05 | Information Disclosure | IN2-03 (interpolação do id recusado em mensagem de erro) permanecer aberto | accept | Registrado como todo com correção sugerida e regra de precaução; não é explorável pelo caminho atual, em que a rejeição não chega a log nenhum |
| T-04-17-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote neste plano; `backend/package.json` e o lockfile não são tocados |
</threat_model>

<dod>
- [ ] Zero referências por número de linha em `backend/src/**` e nos arquivos de teste do gap closure
- [ ] Diff de todos os `.js` exclusivamente de comentário, verificado por contagem
- [ ] Contagem antes/depois e residual do restante de `backend/test` registrados no SUMMARY
- [ ] Regra de âncora nomeada escrita como convenção, em uma linha
- [ ] Os 4 arquivos de todo criados, com achado íntegro, motivo, correção sugerida e link de volta
- [ ] Nenhum artefato novo declara `sec-01` resolvido; `secrets.grep.test.js` verde
- [ ] Suíte completa verde com 148 testes; `npm run lint` exit 0
- [ ] Decisão de C9 sobre a redação de REL-04 aplicada ou registrada como mantida
</dod>

<verification>
- `grep -rnE '(\.js:[0-9]|\(:[0-9]|`:[0-9]| :[0-9])' backend/src --include='*.js' | wc -l` imprime 0
- `git diff -- 'backend/**/*.js' | grep -E "^[+-][^+-]" | grep -vE "^[+-][[:space:]]*//" | wc -l` imprime 0
- `npm run test:coverage` sai 0 (148 testes) e `npm run lint` sai 0
- `ls .planning/todos/pending/in2-0*.md | wc -l` imprime 4
- `node --test test/secrets.grep.test.js` sai 0
</verification>

<success_criteria>
- Nenhum comentário de produção referencia outro trecho por número de linha
- Existe um critério executável que detecta a reincidência do padrão
- Os 4 achados Info da rodada 2 estão registrados como todo pendente, íntegros e rastreáveis
- Nenhuma linha de código mudou e a suíte continua com a mesma contagem
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-17-SUMMARY.md` when done.
Commit: `docs(04-17): âncoras estáveis nos comentários e registro de IN2-01..IN2-04 (WR2-06)`
Rollback: revert do commit (volta às referências por número de linha; os todos deixam de existir).
</output>
