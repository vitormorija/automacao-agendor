---
phase: 04-confiabilidade-das-integra-es
plan: 26
type: execute
wave: 19
depends_on: ["04-25"]
files_modified:
  - backend/test/notificationStatus.partialFailure.test.js
  - backend/test/notificationStatus.registroResiliente.test.js
  - backend/test/notificationStatus.canalParcial.test.js
  - backend/test/emailer.timeout.test.js
  - backend/test/agendor.cacheConcurrency.test.js
  - backend/test/agendor.cacheInvalidation.test.js
  - .planning/todos/pending/in2-02-relogio-falso-em-before.md
autonomous: true
gap_closure: true
requirements: [REL-02, REL-03, REL-04]

must_haves:
  truths:
    - "Nenhum arquivo da suíte habilita o relógio falso uma única vez em `before`: os três que avançam o relógio o rearmam por caso"
    - "Existe uma única implementação de `avancarRelogioAte` em circulação — a cópia local do oráculo de REL-02 desapareceu"
    - "O estado global mutável dos dois arquivos de cache é reafirmado em `beforeEach`, e não restaurado na última instrução do caminho feliz"
    - "O todo in2-02 sai de pending, porque a correção que ele descrevia foi aplicada nos três arquivos"
  artifacts:
    - path: "backend/test/emailer.timeout.test.js"
      provides: "Consumidor do helper compartilhado, sem cópia local"
      contains: "helpers/fakeTimers"
    - path: "backend/test/agendor.cacheConcurrency.test.js"
      provides: "Estado neutro reafirmado em beforeEach"
      contains: "beforeEach"
  key_links:
    - from: "backend/test/helpers/fakeTimers.js"
      to: "os arquivos que avançam o relógio falso"
      via: "importação única do helper corrigido no 04-13"
      pattern: "helpers/fakeTimers"
---

<objective>
Fechar **WR3-04**, **WR3-05** e **WR3-07** — os três achados sobre o **instrumento**, não sobre o produto. Os
três produzem o mesmo dano: um vermelho atribuído ao **ator errado**, apontando para um defeito de produção
que não existe. Numa suíte que existe para ser o oráculo de quem é notificado, isso corrói a confiança em
tudo o mais.

Purpose:

- **WR3-04:** os dois arquivos criados na rodada anterior (`registroResiliente`, `canalParcial`) copiaram o
  `before` de `partialFailure` em vez do `beforeEach` corrigido que `agendor.retry429.test.js` carrega desde
  o 04-11 — o mesmo arquivo que descobriu o problema e o documentou. Cada `tick(10000)` de
  `avancarRelogioAte` deixa o relógio adiantado para o caso seguinte, até 200 s por chamada. A margem é
  estreita e **medida**: a fixture tem negócios de fronteira em 0 ms e +1 ms do corte de 15 dias. Hoje esses
  negócios não são servidos por esses arquivos, o que é a única razão de estarem verdes.
- **WR3-05:** a cópia local de `avancarRelogioAte` em `emailer.timeout.test.js` mantém o defeito que o 04-13
  corrigiu no helper compartilhado (`then` com um único argumento, sem ramo de rejeição). O motivo de
  mantê-la — não trocar o instrumento e o objeto medido na mesma rodada — **expirou** quando o 04-17
  terminou de mexer no `emailer.js`, e os dois SUMMARYs registram isso. O risco é latente, não ativo: nenhum
  dos casos daquele arquivo exercita o caminho de rejeição.
- **WR3-07:** os dois arquivos de cache restauram o estado global mutável na **última instrução do corpo do
  `test()`**, no caminho feliz. Qualquer asserção que falhe antes disso deixa o `routeHandler` respondendo
  pelo cenário errado para os casos seguintes.

Output: três edições mecânicas em seis arquivos de teste, zero linhas de produção, e um todo fechado.

## O que NÃO muda

- **Nenhum arquivo de `backend/src/`.** O diff de produção deste plano é vazio, e isso é critério de aceite.
- Nenhuma asserção é acrescentada, removida ou alterada. Só hooks, importações e a movimentação de
  instruções de restauração.
- `backend/test/helpers/fakeTimers.js`: intocado. Ele é o alvo da importação, não o objeto da edição.
</objective>

<baseline>
## Fatos medidos (2026-08-05)

| Item | Valor medido | Como |
|---|---|---|
| Arquivos que habilitam `'Date'` **e** `'setTimeout'` num `before` de topo | 3: `notificationStatus.partialFailure`, `notificationStatus.registroResiliente`, `notificationStatus.canalParcial` | `grep -n "mock.timers" test/*.js` cruzado com `grep -nE "^before\(" test/*.js` |
| `before(` de topo nesses 3 arquivos | exatamente 1 por arquivo, e é o do relógio | `grep -cE "^before\(" test/notificationStatus.*.test.js` |
| Precedente correto | `agendor.retry429.test.js`, com `mock.timers.reset()` seguido de `enable(...)` dentro do `beforeEach`, mais 8 linhas de comentário explicando por quê | leitura do arquivo |
| Cópia local do helper | `emailer.timeout.test.js`, `async function avancarRelogioAte(promessa)` com `promessa.then(valor => …)` de UM argumento | leitura do arquivo |
| Casos daquele arquivo que exercitam rejeição | nenhum (`sendStaleNotification` RESOLVE na exaustão, D-03) | leitura dos casos |
| Restauração no caminho feliz | `agendor.cacheConcurrency` (`cenarioAtivo` na última instrução do caso do espelho) e `agendor.cacheInvalidation` (`dealsServidos` no fim do cenário (3) e `delete ORG_CATEGORY[201]` no fim do cenário (1)) | leitura dos arquivos |
| Estado mutável de `cacheInvalidation` | `dealsServidos`, `orgQueFalha` e o dicionário `ORG_CATEGORY` | leitura do arquivo |
| Estado mutável de `cacheConcurrency` | `cenarioAtivo` e os manipuladores de suspensão | leitura do arquivo |

## Decisões travadas (não reabrir)

- **D-WR3-04-a:** a correção é a do `agendor.retry429.test.js`, byte a byte: `mock.timers.reset()` **antes**
  de `enable(...)`, porque `enable()` lança se os temporizadores já estiverem habilitados; e o `enable` passa
  a viver no `beforeEach` já existente de cada arquivo, junto dos resets de contador. O `before` de topo
  desaparece.
- **D-WR3-05-a:** `emailer.timeout.test.js` passa a importar `avancarRelogioAte` de `./helpers/fakeTimers` e
  a cópia local é removida. O contrato observável é o mesmo no ramo de sucesso — o único exercitado por
  aquele arquivo — e o meta-teste `fakeTimers.helper.test.js` já cobre os três ramos do helper.
- **D-WR3-07-a:** a restauração vira **reafirmação de estado neutro** num `beforeEach`, como os arquivos de
  `notificationStatus.*` já fazem para `modoEnvio`/`modoRegistro`. As instruções de restauração no fim dos
  corpos de `test()` são removidas, para que exista **um** lugar responsável pelo estado.
- **D-WR3-07-b:** o `beforeEach` de `cacheInvalidation` reafirma **também** `ORG_CATEGORY[201]` no valor de
  origem (removendo a chave), porque o cenário (1) a altera. Deixá-la de fora repetiria o achado, uma
  variável adiante.
- **D-WR3-04-b:** nenhum outro arquivo entra. Os demais habilitam apenas `'Date'` (sem `setTimeout`) e
  portanto não avançam o relógio — o executor confirma isso por medição antes de concluir.
</baseline>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-13-SUMMARY.md
@.planning/todos/pending/in2-02-relogio-falso-em-before.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-05. Não explorar o codebase. -->

`backend/test/agendor.retry429.test.js`, `beforeEach` — o modelo a copiar: `mock.timers.reset()`,
`mock.timers.enable({ apis: ['Date', 'setTimeout'], now: FIXED_NOW })`, e em seguida os resets de contadores
e modos. O comentário acima dele explica a contaminação de ordem com o precedente medido (30 s moveram o
`cutoff` e trouxeram os negócios 102 e 104 para dentro do golden).

`backend/test/helpers/fakeTimers.js` — `async function avancarRelogioAte(promessa)` com os DOIS ramos do
`then`, a falha explícita ANTES do `await encerrada`, e `module.exports = { avancarRelogioAte }`. O bloco de
comentário do topo tem uma nota sobre a duplicação deliberada em `emailer.timeout.test.js` que precisa ser
atualizada quando a cópia sumir.

`backend/test/emailer.timeout.test.js` — a cópia local fica entre as constantes de negócio sintético e o
`beforeEach`; os casos que a usam habilitam o relógio falso **dentro do próprio caso**
(`mock.timers.enable({ apis: ['setTimeout'] })` e `mock.timers.reset()` num `finally`), padrão que **não
muda** neste plano.

`backend/test/agendor.cacheInvalidation.test.js` — `beforeEach` atual contém apenas
`fake.get.mock.resetCalls()`.
`backend/test/agendor.cacheConcurrency.test.js` — não tem `beforeEach` hoje.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WR3-04 — relógio falso rearmado por caso nos três arquivos, e o todo in2-02 fechado</name>
  <files>backend/test/notificationStatus.partialFailure.test.js, backend/test/notificationStatus.registroResiliente.test.js, backend/test/notificationStatus.canalParcial.test.js, .planning/todos/pending/in2-02-relogio-falso-em-before.md</files>
  <read_first>
    - `backend/test/agendor.retry429.test.js` — o `beforeEach` modelo e o comentário que o justifica
    - `backend/test/notificationStatus.partialFailure.test.js`, `backend/test/notificationStatus.registroResiliente.test.js`, `backend/test/notificationStatus.canalParcial.test.js` — os três `before` e os três `beforeEach` existentes
    - `.planning/todos/pending/in2-02-relogio-falso-em-before.md` (o todo que esta task fecha)
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção WR3-04
  </read_first>
  <action>
    Nos **três** arquivos: mover a habilitação do relógio falso do `before` de topo para o `beforeEach` já
    existente, precedida de `mock.timers.reset()`, exatamente como em `agendor.retry429.test.js`. O `before`
    de topo deixa de existir; o comentário que hoje o acompanha (explicando por que `setTimeout` entra junto
    de `Date`) é **preservado** e passa a acompanhar o `beforeEach`, acrescido de 2 a 3 linhas dizendo por
    que o rearme é por caso: cada `tick(10000)` de `avancarRelogioAte` deixa o relógio adiantado, e o
    `cutoff` de 15 dias anda junto — o precedente medido está em `agendor.retry429.test.js`. Citar `in2-02`
    como o registro que descreveu isto.

    Manter o `after` com `mock.timers.reset()` como está nos três.

    Confirmar por medição — e registrar no SUMMARY — que **nenhum outro** arquivo da suíte habilita
    `'setTimeout'` num `before` de topo. Se aparecer outro, **não** editá-lo: reportar no SUMMARY, para que
    a decisão seja explícita em vez de silenciosa.

    **Fechar o todo `in2-02`:** mover `.planning/todos/pending/in2-02-relogio-falso-em-before.md` para
    `.planning/todos/completed/` com `git mv`, ajustando o frontmatter (`status: completed`, um campo
    `resolved: 2026-08-05` e `resolved_by: 04-26`) e acrescentando ao fim do corpo uma seção curta dizendo
    que a correção foi aplicada nos **três** arquivos, não só no `partialFailure` que o todo nomeava.
    **Não** editar nenhum outro todo — `in-01`, `rel-02b` e `sec-01` mantêm as prioridades e o estado atuais
    por decisão do usuário.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/notificationStatus.partialFailure.test.js &amp;&amp; node --test test/notificationStatus.registroResiliente.test.js &amp;&amp; node --test test/notificationStatus.canalParcial.test.js &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os três arquivos saem 0
    - `grep -cE "^before\(" backend/test/notificationStatus.partialFailure.test.js` retorna 0, e o mesmo comando retorna 0 para `registroResiliente` e para `canalParcial`
    - Nos três arquivos, `grep -c "mock.timers.reset()"` retorna pelo menos 2 (o do `beforeEach` e o do `after`)
    - Nenhuma asserção mudou: `git diff backend/test/notificationStatus.partialFailure.test.js backend/test/notificationStatus.registroResiliente.test.js backend/test/notificationStatus.canalParcial.test.js | grep -E "^[+-][^+-]" | grep -c "assert"` retorna 0
    - `ls .planning/todos/pending/in2-02-relogio-falso-em-before.md` falha e `ls .planning/todos/completed/in2-02-relogio-falso-em-before.md` sucede
    - `git diff --name-only .planning/todos/pending/` não lista `in-01-status-pending-na-ui.md`, `rel-02b-deadline-global-smtp.md` nem `sec-01-rotate-agendor-token.md`
    - `git diff --name-only backend/src/` está vazio
  </acceptance_criteria>
  <done>Nenhum arquivo que avança o relógio falso o herda adiantado do caso anterior, e o registro que previu isso está fechado.</done>
</task>

<task type="auto">
  <name>Task 2: WR3-05 — o oráculo de REL-02 passa a usar o helper compartilhado</name>
  <files>backend/test/emailer.timeout.test.js, backend/test/helpers/fakeTimers.js</files>
  <read_first>
    - `backend/test/emailer.timeout.test.js` (arquivo inteiro — a cópia local e os casos que a usam)
    - `backend/test/helpers/fakeTimers.js` (o helper corrigido no 04-13, incluindo a nota sobre a duplicação deliberada)
    - `backend/test/fakeTimers.helper.test.js` (o meta-teste que cobre os três ramos do helper)
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção WR3-05
    - `.planning/phases/04-confiabilidade-das-integra-es/04-13-SUMMARY.md` (a justificativa que expirou)
  </read_first>
  <action>
    Em `emailer.timeout.test.js`: remover a definição local de `avancarRelogioAte` e importá-la de
    `./helpers/fakeTimers`, ao lado das demais importações do topo. O bloco de comentário que hoje explica a
    cópia (por que não `tickAsync`, o alternar entre `setImmediate` e `tick`) é substituído por 3 a 4 linhas
    dizendo que o helper compartilhado é a única implementação em circulação, que o motivo de manter uma
    cópia — não trocar instrumento e objeto medido na mesma rodada — expirou quando o `emailer.js` parou de
    mudar, e que o contrato observável é o mesmo no ramo de sucesso, o único que este arquivo exercita.
    Nenhum caso, nenhuma asserção e nenhum `mock.timers.enable` de dentro dos casos muda.

    Em `helpers/fakeTimers.js`: **apenas comentário** — atualizar a nota do topo que hoje declara a
    duplicação deliberada, para registrar que ela deixou de existir e que o helper é agora a única
    implementação. Nenhuma linha de código do helper pode mudar.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/emailer.timeout.test.js &amp;&amp; node --test test/fakeTimers.helper.test.js &amp;&amp; node --test test/agendor.retry429.test.js &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `emailer.timeout.test.js` sai 0 com todos os seus casos
    - `grep -c "async function avancarRelogioAte" backend/test/emailer.timeout.test.js` retorna 0
    - `grep -c "helpers/fakeTimers" backend/test/emailer.timeout.test.js` retorna 1
    - Existe uma única implementação na suíte: `grep -rc "async function avancarRelogioAte" backend/test/ backend/test/helpers/ 2>/dev/null | grep -v ":0" ` lista apenas `helpers/fakeTimers.js`
    - Nenhuma asserção mudou: `git diff backend/test/emailer.timeout.test.js | grep -E "^[+-][^+-]" | grep -c "assert"` retorna 0
    - Diff do helper exclusivamente de comentário: `git diff backend/test/helpers/fakeTimers.js | grep -E "^[+-][^+-]" | grep -vE "^[+-][[:space:]]*//" | wc -l` imprime `0`
    - `git diff --name-only backend/src/` está vazio
  </acceptance_criteria>
  <done>Não existe mais um instrumento sabidamente defeituoso guardado dentro da rede de testes do Core Value.</done>
</task>

<task type="auto">
  <name>Task 3: WR3-07 — estado neutro reafirmado em beforeEach nos dois arquivos de cache</name>
  <files>backend/test/agendor.cacheConcurrency.test.js, backend/test/agendor.cacheInvalidation.test.js</files>
  <read_first>
    - `backend/test/agendor.cacheConcurrency.test.js` (arquivo inteiro — as variáveis mutáveis lidas no `routeHandler` e a restauração no fim do caso do espelho)
    - `backend/test/agendor.cacheInvalidation.test.js` (arquivo inteiro — `dealsServidos`, `orgQueFalha`, `ORG_CATEGORY` e as restaurações no fim dos cenários (1) e (3))
    - `backend/test/notificationStatus.canalParcial.test.js` — o padrão de `beforeEach` que reafirma estado neutro
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção WR3-07
  </read_first>
  <action>
    Nos **dois** arquivos: acrescentar (ou estender, no caso do `cacheInvalidation`, que já tem um) um
    `beforeEach` que **reafirma o estado neutro** de todas as variáveis mutáveis lidas dentro do
    `routeHandler`, e remover as instruções de restauração que hoje moram na última linha do corpo dos
    `test()`.

    - `agendor.cacheConcurrency.test.js`: reafirmar `cenarioAtivo` no valor inicial e zerar os manipuladores
      de suspensão e os contadores que o `routeHandler` consulta. Remover a restauração final do caso do
      espelho.
    - `agendor.cacheInvalidation.test.js`: reafirmar `dealsServidos` na fixture, `orgQueFalha` em nulo e
      remover a chave `201` do dicionário `ORG_CATEGORY`, preservando o `fake.get.mock.resetCalls()` que já
      existe. Remover a restauração final dos cenários (1) e (3).

    Comentário (PT-BR, 3 a 5 linhas por arquivo) explicando **por que** o `beforeEach` e não a última
    instrução do caso: a restauração no caminho feliz não roda quando uma asserção falha antes dela, e o
    estado sujo faz o `routeHandler` responder pelo cenário errado nos casos seguintes — o vermelho seguinte
    vem com a mensagem de outro caso e aponta para um defeito de produção que não existe. É o mesmo custo
    que WR2-03 usou para justificar o conserto do helper, aplicado ao instrumento.

    **Atenção à ordem de declaração dos casos:** o cabeçalho de `cacheInvalidation` declara que a ordem faz
    parte do teste (o cenário (2) precisa medir uma execução com o cache frio). O `beforeEach` **não** pode
    reordenar nem renomear nada; ele apenas garante que cada caso comece do mesmo estado. Se alguma asserção
    ficar vermelha depois da mudança, PARAR e reportar — isso significaria que algum caso dependia da
    contaminação, e essa informação é valiosa demais para ser "consertada" no escuro.

    Ao final: rodar a suíte completa com cobertura e o lint, e registrar o total medido.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/agendor.cacheConcurrency.test.js &amp;&amp; node --test test/agendor.cacheInvalidation.test.js &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Os dois arquivos saem 0
    - `grep -c "beforeEach(" backend/test/agendor.cacheConcurrency.test.js` retorna 1 e o mesmo comando em `agendor.cacheInvalidation.test.js` retorna 1
    - `grep -c "cenarioAtivo = 'limpeza-apaga-leitura'" backend/test/agendor.cacheConcurrency.test.js` retorna 1 (o valor neutro é afirmado em UM lugar, o `beforeEach`, e não mais no fim do caso)
    - `grep -c "delete ORG_CATEGORY\[201\]" backend/test/agendor.cacheInvalidation.test.js` retorna 1 e ele está dentro do `beforeEach`
    - Nenhuma asserção mudou: `git diff backend/test/agendor.cacheConcurrency.test.js backend/test/agendor.cacheInvalidation.test.js | grep -E "^[+-][^+-]" | grep -c "assert"` retorna 0
    - `npm run test:coverage` sai 0 e o total de testes é **igual** ao da entrada deste plano (este plano não acrescenta casos) — o número medido vai literal para o SUMMARY
    - `npm run lint` sai 0
    - `git diff --name-only backend/src/` está vazio — o diff de produção deste plano inteiro é zero
  </acceptance_criteria>
  <done>Um caso que falhe deixa de contaminar os seguintes nos dois arquivos que ramificam por estado global.</done>
</task>

</tasks>

<rastreabilidade>
| Requisito | Achado | Tarefa | Teste | Evidência |
|---|---|---|---|---|
| REL-05 (integridade do oráculo) | WR3-04 | Task 1 | os 3 arquivos `notificationStatus.*` | zero `before(` de topo; suíte verde |
| REL-02 (integridade do oráculo) | WR3-05 | Task 2 | `emailer.timeout` e `fakeTimers.helper` | uma única implementação do helper |
| REL-04 (integridade do oráculo) | WR3-07 | Task 3 | os 2 arquivos de cache | estado neutro num único lugar |
</rastreabilidade>

<matriz_de_teste>
| Cenário | Tipo | Arquivo | Setup → Ação → Esperado | Regressão protegida |
|---|---|---|---|---|
| Relógio rearmado por caso | mecânico | os 3 `notificationStatus.*` | `before` → `beforeEach` com `reset()` | contaminação de ordem movendo o `cutoff` |
| Helper único | mecânico | `emailer.timeout` | cópia local → importação | rejeição órfã atribuída ao caso errado |
| Estado neutro em `beforeEach` | mecânico | os 2 de cache | restauração no fim → reafirmação no início | cascata de vermelhos com a mensagem errada |
| Suíte inteira | regressão | todos | contagem de testes idêntica à da entrada | perda silenciosa de caso ao mexer em hooks |
</matriz_de_teste>

<matriz_de_risco>
| # | Risco | Prob. | Impacto | Mitigação | Sinal | Rollback |
|---|---|---|---|---|---|---|
| R3-32 | `enable()` lançar por já haver temporizadores habilitados ao mover para `beforeEach` | alta se o `reset()` for esquecido | médio | D-WR3-04-a exige `reset()` antes; o modelo é copiado de um arquivo verde | erro "timers already enabled" | acrescentar o `reset()` |
| R3-33 | Algum caso depender da contaminação que o `beforeEach` remove | média | médio | A ação manda PARAR e reportar em vez de "consertar" a asserção | vermelho novo num caso não editado | reportar e planejar à parte |
| R3-34 | Mudar asserção junto com hook, disfarçando alteração de oráculo | média | alto | Critério conta zero asserções no diff dos seis arquivos | contagem maior que 0 | revert do commit |
| R3-35 | O helper compartilhado divergir em comportamento do que a cópia fazia no ramo de sucesso | baixa | médio | `fakeTimers.helper.test.js` cobre os três ramos; `emailer.timeout` roda inteiro no verify | qualquer caso de `emailer.timeout` vermelho | reverter a Task 2 isoladamente |
</matriz_de_risco>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Instrumento de teste → veredito da suíte | Um instrumento defeituoso credita a falha ao ator errado |
| Estado global de módulo → casos seguintes | O `routeHandler` ramifica por variáveis que sobrevivem entre casos |
| Relógio falso → `cutoff` de elegibilidade | Avançar o relógio move a fronteira de 15 dias que decide quem é notificado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26-01 | Repudiation | Vermelho atribuído ao caso errado por rejeição órfã ou estado sujo | mitigate | Helper único (Task 2) e estado neutro em `beforeEach` (Tasks 1 e 3) |
| T-04-26-02 | Tampering (do oráculo) | Edição de hook que arraste uma asserção junto | mitigate | Critério de zero asserções no diff dos seis arquivos |
| T-04-26-03 | Denial of Service (confiança) | Suíte que fica verde por contaminação em vez de por comportamento | mitigate | Rearme por caso e reafirmação de estado; contagem de testes idêntica à da entrada |
| T-04-26-04 | Tampering | Caso que dependa da contaminação sendo "consertado" no escuro | mitigate | Ordem explícita de PARAR e reportar |
| T-04-26-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote; nenhum arquivo de produção tocado |
</threat_model>

<dod>
- [ ] Zero `before(` de topo nos três arquivos que avançam o relógio
- [ ] Uma única implementação de `avancarRelogioAte` na suíte
- [ ] Estado neutro reafirmado em `beforeEach` nos dois arquivos de cache
- [ ] Zero asserções alteradas nos seis arquivos
- [ ] `git diff --name-only backend/src/` vazio no plano inteiro
- [ ] Todo `in2-02` movido para `completed/`; nenhum outro todo editado
- [ ] Suíte completa verde com o mesmo total de testes da entrada; `npm run lint` exit 0
</dod>

<verification>
- Os seis arquivos de teste saem 0 individualmente
- `npm run test:coverage` sai 0 com o total inalterado e `npm run lint` sai 0
- `git diff --name-only backend/src/` está vazio
- `ls .planning/todos/completed/in2-02-relogio-falso-em-before.md` sucede
</verification>

<success_criteria>
- Nenhum instrumento da suíte pode mais atribuir uma falha ao ator errado por contaminação de ordem
- A rede de testes do Core Value não guarda mais uma cópia sabidamente defeituosa do seu próprio helper
- Zero linhas de produção mudaram
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-26-SUMMARY.md` when done.
Commit: `test(04-26): relógio por caso, helper único e estado neutro em beforeEach (WR3-04, WR3-05, WR3-07)`
Rollback: revert do commit. Nenhum comportamento de produção depende deste plano.
</output>
