---
phase: 04-confiabilidade-das-integra-es
plan: 36
type: tdd
wave: 29
depends_on: [04-35]
gap_closure: true
autonomous: true
requirements: [REL-03, REL-06]
files_modified:
  - backend/test/scheduler.resilience.test.js
  - backend/test/scheduler.categoriaIndecidivel.test.js
  - backend/src/scheduler.js
  - frontend/src/components/Dashboard.jsx

must_haves:
  truths:
    - "Uma rodada que CONCLUIU com pelo menos um negócio pulado (dedup, categoria, funil ou sem destinatário) mostra ao operador o resumo de envio, e NÃO um toast de erro"
    - "Uma chamada recusada pelo lock de concorrência continua mostrando ao operador o motivo escrito, e não um toast vazio"
    - "Uma rodada que CONCLUIU com alarme agregado (supressão total / forma do payload) leva esse alarme à tela no caminho MANUAL, e não só no polling de status"
    - "O contrato antigo do lock (`skipped: true`, `reason`) continua no payload — nenhum consumidor não medido quebra"
  artifacts:
    - path: "backend/src/scheduler.js"
      provides: "Retorno do lock de concorrência desambiguado com a chave própria `execucaoIgnorada`, preservando `skipped: true` e `reason`"
      contains: "execucaoIgnorada"
    - path: "frontend/src/components/Dashboard.jsx"
      provides: "sendNow passa a ramificar por `execucaoIgnorada` e a exibir o alarme agregado da rodada"
      contains: "execucaoIgnorada"
    - path: "backend/test/scheduler.resilience.test.js"
      provides: "Caso (6) — a chamada concorrente traz a chave nova E o motivo escrito"
    - path: "backend/test/scheduler.categoriaIndecidivel.test.js"
      provides: "Cenário K — a rodada que CONCLUIU com `skipped > 0` NÃO traz a chave nova"
  key_links:
    - from: "backend/src/scheduler.js"
      to: "backend/src/routes/notifications.js"
      via: "POST /api/notifications/run devolve o objeto inteiro de runCheck (`res.json(result)`)"
      pattern: "res\\.json\\(result\\)"
    - from: "backend/src/routes/notifications.js"
      to: "frontend/src/components/Dashboard.jsx"
      via: "sendNow — a ÚNICA superfície de feedback do disparo manual"
      pattern: "notifications/run"
---

<objective>
Fechar **CR5-01**, o blocker da rodada 5: `runCheck` usa a chave `skipped` para **duas coisas
incompatíveis** — o contrato `{ skipped: true, reason }` do lock de concorrência e o **contador**
`results.skipped` —, e o único consumidor do disparo manual ramifica por ela.

Medido pelo revisor por sonda, no cenário do apagão: `results.skipped: 3` → `if (result.skipped)`
avalia `true` → `toast.error(result.reason)` com `reason` **undefined** → **toast vermelho em
branco**. Basta **um** negócio deduplicado — o caso banal de clicar "Enviar notificações" depois do
cron das 8h — para o operador receber erro sem mensagem no lugar do resumo.

Purpose: o dano não é cosmético. Um vermelho sem texto depois de um envio bem-sucedido produz
exatamente a conclusão que o comentário do alarme do 04-35 diz querer impedir — *"um operador que
conclua ter perdido envios dispara a rodada de novo e gera duplicatas"* (ameaça T-04-35-05). E, no
apagão, ele **anula a metade audível do CR4-01 no caminho manual**: o operador vê um vermelho vazio
que não distingue apagão de dia normal com dedup, e o array `results.errors` — onde o alarme mora —
nunca chega à tela pelo disparo.

Output: a chave `execucaoIgnorada` no retorno do lock (com `skipped: true` e `reason` preservados
por compatibilidade), o consumidor ramificando por ela, o alarme agregado exibido no caminho manual,
e o PAR de casos que impede um conserto pela metade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md
@.planning/phases/04-confiabilidade-das-integra-es/04-28-SUMMARY.md

Convenções que valem aqui e não se negociam: comentários e mensagens em português; Biome
(`lineWidth` 80) é o único formatador — `npm run format` decide a quebra de linha, não o executor;
`node:test` nativo, um processo por arquivo de teste; referência a outro trecho **por âncora
nomeada** (função, identificador, nome de caso de teste), **nunca** por número de linha (WR2-06).
O frontend **não tem testes**: o gate dele é `cd frontend && npm run build`.
</context>

<interfaces>
O executor NÃO precisa explorar o repositório. Tudo abaixo foi medido no arquivo real em
2026-08-05, na entrada deste plano.

**`backend/src/scheduler.js` — estado de ENTRADA**

- O guard do topo de `runCheck`: `if (isRunning) return { skipped: true, reason: 'Verificação já em
  andamento' };` — **1** ocorrência não-comentário de `skipped: true` no arquivo inteiro.
- O literal de `results` nasce com `skipped: 0` (o **contador**), ao lado de
  `skippedCategoriaIndecidivel: 0` e `funilNaoAvaliado: 0`.
- `results.skipped++` aparece **4** vezes não-comentário: dedup do dia, categoria indecidível, funil
  e "sem destinatário".
- `execucaoIgnorada`: **0** ocorrências em `backend/` e em `frontend/` — o símbolo é novo.
- `results.error = ` não-comentário: **3** (o `catch` externo + os dois alarmes agregados).

**`backend/src/routes/notifications.js` — o transporte**

- `POST /api/notifications/run` faz `const result = await runCheck(); res.json(result);` — devolve o
  objeto **inteiro**, sem projeção. `grep -c 'res.json(result)'` = **2** no arquivo (o handler
  `GET /` do histórico e este) — nenhum dos dois projeta campos.

**`frontend/src/components/Dashboard.jsx` — o ÚNICO consumidor**

- `sendNow()` faz `fetch('/api/notifications/run', { method: 'POST' })`, e ramifica:
  `if (result.skipped) toast.error(result.reason, ...)` senão
  `toast.success('${result.notified} notificação(ões) enviada(s) de ${result.stale} negócio(s)
  parado(s)')`.
- Medido em `frontend/src` inteiro: `skipped` = **1** ocorrência (esta), `reason` = **1** (esta),
  `notifications/run` = **1** (só `sendNow`), `lastRunResult` = **1** (o `const lastRun` de
  `Dashboard.jsx`, que lê `errors` — nunca `error`).
- O bloco "Erros na última execução" já existe no JSX e é renderizado por
  `lastRun?.errors?.length > 0` — mas ele é alimentado por `fetchStatus()`, que roda no **polling de
  2 minutos**, não pela resposta do disparo.
- `react-hot-toast` renderiza `undefined` como conteúdo vazio: é daí que sai o toast em branco.

**Os testes que tocam este contrato (estado de ENTRADA)**

| Arquivo | Casos | Papel |
|---|---|---|
| `backend/test/scheduler.resilience.test.js` | **5** (`^test(` = 5) | Caracterização do lock. O caso **(3)** já assere que uma rodada concluída traz `reason === undefined` e `typeof r.skipped === 'number'`; o caso **(4)** arma a concorrência com `suspenderUsuarios()` / `liberarUsuarios()` e assere `concorrente.skipped === true` e `concorrente.reason === 'Verificação já em andamento'` |
| `backend/test/scheduler.categoriaIndecidivel.test.js` | **10** (A–J) | O oráculo de quem-recebe. O cenário **E** (dois negócios no funil Beefor, organizações sãs) produz uma rodada que CONCLUIU com `r.skipped === 2` — é a armação exata de que o cenário K precisa |

**Baseline:** `cd backend && npm test` → **192/192**; `npm run lint` → exit 0, **44** warnings.
</interfaces>

<escopo_travado>
Decisão do usuário de 2026-08-05, vinculante. Este plano fecha **CR5-01 e só ele**.

- **NÃO** mexer na posição do contador `skippedCategoriaIndecidivel` nem na condição do alarme de
  categoria — isso é o **WR5-01**, e é o plano **04-37**. Gate na Task 2:
  `git diff -U0 backend/src/scheduler.js | grep -c 'skippedCategoriaIndecidivel'` = **0**.
- **NÃO** renomear `results.skipped` (o contador). O nome do contador está pinado por asserção em
  cinco arquivos de teste e renomeá-lo é mudança de contrato sem achado que a peça.
- **NÃO** transformar `results.error` num campo por causa (isso é o Info **IN5-01**, que vira todo no
  plano 04-38 desta mesma rodada).
- A exibição do alarme agregado no caminho manual **faz parte** do conserto de CR5-01, e não é
  ampliação: a consequência 3 do achado é literalmente *"anula a metade audível do CR4-01 justamente
  no disparo manual"*, e o bloco `Fix` do revisor traz as duas metades juntas.
</escopo_travado>

<inventario_de_irmaos>
Mandato estrutural da fase, com as **duas cláusulas novas da r5**. Cada item carrega trabalho
verificável — grep, caso de teste ou medição já feita. Item nomeado sem gate é a lacuna que reabriu
esta fase cinco vezes.

## Grupo 1 — Irmãos do construto (as outras superfícies do mesmo contrato)

| # | Construção | Classificação | Trabalho verificável |
|---|---|---|---|
| 1 | `runCheck` — o guard `if (isRunning)` | **corrigida** | Caso **(6)** de `scheduler.resilience.test.js`: a chamada concorrente traz `execucaoIgnorada === true` **e** `reason` string não vazia |
| 2 | `runCheck` — o retorno da rodada que CONCLUIU | **corrigida (pelo lado negativo)** | Cenário **K** de `scheduler.categoriaIndecidivel.test.js`: rodada concluída com `r.skipped === 2` **não** traz `execucaoIgnorada`. Sem este caso, um conserto que apenas trocasse o nome no consumidor deixaria o lock silencioso — é o par que o revisor exige |
| 3 | `runCheckOnly` (prévia, `POST /check`) | **verificada-e-sã por medição** | Ela **não** tem lock e **não** devolve `skipped` nenhum: devolve um array. Medido: `grep -c 'skipped' backend/src/scheduler.js` na região de `runCheckOnly` = **0**, e o consumidor `checkOnly()` ramifica por `typeof result.total === 'number'`. Gate na Task 2: `git diff -U0 backend/src/scheduler.js \| grep -c 'runCheckOnly\|seraNotificado'` = **0** |
| 4 | `runWeeklySummary` | **verificada-e-sã por medição** | Não tem lock `isRunning` — comportamento ATUAL deliberadamente pinado pelo caso (5) de `scheduler.resilience.test.js`, que diz por escrito que "nada aqui pede que ela ganhe um". Não devolve payload a consumidor de UI. Gate: ausente do diff |
| 5 | `dealResult.skipped` (booleano POR NEGÓCIO) x `results.skipped` (contador da rodada) | **verificada-e-sã por medição** | Mesmo nome, objetos diferentes, e **nenhum consumidor de UI lê o de negócio**: `grep -ro 'skipped' frontend/src \| wc -l` = **1** hoje, e essa única ocorrência é justamente a linha defeituosa que a Task 2 remove — o valor esperado ao final é **0**. Quem lê `deals[].skipped` são os testes A–J |
| 6 | O bloco "Erros na última execução" do JSX | **verificada-e-sã e REUSADA** | Já existe e é alimentado por `status.lastRunResult` do polling de 2 min. A Task 2 **não** o edita: acrescenta a exibição do mesmo array no caminho do disparo. Gate: `git diff -U0 frontend/src/components/Dashboard.jsx \| grep -c 'lastRun?.errors'` = **0** |

## Grupo 2 — CLÁUSULA (a): DIREÇÃO REVERSA — o que pode NEUTRALIZAR o construto novo

A pergunta não é "o irmão está são?", é **"o que faz o que eu acabei de construir não funcionar, sem
deixar nenhum teste vermelho?"**. Foi não fazer esta pergunta que produziu o WR5-01.

| # | O que pode neutralizar | Por que | Gate |
|---|---|---|---|
| 1 | A rota projetar campos em vez de devolver o objeto | Se `POST /run` passar a montar `res.json({ notified, stale })`, a chave nova some do payload e o consumidor volta ao ramo de sucesso **em silêncio** | `grep -c 'res.json(result)' backend/src/routes/notifications.js` = **2** (inalterado) e `git diff --name-only -- backend/src/routes/` **vazio** na Task 2 |
| 2 | Um SEGUNDO produtor de `{ skipped: true }` sem a chave nova | Um segundo caminho de recusa (outro lock, um guard novo) reintroduziria o toast vazio por um caminho que o caso (6) não cobre | `grep -v '^\s*//' backend/src/scheduler.js \| grep -c 'skipped: true'` = **1** |
| 3 | Um SEGUNDO consumidor de `POST /run` | Outra tela chamando a rota repetiria a ramificação defeituosa | `grep -rn 'notifications/run' frontend/src \| wc -l` = **1** |
| 4 | O `catch {}` de `sendNow` | Uma resposta não-JSON cai no `toast.error('Erro ao enviar notificações')` — que **tem texto** e portanto não reproduz o defeito | Verificado por leitura; gate: a mensagem literal continua no arquivo (`grep -c 'Erro ao enviar notificações'` = **1**) |
| 5 | O alarme agregado chegar vazio | Se `results.errors` fosse preenchido em rodada normal, o toast novo viraria ruído diário | O par D/E (04-28) e I/J (04-35) já pinam que `r.errors.length === 0` em rodada sã; o cenário **K** volta a medi-lo (`r.errors.length === 0`) |
| 6 | `execucaoIgnorada` nascer no literal de `results` | Se alguém "padronizar" o campo colocando-o no literal com valor falso, o consumidor continua correto; se colocar com valor **verdadeiro** ou como contador, toda rodada vira recusa | Cenário **K**: `r.execucaoIgnorada === undefined` numa rodada concluída. Gate estático: `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'execucaoIgnorada' \| wc -l` = **1** (só no retorno do guard) |

## Grupo 3 — CLÁUSULA (b): RETROATIVIDADE DA JUSTIFICATIVA

O comentário novo vai **condenar** uma forma: *"uma chave que significa duas coisas incompatíveis, e
um consumidor que não tem informação para separá-las"*. A cláusula obriga a grepar os MESMOS
arquivos por construções que essa frase condena, e classificar cada uma.

| Construção encontrada | Classificação | Medição / dono |
|---|---|---|
| `results.error` — significa "a rodada MORREU" (catch externo) **e** "a rodada concluiu com alarme" (04-28, 04-35) | **fora-de-escopo-com-medição** | É exatamente o Info **IN5-01**. Medido: `results.error = ` não-comentário = **3**; e **nenhum** consumidor de UI lê o campo escalar — `grep -rn 'lastRunResult' frontend/src` = **1**, e essa linha lê `errors`. Dano latente, não realizado. Dono: todo `in5-01`, criado no plano **04-38** desta mesma rodada |
| `skipped` como booleano por negócio **e** como contador da rodada | **verificada-e-sã por medição** | Item 5 do Grupo 1: objetos diferentes e zero consumidores de UI do booleano por negócio depois desta Task |
| `reason` (motivo da RECUSA) x `skipReason` (motivo da SUPRESSÃO de um negócio) | **verificada-e-sã por medição** | Nomes distintos, e a distinção é justamente a que o conserto preserva. Medido: `grep -ro '\breason\b' frontend/src \| wc -l` = **1** hoje (a linha defeituosa) → **1** depois (a linha corrigida, dentro do ramo certo); `skipReason` em `frontend/src` = **0**, com dono já existente (`cr4-01c`) |
| `results.skipped` incrementado por QUATRO causas distintas | **verificada-e-sã, com dono anterior** | É o achado que o 04-28 já enfrentou criando contador dedicado; o comentário do literal de `results` já enumera as quatro causas por escrito. Gate: `results.skipped++` não-comentário = **4** (inalterado) |
</inventario_de_irmaos>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — o PAR que fecha o contrato: a recusa fala, a rodada concluída cala</name>

  <read_first>
    - `backend/test/scheduler.resilience.test.js` — o **cabeçalho** (a lista (1)–(5) e o porquê de
      cada um), os controles `usuariosDevemFalhar` / `suspenderUsuarios` / `liberarUsuarios`, o
      `beforeEach`, e os casos **(3)** e **(4)** por inteiro: são o molde do caso novo.
    - `backend/test/scheduler.categoriaIndecidivel.test.js` — o **cabeçalho** (a lista A–J), a
      armação (`servirDealsDoFunilBeefor`, `USUARIOS`, `envios`, `avancarRelogioAte`, o `beforeEach`)
      e o cenário **E** por inteiro: é o molde do cenário K.
    - `backend/src/scheduler.js` — só o guard do topo de `runCheck` e o literal de `results`
      (âncoras nomeadas). **Não editar** nada em `backend/src/` nesta task.
    - As seções `<escopo_travado>` e `<inventario_de_irmaos>` deste plano.
  </read_first>

  <files>backend/test/scheduler.resilience.test.js, backend/test/scheduler.categoriaIndecidivel.test.js</files>

  <behavior>
    **Caso (6) de `scheduler.resilience.test.js` — a recusa continua FALANDO.**
    Aditivo, ao final do arquivo, depois do caso (5), com o seu próprio cabeçalho de seção no estilo
    de caixa do arquivo, nomeando o achado (CR5-01). Arma a concorrência exatamente como o caso (4):
    `usuariosDevemFalhar = false; suspenderUsuarios();`, dispara `runCheck()` **sem** `await` (a
    janela em que `isRunning` já é `true`), aguarda a segunda chamada e, no fim, chama
    `liberarUsuarios()` e `await` na primeira — sem isso o arquivo termina com promessa pendurada
    gravando num banco já fechado pelo `after`.

    Asserções, nesta ordem:
    (a) `concorrente.execucaoIgnorada === true` — **é aqui que o achado fica vermelho**;
    (b) `typeof concorrente.reason === 'string' && concorrente.reason.length > 0` — a recusa continua
    carregando motivo escrito, que é o que o consumidor vai exibir;
    (c) `concorrente.skipped === true` — o contrato ANTIGO fica de pé (compatibilidade), e é isto que
    impede um conserto de quebrar consumidor não medido;
    (d) `concorrente.stale === undefined` — prova que a recusa não executou rodada nenhuma.

    O caso (4) fica **byte a byte**: ele é caracterização golden do contrato antigo, e é justamente o
    contraste entre os dois que documenta a compatibilidade. O caso (6) duplica ~6 linhas de armação
    de propósito, e o comentário registra esse motivo.

    **Cenário K de `scheduler.categoriaIndecidivel.test.js` — a rodada que CONCLUIU CALA.**
    Aditivo, ao final do arquivo, depois de J, com cabeçalho de seção próprio. Armação **idêntica à
    do cenário E** (`servirDealsDoFunilBeefor(primeiro, segundo)` com `orgsQueFalham` vazia), com ids
    novos — a dedup do próprio SUT acopla os casos entre si, então id reusado é caso contaminado.
    A escolha da armação de E não é acaso: ela é a única da suíte que produz uma rodada CONCLUÍDA com
    `results.skipped > 0` **sem** nenhum alarme, que é exatamente o estado que o consumidor não podia
    distinguir de uma recusa.

    Asserções, nesta ordem — comportamento primeiro, instrumento depois:
    (a) `r.skipped === 2` — pré-condição: a rodada tem negócios pulados, o gatilho do defeito;
    (b) `r.notified === 0` e `typeof r.ranAt === 'string'` — a rodada **concluiu**;
    (c) `r.errors.length === 0` — nenhum alarme agregado nesta rodada (o toast novo da Task 2 não
    dispara aqui);
    (d) `r.execucaoIgnorada === undefined` — **a metade negativa do par**: uma rodada concluída não
    pode ser lida como recusa, por mais negócios pulados que tenha;
    (e) `r.reason === undefined` — o motivo de recusa também não vaza para uma rodada concluída.

    A asserção (d) fica **verde já no RED**, e isso é esperado e declarado: hoje `execucaoIgnorada`
    não existe em lugar nenhum, então ela é `undefined` por ausência. O valor do caso não está no
    vermelho — está em impedir que a Task 2 "resolva" o CR5-01 marcando toda rodada como ignorada, ou
    colocando o campo no literal de `results` com valor verdadeiro. É o guarda-corpo que o revisor
    exige por escrito: *"sem o par, um conserto que apenas troque o nome no consumidor deixa o lock
    silencioso"*.

    Os dois cabeçalhos de arquivo ganham a linha do caso novo na lista de cenários, com uma frase
    dizendo por que ele existe.
  </behavior>

  <action>
    Acrescente o caso (6) ao final de `backend/test/scheduler.resilience.test.js` e o cenário K ao
    final de `backend/test/scheduler.categoriaIndecidivel.test.js`, conforme o `<behavior>`.

    Reuse a armação existente inteira nos dois arquivos. **Não** altere `installFakeAxios`,
    `USUARIOS`, `MOLDE`, `servirDeals`, `servirDealsDoFunilBeefor`, `servirDealsComFunil`, o
    `beforeEach`, o `after`, nem qualquer asserção dos casos (1)–(5) e A–J.

    Escolha ids de negócio ainda não usados no arquivo de cenários (os existentes vão de 2301 a
    2392 na faixa das rodadas A–J; use uma faixa nova e declare a razão no comentário: a dedup do
    SUT acopla casos que compartilham id).

    Rode `npm run format` antes de medir qualquer contagem — o Biome decide a quebra de linha, e
    contagem medida antes dele diverge depois (lição registrada no 04-32).

    NÃO edite nada em `backend/src/` nem em `frontend/` nesta task. O vermelho é o produto.

    Vermelho ESPERADO, para conferência: o caso **(6)** falha na asserção **(a)**, com a forma
    `undefined !== true`. O cenário **K** fica **VERDE inteiro** — as cinco asserções passam hoje.

    Se o caso (6) falhar em (b), (c) ou (d) — e SOMENTE nessas —, **PARE e reporte**: significa que a
    armação de concorrência não reproduziu a janela do lock, e o vermelho não é o achado.
    Se o cenário K ficar VERMELHO em qualquer asserção, **PARE e reporte**: ou a armação de E não
    produz mais uma rodada concluída com `skipped > 0`, ou existe um caminho não medido definindo
    `execucaoIgnorada`/`reason` numa rodada concluída — e nos dois casos a premissa do plano caiu.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && node --test test/scheduler.resilience.test.js; test $? -ne 0 && node --test test/scheduler.categoriaIndecidivel.test.js</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | O RED existe e é do caso certo | `cd backend && node --test test/scheduler.resilience.test.js` | exit != 0, exatamente **1 fail** (o caso 6) e **5 pass** |
    | E falha na asserção certa | saída do runner | a linha de falha do caso (6) é a de `execucaoIgnorada` (`undefined !== true`) — nunca uma de `reason`, `skipped` ou `stale` |
    | O cenário K está verde | `cd backend && node --test test/scheduler.categoriaIndecidivel.test.js` | exit **0**, **11 pass** (A–J + K) |
    | Casos no oráculo do lock | `grep -c '^test(' backend/test/scheduler.resilience.test.js` | **6** (era 5) |
    | Cenários no oráculo de quem-recebe | `grep -c '^test(' backend/test/scheduler.categoriaIndecidivel.test.js` | **11** (era 10) |
    | O símbolo novo entrou nos dois oráculos | `grep -ro 'execucaoIgnorada' backend/test \| wc -l` | **≥ 2** (ocorrências, não linhas) |
    | O caso (4) ficou byte a byte | `git diff -U0 backend/test/scheduler.resilience.test.js \| grep -c 'Verificação já em andamento'` | **0** — o literal do caso (4) não aparece no diff |
    | As duas tasks são ADITIVAS | `git diff -U0 backend/test/ \| grep -c '^-[^-]'` | **0** |
    | Nenhum código de produção mudou | `git diff --name-only -- backend/src/ frontend/` | **vazio** |
    | Referência por número de linha não entrou | `git diff -U0 backend/test/ \| grep -c '\.js:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    O caso (6) está vermelho na asserção de `execucaoIgnorada`, o cenário K está verde e é o
    guarda-corpo negativo, `backend/src/` e `frontend/` intocados, e o commit
    `test(04-36): RED do contrato do lock` registra o vermelho.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — desambiguar no PRODUTOR e devolver o alarme ao caminho manual</name>

  <read_first>
    - `backend/src/scheduler.js` — o guard do topo de `runCheck`, o literal de `results` (com o
      comentário que enumera as quatro causas de `results.skipped`), e os DOIS blocos de alarme
      agregado depois do laço (o de FORMA do funil, do 04-35, e o de supressão TOTAL por categoria,
      do 04-28). **Não editar** nenhum dos dois blocos.
    - `frontend/src/components/Dashboard.jsx` — `sendNow()` inteira, `checkOnly()` (para NÃO
      confundir os dois consumidores), e o bloco JSX "Erros na última execução" alimentado por
      `lastRun?.errors?.length`.
    - `backend/src/routes/notifications.js` — os handlers `POST /run` e `POST /check`, só para
      confirmar que nenhum projeta campos. **Não editar.**
    - As seções `<escopo_travado>` e `<inventario_de_irmaos>` deste plano.
  </read_first>

  <files>backend/src/scheduler.js, frontend/src/components/Dashboard.jsx</files>

  <action>
    **(a) O produtor — `backend/src/scheduler.js`, guard do topo de `runCheck`.** Acrescente ao
    objeto devolvido a chave `execucaoIgnorada: true`, **mantendo** `skipped: true` e `reason`
    exatamente como estão. A desambiguação é no PRODUTOR de propósito: o consumidor não tem
    informação para separar um booleano de um contador — os dois chegam como `skipped` truthy — e
    qualquer conserto no consumidor seria adivinhação de tipo.

    O comentário acima do guard precisa registrar, com âncoras nomeadas e sem número de linha:
    (i) que `skipped` designava **duas coisas incompatíveis** — o contrato de recusa deste guard e o
    **contador** `results.skipped`, que quatro causas incrementam (dedup do dia, categoria
    indecidível, funil e "sem destinatário") —, e que o consumidor `sendNow` de `Dashboard.jsx`
    ramificava por ela produzindo `toast.error(result.reason)` com `reason` **undefined**: erro
    vermelho **sem texto** em toda rodada com ao menos um negócio pulado;
    (ii) que `skipped: true` **fica** por compatibilidade com consumidores não medidos, e que o caso
    (6) de `scheduler.resilience.test.js` pina as duas chaves ao mesmo tempo justamente para que
    ninguém remova a antiga "limpando" o contrato;
    (iii) que `execucaoIgnorada` **não** nasce no literal de `results`, e que isso é decisão: uma
    rodada que CONCLUI não deve carregar o campo, e é o cenário **K** de
    `scheduler.categoriaIndecidivel.test.js` que reprova quem o colocar lá com valor verdadeiro;
    (iv) a ameaça que isso mitiga, por extenso: um vermelho sem texto depois de um envio
    bem-sucedido leva o operador a concluir que perdeu envios e a disparar a rodada de novo,
    gerando **duplicatas** — a mesma ameaça que o comentário do alarme do 04-35 já nomeia.

    **Nada mais** de `backend/src/scheduler.js` é editado. Em particular: o contador
    `skippedCategoriaIndecidivel`, a posição dos incrementos e as condições dos dois alarmes ficam
    byte a byte — isso é o WR5-01, escopo do plano 04-37.

    **(b) O consumidor — `frontend/src/components/Dashboard.jsx`, `sendNow()`.** Troque a
    ramificação `if (result.skipped)` por `if (result.execucaoIgnorada)`, e dê ao `toast.error` um
    texto de reserva (`result.reason || 'Verificação já em andamento'`) para que a superfície nunca
    mais possa renderizar vazio, mesmo que o motivo suma do payload.

    No ramo de sucesso, **depois** do `toast.success` do resumo, exiba o alarme agregado da rodada
    quando ele existir: se `result.errors` tiver ao menos um item, emita um `toast.error` com o
    primeiro item, num `id` de toast **diferente** do `toastId` do resumo (senão ele substituiria a
    mensagem de sucesso em vez de somar) e com duração maior. Esta é a metade audível do CR4-01 no
    caminho manual: hoje o alarme só aparece no bloco "Erros na última execução", alimentado pelo
    polling de 2 minutos.

    O comentário em português acima do `if` registra: por que a chave lida é `execucaoIgnorada` e não
    `skipped` (a colisão medida); que `results.errors` é o **único** array que a UI renderiza e que o
    alarme é **por rodada**, não por negócio, então um toast basta; e que uma rodada sã traz o array
    vazio — pinado pelos pares D/E (04-28), I/J (04-35) e pelo cenário K.

    **Não** edite `checkOnly()`, o bloco JSX "Erros na última execução", `aNotificarCount`, nem
    qualquer outro trecho do arquivo.

    **(c) Formatação e prova.** Rode `npm run format` no backend e no frontend antes de medir
    contagens. Não instale nada: `package.json` e lockfiles ficam intocados (T-04-36-SC).
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH"; cd backend && npm test && npm run lint && cd ../frontend && npm run build && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | Suíte inteira verde | `cd backend && npm test` | **194/194** (era 192; +1 no lock, +1 em K) |
    | Cobertura | `cd backend && npm run test:coverage` | exit **0** — registrar no SUMMARY o medido para `scheduler.js`; divergência é registrada com o número, nunca forçada |
    | Lint do backend | `cd backend && npm run lint` | exit **0**, **44** warnings (baseline) |
    | Build do frontend (o gate dele) | `cd frontend && npm run build` | exit **0** |
    | Lint do frontend | `cd frontend && npm run lint` | exit **0**, **60** warnings (baseline) |
    | A chave nova existe no produtor, e só no guard | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'execucaoIgnorada' \| wc -l` | **1** |
    | O contrato antigo ficou de pé | `grep -v '^\s*//' backend/src/scheduler.js \| grep -c 'skipped: true'` | **1** (inalterado) |
    | O contador não foi renomeado | `grep -v '^\s*//' backend/src/scheduler.js \| grep -o 'results.skipped++' \| wc -l` | **4** (inalterado) |
    | O WR5-01 NÃO foi tocado | `git diff -U0 backend/src/scheduler.js \| grep -c 'skippedCategoriaIndecidivel\|funilNaoAvaliado'` | **0** |
    | A prévia não foi tocada | `git diff -U0 backend/src/scheduler.js \| grep -c 'runCheckOnly\|seraNotificado'` | **0** |
    | O consumidor deixou de ler a chave ambígua | `grep -ro 'skipped' frontend/src \| wc -l` | **0** (era 1) |
    | E passou a ler a chave própria | `grep -ro 'execucaoIgnorada' frontend/src \| wc -l` | **1** |
    | O toast de recusa nunca mais fica vazio | `grep -c "result.reason ||" frontend/src/components/Dashboard.jsx` | **1** |
    | O alarme agregado chega ao caminho manual | `grep -c 'result.errors' frontend/src/components/Dashboard.jsx` | **≥ 1** |
    | O toast do alarme não substitui o do resumo | inspeção do `id` passado ao `toast.error` novo | `id` **diferente** do `toastId` do `toast.success` |
    | O bloco JSX de erros não foi editado | `git diff -U0 frontend/src/components/Dashboard.jsx \| grep -c 'lastRun?.errors'` | **0** |
    | `checkOnly` não foi editada | `git diff -U0 frontend/src/components/Dashboard.jsx \| grep -c 'result.total'` | **0** |
    | A rota não foi tocada | `git diff --name-only -- backend/src/routes/` | **vazio** |
    | Nenhum teste foi editado para ficar verde | `git diff --name-only -- backend/test/` | **vazio** |
    | Nenhum pacote instalado | `git status --porcelain package.json package-lock.json backend/package.json backend/package-lock.json frontend/package.json frontend/package-lock.json` | **vazio** |
    | Referência por número de linha não entrou | `git diff -U0 backend/src/ frontend/src/ \| grep -c '\.js:[0-9]\|\.jsx:[0-9]'` | **0** |
  </acceptance_criteria>

  <done>
    O caso (6) está verde sem que nenhum arquivo de teste tenha sido tocado, o cenário K continua
    verde, a suíte inteira passa em 194/194, backend e frontend saem 0 em lint e build, o diff de
    `scheduler.js` é estritamente o guard do topo mais comentário, e o commit `fix(04-36): GREEN`
    registra a mudança.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| `runCheck` → `POST /run` → Dashboard | O payload da rodada atravessa a rota sem projeção e é renderizado na UI autenticada; é a ÚNICA superfície de feedback do disparo manual |
| Operador → botão "Enviar notificações" | A leitura que o operador faz do feedback decide se ele dispara a rodada de novo — e o disparo tem efeito irreversível (e-mail) |
| `results.errors` → `toast.error` | Texto vindo do backend é renderizado na tela sem sanitização adicional |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-04-36-01 | Spoofing | `sendNow` / `Dashboard.jsx` | **mitigate** | Um toast vermelho **em branco** depois de um envio bem-sucedido falsifica o desfecho da rodada: o operador conclui que perdeu envios, dispara de novo e gera **duplicatas** (a ameaça T-04-35-05, realizada). A ramificação passa a ser por `execucaoIgnorada`, que só existe no caminho de recusa; e o toast de recusa ganha texto de reserva, de modo que a superfície não pode renderizar vazio nem quando `reason` some. Pinado pelo PAR caso (6) + cenário K |
| T-04-36-02 | Repudiation | Alarme agregado da rodada no caminho manual | **mitigate** | No apagão, o alarme do 04-28/04-35 existia apenas em `results.errors` e só chegava à tela pelo polling de 2 min — o disparo manual não deixava rastro visível do apagão para quem o operou. `sendNow` passa a exibir o primeiro item do array num toast próprio, em `id` distinto, sem substituir o resumo |
| T-04-36-03 | Tampering | Contrato do lock (`skipped: true`, `reason`) | **mitigate** | Renomear em vez de acrescentar quebraria consumidores não medidos do payload. As duas chaves antigas ficam, e o caso (6) assere as **três** ao mesmo tempo, de modo que remover a antiga fica vermelho |
| T-04-36-04 | Information Disclosure | Texto exibido no `toast.error` do alarme | **mitigate** | O conteúdo de `results.errors` é montado no backend com **apenas inteiros e texto fixo** (regra CR-02 do 04-09, aplicada pelos alarmes do 04-28 e do 04-35 e pinada nos gates daqueles planos). Esta task **não** cria mensagem nova no backend e **não** interpola objeto de erro no frontend — gate: `git diff --name-only -- backend/src/routes/` vazio e nenhum bloco de alarme de `scheduler.js` no diff |
| T-04-36-05 | Denial of Service | Volume de toasts por disparo | **accept (verificada)** | No máximo **dois** toasts por clique: o resumo e, quando existir, **um** alarme por rodada — o alarme é agregado por construção (uma linha por rodada, pinado por `r.errors.length === 1` nos cenários D e I). Não há caminho para N toasts por negócio |
| T-04-36-SC | Tampering | Instalação de pacotes (npm/pip/cargo) | **mitigate** | Este plano **não instala nada** — nenhuma dependência nova, nenhum bump. Gate: `git status --porcelain` dos três `package.json` e dos três lockfiles vazio na Task 2. Não havendo instalação, o portão humano de legitimidade de pacote não se aplica |
</threat_model>

<verification>
1. `cd backend && npm test` — **194/194** verdes (era 192).
2. `cd backend && npm run lint` — exit 0, 44 warnings (baseline inalterado).
3. `cd frontend && npm run build` — exit 0 (o gate do frontend; não há testes lá).
4. `cd frontend && npm run lint` — exit 0, 60 warnings (baseline inalterado).
5. O PAR provado: caso **(6)** (a recusa traz a chave nova E o motivo escrito) e cenário **K** (a
   rodada concluída com `skipped > 0` **não** traz a chave nova).
6. `grep -ro 'skipped' frontend/src | wc -l` = **0** — a chave ambígua não é mais lida na UI.
7. Os casos (1)–(5) e A–J verdes **sem edição**: `git diff --name-only -- backend/test/` vazio na
   Task 2.
8. O inventário de irmãos, incluindo as duas cláusulas novas: cada item classificado tem grep ou
   caso de teste, e os fora-de-escopo têm dono nomeado (`in5-01` no plano 04-38, `cr4-01c` já
   existente).
</verification>

<success_criteria>
- Uma rodada que concluiu com negócios pulados mostra o resumo, não um erro — e isso está pinado por
  teste, não por comentário.
- Uma chamada recusada pelo lock continua mostrando o motivo escrito, e não pode mais renderizar
  vazio nem se `reason` sumir do payload.
- O alarme agregado da rodada chega à tela pelo caminho manual, no mesmo clique que o produziu.
- O contrato antigo (`skipped: true`, `reason`) continua no payload, e há caso de teste impedindo que
  ele seja removido a título de limpeza.
- Nada do WR5-01 foi tocado: `skippedCategoriaIndecidivel`, a posição dos incrementos e as condições
  dos dois alarmes estão fora do diff.
</success_criteria>

<output>
Crie `.planning/phases/04-confiabilidade-das-integra-es/04-36-SUMMARY.md` ao terminar.

Registre obrigatoriamente: o RED literal da Task 1 (saída do runner, com a asserção em que o caso (6)
falhou) e o fato de o cenário **K** ter nascido VERDE, com a justificativa do seu valor; todos os
números medidos ao lado dos prescritos, **inclusive as divergências**, registradas com o número
medido e o motivo, nunca ajustadas para bater — esta fase acumulou onze divergências de contagem e
nenhuma foi forçada; a cobertura medida de `scheduler.js`; o inventário de irmãos com a classificação
final de cada item dos **três** grupos (irmãos, direção reversa, retroatividade da justificativa); e o
que este plano **não** fecha (WR5-01 → plano 04-37; `cr4-01b` → plano 04-37; WR5-02..WR5-05 e
IN5-01..IN5-04 → plano 04-38).
</output>
