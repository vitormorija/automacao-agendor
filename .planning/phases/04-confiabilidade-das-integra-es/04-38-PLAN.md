---
phase: 04-confiabilidade-das-integra-es
plan: 38
type: execute
wave: 31
depends_on: [04-37]
gap_closure: true
autonomous: true
requirements: [REL-03, REL-06]
files_modified:
  - .planning/todos/pending/wr5-02-aviso-de-categoria-por-negocio-no-caminho-de-leitura.md
  - .planning/todos/pending/wr5-03-limiar-do-alarme-de-forma-do-funil-com-n-pequeno.md
  - .planning/todos/pending/wr5-04-supressao-por-funil-fora-do-log-estruturado.md
  - .planning/todos/pending/wr5-05-assercoes-de-envio-afrouxadas-perdem-duplicata.md
  - .planning/todos/pending/in5-01-results-error-com-dois-significados.md
  - .planning/todos/pending/in5-02-envelope-nulo-nas-tres-paginacoes.md
  - .planning/todos/pending/in5-03-guarda-de-nulo-larga-demais-no-oraculo-do-resumo.md
  - .planning/todos/pending/in5-04-lote-de-organizacoes-sem-pausa-entre-lotes.md
  - .planning/ROADMAP.md

must_haves:
  truths:
    - "Os quatro warnings e os quatro info da rodada 5 que o usuário excluiu do escopo de correção têm arquivo próprio, com a evidência MEDIDA preservada"
    - "Nenhum achado da rodada 5 fica sem dono — achado sem arquivo não sobrevive à fase"
    - "Nenhum todo existente foi editado, e nenhum achado foi registrado duas vezes"
    - "O ROADMAP registra a gap closure r5 com os três planos e o mandato estrutural da rodada"
  artifacts:
    - path: ".planning/todos/pending/wr5-05-assercoes-de-envio-afrouxadas-perdem-duplicata.md"
      provides: "O residual de PRIORIDADE ALTA da rodada: o oráculo deixou de detectar e-mail DUPLICADO nos três cenários mais novos"
    - path: ".planning/ROADMAP.md"
      provides: "Bloco da gap closure r5 com os planos 04-36..04-38 e as duas cláusulas novas do mandato"
  key_links:
    - from: ".planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md"
      to: ".planning/todos/pending/"
      via: "cada achado fora do escopo de correção vira um arquivo com a medição preservada"
      pattern: "wr5-0|in5-0"
---

<objective>
Fechar o **resíduo documental** da rodada 5. Este plano **não conserta nada**: ele torna rastreável o
que o usuário decidiu não corrigir agora.

Decisão do usuário de 2026-08-05: a gap closure r5 corrige **somente** CR5-01 (plano 04-36) e WR5-01
(plano 04-37, que também reescreve o `cr4-01b`). Os outros **quatro warnings** (WR5-02..WR5-05) e os
**quatro info** (IN5-01..IN5-04) viram **todos pendentes**, no mesmo precedente que já valeu para
IN-01..IN-04 (plano 04-09), IN2-01..IN2-04 (04-18), IN3-01..IN3-08 (04-27) e IN4-02..IN4-06 (04-34).

Purpose: é esse precedente que permitiu ao 04-26 **fechar** o `in2-02` com evidência e ao 04-35
promover o `in3-08` a plano. Achado sem arquivo não sobrevive à fase — e o achado de **prioridade
alta** desta rodada (WR5-05: as asserções de envio afrouxadas de `=== 1` para `>= 1`, que deixaram de
detectar **e-mail duplicado**) é exatamente a metade do Core Value que ninguém deve perder de vista.

Output: oito arquivos novos em `.planning/todos/pending/`, no molde dos `in3-*` e `in4-*`, cada um
carregando a **medição** que o revisor registrou; e o bloco da gap closure r5 no ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md
@.planning/todos/pending/in4-04-leitura-de-success-divergente-entre-ramos.md

Convenções que valem aqui e não se negociam: português; **WR2-06 aplicado ao próprio artefato** —
nenhuma referência por número de linha em nenhum dos oito arquivos; cada item cita função,
identificador, nome de arquivo ou nome de caso de teste. Formato herdado dos `in3-*`/`in4-*`:
frontmatter (`id`, `type: todo`, `status: pending`, `priority`, `created`, `source`,
`resolves_phase: null`, `tags`) + **Onde** / **O que acontece** / **Por que a prioridade é X** /
**Correção proposta**, e a linha final citando o achado original.
</context>

<interfaces>
Estado de ENTRADA medido em 2026-08-05 (depois dos planos 04-36 e 04-37):

- `.planning/todos/pending/` tem **33** arquivos. Depois deste plano: **41**.
- Todos existentes que **NÃO** podem ser editados: `cr-02b`, `cr4-01b` (já reescrito pelo 04-37),
  `cr4-01c`, `in-01`..`in-04`, `in2-01`, `in2-03`, `in2-04`, `in3-01`..`in3-07`, `in3-08b`,
  `in4-02`..`in4-06`, `ops-01`, `rel-02b`, `rel-05b`, `sec-01`, `sec-02`, `ui-01`, `wr3-07b`,
  `wr4-03b`, `wr4-04b`, `wr4-07b`.
- Verificação de duplicidade já feita na entrada deste plano: **nenhum** dos oito achados da r5 tem
  dono entre os 33. Os vizinhos temáticos existem e devem ser **referenciados**, não duplicados —
  `cr-02b` (log cru em `index.js`), `cr4-01c` (`skipReason` invisível na UI), `in4-03` (asserção
  decorativa), `wr4-04b` (fan-out proporcional em `/resolved`).
- Suíte na entrada: **196/196**; `npm run lint` exit 0, 44 warnings. Este plano tem **diff de código
  ZERO** por critério de aceite.

**A evidência medida de cada achado** (fonte: `04-REVIEW-r5.md`; preservar nos arquivos):

| Achado | Evidência que NÃO pode se perder |
|---|---|
| **WR5-02** | O aviso de categoria indecidível é `logger.warn` **por negócio**, dentro do laço de enriquecimento de `getStaleDeals` — exatamente a forma que a justificativa do aviso agregado de funil, escrita pelo 04-35 na **mesma função**, condena por escrito (*"um aviso por negócio produziria N linhas a cada atualização de tela, e um log inundado é um log que ninguém lê: a mitigação viraria o defeito"*). Sonda: 3 negócios → 3 linhas numa única chamada. `getStaleDeals` tem **8** invocações fora do módulo, e `DealsList.jsx` faz auto-refresh a cada **300 s**. No apagão — o cenário que o CR4-01 existe para tornar audível — o sinal agregado fica **soterrado** pelo ruído do irmão. A saída **não** é apagar o aviso: ele é a única fonte que nomeia a **organização**; é agregar por chamada e degradar o detalhe (mesma regra do CR-02: nomes e inteiros, nunca o objeto de erro do axios). Oráculo faltante: **cardinalidade** — hoje nenhum caso da suíte mede quantas linhas de log cada aviso emite |
| **WR5-03** | O alarme de FORMA do funil dispara com `results.stale > 0 && funilNaoAvaliado === stale`, então **N = 1** satisfaz trivialmente — e a mensagem afirma que *"a forma do payload da Agendor pode ter mudado"*, o que é **factualmente falso** para um único negócio sem funil cadastrado no CRM. `results.stale` não é o total do CRM: é o subconjunto parado além do threshold e sem tarefa futura, e um dia com 1-3 negócios é rotina. O oráculo **não** cobre N pequeno: os cenários I e J usam exatamente **2** negócios cada, e uma implementação com piso mínimo continuaria verde nos dois. O dano é o que os cenários E e J foram escritos para evitar: alarme em dia normal treina o operador a ignorar o bloco vermelho, e o apagão real volta a passar despercebido. O mesmo raciocínio **não** vale para o alarme de categoria: lá a mensagem continua verdadeira com N = 1, e o limiar é decisão registrada do usuário |
| **WR5-04** | Os dois contadores de supressão de `sendOwnerWeeklySummary` decidem a **mesma coisa** (quem sai do relatório individual) e são reportados por caminhos incompatíveis: o de **funil** (`skippedByFunnel`) sai por `console.log`, o de **categoria** por `logger.warn`. Em produção o `logger` emite **JSON de uma linha** com `time`/`level`/`message`; `console.log` emite texto cru sem nível. Numa agregação por nível ou por campo, a supressão por funil — que o 04-35 acabou de **alargar** para substring, aumentando o conjunto suprimido — fica **invisível**, e quem investigar "por que este comercial não recebeu o relatório de sexta" encontra metade das causas. O `CLAUDE.md` é explícito, e o módulo **já importa** `logger` e o usa na linha de baixo. O inventário do 04-28 classificou esta função como `verificada-e-sã` olhando a **existência** das duas linhas, não o **mecanismo** de cada uma |
| **WR5-05** (**ALTA**) | Os cenários **H, I e J** de `scheduler.categoriaIndecidivel.test.js` asserem `envios(...) >= 1` — **10 asserções** —, enquanto os irmãos **A, B e C**, no mesmo arquivo, asserem `=== 1`. `=== 1` detecta o envio a menos **e** o envio a mais; `>= 1` detecta só o envio a menos. A **duplicata** é a metade do Core Value que a fase inteira negocia: o trade-off do checkpoint C10, a semântica de sucesso parcial de WR-01, a existência de `houveEnvioConfirmado` nos dois ramos (WR2-01) e a ameaça T-04-35-05. Nos três cenários que exercitam o comportamento **mudado** pela r4/r5, essa metade deixou de ser medida — e **sem justificativa técnica**: nos três, `runCheckOnly` roda antes e `runCheck` roda uma única vez, então o valor exato é 1. Nenhum comentário registra o motivo da troca de forma, ao contrário de todas as outras decisões de instrumento daquele arquivo. Se algum ficar vermelho ao restaurar `=== 1`, **o vermelho é o achado** |
| **IN5-01** | `results.error` passou a significar **duas coisas incompatíveis**: "a rodada MORREU" (o `catch` externo) e "a rodada CONCLUIU, mas houve supressão/forma anômala" (os dois alarmes agregados). Um consumidor que queira responder "o cron das 8h rodou até o fim?" não consegue mais. Dano **latente**: medido, nenhum consumidor lê o campo escalar — `grep -rn "lastRunResult" frontend/src` devolve **1** linha, que lê `errors`. Saída: acrescentar um campo de conclusão explícito antes de `results.duration`, ou mover os alarmes para campo próprio mantendo `results.errors` como superfície de UI. **Anda junto de `cr4-01c`** |
| **IN5-02** | As três paginações de `agendor.js` guardam a chave `data` mas nenhuma guarda o **envelope nulo**: se a borda responder 200 com corpo `null` ou string vazia, a desestruturação lança `TypeError` nas três. O conserto de WR4-05 e o comentário que o acompanha estão corretos quanto à assimetria que existia (a chave ausente); o residual é **uniforme**. O caso `IRMÃS VERIFICADAS` de `agendor.paginacao.test.js` **não** cobre esse formato — ele serve sempre um objeto. Registrado como Info porque é uniforme e não é vizinho aberto por conserto nenhum |
| **IN5-03** | A guarda `assertHtmlSemNuloNoNome` de `emailer.resumoIndecidivel.test.js` varre o corpo **inteiro** do e-mail procurando a substring `null`: qualquer dado legítimo que a contenha — um título de negócio, um nome de organização, um nome de etapa — reprova o caso por motivo **sem relação** com o defeito medido (`ownerName` nulo chegando à saudação). O irmão que procura `undefined` tem o mesmo problema em menor grau. Saída: restringir o escopo ao trecho sob medição (o bloco da saudação) e exigir que o valor capturado não seja `'null'`/`'undefined'` |
| **IN5-04** | O lote de organizações de `getStaleDeals` limita **concorrência** mas **não impõe pausa** entre lotes, enquanto o lote irmão de páginas impõe uma espera de 1 s. A justificativa escrita para o lote de organizações é que retentar em massa *"PROLONGA a própria janela de rate limit"* — mas um teto de concorrência sem pausa reduz o **pico simultâneo** e quase não reduz a **taxa**: 25 organizações continuam saindo em milissegundos, em 3 lotes encostados. O oráculo `agendor.loteDeOrganizacoes.test.js` mede exclusivamente o pico em voo, então a distinção não tem guarda-corpo. Duas saídas: acrescentar a mesma pausa do irmão **e medi-la**, ou ajustar o comentário para afirmar só o que a construção entrega (pico, não taxa) — a segunda é mais barata e não muda comportamento |
</interfaces>

<escopo_travado>
Decisão do usuário de 2026-08-05, vinculante:

- Os oito achados viram **TODOS PENDENTES**, **não planos**. Este plano **não** conserta nenhum deles
  e **não** toca em `backend/` nem em `frontend/`. Critério de aceite nas três tasks:
  `git status --porcelain backend/ frontend/` **vazio**.
- **WR5-05 tem prioridade ALTA**, com o motivo escrito no arquivo: duplicata é a outra metade do Core
  Value ("quem recebe / quem **não** recebe"), e o achado está justamente nos cenários que exercitam
  o comportamento mudado nas duas últimas rodadas.
- **Nenhum todo existente é editado.** Há 33 pendentes; antes de criar cada arquivo, confirme que o
  achado não tem dono. Referenciar um vizinho é obrigatório onde o par existe; **duplicá-lo** é
  proibido.
- **C8 / SEC-01**: o `sec-01` continua ABERTO como risco aceito e **não** é tocado; o valor do
  `AGENDOR_TOKEN` não pode aparecer em nenhum artefato.
</escopo_travado>

<pares_declarados>
Cada par existe para que nenhum dos dois seja fechado pela metade. O par vai **escrito** dentro do
arquivo, com o motivo:

- **wr5-02 + wr5-03** — os dois são sobre **ruído no sinal**: um inunda o log no caminho de leitura,
  o outro dispara alarme sem massa. Fechar um e deixar o outro mantém o operador treinado a ignorar
  vermelho, que é a condição em que o apagão real volta a passar despercebido.
- **wr5-04 + cr-02b** — os dois são sobre **log cru fora do logger estruturado**, em módulos
  diferentes. Candidatos naturais a serem absorvidos pela **Fase 5 (Logging & Padronização de
  Erros)**; o arquivo declara isso.
- **wr5-05 + in4-03** — os dois são sobre **força do oráculo**: uma asserção que não pode falhar e
  uma asserção que deixou de medir metade do contrato. Quem revisar o arquivo de cenários fecha os
  dois na mesma passagem.
- **in5-01 + cr4-01c** — os dois são sobre **superfícies que a UI não distingue**; o próprio revisor
  registrou que eles andam juntos.
- **in5-02 + o caso `IRMÃS VERIFICADAS` de `agendor.paginacao.test.js`** — o conserto entra ali, como
  modo novo da armação, e não num arquivo próprio.
- **in5-03 + in4-03** — higiene do mesmo tipo de instrumento (asserção larga demais x asserção
  decorativa).
- **in5-04 + wr4-04b** — os dois são sobre o **fan-out proporcional ao dado** na borda da Agendor,
  por caminhos diferentes.
</pares_declarados>

<tasks>

<task type="auto">
  <name>Task 1: os quatro WARNINGS da rodada 5 como todos pendentes</name>

  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md` — os achados **WR5-02**,
      **WR5-03**, **WR5-04** e **WR5-05** por inteiro, incluindo os blocos de sonda e de `Fix`.
    - `.planning/todos/pending/in4-04-leitura-de-success-divergente-entre-ramos.md` — o molde de
      formato (frontmatter + as quatro seções + a linha final de origem).
    - `.planning/todos/pending/cr-02b-console-error-objeto-completo-em-index.md` e
      `.planning/todos/pending/in4-03-assercao-decorativa-em-categoriaindecidivel.md` — os vizinhos a
      **referenciar** (e não duplicar), conforme `<pares_declarados>`.
    - A tabela de evidência da seção `<interfaces>` deste plano e a seção `<escopo_travado>`.
  </read_first>

  <files>.planning/todos/pending/wr5-02-aviso-de-categoria-por-negocio-no-caminho-de-leitura.md, .planning/todos/pending/wr5-03-limiar-do-alarme-de-forma-do-funil-com-n-pequeno.md, .planning/todos/pending/wr5-04-supressao-por-funil-fora-do-log-estruturado.md, .planning/todos/pending/wr5-05-assercoes-de-envio-afrouxadas-perdem-duplicata.md</files>

  <action>
    Crie os quatro arquivos com os nomes exatos listados em `<files>`, no molde do `in4-04`.

    **Prioridades, com o motivo escrito dentro de cada arquivo:**
    - `wr5-05` — **alta**. Duplicata é a outra metade do Core Value, e o achado está nos cenários que
      exercitam o comportamento mudado pelas rodadas 4 e 5. É o único ALTA desta leva.
    - `wr5-02` — **média**. Não muda quem recebe; degrada o sinal que o CR4-01 construiu, exatamente
      no cenário em que ele é necessário.
    - `wr5-03` — **média**. Não muda quem recebe; produz vermelho em dia normal, e vermelho em dia
      normal é como se aprende a ignorar vermelho.
    - `wr5-04` — **média**. Não muda quem recebe; some com metade das causas de "por que este
      comercial não recebeu" na agregação de produção, sobre o filtro que o 04-35 acabou de alargar.

    **Cada arquivo carrega a medição da tabela de `<interfaces>`, não a intenção.** Em particular:
    - `wr5-02` precisa registrar que a justificativa que condena a forma foi escrita pelo **04-35 na
      mesma função**, que o aviso por negócio é a **única** fonte que nomeia a organização (a saída é
      agregar, não apagar), e que **nenhum** caso da suíte mede hoje a **cardinalidade** de nenhum
      dos dois avisos.
    - `wr5-03` precisa registrar que os cenários I e J usam **2** negócios cada e que **uma
      implementação com piso e outra sem piso passam nos dois** — por isso o conserto exige o par que
      falta (N = 1 sem funil exige silêncio; N = piso sem funil exige alarme). E precisa registrar
      por que o mesmo raciocínio **não** se aplica ao alarme de categoria: lá a mensagem continua
      verdadeira com N = 1, e o limiar é decisão do usuário.
    - `wr5-04` precisa registrar que o módulo **já importa** o `logger` e o usa na linha seguinte, e
      que as demais ocorrências de log cru do mesmo arquivo são **legado não tocado** por esta
      rodada — o par de contadores de supressão é o que a rodada editou e o que decide destinatário.
    - `wr5-05` precisa registrar as **duas formas** lado a lado (`=== 1` nos cenários antigos,
      `>= 1` nos novos), o que **cada uma** detecta, o número de asserções afetadas, e a instrução
      operacional: restaurar a forma exata e, **se algum ficar vermelho, o vermelho é o achado** — é
      exatamente o que a forma frouxa está escondendo. Registre também, como precedente disponível,
      que os cenários **L** e **M** criados pelo plano 04-37 já usam a forma exata.

    Cada arquivo declara o **par** correspondente da seção `<pares_declarados>`, com o motivo.

    **Convenção WR2-06 no próprio artefato:** nenhuma referência por número de linha em nenhum dos
    quatro. Cite função (`getStaleDeals`, `sendOwnerWeeklySummary`), identificador
    (`skippedByFunnel`, `funilNaoAvaliado`, `assertHtmlSemNuloNoNome`), arquivo ou nome de cenário
    (`H`, `I`, `J`, `A`, `B`, `C`).

    **Não** edite nenhum todo existente, **não** toque em `backend/` nem em `frontend/`, **não**
    instale nada.
  </action>

  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor && test "$(ls .planning/todos/pending/wr5-0*.md | wc -l | tr -d ' ')" = "4" && test "$(git status --porcelain backend/ frontend/ | wc -l | tr -d ' ')" = "0" && echo ok</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | Os quatro arquivos existem | `ls .planning/todos/pending/wr5-0*.md \| wc -l` | **4** |
    | Frontmatter válido nos quatro | `grep -c '^status: pending' .planning/todos/pending/wr5-0*.md` | **1** em cada |
    | WR5-05 é o único ALTA da leva | `grep -l 'priority: alta' .planning/todos/pending/wr5-0*.md` | apenas `wr5-05-...` |
    | Os demais são média | `grep -c 'priority: média' .planning/todos/pending/wr5-0*.md` | **1** em cada um dos outros três |
    | Cada arquivo cita a origem | `grep -c 'r5' .planning/todos/pending/wr5-0*.md` | **≥ 1** em cada |
    | Cada arquivo declara o seu par | `grep -ciE 'cr-02b\|in4-03\|wr5-02\|wr5-03' .planning/todos/pending/wr5-0*.md` | **≥ 1** em cada |
    | WR5-05 traz as DUAS formas medidas | `grep -c '>= 1' .planning/todos/pending/wr5-05-assercoes-de-envio-afrouxadas-perdem-duplicata.md` | **≥ 1** |
    | E nomeia a duplicata explicitamente | `grep -ci 'duplicat' .planning/todos/pending/wr5-05-assercoes-de-envio-afrouxadas-perdem-duplicata.md` | **≥ 2** |
    | WR5-02 registra a cardinalidade como oráculo faltante | `grep -ci 'cardinalidade' .planning/todos/pending/wr5-02-aviso-de-categoria-por-negocio-no-caminho-de-leitura.md` | **≥ 1** |
    | WR5-03 registra que I e J usam 2 negócios | `grep -c 'I e J' .planning/todos/pending/wr5-03-limiar-do-alarme-de-forma-do-funil-com-n-pequeno.md` | **≥ 1** |
    | Convenção WR2-06 nos quatro | `grep -cE '\.js:[0-9]\|linhas? [0-9]' .planning/todos/pending/wr5-0*.md` | **0** em cada |
    | Tamanho compatível com o molde da fase | `wc -l .planning/todos/pending/wr5-0*.md` | entre **40** e **95** linhas cada |
    | Nenhum todo existente foi editado | `git status --porcelain .planning/todos/pending/ \| grep -c '^ M'` | **0** |
    | Diff de código ZERO | `git status --porcelain backend/ frontend/` | **vazio** |
    | Nenhum valor de segredo | `grep -riE 'AGENDOR_TOKEN[[:space:]]*=\|Bearer \|Token [A-Za-z0-9]{16}' .planning/todos/pending/wr5-0*.md \| wc -l` | **0** |
  </acceptance_criteria>

  <done>
    Os quatro warnings da r5 têm arquivo próprio com a medição preservada, WR5-05 marcado como alta,
    nenhum todo existente tocado, `backend/` e `frontend/` intocados, e o commit
    `docs(04-38): WR5-02..WR5-05 como todos pendentes` registra a leva.
  </done>
</task>

<task type="auto">
  <name>Task 2: os quatro INFO da rodada 5 como todos pendentes</name>

  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md` — a seção **Info** inteira
      (IN5-01 a IN5-04).
    - `.planning/todos/pending/cr4-01c-skipreason-invisivel-na-ui.md` e
      `.planning/todos/pending/wr4-04b-fanout-proporcional-em-resolved.md` — os vizinhos a
      **referenciar** (e não duplicar).
    - A tabela de evidência da seção `<interfaces>` deste plano e a seção `<pares_declarados>`.
  </read_first>

  <files>.planning/todos/pending/in5-01-results-error-com-dois-significados.md, .planning/todos/pending/in5-02-envelope-nulo-nas-tres-paginacoes.md, .planning/todos/pending/in5-03-guarda-de-nulo-larga-demais-no-oraculo-do-resumo.md, .planning/todos/pending/in5-04-lote-de-organizacoes-sem-pausa-entre-lotes.md</files>

  <action>
    Crie os quatro arquivos com os nomes exatos listados em `<files>`, no mesmo molde da Task 1.

    **Prioridades, com o motivo escrito dentro de cada arquivo:**
    - `in5-01` — **média**. O dano é **latente** (medido: nenhum consumidor lê o campo escalar hoje),
      mas o campo é o que responde "o cron rodou até o fim?", e a ambiguidade nasceu nesta fase.
    - `in5-02` — **média**. Uniforme às três paginações e capaz de derrubar a rodada por `TypeError`,
      mas exige uma resposta 200 com corpo nulo — não é vizinho aberto por conserto nenhum.
    - `in5-03` — **baixa**. Higiene de instrumento: produz vermelho pelo motivo errado, sem tocar
      produção.
    - `in5-04` — **baixa**. Duas saídas legítimas, uma delas sem mudança de comportamento (ajustar o
      comentário para afirmar só o pico, não a taxa).

    **Registros obrigatórios:**
    - `in5-01` precisa registrar a medição que o classifica como latente
      (`grep -rn "lastRunResult" frontend/src` = **1** linha, que lê `errors`) e as **duas** saídas
      possíveis; e declarar o par com `cr4-01c`. Precisa também registrar que o plano **04-37**
      **estreitou a mensagem** do alarme de categoria, o que reduz o dano de leitura humana mas
      **não** resolve a ambiguidade do campo para um consumidor programático.
    - `in5-02` precisa dizer que o conserto entra como **modo novo da armação** do caso
      `IRMÃS VERIFICADAS` de `agendor.paginacao.test.js`, e não em arquivo próprio.
    - `in5-03` precisa nomear o helper (`assertHtmlSemNuloNoNome`), os cenários que o usam, e a saída
      (restringir ao bloco da saudação).
    - `in5-04` precisa registrar as **duas** saídas e recomendar a barata (ajustar a afirmação do
      comentário), com a distinção **pico x taxa** escrita por extenso; e declarar o par com
      `wr4-04b`.

    Convenção **WR2-06** nos quatro. **Nenhum** todo existente é editado; **nada** em `backend/` ou
    `frontend/`; nenhum pacote instalado.
  </action>

  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor && test "$(ls .planning/todos/pending/in5-0*.md | wc -l | tr -d ' ')" = "4" && test "$(ls .planning/todos/pending/ | wc -l | tr -d ' ')" = "41" && test "$(git status --porcelain backend/ frontend/ | wc -l | tr -d ' ')" = "0" && echo ok</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | Os quatro arquivos existem | `ls .planning/todos/pending/in5-0*.md \| wc -l` | **4** |
    | Total de pendentes | `ls .planning/todos/pending/ \| wc -l` | **41** (era 33) |
    | Prioridades conforme a decisão | `grep -h '^priority:' .planning/todos/pending/in5-0*.md` | média, média, baixa, baixa |
    | IN5-01 traz a medição que o torna latente | `grep -c 'lastRunResult' .planning/todos/pending/in5-01-results-error-com-dois-significados.md` | **≥ 1** |
    | E declara o par com cr4-01c | `grep -c 'cr4-01c' .planning/todos/pending/in5-01-results-error-com-dois-significados.md` | **≥ 1** |
    | IN5-02 aponta o arquivo de oráculo certo | `grep -c 'agendor.paginacao.test.js' .planning/todos/pending/in5-02-envelope-nulo-nas-tres-paginacoes.md` | **≥ 1** |
    | IN5-03 nomeia o helper | `grep -c 'assertHtmlSemNuloNoNome' .planning/todos/pending/in5-03-guarda-de-nulo-larga-demais-no-oraculo-do-resumo.md` | **≥ 1** |
    | IN5-04 escreve a distinção pico x taxa | `grep -ci 'pico' .planning/todos/pending/in5-04-lote-de-organizacoes-sem-pausa-entre-lotes.md` | **≥ 1** |
    | E declara o par com wr4-04b | `grep -c 'wr4-04b' .planning/todos/pending/in5-04-lote-de-organizacoes-sem-pausa-entre-lotes.md` | **≥ 1** |
    | Convenção WR2-06 nos quatro | `grep -cE '\.js:[0-9]\|linhas? [0-9]' .planning/todos/pending/in5-0*.md` | **0** em cada |
    | Tamanho compatível com o molde | `wc -l .planning/todos/pending/in5-0*.md` | entre **40** e **95** linhas cada |
    | Nenhum todo existente foi editado | `git status --porcelain .planning/todos/pending/ \| grep -c '^ M'` | **0** |
    | Diff de código ZERO | `git status --porcelain backend/ frontend/` | **vazio** |
    | Nenhum valor de segredo | `grep -riE 'AGENDOR_TOKEN[[:space:]]*=\|Bearer \|Token [A-Za-z0-9]{16}' .planning/todos/pending/in5-0*.md \| wc -l` | **0** |
  </acceptance_criteria>

  <done>
    Os quatro info da r5 têm arquivo próprio com a medição preservada e o par declarado, o diretório
    de pendentes tem 41 arquivos, nenhum todo existente foi tocado, e o commit
    `docs(04-38): IN5-01..IN5-04 como todos pendentes` registra a leva.
  </done>
</task>

<task type="auto">
  <name>Task 3: o ROADMAP registra a gap closure r5</name>

  <read_first>
    - `.planning/ROADMAP.md` — a seção **Phase 4** inteira: a linha `**Plans**`, os blocos de gap
      closure **r1** a **r4** (é o molde exato do bloco novo, incluindo a frase que declara o
      **requisito estrutural** de cada rodada) e os **Success Criteria** de 1 a 8.
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW-r5.md` — a seção *"Avaliação
      pedida: o mandato INVENTÁRIO DE IRMÃOS funcionou?"*, que é a origem das duas cláusulas novas.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
    Acrescente, **depois** do bloco `**Gap closure r4**` e antes da linha `### Phase 5`, um bloco
    `**Gap closure r5**` no mesmo formato dos anteriores, contendo:

    - a fonte (`04-REVIEW-r5.md`, `round: 5`, `issues_found` — 1 critical, 5 warning, 4 info) e o
      fato de que a rodada 5 verificou ceticamente as conclusões da r4 e **REABRIU a fase pela quinta
      vez**;
    - o número de planos (**3**, aditivos, waves 29-31, `gap_closure: true`) e o **escopo travado
      pelo usuário**: corrigir somente CR5-01 e WR5-01, reescrever o `cr4-01b`, e registrar os demais
      oito achados como todos pendentes;
    - a **mudança de perfil** da rodada, que é o que a distingue das quatro anteriores: os achados
      **não mudam quem recebe** — são defeitos nos **instrumentos**, que quebram os sinais
      construídos para perceber falha. O eixo "quem recebe" está estável desde o 04-21;
    - o **requisito estrutural** da rodada, no mesmo lugar em que a r3 e a r4 declararam os seus: o
      **INVENTÁRIO DE IRMÃOS** é **mantido** — o revisor verificou que ele funcionou e não virou
      cerimônia —, acrescido das **duas cláusulas** que fecham o seu ponto cego: **(a) direção
      reversa** (listar o que pode **neutralizar** o construto novo, e não só se os irmãos estão
      sãos) e **(b) retroatividade da justificativa** (todo comentário novo que **condena** uma forma
      vira grep obrigatório no mesmo arquivo por construções que ela condena). Registre que o ponto
      cego foi medido **três vezes** nesta rodada e que ele nasceu de o 04-28 ter classificado a
      dedup como sã com **evidência correta para a pergunta errada**;
    - a lista dos três planos, todos marcados como pendentes (`- [ ]`), com a descrição de uma linha:
      - `04-36-PLAN.md` — a chave `skipped` deixa de significar duas coisas, e o toast de erro em
        branco do disparo manual acaba (CR5-01, blocker)
      - `04-37-PLAN.md` — o alarme de supressão total deixa de ser desarmado por qualquer `continue`
        anterior, e o todo `cr4-01b` é reescrito pelo mecanismo (WR5-01)
      - `04-38-PLAN.md` — WR5-02..WR5-05 e IN5-01..IN5-04 como todos pendentes, e a gap closure r5 no
        ROADMAP

    Atualize a linha `**Plans**` da Phase 4 para incluir a rodada 5 no somatório, seguindo a forma já
    usada ("7 planos originais + 4 (r1) + 7 (r2) + 9 (r3) + 7 (r4) + …"). **Meça** o total no arquivo
    e registre o número medido: a linha atual soma **34** e existem **35** planos executados
    (04-01..04-35) — se a divergência se confirmar, corrija o somatório e **registre-a no SUMMARY com
    o número medido**, sem reescrever nada além dele.

    **NÃO** acrescente Success Criteria novo e **NÃO** reescreva os itens 1 a 8: os achados da r5 são
    defeitos de **instrumento** dos comportamentos que os itens 7 e 8 já garantem, e a lição da
    decisão C9 vale aqui — um critério que nomeia mecanismo faz o verificador procurar por um
    identificador que o refactor seguinte apaga. O comportamento garantido não mudou.

    **NÃO** toque na tabela de rastreabilidade de requisitos nem em `.planning/REQUIREMENTS.md`:
    nenhum requisito novo nasceu nesta rodada.

    Não instale nada; nenhum arquivo de `backend/` ou `frontend/` entra no diff.
  </action>

  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor && grep -q 'Gap closure r5' .planning/ROADMAP.md && test "$(git status --porcelain backend/ frontend/ .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" = "0" && echo ok</automated>
  </verify>

  <acceptance_criteria>
    | Critério | Comando | Esperado |
    |---|---|---|
    | O bloco novo existe | `grep -c 'Gap closure r5' .planning/ROADMAP.md` | **1** |
    | Os três planos estão listados como pendentes | `grep -cE '^- \[ \] 04-3[678]-PLAN.md' .planning/ROADMAP.md` | **3** |
    | E nenhum deles foi marcado como concluído | `grep -cE '^- \[x\] 04-3[678]-PLAN.md' .planning/ROADMAP.md` | **0** |
    | As duas cláusulas novas estão nomeadas | `grep -ciE 'direção reversa' .planning/ROADMAP.md` | **≥ 1** |
    | E a segunda também | `grep -ciE 'retroatividade' .planning/ROADMAP.md` | **≥ 1** |
    | O inventário de irmãos é declarado MANTIDO | `grep -ci 'inventário de irmãos' .planning/ROADMAP.md` | **≥ 2** (o da r4 + o da r5) |
    | A mudança de perfil está registrada | `grep -ci 'instrumento' .planning/ROADMAP.md` | **≥ 1** |
    | Os blocos das rodadas 1-4 ficaram byte a byte | `git diff -U0 .planning/ROADMAP.md \| grep -c '^-[^-]'` | **1** — apenas a linha `**Plans**`, reescrita com o somatório novo |
    | Nenhum Success Criteria novo | `git diff -U0 .planning/ROADMAP.md \| grep -c 'Success Criteria'` | **0** |
    | A tabela de rastreabilidade está intocada | `git diff -U0 .planning/ROADMAP.md \| grep -c '\| Phase'` | **0** |
    | REQUIREMENTS intocado | `git status --porcelain .planning/REQUIREMENTS.md` | **vazio** |
    | Diff de código ZERO | `git status --porcelain backend/ frontend/` | **vazio** |
  </acceptance_criteria>

  <done>
    O ROADMAP registra a gap closure r5 com os três planos, o escopo travado, a mudança de perfil e o
    mandato estrutural com as duas cláusulas novas; os blocos das rodadas anteriores estão byte a
    byte; e o commit `docs(04-38): gap closure r5 no ROADMAP` registra a mudança.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Achado de code review → artefato de planejamento | O texto do revisor é copiado para arquivos versionados; qualquer valor sensível citado ali se torna permanente no histórico do repositório |
| `.planning/todos/pending/` → planejamento futuro | Estes arquivos são a entrada de `/gsd:plan-phase` das fases seguintes: um achado sem arquivo desaparece, e um arquivo que descreve a causa errada faz alguém fechá-lo com o defeito de pé (é literalmente o que aconteceu com o `cr4-01b`) |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-04-38-01 | Information Disclosure | Os oito artefatos novos | **mitigate** | Nenhum valor de segredo entra: o `AGENDOR_TOKEN` permanece com rotação pendente (C8 / SEC-01, risco aceito) e o seu valor nunca é exibido. Gate de grep nas Tasks 1 e 2. Os arquivos citam identificadores e inteiros, nunca conteúdo de `.env` nem corpo de erro de borda |
| T-04-38-02 | Tampering | Todos existentes (33 arquivos) | **mitigate** | Editar um todo com prioridade decidida pelo usuário apagaria decisão registrada. Gate nas três tasks: `git status --porcelain .planning/todos/pending/ \| grep -c '^ M'` = **0** — as únicas mudanças são arquivos **não rastreados** |
| T-04-38-03 | Repudiation | Achado sem dono | **mitigate** | Oito achados fora do escopo de correção viram oito arquivos, cada um com a **medição** que o sustenta. Sem isso, a rodada 6 os redescobriria como novos — e a fase já tem cinco rodadas de precedente sobre o custo disso |
| T-04-38-04 | Tampering | `.planning/ROADMAP.md` | **mitigate** | O ROADMAP carrega critérios de sucesso aprovados pelo usuário (itens 4, 7 e 8, com redação decidida em C9). Este plano só **acrescenta** um bloco e reescreve a linha de somatório. Gate: exatamente **1** linha removida no diff, zero ocorrências de `Success Criteria` e da tabela de rastreabilidade |
| T-04-38-SC | Tampering | Instalação de pacotes (npm/pip/cargo) | **mitigate** | Este plano **não instala nada** e não toca em código. Gate: `git status --porcelain backend/ frontend/` vazio nas três tasks. Não havendo instalação, o portão humano de legitimidade de pacote não se aplica |
</threat_model>

<verification>
1. `ls .planning/todos/pending/ | wc -l` = **41** (era 33): os oito achados da r5 têm dono.
2. `grep -c 'priority: alta' .planning/todos/pending/wr5-05-*.md` = **1** — o único ALTA da leva, com
   o motivo escrito (duplicata é a outra metade do Core Value).
3. Convenção WR2-06 aplicada aos próprios artefatos: `grep -cE '\.js:[0-9]|linhas? [0-9]'` = **0** nos
   oito.
4. `git status --porcelain .planning/todos/pending/ | grep -c '^ M'` = **0** — nenhum todo existente
   editado.
5. `git status --porcelain backend/ frontend/` **vazio** nas três tasks — diff de código ZERO, que é
   a prova de que nenhuma mudança de comportamento se disfarçou de documentação (mesmo critério do
   04-27 e do 04-34).
6. O ROADMAP tem o bloco `Gap closure r5` com os três planos, o escopo travado, a mudança de perfil e
   o mandato estrutural com as duas cláusulas novas.
7. Como este plano não toca em código, a suíte permanece em **196/196** e o lint em exit 0 com 44
   warnings — executados ao final como **prova**, não como gate de mudança.
</verification>

<success_criteria>
- Os oito achados que o usuário excluiu do escopo de correção têm arquivo próprio, cada um com a
  medição que o sustenta — não com a intenção.
- WR5-05 está marcado como **alta**, com o motivo escrito: é a detecção de e-mail **duplicado** que a
  suíte perdeu, e duplicata é a metade do Core Value que a fase inteira negocia.
- Cada arquivo declara o seu par, para que nenhum dos dois seja fechado pela metade.
- Nenhum todo existente foi editado e nenhum achado foi registrado duas vezes.
- O ROADMAP registra a rodada 5, o seu escopo travado e o mandato estrutural com as duas cláusulas
  novas — e os blocos das rodadas anteriores estão byte a byte.
</success_criteria>

<output>
Crie `.planning/phases/04-confiabilidade-das-integra-es/04-38-SUMMARY.md` ao terminar.

Registre obrigatoriamente: a lista dos oito arquivos criados com o número de linhas e a prioridade de
cada um; a verificação de duplicidade (qual todo existente é vizinho de qual achado novo, e por que
nenhum foi duplicado); o número medido de pendentes antes e depois; a divergência do somatório da
linha `**Plans**` do ROADMAP, **com o número medido** (esta fase acumulou onze divergências de
contagem e nenhuma foi forçada); a prova de diff de código zero; e o estado final da fase 04 — o que
fica **aberto** depois da gap closure r5, nomeadamente `cr4-01b` (o caminho do negócio sem
organização), os oito achados desta leva e os demais 33 todos pendentes.
</output>
