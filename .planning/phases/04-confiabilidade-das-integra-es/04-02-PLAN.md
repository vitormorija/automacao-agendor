---
phase: 04-confiabilidade-das-integra-es
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/src/agendor.js
  - backend/src/routes/deals.js
  - backend/test/scheduler.failsafe.test.js
  - .planning/todos/pending/ui-01-toast-de-erro-no-check.md
autonomous: true
requirements: [REL-06]

must_haves:
  truths:
    - "Quando a consulta de tarefas futuras falha, a rodada termina com ZERO notificações enviadas"
    - "Quando a consulta de tarefas futuras falha, nenhuma linha nova entra no notification_log"
    - "A falha fica registrada em results.error"
    - "O lock isRunning é liberado após a falha"
    - "A rodada seguinte executa normalmente"
    - "Com a consulta bem-sucedida, o filtro por tarefa futura continua idêntico ao de hoje"
    - "GET /api/deals/stale responde 500 com shape { error } quando /tasks falha"
  artifacts:
    - path: "backend/src/agendor.js"
      provides: "getDealsWithFutureTasks com propagação da falha (rethrow) no lugar do catch→break"
      contains: "throw err"
    - path: "backend/test/scheduler.failsafe.test.js"
      provides: "Os 5 cenários literais da Decisão Q2 + consumidores B2 (runCheckOnly) e B3 (GET /stale)"
      min_lines: 150
    - path: "backend/src/routes/deals.js"
      provides: "Seam aditivo staleHandler para permitir o teste obrigatório do consumidor B3"
      contains: "module.exports.staleHandler"
    - path: ".planning/todos/pending/ui-01-toast-de-erro-no-check.md"
      provides: "Registro da consequência conhecida de UI (toast verde sob resposta 500), fora de escopo"
  key_links:
    - from: "backend/src/agendor.js getDealsWithFutureTasks"
      to: "backend/src/scheduler.js runCheck (Promise.all de :54-58)"
      via: "rejeição propagada até o catch de :171"
      pattern: "throw err"
    - from: "backend/test/scheduler.failsafe.test.js"
      to: "backend/src/routes/deals.js staleHandler"
      via: "invocação direta do handler com res falso mínimo"
      pattern: "staleHandler"
---

<objective>
Tornar `getDealsWithFutureTasks` **completo ou falha explícita** — nunca parcial silencioso (REL-06,
Decisão Q2). Uma falha na consulta de tarefas propaga até `runCheck`, que registra o erro e **encerra a
rodada sem disparar nenhuma notificação**; o `finally` existente libera o lock; a rodada seguinte executa.

Purpose: hoje o `catch { console.error; break }` de `agendor.js:224-227` devolve um `Set` **parcial**, e
`scheduler.js:61` usa esse `Set` como **decisão de segurança** (`staleDeals.filter(d => !futureTasks.has(d.id))`).
Proteção parcial = deals que têm tarefa futura agendada podem ser notificados indevidamente. Com o timeout
de 15s que o 04-03 introduz, esse caminho passa a ser **alcançável por lentidão** — por isso o fail-safe
precisa existir **antes** (dependência dura declarada no contrato §14).

Output: mudança de 3 linhas em `agendor.js`, seam aditivo em `routes/deals.js`, novo arquivo de teste com
7 cenários, e um todo registrando a consequência conhecida de UI.

## Inventário de consumidores do comportamento alterado (DoD do contrato §18)

`getDealsWithFutureTasks` tem **3 consumidores** (o contrato §7 listava 2; o terceiro é achado do 04-RESEARCH):

| # | Local | Uso do `Set` | Efeito do fail-safe |
|---|---|---|---|
| B1 | `scheduler.js:57` (`runCheck`) | **Decisão de segurança** — filtro de `:61` | `Promise.all` rejeita → catch de `:171` registra → `finally` libera o lock → rodada com 0 notificações. **É a razão de existir do Q2** |
| B2 | `scheduler.js:293` (`runCheckOnly`) | Mesmo filtro, **sem envio** | Passa a lançar; `POST /api/notifications/check` (`routes/notifications.js:35-42`) já tem try/catch → responde 500 `{ error }` |
| B3 | `routes/deals.js:17` (`GET /api/deals/stale`) | **Apenas decorativo** (`hasFutureTask`, `:23`) — não filtra nada | Passa a lançar; o catch de `:31-34` responde 500 `{ error }`. A aba "Negócios" deixa de carregar enquanto `/tasks` estiver falhando |

**Decisão humana registrada em 2026-08-04:** o 500 de B3 é **ACEITO**. Um `try/catch` local em `deals.js`
reintroduziria exatamente o parcial silencioso que este plano remove, e `DealsList.jsx:80` já exibe a
mensagem de erro (nada quebra em silêncio). Em contrapartida, **o teste do cenário B3 é obrigatório**.

## Consequência conhecida deixada fora de escopo (decisão humana 2026-08-04)

`Dashboard.jsx:63-82` (`checkOnly`) só entra no `catch` em erro de rede/JSON. Numa resposta **500 com corpo
JSON** — exatamente o que este plano passa a produzir — `r.json()` resolve, `result.total` fica `undefined`,
e o `toast.success` executa mesmo assim, exibindo em verde `"undefined negócio(s) parado(s) encontrado(s)"`.
É defeito **pré-existente** do frontend que o Q2 torna alcançável. UI está fora do escopo desta fase
(04-CONTEXT §Phase Boundary): **não consertar aqui** — registrar como todo (Task 2).

## Achado que dispensa trabalho previsto

O risco R-12 do contrato ("golden WR-02 pinava o catch→break") **não existe**: `agendor.futureTasks.test.js`
nunca exercita o catch (o `routeHandler` do fake nunca lança) e o relatório de cobertura confirma
`agendor.js:225-227` descoberto. **Nenhum teste existente precisa ser atualizado neste plano.** Se, contra o
esperado, algum teste existente ficar vermelho, isso é informação: parar e registrar, nunca editar o teste
para fazer passar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-CONTEXT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-RESEARCH.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-04. Não explorar o codebase. -->

`backend/src/agendor.js:201-232` — `getDealsWithFutureTasks() -> Promise<Set<number>>`. Laço `while (true)`
com `api.get('/tasks', { params: { dueDateGt: yesterday, per_page: 100, page } })`; o `catch (err)` de
`:224-227` faz `console.error('[Agendor] Erro ao buscar tarefas futuras:', err.message)` e `break`.
`agendor.js` **não** importa `./logger` hoje.

`backend/src/agendor.js:100-116` — `fetchDealsPage`: o padrão do repositório para "erro não-retentável
propaga" (`throw err` na linha 113). Só 429 retenta.

`backend/src/scheduler.js:54-58` — `const [staleDeals, users, futureTasks] = await Promise.all([...])`.
`:61` — `const dealsToNotify = staleDeals.filter((d) => !futureTasks.has(d.id));`
`:171-177` — `catch { results.error = err.message; logger.error('[Scheduler] Erro na verificação:', err); }
finally { isRunning = false; lastRunResult = results; }`

`backend/src/routes/deals.js:11-35` — `router.get('/stale', async (req, res) => { ... })`; catch em `:31-34`
faz `console.error(err)` e `res.status(500).json({ error: err.message })`.

`backend/src/routes/auth.js:359-369` — padrão do seam aditivo:
`module.exports = router;` seguido de comentário box-drawing e `module.exports.<nome> = <fn>;`.

`backend/src/db.js` — `getNotificationLogs({ limit, offset }) -> { logs, total }`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — seam em routes/deals.js e os 7 cenários do fail-safe (Decisão Q2 + B2 + B3)</name>
  <files>backend/src/routes/deals.js, backend/test/scheduler.failsafe.test.js</files>
  <behavior>
    - Cenário Q2-1: `/tasks` falha → `runCheck()` resolve, ZERO envios pelo stub de nodemailer e `getNotificationLogs({limit:100}).total === 0`
    - Cenário Q2-2: `/tasks` falha → `results.error` é string não vazia
    - Cenário Q2-3: `/tasks` falha → `getStatus().isRunning === false`
    - Cenário Q2-4: após a falha, com `/tasks` normal, a rodada seguinte roda (`skipped === undefined`, `error === undefined`)
    - Cenário Q2-5: caminho feliz idêntico — deal com tarefa futura segue filtrado, deal sem tarefa é notificado
    - Cenário B2: `/tasks` falha → `runCheckOnly()` **rejeita** (`assert.rejects`)
    - Cenário B3: `/tasks` falha → `staleHandler(req, res)` responde `500` com corpo `{ error: <string> }`
  </behavior>
  <read_first>
    - `backend/src/routes/deals.js` (integral — 37 linhas)
    - `backend/src/routes/auth.js:354-369` — padrão literal do seam aditivo
    - `backend/src/routes/config.js:95-101` — precedente de seam (`module.exports.ALLOWED_KEYS`) e `backend/test/config.route.smtpPass.test.js:13-52` — como um teste assere que o seam não quebra o contrato do router
    - `backend/test/scheduler.resilience.test.js` (criado no 04-01) — bootstrap, fake e stubs a REUSAR como molde
    - `backend/src/agendor.js:201-232` — o laço e o catch alvo
    - `backend/src/scheduler.js:26-29, 54-72, 109-164, 171-180, 288-305`
    - `.planning/phases/04-confiabilidade-das-integra-es/04-RESEARCH.md` §"Code Examples" (esqueleto dos 5 cenários de Q2 e o erro fiel `ECONNABORTED`)
    - `.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md` §"Sem Análogo Direto" (Opção A: seam aditivo + `res` falso mínimo)
  </read_first>
  <action>
    Parte A — seam aditivo em `backend/src/routes/deals.js` (**Opção A do 04-PATTERNS §Sem Análogo**, escolhida
    para toda a fase; `supertest`/`nock` são PROIBIDOS por PC-5 e pela pesquisa). Converter a arrow inline
    em uma função nomeada `async function staleHandler(req, res)` **sem alterar uma única linha do corpo**,
    registrar a rota com `router.get('/stale', staleHandler);`, e após `module.exports = router;` adicionar
    comentário de seção box-drawing em PT-BR no padrão de `auth.js:361-369` seguido de
    `module.exports.staleHandler = staleHandler;`. O shape de erro `{ error: err.message }` e o
    `console.error(err)` do catch **permanecem exatamente como estão** (PC-4 e a regra "manter a forma que
    a rota já usa").

    Parte B — criar `backend/test/scheduler.failsafe.test.js`. Cabeçalho em PT-BR declarando: teste do
    **novo fluxo** (não caracterização), REL-06 / Decisão Q2, e a lista dos 7 cenários. Reusar o bootstrap
    de `scheduler.resilience.test.js`: `makeTmpDbPath` → `process.env.DB_PATH` → `require('./setup')` →
    `installFakeAxios` → stub de `nodemailer.createTransport` contando envios → `require('../src/db')`,
    `require('../src/scheduler')`, `require('../src/routes/deals')`. Fixar `Date` com
    `mock.timers.enable({ apis: ['Date'], now: FIXED_NOW })`, `FIXED_NOW = new Date('2026-06-01T00:00:00.000Z').getTime()`.

    Controlar a falha por flag mutável do arquivo (`let tarefasDevemFalhar = false`) **dentro** do
    `routeHandler` — nunca reinstalar o stub (Pitfall 4). O erro injetado em `/tasks` deve ser fiel ao que o
    axios produz num timeout: `Object.assign(new Error('timeout of 15000ms exceeded'), { code: 'ECONNABORTED' })`
    — **não** `ETIMEDOUT` (o adaptador HTTP do axios usa `ECONNABORTED` porque
    `transitional.clarifyTimeoutError` é `false` por padrão).

    Rotas do fake: `/deals` a partir de `./fixtures/synthetic/deals-page.json` com
    `meta.totalCount = dealsPage.length`; `/organizations/:id` com default `'Lead'` e `205: 'Parceiro'`;
    `/users` devolvendo os ids `11` e `13` com `contact.email` preenchido e sem `links.next`;
    `/tasks` devolvendo `{ data: { data: TAREFAS } }` onde `TAREFAS` é uma lista mutável do arquivo (para o
    cenário Q2-5, conter uma tarefa não finalizada com `dueDate` futuro apontando para o deal `101`, de modo
    que só o deal `103` seja notificado).

    Para o cenário B3, invocar `staleHandler` diretamente com um `req` mínimo (`{ query: {} }`) e um `res`
    falso mínimo que acumule estado: um objeto com `statusCode`, `body`, um método `status(codigo)` que grava
    e devolve `this`, e um `json(corpo)` que grava e devolve `this`. Asserir `res.statusCode === 400 + 100`
    não — asserir literalmente `res.statusCode === 500` e `typeof res.body.error === 'string'`, **e** que
    `res.body` não possui a chave `ok` (o `/stale` usa a forma `{ error }`, não `{ ok:false, error }`).
    Asserir também `typeof require('../src/routes/deals') === 'function'` para provar que o seam não quebrou
    o contrato do router (padrão de `config.route.smtpPass.test.js:49-52`).

    Esta task é o passo **RED**: com o `catch→break` ainda em `agendor.js`, os cenários Q2-1, Q2-2, Q2-3, B2
    e B3 devem **falhar**. Registrar no output da execução quais falharam — é a prova de que o teste mede o
    defeito, e não algo já verdadeiro.
  </action>
  <verify>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && node --test test/scheduler.failsafe.test.js; test $? -ne 0 && echo "RED confirmado (falhas esperadas antes da correção)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/scheduler.failsafe.test.js` sai com código **diferente de 0** e as falhas incluem os cenários Q2-1, Q2-2, Q2-3, B2 e B3
    - Os cenários Q2-4 e Q2-5 (caminho feliz) já passam nesta etapa
    - `git diff backend/src/routes/deals.js` não altera nenhuma linha do corpo do handler: apenas a assinatura vira `async function staleHandler(req, res) {`, a rota vira `router.get('/stale', staleHandler);` e o bloco de seam é adicionado ao fim
    - `grep -c "module.exports.staleHandler" backend/src/routes/deals.js` retorna 1
    - `grep -c "res.status(500).json({ error: err.message })" backend/src/routes/deals.js` retorna 1 (shape de erro preservado)
    - O arquivo de teste **não** importa `supertest` nem `nock`: `grep -E "supertest|nock" backend/test/scheduler.failsafe.test.js` não retorna nada
    - O erro injetado em `/tasks` usa `code: 'ECONNABORTED'` e a mensagem `timeout of 15000ms exceeded`
    - `npm test` continua verde para todos os demais arquivos (apenas `scheduler.failsafe.test.js` vermelho)
  </acceptance_criteria>
  <done>O teste do novo fluxo existe, é executável e mede o defeito: falha antes da correção pelos motivos certos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — propagar a falha em getDealsWithFutureTasks + registrar o todo de UI</name>
  <files>backend/src/agendor.js, .planning/todos/pending/ui-01-toast-de-erro-no-check.md</files>
  <behavior>
    - Os 7 cenários da Task 1 passam a ficar verdes
    - Nenhum teste pré-existente do repositório muda de resultado
  </behavior>
  <read_first>
    - `backend/src/agendor.js` (integral — atenção a `:1-9`, `:100-116` como padrão de propagação, `:201-232` alvo)
    - `backend/src/logger.js` (integral — assinatura de `logger.error` e formatação por nível)
    - `backend/src/scheduler.js:171-177` — o formato `logger.error('[Scheduler] Erro no resumo semanal:', err.message)` de `:243` é o estilo a espelhar
    - `backend/test/scheduler.failsafe.test.js` (criado na Task 1)
    - `.planning/todos/pending/sec-02-dependency-vulnerabilities.md` — formato de arquivo de todo do projeto
  </read_first>
  <action>
    Parte A — em `backend/src/agendor.js`, substituir o corpo do `catch (err)` de `:224-227`. Trocar
    `console.error('[Agendor] Erro ao buscar tarefas futuras:', err.message)` + `break` por uma chamada
    `logger.error(...)` com a tag `[Agendor]` mantendo a mesma frase em PT-BR e passando **`err.message`**
    (nunca o objeto `err` inteiro nem `err.config`: o `config` do axios carrega o header
    `Authorization: Token <AGENDOR_TOKEN>`), imediatamente seguida de `throw err;`. Acrescentar
    `const logger = require('./logger');` no topo do arquivo, junto do `require('axios')` — é código **novo**,
    logo usa `logger` e não `console.*` (PC-11). O `console.log` de `:230` (contagem de deals protegidos)
    **permanece intocado** — é legado que a Fase 5 migra.

    Acrescentar acima do `catch` um comentário em PT-BR explicando a DECISÃO (padrão do arquivo, ver
    `:57-61`): o resultado desta função é usado por `scheduler.js:61` como decisão de quem NÃO é notificado;
    um `Set` parcial notifica indevidamente; portanto o contrato passa a ser "completo ou exceção" (REL-06 /
    Decisão Q2), e a rodada inteira é abortada pelo `catch`/`finally` já existentes de `runCheck`.

    **Nada mais em `agendor.js` pode mudar** — sem tocar `fetchDealsPage`, `getStaleDeals`, o cache, ou o
    `module.exports`.

    Parte B — criar `.planning/todos/pending/ui-01-toast-de-erro-no-check.md` com prioridade **média** e
    destino sugerido "fase de UI / pós-Fase 5", descrevendo: `Dashboard.jsx:63-82` (`checkOnly`) exibe
    `toast.success` mesmo quando `/api/notifications/check` responde 500 com corpo JSON, porque `r.json()`
    resolve e `result.total` fica `undefined` — resultando em `"undefined negócio(s) parado(s) encontrado(s)"`
    em verde; e `Dashboard.jsx:84-106` (`sendNow`) tem o análogo mais brando (mostra "0 notificação(ões)
    enviada(s) de 0 negócio(s)" numa rodada abortada, porque `runCheck` nunca lança). Registrar que é defeito
    **pré-existente** tornado alcançável pelo REL-06, que a UI está fora do escopo da Fase 4 por decisão do
    04-CONTEXT, e que a correção é verificar `r.ok` antes de tratar o corpo como sucesso.

    Ao final, rodar a suíte completa com cobertura e o lint.
  </action>
  <verify>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && node --test test/scheduler.failsafe.test.js && npm run test:coverage && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/scheduler.failsafe.test.js` sai 0 com os **7** cenários verdes
    - `npm run test:coverage` sai 0 e nenhum piso é violado (lines/statements/functions ≥ 20, branches ≥ 60)
    - `npm run lint` sai 0
    - `grep -n "throw err" backend/src/agendor.js` retorna **duas** ocorrências (a de `fetchDealsPage:113` e a nova de `getDealsWithFutureTasks`)
    - `grep -c "console.error('\[Agendor\] Erro ao buscar tarefas futuras" backend/src/agendor.js` retorna 0
    - `grep -c "require('./logger')" backend/src/agendor.js` retorna 1
    - `grep -v '^\s*//' backend/src/agendor.js | grep -c "console.log" ` retorna 1 (só o legado de `:230`, preservado)
    - A chamada de log passa `err.message` — `grep -n "logger.error" backend/src/agendor.js` não mostra o objeto `err` cru nem `err.config`
    - Nenhum teste pré-existente foi editado: `git diff --name-only backend/test/` lista somente `backend/test/scheduler.failsafe.test.js`
    - `.planning/todos/pending/ui-01-toast-de-erro-no-check.md` existe e cita `Dashboard.jsx:63-82`
  </acceptance_criteria>
  <done>REL-06 entregue: consulta de tarefas futuras é completa ou falha explícita, com os 7 cenários verdes e a consequência de UI registrada como todo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → API Agendor `/tasks` | Fonte externa cuja falha hoje produz um resultado parcial usado como decisão de segurança |
| cliente → `GET /api/deals/stale` e `POST /api/notifications/check` | Superfície HTTP que passa a poder responder 500 em vez de dado incompleto |
| backend → SQLite `notification_log` | Escrita que deixa de acontecer quando a rodada é abortada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering (integridade da decisão de negócio) | `agendor.js` `getDealsWithFutureTasks` → `scheduler.js:61` | mitigate | Substituir `catch { console.error; break }` por `logger.error(err.message)` + `throw err`: o contrato vira "Set completo ou exceção"; cenários Q2-1 e Q2-5 provam 0 notificações na falha e filtro idêntico no sucesso |
| T-04-02-02 | Repudiation | Registro da rodada abortada | mitigate | `results.error` preenchido pelo catch de `scheduler.js:171` e `lastRunResult` atualizado pelo `finally`; cenário Q2-2 assere |
| T-04-02-03 | Denial of Service (disponibilidade da aba "Negócios") | `routes/deals.js` `GET /stale` (consumidor B3) | accept | Decisão humana 2026-08-04: aceitar o 500. Um try/catch local reintroduziria o parcial silencioso; `DealsList.jsx:80` já exibe erro. Cenário B3 pina o shape `{ error }` |
| T-04-02-04 | Information Disclosure | Novo `logger.error` em `agendor.js` | mitigate | Logar apenas `err.message`; nunca `err` cru nem `err.config` (que carrega o header `Authorization: Token <AGENDOR_TOKEN>`) |
| T-04-02-05 | Denial of Service (rodada perdida por erro transitório) | `runCheck` | accept | Risco R-10 do contrato: a cadência diária recupera e o 04-06 torna a retentativa real; sinal de alerta é `results.error` recorrente em rodadas seguidas; rollback = revert deste commit |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote neste plano; `package.json` e `package-lock.json` fora de `files_modified` |
</threat_model>

<verification>
- 7 cenários verdes em `backend/test/scheduler.failsafe.test.js`
- `npm run test:coverage` e `npm run lint` saem 0
- Diff de produção limitado a: 3 linhas em `agendor.js` (require do logger, log, throw) + comentário de decisão, e o seam aditivo de `routes/deals.js`
- Nenhum teste pré-existente editado (confirma o Achado 1 da pesquisa: WR-02 não pinava o catch)
</verification>

<success_criteria>
- REL-06 comprovado pelos 5 cenários literais da Decisão Q2 + B2 + B3
- Inventário de consumidores (B1/B2/B3) registrado neste PLAN e repetido no SUMMARY
- Todo `ui-01-toast-de-erro-no-check` criado em `.planning/todos/pending/`
- Suíte, cobertura e lint verdes
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-02-SUMMARY.md` when done.
Commit: `fix(04-02)!: falha na consulta de tarefas futuras aborta a rodada sem notificar (fail-safe, REL-06)`
Rollback: revert do commit (volta ao parcial silencioso atual — estado conhecido).
</output>
