---
phase: 04-confiabilidade-das-integra-es
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - backend/src/emailer.js
  - backend/test/emailer.timeout.test.js
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "Todo transporte SMTP criado pelo projeto nasce com connectionTimeout, greetingTimeout e socketTimeout explícitos"
    - "Esgotar as 3 tentativas devolve { success: false } sem lançar exceção (D-03: registrar e seguir para o próximo destinatário, comportamento preservado)"
    - "Uma falha de rede seguida de sucesso ainda envia, recriando o transporter no retry"
    - "O retorno por destinatário continua sendo { to, success, error? }"
  artifacts:
    - path: "backend/src/emailer.js"
      provides: "Os três timeouts de D-02 na função fábrica createTransporter"
      contains: "socketTimeout: 30000"
    - path: "backend/test/emailer.timeout.test.js"
      provides: "Prova dos 3 timeouts + caracterização da exaustão e da recriação do transporter"
      min_lines: 120
  key_links:
    - from: "backend/src/emailer.js createTransporter"
      to: "nodemailer.createTransport"
      via: "objeto de opções único usado pelos 6 call-sites"
      pattern: "connectionTimeout"
    - from: "backend/test/emailer.timeout.test.js"
      to: "backend/src/emailer.js verifySmtp e sendStaleNotification"
      via: "caminhos públicos que atravessam a fábrica privada"
      pattern: "verifySmtp|sendStaleNotification"
---

<objective>
Fechar REL-02: os três timeouts explícitos de D-02 — `connectionTimeout: 10000`, `greetingTimeout: 10000`,
`socketTimeout: 30000` — na **função fábrica** `createTransporter()`, e caracterizar o comportamento de
exaustão que D-03 manda preservar.

Purpose: hoje valem os defaults do nodemailer (2min / 30s / **10min**). Com `sendMailWithRetry` fazendo 3
tentativas, um destinatário problemático pode segurar a rodada por ~30 minutos. Com `socketTimeout` de 30s,
o pior caso por e-mail cai para ~1min40s. A configuração vive na fábrica porque ela é o **único** ponto por
onde passam os 6 call-sites — repetir em cada chamada seriam 6 lugares para divergir.

Output: uma mudança de 3 linhas em `emailer.js` cobrindo 6 call-sites, e um arquivo de teste que serve de
**oráculo do caminho de envio** para o major do nodemailer no 04-05.

## Nota de sequência (dependência dura)

O 04-05 (nodemailer 6→9) **depende duramente** deste plano: `emailer.js` tem 7,16% de cobertura hoje, então
"78 testes verdes sob v9" é evidência **fraca** para o caminho de envio. Estes testes são o oráculo real do
major. Nodemailer aqui ainda é **6.x** — não antecipar o bump.

## Os 6 call-sites cobertos por esta única mudança (grep confirmado em 2026-08-04)

| Linha | Função | Chega ao usuário? |
|---|---|---|
| `emailer.js:197` | `sendMailWithRetry` — recriação do transporter no retry | sim (caminho de retentativa) |
| `emailer.js:206` | `sendStaleNotification` | sim — o alerta diário |
| `emailer.js:383` | `sendWeeklySummary` (admins) | sim |
| `emailer.js:404` | `verifySmtp` → `POST /api/config/test-smtp` | sim (UI de config) |
| `emailer.js:409` | `sendResetPasswordEmail` | sim (reset de senha) |
| `emailer.js:689` | `sendOwnerWeeklySummary` | sim |
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-CONTEXT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-RESEARCH.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-04. Não explorar o codebase. -->

`backend/src/emailer.js:12-22` — `createTransporter()` devolve `nodemailer.createTransport({ host, port,
secure, auth: { user, pass } })`, com `host/port/user` vindos de `getConfig(...)` e
`pass: (process.env.SMTP_PASS || '').trim()`. **Não é exportada.**

`backend/src/emailer.js:178-203` — `sendMailWithRetry(transporter, mailOptions, retries = 3)`:
classifica erro de rede por `err.code === 'ECONNRESET' || err.code === 'ETIMEDOUT' ||
err.message?.toLowerCase().includes('timeout') || err.message?.toLowerCase().includes('econnreset')`;
espera `attempt * 3000` (3s, 6s); emite `console.warn('[Emailer] Tentativa ...')`; recria o transporter com
`transporter = createTransporter()`; e na exaustão **retorna** `{ success: false, error: err.message }`
(não lança). **Comportamento pinado — não alterar nada aqui.**

`sendStaleNotification({ deal, ownerEmail, authorEmail, logId })` → `Promise<Array<{ to, success, error? }>>`
— um item por destinatário; o e-mail do autor só é enviado se `authorEmail && authorEmail !== ownerEmail`.

`verifySmtp()` → `createTransporter().verify()` — caminho público mais barato até a fábrica privada.

Valores medidos dos defaults do nodemailer, **idênticos** em 6.10.1 (instalado) e 9.0.4 (alvo do 04-05):
`CONNECTION_TIMEOUT = 2*60*1000`, `GREETING_TIMEOUT = 30*1000`, `SOCKET_TIMEOUT = 10*60*1000`
(`lib/smtp-connection/index.js:14-16`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Três timeouts na fábrica + prova de que o transporte os recebe</name>
  <files>backend/src/emailer.js, backend/test/emailer.timeout.test.js</files>
  <behavior>
    - `nodemailer.createTransport` recebe `connectionTimeout: 10000`, `greetingTimeout: 10000` e `socketTimeout: 30000`
    - `host`, `port`, `secure` e `auth.user` continuam vindo de `getConfig(...)`, e `auth.pass` de `process.env.SMTP_PASS`
  </behavior>
  <read_first>
    - `backend/src/emailer.js:1-30` — o comentário-bloco acima da fábrica (padrão do arquivo: explica a DECISÃO, não a mecânica) e a fábrica atual
    - `backend/src/emailer.js:175-215` — `sendMailWithRetry` e o início de `sendStaleNotification`
    - `backend/test/emailer.smtpPass.test.js` (arquivo inteiro — **é o molde exato** deste teste: mesma borda, mesmo mock, mesmo caminho público)
    - `backend/test/setup.js:26-40` — `SMTP_PASS` e `ADMIN_EMAIL` são sobrescritos **sem guarda**: qualquer atribuição do teste tem de vir DEPOIS do `require('./setup')`
    - `.planning/phases/04-confiabilidade-das-integra-es/04-RESEARCH.md` §"Padrão 4" e §"Code Examples" (inspecionar as opções entregues ao transporte)
  </read_first>
  <action>
    Parte A — em `backend/src/emailer.js:12-22`, acrescentar ao objeto passado a `nodemailer.createTransport`
    exatamente três chaves: `connectionTimeout: 10000`, `greetingTimeout: 10000` e `socketTimeout: 30000`.
    Manter `host`, `port`, `secure` e `auth` intactos. Acrescentar ao comentário-bloco já existente acima da
    fábrica (ou logo abaixo dele, no mesmo estilo) um parágrafo em PT-BR explicando a DECISÃO D-02: os
    defaults do nodemailer são 2min/30s/**10min**, e o socket de 10 minutos é o que permite uma única
    tentativa segurar a rodada inteira; com 30s e as 3 tentativas já existentes de `sendMailWithRetry`, o
    pior caso por e-mail cai de ~30min para ~1min40s; a configuração fica na **fábrica** porque é o único
    ponto por onde passam os 6 call-sites. **Nenhuma outra linha de `emailer.js` pode mudar** — em especial
    `sendMailWithRetry` (`:178-203`), que é o oráculo dos testes da Task 2, e o `console.warn` de `:192`,
    que o contrato manda **preservar** (a Fase 5 migra).

    Parte B — criar `backend/test/emailer.timeout.test.js` com cabeçalho em PT-BR nomeando REL-02/D-02 e
    listando os invariantes. Bootstrap no padrão de `emailer.smtpPass.test.js`: `require('./setup')` →
    (se necessário) `process.env.SMTP_PASS = ...` **depois** do setup → `setConfig` de `smtp_host`,
    `smtp_port`, `smtp_user`, `smtp_from` com valores sintéticos `*.invalid` →
    `mock.method(nodemailer, 'createTransport', (opts) => { capturado = opts; return { verify: async () => true,
    sendMail: async () => ({}) }; })` → `require('../src/emailer')`.

    Cenário desta task: chamar `await verifySmtp()` (caminho público mais barato até a fábrica privada, sem
    abrir seam em `emailer.js`) e asserir **campo a campo**: `capturado.connectionTimeout === 10000`,
    `capturado.greetingTimeout === 10000`, `capturado.socketTimeout === 30000`, e que
    `capturado.host`/`capturado.port` refletem o que foi semeado via `setConfig`.

    **PC-13 — regra de segurança do arquivo:** nunca imprimir `capturado` inteiro em mensagem de asserção,
    nem incluí-lo em `assert.deepStrictEqual` — o objeto contém `auth.pass`. Asserir apenas campos
    individuais, como faz `emailer.smtpPass.test.js`.
  </action>
  <verify>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && node --test test/emailer.timeout.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/emailer.timeout.test.js` sai 0
    - `grep -c "connectionTimeout: 10000" backend/src/emailer.js` retorna 1
    - `grep -c "greetingTimeout: 10000" backend/src/emailer.js` retorna 1
    - `grep -c "socketTimeout: 30000" backend/src/emailer.js` retorna 1
    - As três chaves estão **dentro de `createTransporter`** e não repetidas em nenhum call-site: `grep -c "connectionTimeout" backend/src/emailer.js` retorna 1
    - `git diff backend/src/emailer.js` não altera nenhuma linha entre `:178` e `:203` (`sendMailWithRetry`)
    - `grep -c "console.warn" backend/src/emailer.js` retorna 1 (o legado de `:192`, preservado)
    - Nenhuma asserção do arquivo de teste imprime o objeto de opções inteiro nem `auth.pass`
    - `npm test` sai 0 sem nenhum teste pré-existente editado
  </acceptance_criteria>
  <done>Os 6 call-sites SMTP passam a nascer com teto de tempo, provado por inspeção das opções entregues ao transporte.</done>
</task>

<task type="auto">
  <name>Task 2: Caracterizar exaustão sem throw, recriação do transporter e retorno por destinatário</name>
  <files>backend/test/emailer.timeout.test.js</files>
  <read_first>
    - `backend/test/emailer.timeout.test.js` (estado após a Task 1)
    - `backend/src/emailer.js:178-245` — `sendMailWithRetry` completo e `sendStaleNotification` (montagem do array `results`)
    - `backend/test/auth.test.js:51-63` — padrão `mock.timers` com `try/finally` e `mock.timers.reset()` garantido
    - `.planning/phases/04-confiabilidade-das-integra-es/04-RESEARCH.md` §Pitfall 2 (erros **fiéis** ao que o nodemailer produz) e §Pitfall 3 (`tickAsync`, não `tick`)
  </read_first>
  <action>
    Acrescentar ao mesmo arquivo os cenários de caracterização, alcançando `sendMailWithRetry` pelo caminho
    público `sendStaleNotification({ deal, ownerEmail, authorEmail, logId })` (a função não é exportada; não
    abrir seam em `emailer.js`). Usar um objeto `deal` sintético mínimo com os campos que o template lê
    (`id`, `title`, `ownerName`, `authorName`, `organization`, `funnel`, `dealType`, `daysSinceUpdate`,
    `updatedAt`, `webUrl`).

    **Erros injetados devem ser fiéis ao que o nodemailer realmente produz** (o `_formatError` do nodemailer
    **sobrescreve** `err.code` — um `ECONNRESET` nativo chega ao retry como `code: 'ESOCKET'`, e o único
    ramo que de fato o captura é o da **mensagem**):
    - timeout de conexão/greeting/socket → `Object.assign(new Error('Connection timeout'), { code: 'ETIMEDOUT' })`
    - reset de conexão → `Object.assign(new Error('read ECONNRESET'), { code: 'ESOCKET' })`
    **Não** usar `{ code: 'ECONNRESET' }` — é um caminho que o nodemailer real não produz e daria um verde falso.

    Cenários:
    (2) **Exaustão sem throw:** `sendMail` rejeita nas 3 tentativas. Habilitar
        `mock.timers.enable({ apis: ['setTimeout'] })` dentro de `try/finally` com `mock.timers.reset()`;
        **iniciar** a promise sem `await`, então `await mock.timers.tickAsync(3000)` e
        `await mock.timers.tickAsync(6000)` (o `tick` síncrono não deixa as microtasks drenarem entre as
        tentativas), e só depois `await` no resultado. Asserir que a promise **resolve** (não rejeita), que
        o item do destinatário tem `success === false` e `typeof error === 'string'`, e que `sendMail` foi
        chamado exatamente **3** vezes.
    (3) **Sucesso após 1 falha:** primeira chamada rejeita com o erro fiel, segunda resolve. Asserir
        `success === true` e que `nodemailer.createTransport` foi chamado **mais de uma vez** — é o que pina
        a recriação do transporter de `emailer.js:197`. Mesma disciplina de `mock.timers` (um único
        `tickAsync(3000)`).
    (4) **Retorno por destinatário:** com `ownerEmail` e `authorEmail` distintos e ambos com sucesso, o
        array devolvido tem 2 itens, cada um com as chaves `to` e `success` (e `error` apenas quando
        `success === false`), e o campo `to` de cada item bate com o e-mail correspondente. Acrescentar o
        caso `authorEmail === ownerEmail` → **1** item apenas.

    Se algum cenário exigir um stub conflitante que não caiba na mesma configuração de mock do arquivo,
    **criar um segundo arquivo de teste** em vez de reinstalar stubs no meio (`node --test` isola por
    arquivo, não por `test()` — convenção estabelecida na Fase 3).

    Ao final, rodar suíte completa com cobertura e lint. Atenção ao Pitfall 7: `emailer.js` tem 7,16% de
    cobertura, então o efeito líquido esperado é de **alta**; ainda assim, `npm run test:coverage` (e não só
    `npm test`) é o que detecta a exceção.
  </action>
  <verify>
    <automated>cd backend && export PATH="$HOME/bin:$PATH" && node --test test/emailer.timeout.test.js && npm run test:coverage && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/emailer.timeout.test.js` sai 0 com ≥ 5 testes passando
    - A suíte inteira roda em menos de 10 segundos (prova de que `mock.timers` está sendo usado — sem ele a exaustão custaria ~9s por cenário)
    - `npm run test:coverage` sai 0; nenhum piso violado (lines/statements/functions ≥ 20, branches ≥ 60); a cobertura de `emailer.js` é **maior** que o baseline de 7,16%
    - `npm run lint` sai 0
    - `grep -c "ECONNRESET'" backend/test/emailer.timeout.test.js` — se houver ocorrência, ela está associada a `code: 'ESOCKET'` na mensagem, nunca a `code: 'ECONNRESET'`
    - O cenário de exaustão assere `sendMail` chamado exatamente 3 vezes e resultado `{ success: false }` **sem** `assert.rejects`
    - O cenário de sucesso-após-falha assere `createTransport` chamado mais de uma vez
    - `git diff --name-only backend/src/` está vazio nesta task (nenhuma mudança de produção)
  </acceptance_criteria>
  <done>O caminho de envio SMTP tem oráculo real: exaustão, retentativa e retorno por destinatário estão pinados antes do major do nodemailer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → servidor SMTP | Borda de saída sem teto de tempo; substituída nos testes por `mock.method(nodemailer, 'createTransport')` |
| `process.env.SMTP_PASS` → objeto de opções do transporte | Credencial que trafega no objeto inspecionado pelos testes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service | `emailer.js` `createTransporter` (6 call-sites) | mitigate | `connectionTimeout: 10000`, `greetingTimeout: 10000`, `socketTimeout: 30000` na fábrica; pior caso por e-mail cai de ~30min para ~1min40s |
| T-04-04-02 | Information Disclosure | Asserções sobre o objeto de opções do transporte | mitigate | PC-13: asserir campos individuais; nunca imprimir o objeto (contém `auth.pass`) nem usar `deepStrictEqual` sobre ele; `setup.js` força `SMTP_PASS=''` |
| T-04-04-03 | Denial of Service (timeout curto demais) | Valor de `socketTimeout` | accept | Risco R-4: 30s é generoso para SMTP; sinal de alerta é `ETIMEDOUT` frequente em produção; rollback é 1 linha |
| T-04-04-04 | Tampering | Semântica de retry de `sendMailWithRetry` | mitigate | D-03 proíbe alterar; testes pinam contagem (3), esperas (3s/6s), recriação do transporter e retorno `{ success:false }` sem throw |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação neste plano — `nodemailer` continua em 6.x aqui; o bump é escopo exclusivo do 04-05 |
</threat_model>

<verification>
- `node --test test/emailer.timeout.test.js` sai 0
- `npm run test:coverage` e `npm run lint` saem 0; cobertura de `emailer.js` acima do baseline de 7,16%
- Diff de produção limitado a 3 chaves + comentário dentro de `createTransporter`
- `nodemailer` ainda em 6.x ao fim deste plano (`npm ls nodemailer` mostra 6.10.x)
</verification>

<success_criteria>
- REL-02 comprovado: as 3 opções de D-02 presentes na fábrica e verificadas por teste
- Exaustão sem throw, recriação do transporter e retorno por destinatário caracterizados
- Retry intocado (contagem, esperas e `console.warn` preservados)
- Suíte, cobertura e lint verdes
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-04-SUMMARY.md` when done.
Commit: `feat(04-04): timeouts explícitos no transporte SMTP (REL-02)`
Rollback: revert do commit.
</output>
