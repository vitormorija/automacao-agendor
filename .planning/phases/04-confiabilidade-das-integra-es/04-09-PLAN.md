---
phase: 04-confiabilidade-das-integra-es
plan: 09
type: execute
wave: 2
depends_on: ["04-08"]
files_modified:
  - backend/src/routes/deals.js
  - backend/src/agendor.js
  - backend/src/routes/notifications.js
  - backend/test/deals.errorLog.test.js
  - backend/test/dealId.validation.test.js
  - .planning/todos/pending/in-01-status-pending-na-ui.md
  - .planning/todos/pending/in-02-seams-fora-do-module-exports.md
  - .planning/todos/pending/in-03-comentario-emailer-timeout.md
  - .planning/todos/pending/in-04-escape-html-no-test-card.md
  - .planning/todos/pending/cr-02b-console-error-objeto-completo-em-index.md
autonomous: false
gap_closure: true
requirements: [REL-01, REL-06]

must_haves:
  truths:
    - "Uma falha da borda Agendor em GET /api/deals/stale não escreve o AGENDOR_TOKEN em nenhum stream de log"
    - "GET /api/deals/stale continua respondendo 500 com o shape { error } quando a consulta de tarefas futuras falha"
    - "getDealById recusa um id que não seja inteiro positivo ANTES de emitir qualquer requisição HTTP"
    - "POST /api/notifications/test-card grava deal_id numérico no notification_log, mesmo com corpo de requisição hostil"
    - "Os itens do 04-REVIEW deixados fora desta rodada estão registrados como todo rastreável"
  artifacts:
    - path: "backend/src/routes/deals.js"
      provides: "Log de erro por logger.error com apenas err.message"
      contains: "logger.error"
    - path: "backend/src/agendor.js"
      provides: "Validação de id inteiro positivo em getDealById"
      contains: "Number.isInteger"
    - path: "backend/test/deals.errorLog.test.js"
      provides: "Prova de que o objeto de erro do axios (com config.headers) não chega ao log"
      min_lines: 60
    - path: "backend/test/dealId.validation.test.js"
      provides: "Prova de que id não numérico é recusado sem chamada HTTP e que o escritor grava número"
      min_lines: 70
  key_links:
    - from: "backend/src/routes/deals.js catch"
      to: "backend/src/logger.js"
      via: "logger.error com err.message (nunca o objeto de erro)"
      pattern: "logger\\.error\\('\\[Deals\\]"
    - from: "backend/src/routes/notifications.js POST /test-card"
      to: "notification_log.deal_id"
      via: "Number.parseInt do corpo da requisição"
      pattern: "Number\\.parseInt\\(req\\.body\\.dealId"
---

<objective>
Fechar **CR-02** (crítico: `AGENDOR_TOKEN` escrito em disco no log de erro do PM2) e **WR-03** (id de
negócio interpolado no path sem validação, com um escritor controlado pelo usuário), e registrar a triagem
dos achados do `04-REVIEW.md` que ficam fora desta rodada.

Purpose:

**CR-02 — verificado empiricamente pelo orquestrador.** `backend/src/routes/deals.js:32` faz
`console.error(err)` com o `AxiosError` inteiro. `util.inspect` de um AxiosError imprime as propriedades
próprias enumeráveis, entre elas `config`, e `config.headers.Authorization` carrega
`Token <AGENDOR_TOKEN>`. Em produção esse stream vai para `/opt/agendor/logs/pm2-error.log`
(`ecosystem.config.js:20`), **persistido em disco**. O caminho até esse `console.error` foi aberto por esta
mesma fase: antes do 04-02 a falha de `/tasks` era engolida em `agendor.js`; hoje ela propaga e o
`staleHandler` responde 500 (pinado por `scheduler.failsafe.test.js`, caso B3), o que torna o caminho
rotineiro sempre que a API Agendor estiver lenta — o cenário que o timeout de 15s do REL-01 tornou
alcançável. O agravante: **o próprio 04-02 reconheceu a ameaça** e a corrigiu do outro lado, em
`agendor.js:286-287` ("Só a mensagem é logada: o objeto de erro do axios carrega `config.headers`"), no
mesmo plano que editou `routes/deals.js`. É inconsistência interna da fase, não defeito herdado intocado.

**WR-03 — função criada nesta fase (04-03).** `getDealById` faz `api.get` com o `id` interpolado no path,
confiando que ele é inteiro. O comentário de `routes/notifications.js:220-221` afirma que "o `id` vem só de
`getNotifiedDeals()` (do banco)" — verdade, mas o banco não garante o tipo: `notification_log.deal_id` tem
afinidade `INTEGER` **sem `STRICT`**, e `POST /api/notifications/test-card` grava
`deal_id: req.body.dealId || 0`, valor vindo direto do corpo da requisição. Verificado:
`deal_id: '../users'` sobrevive como TEXT e a url final vira `https://api.agendor.com.br/users` — ou seja,
um usuário autenticado faz o backend consultar **outro endpoint da Agendor com o token de serviço**, na
próxima `GET /api/notifications/resolved`. Impacto limitado (exige auth, é GET, e a resposta só alimenta
`updatedAt`/`dealStatus`), mas a validação custa uma linha e a função nasceu agora.

Efeito colateral que a validação também conserta: hoje uma linha com `deal_id` textual faz a consulta
retornar dado de outro recurso; depois da correção ela rejeita, o `catch` por item absorve, e a linha
aparece como não-resolvida — sem consulta indevida.

Output: duas correções de uma linha cada em produção (mais um seam de teste), dois arquivos de teste novos e
cinco todos de triagem.

## Fronteira explícita — o que este plano NÃO faz

- **Não** escapa HTML no template de e-mail (`dealEmailHtml`). `title`/`organization`/`ownerName`/`webUrl`
  do `test-card` chegam sem escape ao template (IN-04). São linhas **não tocadas por esta fase** e mexer nelas
  exigiria atualizar o conteúdo do template, que o contrato da fase declara inalterado — vira todo (Task 3).
- **Não** toca `backend/src/index.js:107`. Aquele `console.error(err)` é guardado por
  `process.env.NODE_ENV !== 'production'`, ou seja, **não executa em produção** e não escreve o token no
  `pm2-error.log`. Além disso `index.js` está fora do `include` do `.c8rc.json` e não tem harness de teste —
  mudá-lo sem teste violaria a constraint de processo do `CLAUDE.md`. Vira todo (Task 3).
- **Não** move os seams de teste para dentro do bloco `module.exports` (IN-02) — cosmético, e misturá-lo com
  correção de segurança contraria "não misturar refatoração estrutural com outro trabalho". Vira todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md
@.planning/phases/04-confiabilidade-das-integra-es/04-DELIVERY-CONTRACT.md
@.planning/phases/04-confiabilidade-das-integra-es/04-PATTERNS.md
@.planning/phases/04-confiabilidade-das-integra-es/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do repositório em 2026-08-04. Não explorar o codebase. -->

`backend/src/routes/deals.js:1-50` — o arquivo inteiro tem 50 linhas. `staleHandler(req, res)` roda
`Promise.all([getStaleDeals, getUsers, getDealsWithFutureTasks])` e no `catch` faz
`console.error(err); res.status(500).json({ error: err.message });`. O módulo **não** importa `../logger`
hoje. `module.exports = router;` na `:39` e o seam `module.exports.staleHandler = staleHandler;` na `:50`.

`backend/src/logger.js` — expõe `{ info, warn, error, debug }`. Chamada no formato
`logger.error('[Tag] mensagem:', valor)` (ver `scheduler.js:192`, `agendor.js:289`). Em produção emite JSON
de uma linha; um `Error` passado como valor é expandido para `.stack`, então **nunca** passar o objeto de erro.

`backend/src/agendor.js:62-76` — `getDealById(id)`: comentário-bloco explicando por que ela NÃO engole a
falha, seguido de `const { data } = await api.get` com o id interpolado no path relativo e
`return data.data || null;`. Chamada única em produção: `routes/notifications.js:222`, dentro do
`Promise.all` cujo `catch` por item (`:239-241`) devolve `{ ...d, resolved: d.resolved === 1 }`.

`backend/src/routes/notifications.js:54-110` — `router.post('/test-card', async (req, res) => {...})`
(handler anônimo hoje). Grava `deal_id: req.body.dealId || 0` na `:87` e monta a `webUrl` interpolando
`req.body.dealId || '5620'` na `:82`. `module.exports = router;` na `:265`, bloco de seams em `:267-275`
com `module.exports.resolvedHandler = resolvedHandler;`.

`backend/src/db.js` — `logNotification({ deal_id, deal_title, owner_name, owner_email, admin_email, days_stale, status, error, deal_updated_at, deal_type, web_url })` devolve o result do better-sqlite3
(com `lastInsertRowid`). `getNotificationLogs({ limit, offset })` devolve `{ logs, total }`.

`backend/test/notifications.resolved.test.js:29-95` — o **molde exato** de bootstrap para as duas tasks:
`makeTmpDbPath()` + `process.env.DB_PATH`, depois `require('./setup')`, depois `installFakeAxios(...)`,
depois os requires de `src/`. Inclui o espião do `axios.get` GLOBAL (`:87-91`) e o `res` falso mínimo.

`backend/test/scheduler.failsafe.test.js:141-154` — `resFalso()`: objeto com `statusCode`, `body`,
`status(codigo)` que grava e devolve `this`, e `json(corpo)` que grava e devolve `this`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-02 — o objeto de erro do axios nunca chega ao log de GET /api/deals/stale</name>
  <files>backend/src/routes/deals.js, backend/test/deals.errorLog.test.js</files>
  <behavior>
    - Quando a borda Agendor falha com um erro que carrega `config.headers.Authorization`, o log emitido NÃO contém o valor do token
    - `console.error` não é chamado nenhuma vez durante o tratamento do erro da rota
    - `logger.error` é chamado com a tag `[Deals]` e com a MENSAGEM do erro (não com o objeto)
    - A resposta continua sendo 500 com o shape `{ error }` (sem a chave `ok`)
  </behavior>
  <read_first>
    - `backend/src/routes/deals.js` (arquivo inteiro — 50 linhas)
    - `backend/src/logger.js` (arquivo inteiro — formato de emissão e expansão de `Error`)
    - `backend/src/agendor.js:278-292` — o comentário e o `logger.error('[Agendor] …', err.message)` que o 04-02 escreveu pelo mesmo motivo; copiar o raciocínio
    - `backend/test/notifications.resolved.test.js:29-95` — bootstrap (tmpDb + fakeAxios + espião do axios global)
    - `backend/test/scheduler.failsafe.test.js:141-154, 259-287` — `resFalso()` e o caso B3, que já pina o 500 `{ error }` desta rota
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção CR-02
  </read_first>
  <action>
    **(a) Teste primeiro — `backend/test/deals.errorLog.test.js`.** Cabeçalho em PT-BR nomeando CR-02 e
    explicando por que o sink importa: em produção o stream do `console.error` vai para
    `/opt/agendor/logs/pm2-error.log`, persistido em disco, e o objeto do axios carrega
    `config.headers.Authorization`.

    Bootstrap no molde de `notifications.resolved.test.js`: `makeTmpDbPath()` mais `process.env.DB_PATH`,
    depois `require('./setup')`, depois `installFakeAxios(...)`, e só então `require('../src/logger')` e
    `require('../src/routes/deals')`.

    Constante `const TOKEN_SINTETICO = 'AGENDOR-TOKEN-SINTETICO-DO-TESTE';` — valor deliberadamente NÃO
    parecido com credencial real (nada de UUID), para não disparar o job `secrets`/gitleaks do CI.

    Erro injetado, fiel ao formato do axios: um `Error('timeout of 15000ms exceeded')` com
    `code: 'ECONNABORTED'`, `isAxiosError: true` e um `config` contendo `url: '/tasks'` e
    `headers: { Authorization: 'Token ' + TOKEN_SINTETICO }`. O stub faz `/tasks` rejeitar com ele (é a borda
    que `getDealsWithFutureTasks` propaga desde o 04-02); `/deals`, `/organizations/:id` e `/users`
    respondem normalmente.

    Espiões: `const logErro = mock.method(logger, 'error', () => {});` e
    `const consoleErro = mock.method(console, 'error', () => {});`. Zerar ambos com `resetCalls()`
    imediatamente antes de invocar o handler, para que a contagem seja local ao caso.

    Caso único, com três asserções:
    1. **Pré-condição e shape:** chamar `staleHandler({ query: {} }, res)` com o `resFalso()` e asserir
       `res.statusCode === 500` e `typeof res.body.error === 'string'`. Sem isso o caso não prova que o
       caminho de erro foi alcançado.
    2. **Nenhum objeto vai para o console:** `assert.equal(consoleErro.mock.callCount(), 0, ...)`.
    3. **O token não aparece no que foi logado:** serializar TODOS os argumentos de TODAS as chamadas de
       `logErro` com `util.inspect(args, { depth: null })` e asserir
       `assert.equal(serializado.includes(TOKEN_SINTETICO), false, 'o log carregou o token da API Agendor')`.
       **A mensagem da asserção não pode conter o valor do token nem o objeto serializado** (mesma disciplina
       de PC-13 para `auth.pass`). Asserir também que o serializado contém `'[Deals]'` e
       `'timeout of 15000ms exceeded'`, provando que o log útil continua existindo.

    Rodar e registrar o RED (hoje a asserção 2 falha, porque `console.error(err)` executa).

    **(b) Correção — `backend/src/routes/deals.js`.** Acrescentar `const logger = require('../logger');`
    junto dos demais `require` do topo (o arquivo não separa externos de locais por linha em branco — seguir
    o estilo existente). Trocar `console.error(err);` por
    `logger.error('[Deals] Erro ao listar negócios parados:', err.message);`, com comentário em PT-BR
    imediatamente acima explicando a decisão: o objeto de erro do axios carrega
    `config.headers.Authorization: Token <AGENDOR_TOKEN>`, e em produção esse stream é persistido em disco —
    mesma razão já registrada em `agendor.js:286-287`. A linha `res.status(500).json({ error: err.message });`
    **não muda** (o shape `{ error }` é pinado pelo caso B3 de `scheduler.failsafe.test.js`).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/deals.errorLog.test.js &amp;&amp; node --test test/scheduler.failsafe.test.js &amp;&amp; node --test test/secrets.grep.test.js &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/deals.errorLog.test.js` sai 0 com o caso verde
    - `node --test test/scheduler.failsafe.test.js` sai 0 e `git diff --name-only backend/test/scheduler.failsafe.test.js` está vazio
    - `node --test test/secrets.grep.test.js` sai 0 (o valor sintético não é classificado como segredo)
    - `grep -c "console.error" backend/src/routes/deals.js` retorna 0
    - `grep -c "logger.error('\[Deals\]" backend/src/routes/deals.js` retorna 1
    - `grep -c "require('../logger')" backend/src/routes/deals.js` retorna 1
    - `grep -c "err.message" backend/src/routes/deals.js` retorna 2 (o log e a resposta) — nenhum ponto passa `err` inteiro
    - `npm run test:coverage` e `npm run lint` saem 0
    - O diff de `backend/src/routes/deals.js` adiciona no máximo 2 linhas de código (o `require` e o `logger.error`) mais comentário, e não remove nada além do `console.error(err);`

    **Contrato de sanitização de log (exigido pelo usuário, 2026-08-04):**
    - **Só mensagem, código e contexto seguro chegam ao log.** O `logger.error` de `deals.js` recebe uma
      string de contexto mais `err.message`, e opcionalmente `err.code`. Nada mais. Nenhum ponto do arquivo
      passa `err`, `err.config`, `err.request`, `err.response` ou qualquer objeto que os contenha.
    - **Nunca registrar o AxiosError inteiro, `config`, `headers` ou `Authorization`.** Verificação por
      código, não por leitura: `grep -nE "logger\.(error|warn|info|debug)\([^)]*\berr\b[^.)]" backend/src/routes/deals.js`
      não retorna nada (não existe log que passe `err` sem acessar uma propriedade escalar dele), e
      `grep -cE "err\.(config|request|response|stack)" backend/src/routes/deals.js` retorna 0.
    - **O teste usa token SINTÉTICO e prova que ele não aparece na saída.** O teste injeta um erro cuja
      `config.headers.Authorization` contém um valor sintético reconhecível (ex.: `Token TOKEN-SINTETICO-DE-TESTE`),
      captura TODOS os argumentos entregues ao logger, serializa-os com `util.inspect(args, { depth: null })`
      e assere que a string sintética **não** aparece. Assere também que `console.error` não foi chamado.
    - **O teste NUNCA lê nem exibe o token real.** Proibido referenciar `process.env.AGENDOR_TOKEN`,
      `backend/.env` ou o valor real em qualquer ponto do teste ou da saída:
      `grep -cE "AGENDOR_TOKEN|process\.env\.AGENDOR" backend/test/deals.errorLog.test.js` retorna 0.
      O valor sintético deve ser escolhido para não ser classificado como segredo por
      `test/secrets.grep.test.js` (que precisa continuar verde).
    - **Higiene de logs anteriores, verificada sem imprimir conteúdo sensível.** Checar se os logs locais já
      contêm o token, reportando apenas a CONTAGEM — nunca o trecho. Comando a registrar no SUMMARY:
      `[ -f backend/.env ] && grep -c -F -f <(grep '^AGENDOR_TOKEN=' backend/.env | cut -d= -f2-) backend/logs/*.log 2>/dev/null || echo 0`
      — se qualquer contagem for maior que 0, NÃO imprimir a linha encontrada; registrar o fato no SUMMARY,
      recomendar a limpeza dos arquivos afetados e conectar ao todo `sec-01-rotate-agendor-token` (um token
      já persistido em disco exige rotação, não só limpeza). A verificação é **somente leitura**: este plano
      não apaga nem trunca arquivo de log.
  </acceptance_criteria>
  <done>CR-02 fechado: nenhuma falha da borda Agendor nesta rota escreve o token em disco, e existe um teste que falha se o objeto de erro voltar ao log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-03 — getDealById recusa id não-inteiro, e o escritor controlado pelo usuário grava número</name>
  <files>backend/src/agendor.js, backend/src/routes/notifications.js, backend/test/dealId.validation.test.js</files>
  <behavior>
    - `getDealById('../users')` rejeita com `[Agendor] id de negócio inválido` e NENHUMA requisição HTTP é emitida
    - `getDealById(101)` e `getDealById('101')` continuam consultando `/deals/101` (o id vem do banco e pode chegar como string numérica)
    - `POST /api/notifications/test-card` com `dealId: '../users'` grava `deal_id === 0` (número) no `notification_log`
    - `POST /api/notifications/test-card` com `dealId: '5620'` grava `deal_id === 5620` (número)
    - O shape de `GET /api/notifications/resolved` não muda: o `catch` por item continua absorvendo a rejeição
  </behavior>
  <read_first>
    - `backend/src/agendor.js:62-76` — `getDealById` e o comentário-bloco que explica por que ela NÃO engole a falha
    - `backend/src/routes/notifications.js:54-110` — o handler `/test-card` inteiro, incluindo a `:82` e a `:87`
    - `backend/src/routes/notifications.js:199-275` — `resolvedHandler`, o `catch` por item e o bloco de seams existente
    - `backend/test/notifications.resolved.test.js` (arquivo inteiro — bootstrap e os 7 casos que não podem quebrar)
    - `backend/src/db.js:176-232` — `logNotification` e `getNotificationLogs`
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção WR-03
  </read_first>
  <action>
    **(a) Teste primeiro — `backend/test/dealId.validation.test.js`.** Cabeçalho em PT-BR nomeando WR-03 e
    explicando a cadeia concreta: `test-card` grava `deal_id` do corpo da requisição; a coluna tem afinidade
    `INTEGER` **sem `STRICT`**, então texto sobrevive; `GET /resolved` passa esse valor a `getDealById`; a url
    final consulta outro recurso da Agendor com o token de serviço.

    Bootstrap no molde de `notifications.resolved.test.js`: tmpDb mais `DB_PATH`, depois `require('./setup')`,
    depois `installFakeAxios(...)`, depois os requires. O stub responde
    `{ data: { data: { id, updatedAt, dealStatus } } }` para qualquer url que comece com `/deals/`, e
    `{ data: { data: [] } }` no resto. Stubar `nodemailer.createTransport` devolvendo
    `{ verify: async () => true, sendMail: async () => ({}) }` — o `/test-card` chama
    `sendStaleNotification`. **PC-13:** não capturar nem imprimir o objeto de opções.

    Cinco casos:
    1. `await assert.rejects(() => getDealById('../users'), /id de negócio inválido/)` **e**
       `assert.equal(fake.get.mock.callCount(), 0, 'a validação precisa acontecer ANTES da requisição')`
       (zerar as chamadas com `resetCalls()` imediatamente antes).
    2. Mesma prova para `getDealById(0)` e `getDealById(null)` — inteiro **positivo** é o contrato.
    3. `await getDealById(101)` e depois `fake.get.mock.calls[0].arguments[0] === '/deals/101'`.
    4. **Não-regressão obrigatória:** `await getDealById('101')` (string numérica, a forma como o valor sai do
       SQLite) também consulta `'/deals/101'`. Sem este caso a validação poderia quebrar o caminho real de
       produção.
    5. `/test-card`: invocar o handler pelo seam com
       `req = { body: { email: 'destino@exemplo.invalid', dealId: '../users' } }` e o `resFalso()`; depois ler
       a linha por `db.getNotificationLogs({ limit: 100 })` e asserir `typeof deal_id === 'number'` e
       `deal_id === 0`. Repetir com `dealId: '5620'` e asserir `deal_id === 5620`.

    **(b) Correção em `backend/src/agendor.js`.** Em `getDealById`, antes da chamada: converter com
    `Number(id)` para uma const local `dealId`, e se `!Number.isInteger(dealId) || dealId <= 0`, lançar
    `new Error` com a mensagem `[Agendor] id de negócio inválido: ` seguida de `String(id)`. Interpolar
    `dealId` (o número já convertido) no path, não o argumento cru. Comentário curto em PT-BR acima da guarda
    dizendo **por que**: a coluna `notification_log.deal_id` não é `STRICT` e um dos escritores é o corpo de
    uma requisição autenticada; sem a guarda o path composto consulta outro recurso da Agendor com o token de
    serviço. O comentário-bloco existente de `:62-72` continua válido e **não deve ser removido**.

    **(c) Correção em `backend/src/routes/notifications.js`.** Trocar `deal_id: req.body.dealId || 0` (`:87`)
    por `deal_id: Number.parseInt(req.body.dealId, 10) || 0`. **Não** tocar a `:82` (`webUrl`) — é IN-04, fora
    de escopo, e vira todo na Task 3.

    Para tornar o caso 5 executável, converter o handler anônimo de `/test-card` em função nomeada
    `async function testCardHandler(req, res)`, registrar com `router.post('/test-card', testCardHandler);` e
    expor `module.exports.testCardHandler = testCardHandler;` **dentro do bloco de seams já existente**
    (`:267-275`), acrescentando uma frase ao comentário daquele bloco explicando que este seam existe para
    provar o tipo gravado em `notification_log.deal_id`. Nenhuma linha do corpo do handler além da `:87` pode
    mudar.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; node --test test/dealId.validation.test.js &amp;&amp; node --test test/notifications.resolved.test.js &amp;&amp; npm run test:coverage &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dealId.validation.test.js` sai 0 com os 5 casos verdes
    - `node --test test/notifications.resolved.test.js` sai 0 e `git diff --name-only backend/test/notifications.resolved.test.js` está vazio
    - `grep -c "Number.isInteger" backend/src/agendor.js` retorna 1
    - `grep -c "Number.parseInt(req.body.dealId" backend/src/routes/notifications.js` retorna 1
    - `grep -c "req.body.dealId || 0" backend/src/routes/notifications.js` retorna 0
    - `grep -c "async function testCardHandler" backend/src/routes/notifications.js` retorna 1 e `grep -c "module.exports.testCardHandler" backend/src/routes/notifications.js` retorna 1
    - A linha da `webUrl` (IN-04) permanece intocada: `grep -c "req.body.dealId || '5620'" backend/src/routes/notifications.js` retorna 1
    - `npm run test:coverage` e `npm run lint` saem 0
    - `git diff backend/src/agendor.js` altera somente o corpo de `getDealById` (mais comentário); `getStaleDeals`, `getOrgCategory` e o bloco `module.exports` ficam intocados
  </acceptance_criteria>
  <done>WR-03 fechado: nenhum valor não-inteiro alcança o path da API Agendor, e o único escritor controlado pelo usuário passa a gravar número.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar como todo os achados do 04-REVIEW deixados fora desta rodada</name>
  <files>.planning/todos/pending/in-01-status-pending-na-ui.md, .planning/todos/pending/in-02-seams-fora-do-module-exports.md, .planning/todos/pending/in-03-comentario-emailer-timeout.md, .planning/todos/pending/in-04-escape-html-no-test-card.md, .planning/todos/pending/cr-02b-console-error-objeto-completo-em-index.md</files>
  <read_first>
    - `.planning/todos/pending/ui-01-toast-de-erro-no-check.md` (arquivo inteiro — formato obrigatório: frontmatter `id/type/status/priority/created/source/resolves_phase/tags`, corpo com "O que acontece", "Por que não foi consertado", "Correção sugerida", "Destino sugerido")
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` seção Info (IN-01 a IN-04) e o último parágrafo de CR-02 (sobre `index.js:107`)
    - `frontend/src/components/NotificationHistory.jsx` (trecho por volta da linha 306 — o ternário que decide o ícone pelo `status === 'sent'`)
    - `backend/src/index.js:103-115` — o middleware global de erro
  </read_first>
  <action>
    Criar cinco arquivos em `.planning/todos/pending/`, todos no formato exato de
    `ui-01-toast-de-erro-no-check.md` (frontmatter mais corpo em PT-BR), cada um citando `arquivo:linha` e
    dizendo **por que ficou fora desta rodada**:

    1. `in-01-status-pending-na-ui.md` — `priority: medium`, tags `[frontend, ux, notificacoes, phase-4-carryover]`.
       O status `'pending'` que o 04-06 introduziu (`scheduler.js:136`) renderiza como falha (X vermelho) em
       `frontend/src/components/NotificationHistory.jsx:306`, porque o consumidor decide o ícone por
       `status === 'sent'`. A linha fica `'pending'` durante todo o envio e **permanentemente** se o processo
       morrer no meio. Fora de escopo: UI está fora da Fase 4 por decisão do `04-CONTEXT.md` (Phase Boundary).
       Correção sugerida: terceiro ramo (relógio/cinza) para `'pending'`.

    2. `in-02-seams-fora-do-module-exports.md` — `priority: low`, tags `[backend, convencao, testes, phase-4-carryover]`.
       `scheduler.js:346`, `routes/deals.js:50` e `routes/notifications.js:275` (mais o `testCardHandler`
       acrescentado na Task 2 deste plano) anexam exports **fora** do bloco `module.exports` único que o
       `CLAUDE.md` exige. O caso mais delicado é `module.exports.runWeeklySummary`: a função não tem lock
       `isRunning` e agora é importável por qualquer módulo de produção. Fora de escopo: cosmético; misturá-lo
       com correção de segurança contraria a constraint de não misturar trabalhos. Correção sugerida: mover os
       quatro para dentro do bloco com comentário `// seam de teste`.

    3. `in-03-comentario-emailer-timeout.md` — `priority: low`, tags `[backend, testes, comentario, phase-4-carryover]`.
       `backend/test/emailer.timeout.test.js:310-311` afirma que "se este caminho passasse a retentar, o teste
       gastaria 9 segundos reais — a lentidão seria o próprio alarme"; `node --test` não impõe timeout por
       caso, então lentidão não falha nada. O alarme real é `assert.equal(enviosTentados, 2)` na `:329`.
       Fora de escopo: comentário sem consequência funcional, em arquivo de teste das ondas 1-7 que esta
       rodada de gap closure não altera.

    4. `in-04-escape-html-no-test-card.md` — **`priority: high`**, tags `[backend, seguranca, email, xss, phase-4-carryover]`.
       `routes/notifications.js:67-83` monta o deal de teste com `title`, `organization`, `ownerName` e
       `dealId` vindos de `req.body`, e `emailer.js:117,158,164` os interpola sem escape (inclusive dentro de
       um atributo `href`). Um usuário autenticado envia HTML arbitrário — inclusive um link para domínio
       próprio — para **qualquer endereço de e-mail**, com o remetente e a identidade visual da empresa. Fora
       de escopo: linhas pré-existentes não tocadas pela Fase 4; a correção mexe no template de e-mail, cujo
       conteúdo o contrato da fase declara inalterado. Correção sugerida: helper `escapeHtml()` aplicado aos
       campos interpolados de `dealEmailHtml`, com teste do novo template.

    5. `cr-02b-console-error-objeto-completo-em-index.md` — `priority: low`, tags `[backend, seguranca, logging, phase-4-carryover]`.
       `backend/src/index.js:107` faz `console.error(err)` com o objeto inteiro, guardado por
       `process.env.NODE_ENV !== 'production'`. **Não** escreve o token no `pm2-error.log` de produção — por
       isso não entrou no CR-02 — mas imprime o objeto do axios em desenvolvimento. Fora de escopo:
       `index.js` está fora do `include` do `.c8rc.json` e não tem harness de teste; alterá-lo sem teste
       violaria a constraint de processo do `CLAUDE.md`. Correção sugerida: junto com LOG-01/LOG-02 na Fase 5,
       logar `err.message` mais `err.stack` em vez do objeto.

    Não editar `.planning/STATE.md` nesta task — o SUMMARY do plano é quem alimenta a atualização de estado.
  </action>
  <verify>
    <automated>ls .planning/todos/pending/in-01-status-pending-na-ui.md .planning/todos/pending/in-02-seams-fora-do-module-exports.md .planning/todos/pending/in-03-comentario-emailer-timeout.md .planning/todos/pending/in-04-escape-html-no-test-card.md .planning/todos/pending/cr-02b-console-error-objeto-completo-em-index.md &amp;&amp; grep -c "^status: pending" .planning/todos/pending/in-04-escape-html-no-test-card.md</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 arquivos existem em `.planning/todos/pending/` e o `ls` acima sai 0
    - Cada um tem frontmatter com as chaves `id`, `type: todo`, `status: pending`, `priority`, `created`, `source`, `resolves_phase`, `tags` — conferir com `grep -c "^priority:" .planning/todos/pending/in-0*.md .planning/todos/pending/cr-02b-*.md` (cada arquivo retorna 1)
    - `in-04-escape-html-no-test-card.md` tem `priority: high`
    - Cada arquivo cita pelo menos uma referência `arquivo:linha` do achado correspondente
    - Cada arquivo tem uma seção explicando por que ficou fora da Fase 4
    - `git status --porcelain backend/` está vazio nesta task (nenhum arquivo de código alterado)
  </acceptance_criteria>
  <done>A triagem do 04-REVIEW está registrada por escrito: nenhum achado foi descartado em silêncio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint C8 — revisar a sanitização dos logs</name>
  <files>backend/src/routes/deals.js, backend/test/deals.errorLog.test.js</files>
  <read_first>
    - `.planning/phases/04-confiabilidade-das-integra-es/04-REVIEW.md` §CR-02 (o vazamento e a evidência)
    - `backend/src/routes/deals.js` (estado final)
    - `backend/test/deals.errorLog.test.js` (arquivo final)
    - `.planning/todos/pending/sec-01-rotate-agendor-token.md` (relação com rotação)
  </read_first>
  <action>
    PAUSAR a execução e apresentar ao humano o checkpoint C8. O usuário pediu explicitamente por este gate
    (2026-08-04) para confirmar que nenhum `AxiosError`, `config`, `headers` ou `Authorization` completo
    pode ser persistido. Reunir e exibir: o `git diff backend/src/routes/deals.js`, a saída do teste de
    sanitização, e o RESULTADO DA CONTAGEM da verificação de logs anteriores — **nunca o conteúdo**.
    Não prosseguir para o 04-10 sem o sinal de retomada.
  </action>
  <what-built>
    `console.error(err)` em `routes/deals.js` substituído por `logger.error` com contexto e `err.message`,
    fechando o caminho pelo qual o `AGENDOR_TOKEN` chegava ao `pm2-error.log`. Mais a validação de `id` em
    `getDealById` (WR-03) e o registro em todo dos achados fora de escopo.
  </what-built>
  <how-to-verify>
    1. Rodar `cd backend && export PATH="$HOME/bin:$PATH" && npm run test:coverage` — deve sair 0.
    2. Ler o `git diff backend/src/routes/deals.js` e confirmar que nenhum ponto passa `err` inteiro,
       `err.config`, `err.request`, `err.response` ou `err.stack` a qualquer função de log.
    3. Confirmar que o teste usa token **sintético** e que ele NÃO aparece na saída serializada do logger.
       Confirmar que o teste em nenhum momento lê `process.env.AGENDOR_TOKEN` ou `backend/.env`.
    4. Confirmar que a verificação de logs anteriores reportou apenas CONTAGEM. Se qualquer contagem for
       maior que 0, o token já está persistido em disco — nesse caso a limpeza dos arquivos não basta e a
       rotação do token (`sec-01`) passa de higiene a resposta a incidente. **Decidir isso explicitamente.**
    5. Confirmar que `test/secrets.grep.test.js` continua verde (o valor sintético não é classificado como
       segredo real).
    6. Confirmar que os 5 arquivos de todo da triagem existem e que nenhum achado do 04-REVIEW foi
       descartado em silêncio.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirmou por escrito que nenhum `AxiosError`, `config`, `headers` ou `Authorization` completo pode chegar ao log
    - Humano confirmou que o teste usa token sintético e nunca lê o real
    - Humano recebeu o resultado da verificação de logs anteriores como CONTAGEM, sem conteúdo sensível, e decidiu sobre a rotação do token
    - Humano autorizou explicitamente a entrada no plano 04-10
  </acceptance_criteria>
  <verify>
    <human-check>Humano confirma a sanitização, o uso de token sintético, decide sobre rotação, e autoriza o 04-10.</human-check>
    <automated>cd backend &amp;&amp; export PATH="$HOME/bin:$PATH" &amp;&amp; npm run test:coverage</automated>
  </verify>
  <done>C8 aprovado por escrito; entrada no 04-10 autorizada.</done>
  <resume-signal>Digite "aprovado" para liberar o 04-10, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `AGENDOR_TOKEN` → stream de log → disco | Segredo de serviço que cruza para um artefato persistente (`/opt/agendor/logs/pm2-error.log`) e sobrevive a restart |
| Corpo de requisição autenticada → `notification_log.deal_id` | Entrada controlada pelo usuário atravessando a coluna sem `STRICT` |
| `notification_log.deal_id` → path da API Agendor | Valor lido do banco interpolado em url e enviado com o token de serviço |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | `routes/deals.js:32` imprimindo o AxiosError inteiro (`config.headers.Authorization`) no `pm2-error.log` | mitigate | Trocar por `logger.error('[Deals] …', err.message)`; teste assere que o token sintético não aparece nos argumentos serializados e que `console.error` não é chamado |
| T-04-09-02 | Elevation of Privilege / SSRF de caminho | `getDealById` interpolando `id` não validado no path (`'../users'` alcança outro recurso com o token de serviço) | mitigate | Guarda `Number.isInteger(dealId) && dealId > 0` antes de qualquer requisição; teste assere zero chamadas HTTP no caminho recusado |
| T-04-09-03 | Tampering | `POST /test-card` gravando `deal_id` textual vindo de `req.body` numa coluna sem `STRICT` | mitigate | `Number.parseInt(req.body.dealId, 10) \|\| 0`; teste lê a linha gravada e assere `typeof === 'number'` |
| T-04-09-04 | Denial of Service (silencioso) | Validação estrita quebrando o caminho legítimo (id chegando como string numérica do SQLite) | mitigate | Caso 4 do teste pina `getDealById('101')` consultando `/deals/101`; `notifications.resolved.test.js` roda sem edição |
| T-04-09-05 | Information Disclosure | Injeção de HTML/link no e-mail via corpo do `test-card` (IN-04) — **não fechado aqui** | transfer | Registrado como todo `in-04-escape-html-no-test-card.md` com `priority: high`; exige mexer no template, declarado inalterado pelo contrato da fase |
| T-04-09-06 | Information Disclosure | Segredo sintético do teste sendo confundido com credencial real pelo gate `secrets`/gitleaks | mitigate | Valor em caixa alta com a palavra `SINTETICO`, sem formato de UUID; `node --test test/secrets.grep.test.js` no verify |
| T-04-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote neste plano; `backend/package.json` e o lockfile não são tocados |
</threat_model>

<verification>
- `node --test test/deals.errorLog.test.js` e `node --test test/dealId.validation.test.js` saem 0
- `node --test test/scheduler.failsafe.test.js`, `node --test test/notifications.resolved.test.js` e `node --test test/secrets.grep.test.js` saem 0, sem edição de nenhum deles
- `npm run test:coverage` e `npm run lint` saem 0
- Diff de produção: 1 linha em `routes/deals.js` (mais o `require` e comentário), o corpo de `getDealById` em `agendor.js`, e 1 linha mais o seam em `routes/notifications.js`
- `git diff --name-only backend/package.json backend/package-lock.json` vazio
- Os 5 todos de triagem existem em `.planning/todos/pending/`
</verification>

<success_criteria>
- Nenhum caminho de erro de `GET /api/deals/stale` escreve o `AGENDOR_TOKEN` em log
- `getDealById` só emite requisição para inteiro positivo, e o caminho legítimo (string numérica do banco) continua funcionando
- `notification_log.deal_id` recebe número mesmo com corpo de requisição hostil
- Todo achado do `04-REVIEW.md` não fechado nesta rodada tem um todo rastreável com justificativa escrita
</success_criteria>

<output>
Create `.planning/phases/04-confiabilidade-das-integra-es/04-09-SUMMARY.md` when done.
Commit: `fix(04-09): token da Agendor fora do log e id de negócio validado (CR-02/WR-03)`
Rollback: revert do commit.
</output>
