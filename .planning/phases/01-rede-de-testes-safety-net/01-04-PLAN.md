---
phase: 01-rede-de-testes-safety-net
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/test/agendor.funnel.test.js
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "shouldNotifyOwner suppresses funnel 'beefor' (exact), 'Beefor' (lowercased), ' beefor ' (trimmed)"
    - "shouldNotifyOwner does NOT suppress 'beefor vendas' or 'beeforx' (exact-match quirk)"
    - "shouldNotifyOwner notifies when funnel is null/missing"
  artifacts:
    - path: "backend/test/agendor.funnel.test.js"
      provides: "beefor funnel suppression characterization incl. exact-string quirks"
      min_lines: 15
  key_links:
    - from: "backend/test/agendor.funnel.test.js"
      to: "backend/src/agendor.js shouldNotifyOwner"
      via: "already-exported pure predicate, no mocks"
      pattern: "shouldNotifyOwner"
---

<objective>
Pin the funnel-based owner-suppression rule (`shouldNotifyOwner` / `NO_OWNER_NOTIFY_FUNNELS = ['beefor']`) with a pure, zero-mock characterization test. This is the highest-fragility rule per CONCERNS.md: suppression is an exact `trim().toLowerCase()` match against `'beefor'`, so near-miss funnel names are NOT suppressed. The test documents that exact-string quirk as current behavior.

Purpose: TEST-04 — lock the Beefor suppression so a funnel rename or a new similarly-named funnel can never silently change who gets notified without failing a test.
Output: one pure characterization test file. No source edit needed — shouldNotifyOwner is already exported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-rede-de-testes-safety-net/01-CONTEXT.md
@.planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md

<interfaces>
<!-- Extracted from backend/src/agendor.js. Already exported — no edit. -->

shouldNotifyOwner(deal) — line 57, ALREADY in module.exports (line 198):
  const funnel = (deal?.funnel || '').trim().toLowerCase();
  return !NO_OWNER_NOTIFY_FUNNELS.includes(funnel);
NO_OWNER_NOTIFY_FUNNELS = ['beefor'] (line 55).
Note the input shape: shouldNotifyOwner reads deal.funnel (a flat string), NOT deal.dealStage.funnel.name. Pass { funnel: '...' }.
Current behavior to pin: 'beefor' -> false (suppress); 'Beefor' -> false; ' beefor ' -> false; 'beefor vendas' -> true (NOT suppressed — exact-match includes() against the array, not substring); 'beeforx' -> true; funnel null / {} -> true (notifies). (RESEARCH Pitfall 6.)

Env: prefix node/npm with `export PATH="$HOME/bin:$PATH"`. Requires plan 01-01 (test runner + setup.js).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Beefor funnel-suppression characterization</name>
  <files>backend/test/agendor.funnel.test.js</files>
  <behavior>
    - shouldNotifyOwner({ funnel: 'beefor' }) === false   // suppress (exact)
    - shouldNotifyOwner({ funnel: 'Beefor' }) === false    // suppress (lowercased)
    - shouldNotifyOwner({ funnel: ' beefor ' }) === false  // suppress (trimmed)
    - shouldNotifyOwner({ funnel: 'beefor vendas' }) === true  // QUIRK: NOT suppressed
    - shouldNotifyOwner({ funnel: 'beeforx' }) === true        // QUIRK: NOT suppressed
    - shouldNotifyOwner({ funnel: null }) === true; shouldNotifyOwner({}) === true
  </behavior>
  <read_first>
    - backend/src/agendor.js (lines 51-60: NO_OWNER_NOTIFY_FUNNELS + shouldNotifyOwner)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pitfall 6", §"Code Examples" (shouldNotifyOwner block)
    - backend/test/setup.js
  </read_first>
  <action>
    Create backend/test/agendor.funnel.test.js: require './setup' first, then require shouldNotifyOwner from '../src/agendor'. Write node:test/assert-strict golden assertions for each behavior above. Mark the near-miss cases ('beefor vendas', 'beeforx') with a Portuguese comment stating they document the CURRENT exact-match behavior (a substring rename would NOT be suppressed) so a future intentional change is a conscious, test-covered decision. Do NOT edit agendor.js — the function is already exported.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node --test test/agendor.funnel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/agendor.funnel.test.js` exits 0
    - All six behaviors above are asserted, including both quirk cases as `true`
    - No modification to backend/src/agendor.js (`git status` shows only the new test file for this plan)
  </acceptance_criteria>
  <done>Beefor funnel suppression and its exact-string quirks are pinned as golden; TEST-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory predicate test; no I/O, no external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | test asserting looser matching than code does | mitigate | Characterization only — pin exact trim/lowercase includes() behavior; quirk cases asserted true, never "fixed" (RESEARCH Pitfall 6) |
</threat_model>

<verification>
- `cd backend && npm test` green including agendor.funnel.test.js.
- `git diff --name-only` for this plan lists only backend/test/agendor.funnel.test.js.
</verification>

<success_criteria>
TEST-04 met: shouldNotifyOwner / NO_OWNER_NOTIFY_FUNNELS suppression is pinned for exact/lowercased/trimmed 'beefor' and for the non-suppressed near-miss quirks, against current behavior.
</success_criteria>

<output>
Create `.planning/phases/01-rede-de-testes-safety-net/01-04-SUMMARY.md` when done.
</output>
