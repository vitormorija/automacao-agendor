---
phase: 01-rede-de-testes-safety-net
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/routes/auth.js
  - backend/test/auth.test.js
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "5 failed attempts from an IP produce a block; checkRateLimit reports blocked with minutesLeft (D-06 expor helpers de rate-limit checkRateLimit/recordFailedAttempt sobre o Map em memória)"
    - "After the block window elapses, checkRateLimit reports not blocked (mock.timers)"
    - "verifyPassword returns true/false for a matching/non-matching bcrypt hash (D-06 testar a verificação de senha via bcrypt no seam)"
    - "verifyPassword returns true/false for legacy plaintext (the $2 discriminator is preserved)"
    - "Requiring auth.js in tests does not touch backend/agendor.db or throw (JWT_SECRET + DB_PATH preset) (D-08 SQLite real em :memory:, sem mockar o módulo db)"
  artifacts:
    - path: "backend/src/routes/auth.js"
      provides: "verifyPassword extracted (DRY) + rate-limit helpers exported on the router"
      contains: "verifyPassword"
    - path: "backend/test/auth.test.js"
      provides: "rate-limit + password verification characterization"
      min_lines: 30
  key_links:
    - from: "backend/test/auth.test.js"
      to: "backend/src/routes/auth.js exports"
      via: "checkRateLimit/recordFailedAttempt/clearAttempts/verifyPassword/_loginAttempts on module.exports"
      pattern: "recordFailedAttempt"
    - from: "backend/src/routes/auth.js login + change-password"
      to: "verifyPassword"
      via: "both duplicated compares replaced by verifyPassword call"
      pattern: "verifyPassword\\("
---

<objective>
Pin the security-sensitive auth logic — login rate-limiting and password verification — with characterization tests. Add two minimal, behavior-preserving auth.js seams: DRY the duplicated bcrypt/plaintext compare into a `verifyPassword` helper (preserving the `$2` discriminator exactly) and expose the rate-limit helpers as properties on the exported router so tests can drive them and reset the in-memory Map between cases.

Purpose: TEST-05 — lock brute-force blocking and password checking so future hardening (Phase 6 SEC-01/SEC-04) cannot silently weaken them.
Output: auth.js seams + the auth characterization test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-rede-de-testes-safety-net/01-CONTEXT.md
@.planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md

<interfaces>
<!-- Extracted from backend/src/routes/auth.js. Executor uses these directly. -->

Rate limit (module-level Map + helpers, lines 33-67):
  loginAttempts = new Map();  MAX_ATTEMPTS = 5;  BLOCK_MINUTES = 15;
  checkRateLimit(ip) -> { blocked:false } | { blocked:true, minutesLeft }  (also deletes entry when block window passed)
  recordFailedAttempt(ip) -> { nowBlocked:true } on the 5th (sets blockedUntil = now + 15min, resets count) | { nowBlocked:false, remaining } otherwise
  clearAttempts(ip) -> deletes the ip entry

Duplicated compare to DRY into verifyPassword (behavior-preserving):
  login (lines 124-126): user.password.startsWith('$2') ? await bcrypt.compare(password, user.password) : password === user.password
  change-password (lines 193-195): identical shape with currentPassword
  Extract: async function verifyPassword(storedHash, plain) returns storedHash.startsWith('$2') ? bcrypt.compare(plain, storedHash) : plain === storedHash
  Replace both call sites with await verifyPassword(user.password, <plain>). The '$2' discriminator MUST be preserved exactly.

Export seam: current bottom is module.exports = router; (line 291). Keep that, then attach checkRateLimit, recordFailedAttempt, clearAttempts, verifyPassword, and _loginAttempts = loginAttempts (for per-test reset). Express only needs module.exports to be the router function; extra props are ignored by app.use() (RESEARCH Assumption A2).

Import side-effects (RESEARCH Pitfall 5): requiring auth.js runs ensureDefaultUsers() (DB via listUsers()) and requires ../secret (throws if JWT_SECRET < 16 chars). Test MUST require './setup' (from plan 01-01) FIRST so JWT_SECRET (32 chars) + DB_PATH (:memory:) + AGENDOR_TOKEN are preset before requiring auth.js.

bcryptjs ^3.0.3 installed — generate a real hash in-test with bcrypt.hashSync / await bcrypt.hash.
Env: prefix node/npm with `export PATH="$HOME/bin:$PATH"`. mock.timers Date-faking verified on Node 22.13.1 (emits an ExperimentalWarning only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth.js seams — verifyPassword DRY + export rate-limit helpers</name>
  <files>backend/src/routes/auth.js</files>
  <read_first>
    - backend/src/routes/auth.js (lines 33-67 rate-limit; 115-155 login compare; 190-203 change-password compare; 291 export)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pattern 4", §"Pitfall 5"
    - .planning/codebase/CONVENTIONS.md (auth.js export is `module.exports = router`, an intentional exception to the single-object rule)
  </read_first>
  <action>
    Add an async verifyPassword(storedHash, plain) helper near the rate-limit section that returns storedHash.startsWith('$2') ? bcrypt.compare(plain, storedHash) : plain === storedHash. Replace the duplicated inline compare at the login handler (lines 124-126) and the change-password handler (lines 193-195) with await verifyPassword(user.password, <plain>) — preserving the exact `$2` discriminator and the existing control flow, status codes, messages, and logging around it.
    Keep module.exports = router; and append property attachments: checkRateLimit, recordFailedAttempt, clearAttempts, verifyPassword, and _loginAttempts (the loginAttempts Map). Add a Portuguese comment noting these are test seams that do not affect Express routing.
    Do NOT change rate-limit logic, MAX_ATTEMPTS, BLOCK_MINUTES, or requireAdmin — those belong to Phase 6.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && JWT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx DB_PATH=':memory:' AGENDOR_TOKEN=test node -e "const r=require('./src/routes/auth');if(typeof r!=='function')throw new Error('router export broken');['checkRateLimit','recordFailedAttempt','clearAttempts','verifyPassword','_loginAttempts'].forEach(k=>{if(r[k]===undefined)throw new Error('missing '+k)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The one-liner boot check prints "ok": module.exports is still the router function AND all five helper props are present
    - `grep -c 'verifyPassword(' backend/src/routes/auth.js` >= 3 (definition + two call sites)
    - `git diff backend/src/routes/auth.js` shows only: verifyPassword added, the two compares replaced, and the export attachments — no changes to messages/status codes/rate-limit constants
    - The `$2` discriminator string is preserved verbatim in verifyPassword
  </acceptance_criteria>
  <done>auth.js exposes verifyPassword + rate-limit helpers with behavior-preserving DRY; server router export intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rate-limit + password verification characterization</name>
  <files>backend/test/auth.test.js</files>
  <read_first>
    - backend/src/routes/auth.js (checkRateLimit, recordFailedAttempt, clearAttempts, verifyPassword after Task 1)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Rate-limit block-expiry with mock.timers", §"Per-Requirement Test Map" (TEST-05 rows)
    - backend/test/setup.js
  </read_first>
  <action>
    Create backend/test/auth.test.js: require './setup' FIRST (presets JWT_SECRET/DB_PATH/AGENDOR_TOKEN so requiring auth.js does not throw or hit the real DB), then require the auth router module. In beforeEach, clear the in-memory Map via _loginAttempts.clear().
    Rate-limit tests: for a fixed ip, the first 4 recordFailedAttempt calls return nowBlocked false; the 5th returns nowBlocked true; checkRateLimit(ip) then reports blocked with a minutesLeft. Enable mock.timers({apis:['Date']}), tick past the 15-minute window (15*60*1000 + 1), and assert checkRateLimit(ip) reports not blocked; reset mock.timers after. Also assert clearAttempts(ip) makes checkRateLimit report not blocked.
    Password tests: generate a bcrypt hash in-test for a known password; assert verifyPassword(hash, correct) resolves true and verifyPassword(hash, wrong) resolves false. Assert the legacy plaintext branch: verifyPassword('plainpw', 'plainpw') true and verifyPassword('plainpw', 'nope') false (documents the `$2` discriminator selecting the plaintext path).
    Add Portuguese "documenta comportamento ATUAL" comments. Do not log real hashes/passwords beyond intentional assertions.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node --test test/auth.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/auth.test.js` exits 0
    - Rate-limit block-after-5 and block-expiry-after-window (via mock.timers) are both asserted
    - verifyPassword is covered for bcrypt match/no-match AND legacy plaintext match/no-match
    - The test requires ./setup before auth.js and does not write to backend/agendor.db (DB_PATH :memory:)
  </acceptance_criteria>
  <done>Login rate-limit and password verification are pinned as golden; TEST-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login IP → rate-limit state | Brute-force protection must not be weakened |
| stored password ↔ verifyPassword | Password compare path must stay bcrypt-first, plaintext-legacy |
| test env → production config/DB | auth.js import side-effects must hit test env, not prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | verifyPassword extraction | mitigate | Behavior-preserving DRY; `$2` discriminator preserved verbatim; golden tests cover both branches |
| T-05-02 | Tampering | test weakening rate-limit assertions | mitigate | Characterization only — pin current 5-attempt/15-min behavior; any change is Phase 6 with its own test |
| T-05-03 | Information Disclosure | test JWT_SECRET / real DB write | mitigate | setup.js sets throwaway JWT_SECRET + DB_PATH :memory:; never reads production backend/.env; no writes to agendor.db |
</threat_model>

<verification>
- Task 1 boot check prints "ok" (router export intact + helpers present).
- `node --test test/auth.test.js` exits 0.
- `cd backend && npm test` green including auth.test.js.
- `git diff backend/src/routes/auth.js` limited to verifyPassword + export attachments.
</verification>

<success_criteria>
TEST-05 met: login rate-limiting (block after 5, clear after window) and password verification (bcrypt + legacy plaintext) are pinned against current behavior, with auth.js seams that change no observable behavior.
</success_criteria>

<output>
Create `.planning/phases/01-rede-de-testes-safety-net/01-05-SUMMARY.md` when done.
</output>
