---
phase: 01-rede-de-testes-safety-net
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/agendor.js
  - backend/test/agendor.pure.test.js
  - backend/test/agendor.getStaleDeals.test.js
  - backend/test/helpers/fakeAxios.js
  - backend/test/fixtures/synthetic/deals-page.json
  - backend/test/fixtures/real-deals.sample.json
  - backend/scripts/capture-fixtures.js
  - .gitignore
autonomous: false
requirements: [TEST-02]
user_setup:
  - service: agendor-api
    why: "One-time capture of a small real deal sample for the anonymized realism fixture (D-10)"
    env_vars:
      - name: AGENDOR_TOKEN
        source: "Agendor account (env only — NEVER commit the token)"

must_haves:
  truths:
    - "getStaleDeals excludes deals created before 2026-01-01 and includes deals updated before now-staleDays (threshold/cutoff pinned)"
    - "getStaleDeals pins the strict `<` cutoff: a deal updated EXACTLY at cutoffDate is EXCLUDED, and a deal updated 1ms before cutoffDate is INCLUDED — a `<`→`<=` regression is caught by the boundary golden"
    - "getStaleDeals excludes EXCLUDED_CATEGORIES, EXCLUDED_OWNERS, and dealStatus/status id != 1 (pinned)"
    - "isExcludedStage pins the substring-match quirk including 'Perdão de contrato' -> excluded"
    - "getDealType classifies Lead vs Negócio per NEGOCIO_CATEGORIES (pinned)"
    - "Extraction changed no observable behavior: stage normalization regex is byte-identical"
    - "The committed real-deal fixture contains no PII and no token, and is committed ONLY after the human-verify approval"
  artifacts:
    - path: "backend/src/agendor.js"
      provides: "getDealType + isExcludedStage exported; isExcludedStage used inline"
      contains: "isExcludedStage"
    - path: "backend/test/agendor.pure.test.js"
      provides: "pure-lane characterization (stage exclusion, deal-type)"
      min_lines: 20
    - path: "backend/test/agendor.getStaleDeals.test.js"
      provides: "integrated-lane characterization (threshold/cutoff-boundary/category/owner/status via axios stub)"
      min_lines: 30
    - path: "backend/test/fixtures/synthetic/deals-page.json"
      provides: "one-per-rule synthetic deals INCLUDING day-boundary cases at cutoffDate and cutoffDate±1ms"
      contains: "id"
    - path: "backend/test/helpers/fakeAxios.js"
      provides: "mock.method(axios,'create',...) installer"
      contains: "axios"
    - path: "backend/test/fixtures/real-deals.sample.json"
      provides: "anonymized real-deal sanity fixture (PII stripped), committed only after human approval"
  key_links:
    - from: "backend/test/agendor.getStaleDeals.test.js"
      to: "backend/src/agendor.js getStaleDeals"
      via: "fakeAxios installed BEFORE require('../src/agendor')"
      pattern: "installFakeAxios"
    - from: "backend/src/agendor.js getStaleDeals"
      to: "isExcludedStage"
      via: "inline call replacing lines 138-139"
      pattern: "isExcludedStage\\(deal\\.dealStage"
    - from: ".gitignore"
      to: "raw fixture captures + token"
      via: "ignore entries before first fixture commit"
      pattern: "raw"
---

<objective>
Pin the current inclusion/exclusion behavior of `getStaleDeals()` with characterization (golden) tests using the two-lane strategy: a pure lane (no mocks) for stage-exclusion and deal-type, and an integrated lane (axios stub + fixed clock) for threshold/cutoff-boundary/category/owner/status-id rules that stay inline. Add the two minimal, behavior-preserving agendor.js seams and deliver the anonymized real-deal realism fixture (D-10) behind a token-safety checkpoint whose approval is the gate for the fixture's git commit.

Purpose: TEST-02 — fix the "who gets included / excluded" rules of the notification engine so any future refactor or hardening is caught if it drifts (including a silent flip of the strict `<` day-boundary comparison).
Output: agendor.js seams, pure + integrated test files, fakeAxios helper, synthetic fixtures (with day-boundary cases), an anonymized real-deal sample committed only after human approval, capture script, and .gitignore hardening.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rede-de-testes-safety-net/01-CONTEXT.md
@.planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md

<interfaces>
<!-- Extracted from backend/src/agendor.js. Executor uses these directly. -->

Already exported (module.exports line 198): getUsers, getStaleDeals, getDealsWithFutureTasks, shouldNotifyOwner.
To ADD to module.exports: getDealType, isExcludedStage.

getDealType(orgCategory) — line 67, already a standalone function. NEGOCIO_CATEGORIES = ['Cliente','Cliente Ouro','Cliente Bronze']. Returns 'Negócio' for those; 'Lead' otherwise (incl. null/'Lead'/Concorrente/etc.).

Stage exclusion — currently INLINE at lines 138-139:
  const stageName = (deal.dealStage?.name || '').toLowerCase().normalize('NFD').replace(/[<combining-marks>]/g, '');
  if (EXCLUDED_STAGE_WORDS.some(w => stageName.includes(w))) continue;
EXCLUDED_STAGE_WORDS = ['perd','ganh','congelad','suspenso','suspend','arquivad','encerrad','cancelad'].
The diacritic regex literal at line 138 is a combining-marks range (U+0300–U+036F). Copy it BYTE-FOR-BYTE into the new function; do NOT retype or "fix" it (RESEARCH Pitfall 1).

getStaleDeals(staleDays=15) rules (lines 92-161):
  cutoffDate = new Date(Date.now() - staleDays*86400000); startOf2026 = new Date('2026-01-01T00:00:00.000Z');
  THRESHOLD FILTER (line 117): keep deals where `createdAt >= startOf2026 && updatedAt < cutoffDate` — the `<` is STRICT. A deal whose updatedAt equals cutoffDate exactly is EXCLUDED (not stale); a deal 1ms before cutoffDate is INCLUDED. This exact operator is the characterization target of the boundary golden — do NOT change it.
  Then exclude EXCLUDED_CATEGORIES (via org category lookup), EXCLUDED_OWNERS = ['Maria Lobato'], dealStatusId (deal.dealStatus?.id || deal.status?.id) != 1, and isExcludedStage. Fetches pages via api.get('/deals', {params:{deal_status_id:1,...}}) with meta.totalCount/links; org category via api.get('/organizations/:id') -> data.data.category.name.

shouldNotifyOwner(deal) — already exported; NOT edited in this plan (owned by plan 01-04 for TEST-04).

Fixture deal shape consumed by getStaleDeals: id, title, createdAt, updatedAt, owner.{id,name}, author.{id,name}, organization.{id,name}, dealStatus.id/status.id, dealStage.name, dealStage.funnel.name, _webUrl. List envelope: { data:[...], meta:{totalCount}, links:{next} }. /organizations/:id -> { data:{ category:{ name } } }.

Env: prefix all node/npm commands with `export PATH="$HOME/bin:$PATH"`. Node 22.13.1; mock.method + mock.timers verified.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agendor.js seams + pure-lane characterization</name>
  <files>backend/src/agendor.js, backend/test/agendor.pure.test.js</files>
  <behavior>
    - isExcludedStage('Oportunidade Perdida') === true (matches 'perd')
    - isExcludedStage('Ganho') === true; isExcludedStage('Congelado') === true
    - isExcludedStage('Perdão de contrato') === true  // QUIRK: 'perd' substring — documents CURRENT behavior
    - isExcludedStage('Negociação') === false; isExcludedStage(null) === false
    - getDealType('Cliente') === 'Negócio'; getDealType('Cliente Ouro') === 'Negócio'
    - getDealType('Lead') === 'Lead'; getDealType(null) === 'Lead'; getDealType('Concorrente') === 'Lead'
  </behavior>
  <read_first>
    - backend/src/agendor.js (lines 46-71 constants + getDealType; lines 133-140 inline stage logic; line 198 exports)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pattern 1", §"Pitfall 1"
    - backend/test/setup.js (require it first in the test file)
  </read_first>
  <action>
    Seam A: add `getDealType` to the module.exports object in agendor.js (zero code motion — it is already a standalone function).
    Seam B: extract the inline stage-exclusion (lines 138-139) into a new pure function `isExcludedStage(rawStageName)` placed next to EXCLUDED_STAGE_WORDS. Copy the normalization expression (toLowerCase().normalize('NFD').replace(<combining-marks regex>, '')) and the EXCLUDED_STAGE_WORDS.some(...) check BYTE-FOR-BYTE from line 138 — do not retype the diacritic regex. Then replace lines 138-139 in getStaleDeals with a single `if (isExcludedStage(deal.dealStage?.name)) continue;`. Add `isExcludedStage` to module.exports. Keep a Portuguese comment above the function per the repo's "explain the why" style.
    Create backend/test/agendor.pure.test.js: require './setup' first, then require getDealType + isExcludedStage from '../src/agendor'. Write node:test/assert-strict golden tests for the behaviors above, each with a Portuguese comment marking it as "documenta o comportamento ATUAL" (especially the Perdão quirk).
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node --test test/agendor.pure.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/agendor.pure.test.js` exits 0
    - `grep -c 'isExcludedStage' backend/src/agendor.js` >= 3 (definition, inline call, export)
    - `grep -E "getDealType|isExcludedStage" backend/src/agendor.js | grep -q module.exports || grep -q 'isExcludedStage' <(sed -n '198,210p' backend/src/agendor.js)` — both names present in the exports block
    - `git diff backend/src/agendor.js` shows only: getDealType added to exports, isExcludedStage added + exported, and lines 138-139 replaced by the isExcludedStage call — no other logic changes
    - The Perdão-de-contrato quirk is asserted as `true`
  </acceptance_criteria>
  <done>agendor.js exposes getDealType + isExcludedStage with byte-preserving extraction; pure-lane golden tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Integrated-lane characterization via axios stub (incl. day-boundary golden)</name>
  <files>backend/test/helpers/fakeAxios.js, backend/test/fixtures/synthetic/deals-page.json, backend/test/agendor.getStaleDeals.test.js</files>
  <read_first>
    - backend/src/agendor.js (lines 6-9 axios.create; 92-161 getStaleDeals + fetchDealsPage + getOrgCategory; NOTE line 117 `updatedAt < cutoffDate` STRICT)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pattern 2", §"Pitfall 3", §"Per-Requirement Test Map"
    - backend/test/setup.js
  </read_first>
  <action>
    Create backend/test/helpers/fakeAxios.js (side-effect-only helper, NO test() calls): export installFakeAxios(routeHandler) which builds a fake instance whose get(url,config) delegates to routeHandler and installs it via mock.method(axios,'create',...) so agendor.js's module-load api === the fake. routeHandler answers '/deals' (list envelope with data/meta.totalCount/links) and '/organizations/:id' (category name).

    Create backend/test/fixtures/synthetic/deals-page.json: one JSON list envelope with a handful of deals covering each rule boundary. Choose a FIXED pinned clock in the test (e.g. now = 2026-06-01T00:00:00.000Z) so cutoffDate = now - 15 days = 2026-05-17T00:00:00.000Z is deterministic, and express the fixture `updatedAt`/`createdAt` as literal ISO timestamps relative to that pinned clock. Include:
      - an INCLUDED deal (created >=2026-01-01, updated clearly older than 15d, category Lead, status 1, owner not excluded, benign stage)
      - a DAY-BOUNDARY deal updated EXACTLY at cutoffDate (updatedAt = 2026-05-17T00:00:00.000Z) — under the strict `<` this deal is EXCLUDED (per D-09 "limite de dias no boundary")
      - a JUST-STALE deal updated at cutoffDate MINUS 1ms (updatedAt = 2026-05-16T23:59:59.999Z) — INCLUDED (all other rules benign)
      - (optional) a JUST-FRESH deal updated at cutoffDate PLUS 1ms (updatedAt = 2026-05-17T00:00:00.001Z) — EXCLUDED
      - a category-excluded deal (org resolves to 'Parceiro' or 'Inativo (sem resposta)')
      - an owner-excluded deal (owner.name 'Maria Lobato')
      - a status-excluded deal (dealStatus.id 3)
      - a pre-2026 created deal (createdAt < 2026-01-01)
      - a clearly fresh deal (recently updated, well inside 15d)
    Give each deal a distinct, easy-to-read id so the golden id set is legible.

    Create backend/test/agendor.getStaleDeals.test.js: require './setup', install fakeAxios BEFORE require('../src/agendor'), enable mock.timers with the SAME pinned Date chosen above (RESEARCH Pitfall 3) so cutoffDate/daysSinceUpdate are deterministic. Assert the golden output of getStaleDeals(15) as the set of included ids, proving: pre-2026 excluded, fresh excluded, category-excluded, owner 'Maria Lobato' excluded, status!=1 excluded, and the valid stale deal + the cutoffDate-1ms deal INCLUDED.
    BOUNDARY GOLDEN (Blocker fix, D-09): add an explicit assertion that the exact-cutoff deal id is NOT in the result and the cutoffDate-1ms deal id IS in the result, with a Portuguese comment: "documenta o comportamento ATUAL: comparação `<` estrita — igual ao cutoff é EXCLUÍDO; um `<=` faria falhar este teste". This pins the strict `<` so a regression to `<=` (or a flipped comparison) is caught. Do NOT modify line 117 — only characterize it.
    Add Portuguese "documenta comportamento ATUAL" comments throughout. Reset mock.timers in teardown.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node --test test/agendor.getStaleDeals.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/agendor.getStaleDeals.test.js` exits 0
    - The test drives the REAL getStaleDeals (no re-implemented filter) via the axios stub — fakeAxios installed before the agendor require
    - Golden assertion covers threshold+cutoff, the exact-cutoff boundary (excluded) AND cutoff-1ms (included), category, owner, and status-id exclusion in one deterministic run (mock.timers pinned)
    - The boundary assertion is present and would FAIL if line 117 were changed from `<` to `<=` (exact-cutoff deal flips from excluded to included) — reviewer can confirm by reasoning about the pinned dates
    - No network call is made (axios.create stubbed); test runs sub-second
  </acceptance_criteria>
  <done>Integrated-lane golden test pins threshold/category/owner/status rules AND the strict `<` day-boundary of getStaleDeals against a fixed clock.</done>
</task>

<task type="auto">
  <name>Task 3: Real-deal capture script, .gitignore hardening, anonymized sample (staged, NOT committed)</name>
  <files>backend/scripts/capture-fixtures.js, .gitignore, backend/test/fixtures/real-deals.sample.json</files>
  <read_first>
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Code Examples" (capture script + anonymize)
    - .gitignore (current entries: *.db, .env, .env.*, *.csv — no raw-fixture/token rule yet)
    - backend/test/agendor.getStaleDeals.test.js (reuse fakeAxios to feed the sample through getStaleDeals as a sanity check)
  </read_first>
  <action>
    First harden .gitignore: add entries that exclude raw fixture captures (test/fixtures/**/*.raw.json) and reiterate that no token is committed. Do this BEFORE producing any capture output (RESEARCH Assumption A5 / Open Question 3).
    Create backend/scripts/capture-fixtures.js (run-once, manual, never in CI): reads AGENDOR_TOKEN from env (exit with a clear message if unset), fetches a small page of deals, and writes an ANONYMIZED sample to backend/test/fixtures/real-deals.sample.json keeping ONLY the fields getStaleDeals consumes. The anonymizer MUST strip ALL of the following to synthetic labels, not just emails: owner/author names, organization names, deal titles, phone numbers, CPF/CNPJ, and any email addresses. Replace each with deterministic synthetic labels (e.g. "Owner 1", "Org 1", "Deal 1"); keep only structural non-PII fields (ids reassigned to synthetic ranges, dates, status/stage ids, funnel name if generic). Never write the token or raw payloads to a committed path.
    Produce backend/test/fixtures/real-deals.sample.json. If AGENDOR_TOKEN is available in this environment, run the script to generate it; if not, hand-author a small anonymized sample matching the same shape so the fixture exists and the sanity test can run. Add a sanity assertion (extend the integrated test or add a small test) that feeds real-deals.sample.json through getStaleDeals via fakeAxios and asserts it returns an array without throwing (realism smoke, not a golden id set).

    COMMIT GATING (Blocker fix — the fixture must NOT enter git history before human review):
      - When committing this task per the task-commit protocol, commit ONLY the non-PII artifacts by explicit path: `backend/scripts/capture-fixtures.js`, `.gitignore`, and the test file(s) you changed for the sanity assertion.
      - Do NOT `git add` or commit `backend/test/fixtures/real-deals.sample.json` in this task. Leave it on disk (untracked) so the sanity test can read it locally, but keep it OUT of the commit and OUT of the index. Its commit is performed by Task 4 only after human approval.
      - Explicitly verify before finishing: `git status --porcelain backend/test/fixtures/real-deals.sample.json` shows it as untracked (`??`), and `git log -1 --name-only` for this task's commit does NOT list the fixture.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node --test && node -e "const s=require('./test/fixtures/real-deals.sample.json');const j=JSON.stringify(s);process.exit(/@|gmail|\bToken\b|\d{3}\.?\d{3}\.?\d{3}-?\d{2}|\+?\d[\d ().-]{8,}/i.test(j)?1:0)" && git -C /Users/vitormorija/Automacao_agendor status --porcelain backend/test/fixtures/real-deals.sample.json | grep -q '^??' && echo "fixture uncommitted OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` ignores test/fixtures/**/*.raw.json (raw captures never committed)
    - backend/scripts/capture-fixtures.js reads AGENDOR_TOKEN from env only and never writes it to a committed file
    - The anonymizer strips names, org names, titles, emails, phone, and CPF/CNPJ to synthetic labels (not only `@`/email markers)
    - backend/test/fixtures/real-deals.sample.json exists on disk, is valid JSON, and the guard command finds no email/token/phone/CPF markers (exit 0)
    - The fixture is NOT committed by this task: `git status --porcelain` shows it untracked (`^??`), and it is absent from this task's commit `--name-only`
    - `npm test` remains green with the sanity assertion included
  </acceptance_criteria>
  <done>Capture tooling + .gitignore protection committed; an anonymized real-deal sample exists on disk (uncommitted) and passes a realism smoke through getStaleDeals, awaiting the Task 4 approval gate before it enters git history.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Approve + commit the PII-free / token-free real-deal fixture</name>
  <action>Human-only gate that CONTROLS THE COMMIT of the anonymized fixture. Task 3 left backend/test/fixtures/real-deals.sample.json on disk but UNCOMMITTED. A human confirms it contains only synthetic labels and no AGENDOR_TOKEN (threats T-02-01/T-02-02); only on "approved" does the resume action stage and commit the fixture — so it never enters git history before review. If not approved, the file is scrubbed/regenerated and re-reviewed before any commit. No production code is written in this task.</action>
  <what-built>The anonymized backend/test/fixtures/real-deals.sample.json (currently untracked on disk) and the already-committed capture-fixtures.js script + .gitignore hardening. Because capture uses a live Agendor token and touches real PII (threat T-02-01/T-02-02), and because a committed fixture cannot be cleanly removed later without a git history rewrite, a human must confirm the sample is clean BEFORE it is committed for the first time.</what-built>
  <how-to-verify>
    1. Confirm the fixture is not yet in git: `git status --porcelain backend/test/fixtures/real-deals.sample.json` shows `??` (untracked), and `git log --oneline -- backend/test/fixtures/real-deals.sample.json` returns nothing.
    2. Open backend/test/fixtures/real-deals.sample.json and confirm every name/title/org/email/phone is a synthetic label (e.g. "Deal 1", "Owner 1", "Org 1") — no real customer names, org names, emails, phone numbers, CPF/CNPJ, or sensitive titles.
    3. Confirm no AGENDOR_TOKEN or Authorization value appears anywhere in the sample (`grep -iE 'token|authorization|@|gmail' backend/test/fixtures/real-deals.sample.json` returns nothing sensitive).
    4. If clean, reply "approved" — the resume action will run `git add backend/test/fixtures/real-deals.sample.json && git commit -m "test(01-02): add anonymized real-deal realism fixture"`. Otherwise, list exactly what to scrub; Task 3's anonymizer is corrected and the sample regenerated, then re-review — do NOT commit until clean.
  </how-to-verify>
  <read_first>
    - backend/test/fixtures/real-deals.sample.json
    - .gitignore
  </read_first>
  <acceptance_criteria>
    - Before approval: the fixture is untracked and has zero git history (confirmed via git status/log)
    - Human confirmed zero PII (names/org/title/email/phone/CPF) and zero token in the sample
    - The fixture commit happens ONLY after the explicit "approved" signal — the commit is the resume action of this checkpoint, not an earlier task
  </acceptance_criteria>
  <resume-signal>Type "approved" to stage+commit the fixture, else list exactly what to scrub (no commit until clean).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agendor API → repo (fixture capture) | Live token + real PII cross into a candidate committed artifact |
| test → network | Integrated tests must not hit the real Agendor API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | AGENDOR_TOKEN in capture script | mitigate | Token read from env only; .gitignore excludes token/raw captures; fixture commit deferred behind blocking human-verify (commit is the checkpoint's resume action, not an auto task) |
| T-02-02 | Information Disclosure | PII (names/org/titles/email/phone/CPF) in committed fixture | mitigate | anonymize() strips ALL PII classes to synthetic labels; automated guard grep (email/token/phone/CPF) + human-verify checkpoint that gates the FIRST commit — no git history rewrite needed |
| T-02-03 | Tampering | test hitting live Agendor API | mitigate | mock.method(axios,'create',...) stub installed before require; no real HTTP |
| T-02-04 | Tampering | behavior drift during stage-exclusion extraction | mitigate | byte-for-byte regex copy (RESEARCH Pitfall 1) + Perdão quirk golden test + constrained git diff |
| T-02-05 | Tampering | silent flip of the strict `<` day-boundary cutoff (line 117) | mitigate | day-boundary golden pins exact-cutoff=EXCLUDED / cutoff-1ms=INCLUDED so a `<`→`<=` regression fails the suite |
</threat_model>

<verification>
- `cd backend && npm test` green (pure + integrated + boundary + sanity).
- `node --test test/agendor.pure.test.js` and `test/agendor.getStaleDeals.test.js` each green.
- `git diff backend/src/agendor.js` limited to the two seams (getDealType export, isExcludedStage extract+export+inline call); line 117 comparison UNCHANGED.
- Guard grep finds no PII/token/phone/CPF in real-deals.sample.json.
- The real-deal fixture is committed only after the Task 4 approval (not before).
</verification>

<success_criteria>
TEST-02 met: characterization tests pin getStaleDeals inclusion/exclusion by threshold (incl. the strict `<` day-boundary), category, stage (incl. Perdão quirk), owner, and status-id against current behavior; extraction is provably behavior-preserving; a token-safe anonymized real-deal fixture provides a realism sanity check and enters git history only after human approval.
</success_criteria>

<output>
Create `.planning/phases/01-rede-de-testes-safety-net/01-02-SUMMARY.md` when done.
</output>
