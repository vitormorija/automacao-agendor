---
phase: 01-rede-de-testes-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/.c8rc.json
  - backend/src/db.js
  - backend/test/setup.js
  - backend/test/smoke.test.js
autonomous: false
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "`cd backend && npm test` runs the native node:test runner via the package.json `test` script and exits 0 (green) locally and is callable in CI (D-01 native node:test runner, zero new runtime dep; D-03 `test` script in backend/package.json)"
    - "`npm run test:coverage` produces a report-only coverage report via c8 with no test-framework dependency added (D-02 coverage via c8)"
    - "A test file can require db.js/auth.js against an isolated SQLite without touching backend/agendor.db"
    - "Production db path is byte-identical to before when DB_PATH is unset (D-07 db.js accepts DB_PATH env, default unchanged = backend/agendor.db)"
  artifacts:
    - path: "backend/package.json"
      provides: "test + test:coverage scripts"
      contains: "\"test\": \"node --test\""
    - path: "backend/.c8rc.json"
      provides: "report-only coverage config (no thresholds in Phase 1)"
      contains: "reporter"
    - path: "backend/test/setup.js"
      provides: "presets JWT_SECRET, DB_PATH, AGENDOR_TOKEN before any require"
      contains: "process.env.JWT_SECRET"
    - path: "backend/test/smoke.test.js"
      provides: "trivial passing test proving the runner is wired"
      min_lines: 4
    - path: "backend/src/db.js"
      provides: "DB_PATH env seam, default unchanged"
      contains: "process.env.DB_PATH"
  key_links:
    - from: "backend/test/setup.js"
      to: "backend/src/db.js"
      via: "process.env.DB_PATH read at db.js load time"
      pattern: "process\\.env\\.DB_PATH"
    - from: "backend/package.json scripts.test"
      to: "node:test runner"
      via: "node --test"
      pattern: "node --test"
---

<objective>
Stand up the backend test-runner foundation so every later plan can write and run tests. Wires `npm test` to the built-in `node:test` runner, adds a report-only c8 coverage script (gated by a package-legitimacy checkpoint), introduces the shared `test/setup.js` that presets environment before any module load, and adds the one behavior-preserving seam that setup depends on: a `DB_PATH` env override in `db.js` with an unchanged production default.

Purpose: TEST-01 — a runner exists, `npm test` is green, and the test harness never touches the real `backend/agendor.db`.
Output: `backend/package.json` scripts, `backend/.c8rc.json`, `backend/test/setup.js`, `backend/test/smoke.test.js`, `DB_PATH` seam in `backend/src/db.js`, and `c8@12` devDependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rede-de-testes-safety-net/01-CONTEXT.md
@.planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md
@.planning/phases/01-rede-de-testes-safety-net/01-VALIDATION.md

<interfaces>
<!-- Key facts the executor needs. No codebase exploration required. -->

From backend/src/db.js (line 4 — the ONLY line to change):
  const db = new Database(path.join(__dirname, '..', 'agendor.db'));
  Schema DDL, migrations, and default-config seeding all run at module load, so :memory:/temp-file DBs auto-migrate.

From backend/src/secret.js (import side-effect): throws "JWT_SECRET ausente ou muito curto" if process.env.JWT_SECRET is unset or < 16 chars. Any test that transitively requires auth.js will fail at load unless JWT_SECRET is preset.

Environment: node/npm are NOT on the default PATH. Every test/install command must be prefixed with `export PATH="$HOME/bin:$PATH"` (binaries live in /tmp/node-v22.13.1-darwin-arm64/bin/, wrappers in ~/bin). Node is 22.13.1 (node:test + mock.timers verified working).

node:test discovery (Node 22.13.1): runs any *.test.{js,cjs,mjs} plus loose .js directly under test/. Files under test/ that define NO tests run harmlessly. setup.js must be side-effect-only (no test() calls) so accidental discovery is a no-op.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire node:test runner, shared setup, and DB_PATH seam</name>
  <files>backend/package.json, backend/.c8rc.json, backend/test/setup.js, backend/test/smoke.test.js, backend/src/db.js</files>
  <read_first>
    - backend/package.json (current scripts: only start, dev)
    - backend/src/db.js (line 4 is the seam target; read lines 1-10)
    - backend/src/secret.js (understand the JWT_SECRET fail-fast to size the setup preset)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pattern 3", §"Pitfall 5", §"Code Examples"
    - .planning/codebase/CONVENTIONS.md (CommonJS, single-quotes, 2-space, module.exports single block)
  </read_first>
  <action>
    In backend/package.json add two scripts under "scripts": `test` set to `node --test`, and `test:coverage` set to `c8 --reporter=text --reporter=lcov node --test`. Do not remove existing `start`/`dev`.
    Create backend/.c8rc.json as report-only (all:true; include src/**/*.js; exclude test/** and src/index.js; reporters text and lcov). NO coverage thresholds in Phase 1 (thresholds are Phase 2 per RESEARCH Open Question 2).
    Create backend/test/setup.js as a side-effect-only module (NO test() calls) that, at load, sets process.env.JWT_SECRET to a throwaway 32-char string, process.env.DB_PATH to ':memory:', and process.env.AGENDOR_TOKEN to 'test'. Guard each assignment so it does NOT overwrite a value a test set earlier (only set when unset), so per-file overrides (e.g. dedup temp-file DB_PATH) win. Add a Portuguese comment explaining this file exists only to neutralize import side-effects and must never read the production backend/.env.
    Create backend/test/smoke.test.js that requires './setup' first, then asserts a trivial truth via node:assert/strict inside a single node:test test() — proving the runner discovers and executes.
    Apply the DB_PATH seam to backend/src/db.js line 4 ONLY: introduce `const dbPath = process.env.DB_PATH || path.join(__dirname, '..', 'agendor.db')` and pass dbPath to `new Database(...)`. The default arm must be byte-identical to the current path expression so an unset DB_PATH keeps production behavior unchanged. Change nothing else in db.js.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm test` exits 0 with smoke.test.js reported as passing
    - backend/package.json scripts.test equals `node --test`; scripts.test:coverage references `c8`
    - backend/src/db.js contains `process.env.DB_PATH` and the default arm string `path.join(__dirname, '..', 'agendor.db')` is byte-unchanged
    - `git diff backend/src/db.js` shows only the line-4 seam (one line becomes two); no other db.js lines changed
    - backend/test/setup.js sets JWT_SECRET (>=16 chars), DB_PATH, AGENDOR_TOKEN and defines no test() calls
  </acceptance_criteria>
  <done>`npm test` is green using node:test; setup + DB_PATH seam exist; production db default is byte-preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify c8 package legitimacy before install</name>
  <action>Human-only gate: confirm c8@12 is the legitimate istanbul-team coverage package with no install scripts before authorizing the devDependency install. No code is written in this task; it exists solely to satisfy the package-legitimacy gate (threat T-01-SC) since slopcheck was offline during research.</action>
  <what-built>Nothing yet — this checkpoint gates the only new devDependency (c8@12). slopcheck was offline during research, so c8 is tagged [ASSUMED] and legitimacy must be human-confirmed before install (threat T-01-SC).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/c8 and confirm it is the mainstream istanbul-team coverage tool (repo github.com/bcoe/c8), version 12.x, recently maintained.
    2. Confirm c8 declares no `postinstall`/`preinstall` script (RESEARCH verified empty — re-confirm on the registry page or via `npm view c8 scripts`).
    3. Reply "approved" to authorize `npm install --save-dev c8@12`, or describe concerns to block.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Package Legitimacy Audit"
  </read_first>
  <acceptance_criteria>
    - Human has confirmed c8 on npmjs.com and the absence of install scripts
    - Explicit "approved" (or equivalent) recorded before Task 3 installs
  </acceptance_criteria>
  <resume-signal>Type "approved" to install c8@12, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install c8 devDependency and prove coverage runs</name>
  <files>backend/package.json</files>
  <read_first>
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Installation", §"Environment Availability" (network caveat)
  </read_first>
  <action>
    After the checkpoint approval, run `npm install --save-dev c8@12` in backend/ (requires network; if the environment is offline, surface the blocker clearly and stop — TEST-01's core `npm test` already passes without c8). Confirm c8 lands in backend/package.json devDependencies. Run the coverage script once to prove the wrapper works and produces a report (report-only, no threshold gate).
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - backend/package.json devDependencies includes c8 (^12 or 12.x)
    - `npm run test:coverage` exits 0 and emits a text coverage report
    - No coverage threshold causes failure (report-only)
  </acceptance_criteria>
  <done>c8@12 installed; `npm run test:coverage` green and reporting; TEST-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | Tests must never write to production backend/agendor.db |
| npm registry → dev machine | Installing c8 pulls third-party code |
| test env → production config | setup.js must never read the real backend/.env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install c8@12` | mitigate | Blocking human-verify checkpoint before install; c8 [ASSUMED] (slopcheck offline); confirm registry + no postinstall (RESEARCH audit) |
| T-01-01 | Information Disclosure | test/setup.js JWT_SECRET | mitigate | setup.js sets a throwaway JWT_SECRET only; never reads/overrides production backend/.env; value has no production meaning |
| T-01-02 | Tampering | tests touching real DB | mitigate | setup.js presets DB_PATH=':memory:'; db.js default path byte-preserved only for unset env; tests never rely on default |
</threat_model>

<verification>
- `cd backend && npm test` exits 0 (node:test discovers smoke.test.js).
- `git diff backend/src/db.js` shows only the DB_PATH seam.
- After Task 3: `npm run test:coverage` exits 0 with a coverage report.
- `grep -n "process.env.DB_PATH" backend/src/db.js` returns the seam line.
</verification>

<success_criteria>
TEST-01 met: a runner is configured, `npm test` runs `node --test` and is green locally and CI-callable; coverage is available report-only via `npm run test:coverage`; the DB_PATH seam and setup.js isolate tests from production data without changing production behavior.
</success_criteria>

<output>
Create `.planning/phases/01-rede-de-testes-safety-net/01-01-SUMMARY.md` when done.
</output>
