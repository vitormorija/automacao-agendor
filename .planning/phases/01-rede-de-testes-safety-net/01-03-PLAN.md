---
phase: 01-rede-de-testes-safety-net
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/test/helpers/tmpDb.js
  - backend/test/db.dedup.test.js
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "A deal notified today (status 'sent') makes alreadyNotifiedToday return true"
    - "A deal with no notification row makes alreadyNotifiedToday return false"
    - "A deal last notified yesterday makes alreadyNotifiedToday return false (day-boundary pinned)"
    - "Dedup tests run against a real SQLite temp file, never backend/agendor.db"
  artifacts:
    - path: "backend/test/helpers/tmpDb.js"
      provides: "temp-file DB path + cleanup"
      contains: "tmp"
    - path: "backend/test/db.dedup.test.js"
      provides: "alreadyNotifiedToday characterization incl. day boundary"
      min_lines: 25
  key_links:
    - from: "backend/test/db.dedup.test.js"
      to: "backend/src/db.js alreadyNotifiedToday"
      via: "DB_PATH temp file set BEFORE require('../src/db')"
      pattern: "alreadyNotifiedToday"
    - from: "backend/test/db.dedup.test.js"
      to: "same temp file via a second better-sqlite3 connection"
      via: "seed sent_at=yesterday row"
      pattern: "sent_at"
---

<objective>
Pin the same-day dedup rule (`alreadyNotifiedToday`) with real-SQLite characterization tests. Because `logNotification` hardcodes `sent_at = now` and `:memory:` connections are isolated, the day-boundary case ("notified yesterday -> false") is seeded through a second connection to a shared temp-file DB (RESEARCH Pitfall 4).

Purpose: TEST-03 — guarantee the notification engine never re-sends the same deal twice in one day, and that the day boundary behaves as it does today.
Output: a temp-DB helper and the dedup characterization test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-rede-de-testes-safety-net/01-CONTEXT.md
@.planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md

<interfaces>
<!-- Extracted from backend/src/db.js. Executor uses these directly. -->

alreadyNotifiedToday(deal_id) — line 147: today = new Date().toISOString().split('T')[0]; SELECT id FROM notification_log WHERE deal_id = ? AND sent_at LIKE 'today%' AND status = 'sent'; returns boolean.
logNotification({ deal_id, deal_title, owner_name, owner_email, admin_email, days_stale, status, error, deal_updated_at, deal_type, web_url }) — line 133: INSERTs with sent_at = new Date().toISOString() (always "now"), status defaults to whatever is passed. To create a "sent today" row, pass status:'sent'.
notification_log columns: id, deal_id, deal_title, owner_name, owner_email, admin_email, sent_at (TEXT ISO), days_stale, status (default 'sent'), error, + migrated columns (deal_updated_at, deal_type, web_url, resolved, resolved_at, clicked_at). Schema is created at db.js load, so a fresh temp file auto-migrates.

DB_PATH seam (added in plan 01-01): db.js reads process.env.DB_PATH || default. The db module opens ONE connection at require time. To seed a controlled sent_at, the test must set DB_PATH to a temp FILE before require('../src/db'), then open a SECOND better-sqlite3 connection to the same file and INSERT a row with sent_at = yesterday. (:memory: would give the second connection an empty, isolated DB — RESEARCH Pitfall 4.)

Env: prefix node/npm with `export PATH="$HOME/bin:$PATH"`. better-sqlite3 ^9.6.0 already installed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Temp-file DB helper</name>
  <files>backend/test/helpers/tmpDb.js</files>
  <read_first>
    - backend/src/db.js (line 4 seam, alreadyNotifiedToday, logNotification, notification_log schema)
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pitfall 4", §"Pattern 3"
  </read_first>
  <action>
    Create backend/test/helpers/tmpDb.js (side-effect-only helper, NO test() calls): export a function that returns a unique temp-file path (e.g. under os.tmpdir() with a random suffix) and a cleanup function that deletes the file (and any -journal/-wal siblings) ignoring errors. Add a helper to open a raw better-sqlite3 connection to a given path for direct row seeding. Portuguese comments. This file must define no node:test tests so accidental discovery is harmless.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node -e "const h=require('./test/helpers/tmpDb.js');console.log(typeof h)"</automated>
  </verify>
  <acceptance_criteria>
    - tmpDb.js loads without error and exports a temp-path factory + cleanup + raw-connection opener
    - It contains no node:test test() calls (side-effect-free on discovery)
  </acceptance_criteria>
  <done>A reusable temp-file DB helper exists for seeding controlled sent_at rows.</done>
</task>

<task type="auto">
  <name>Task 2: alreadyNotifiedToday characterization (incl. day boundary)</name>
  <files>backend/test/db.dedup.test.js</files>
  <read_first>
    - backend/src/db.js (alreadyNotifiedToday + logNotification)
    - backend/test/helpers/tmpDb.js
    - .planning/phases/01-rede-de-testes-safety-net/01-RESEARCH.md §"Pitfall 4", §"Per-Requirement Test Map" (TEST-03 row)
  </read_first>
  <action>
    Create backend/test/db.dedup.test.js. Set process.env.DB_PATH to a fresh temp-file path from tmpDb BEFORE require('../src/db') (do NOT rely on setup.js's :memory: for this file — the boundary case needs a real file). Then:
    - Case A (today -> true): call logNotification({..., status:'sent'}) for a deal id, then assert alreadyNotifiedToday(id) === true.
    - Case B (no row -> false): assert alreadyNotifiedToday(<unused id>) === false.
    - Case C (yesterday -> false): open a second better-sqlite3 connection to the same temp file, INSERT a notification_log row for a new deal id with sent_at set to yesterday's ISO timestamp and status 'sent', then assert the db singleton's alreadyNotifiedToday(that id) === false.
    Clean up the temp file in an after() hook. Add Portuguese "documenta comportamento ATUAL" comments, especially on the day-boundary case.
  </action>
  <verify>
    <automated>cd /Users/vitormorija/Automacao_agendor/backend && export PATH="$HOME/bin:$PATH" && node --test test/db.dedup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/db.dedup.test.js` exits 0 with all three cases passing
    - The test sets DB_PATH to a temp file before requiring db.js (grep shows DB_PATH assignment precedes the db require)
    - The yesterday case seeds sent_at via a second connection to the SAME file and asserts false
    - after() removes the temp file; backend/agendor.db is untouched (no rows added to the real DB)
  </acceptance_criteria>
  <done>Dedup rule pinned for today/none/yesterday against a real temp-file SQLite; TEST-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → filesystem | Dedup tests must write only to a temp file, never backend/agendor.db |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | test writing to production DB | mitigate | DB_PATH set to os.tmpdir() temp file before require; after() cleanup; never uses default path |
| T-03-02 | Information Disclosure | seeded rows contain data | accept | Rows use synthetic deal ids/titles only; temp file deleted after run; no real PII involved |
</threat_model>

<verification>
- `cd backend && npm test` green including db.dedup.test.js.
- `node --test test/db.dedup.test.js` exits 0.
- No new rows in backend/agendor.db (test uses a temp file; verify by absence of writes / unchanged mtime is not required but temp path is asserted).
</verification>

<success_criteria>
TEST-03 met: alreadyNotifiedToday is pinned for the same-day (true), no-row (false), and yesterday-boundary (false) cases using real SQLite, isolated from production data.
</success_criteria>

<output>
Create `.planning/phases/01-rede-de-testes-safety-net/01-03-SUMMARY.md` when done.
</output>
