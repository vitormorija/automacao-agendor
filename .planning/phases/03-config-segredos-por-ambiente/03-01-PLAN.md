---
phase: 03-config-segredos-por-ambiente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/config.js
  - backend/src/index.js
  - backend/test/setup.js
  - backend/test/config.validateEnv.test.js
  - backend/test/config.dotenvPath.test.js
autonomous: true
requirements: [CFG-03, CFG-04]

must_haves:
  truths:
    - "Existe um único módulo que declara quais variáveis de ambiente são obrigatórias (D-04)"
    - "A regra de validação é uma função pura, chamável com um objeto literal, sem tocar process.env"
    - "Faltando obrigatória com NODE_ENV=production, validateEnv lança; em qualquer outro ambiente apenas avisa (D-05)"
    - "A mensagem de erro nomeia TODAS as variáveis faltantes de uma vez e diz como obter cada valor"
    - "O dotenv carrega backend/.env por caminho absoluto derivado de __dirname, independente do process.cwd() (D-13, Pitfall 1)"
  artifacts:
    - path: "backend/src/config.js"
      provides: "validateEnv/findMissing/buildMessage + array REQUIRED (D-04/D-05/D-06)"
      exports: ["validateEnv", "findMissing", "buildMessage", "REQUIRED"]
    - path: "backend/test/config.validateEnv.test.js"
      provides: "Cobertura dos dois ramos (production lança / dev avisa) + formato da mensagem"
    - path: "backend/test/config.dotenvPath.test.js"
      provides: "Prova de que o path explícito é cwd-independente e de que o dotenv falha em silêncio sem ele"
  key_links:
    - from: "backend/src/index.js:1"
      to: "backend/.env"
      via: "dotenv com path absoluto por __dirname"
      pattern: "require\\('dotenv'\\)\\.config\\(\\{[^)]*path:"
    - from: "backend/src/config.js"
      to: "backend/src/logger.js"
      via: "logger.warn com tag [Config]"
      pattern: "logger\\.warn\\('\\[Config\\]"
---

<objective>
Criar a fundação de configuração desta fase: o módulo central de validação (`backend/src/config.js`)
com a regra numa **função pura testável**, e corrigir o carregamento do `dotenv` para não depender do
`process.cwd()`.

Purpose: CFG-04 exige boot que falha rápido com mensagem clara; CFG-03 exige separação dev/prod
formalizada por `NODE_ENV`. D-13 tornou obrigatório que a correção do `dotenv` venha **antes** de
qualquer ativação do fail-fast — este plano entrega as duas peças **sem** ligá-las ao boot. A ligação
(`require('./config')` no `index.js`) é o plano 03-02, que só roda depois do checkpoint humano de
verificação do `.env` de produção.

Output: `backend/src/config.js` + 2 arquivos de teste + `index.js:1` corrigido + um preset anexado ao
fim de `backend/test/setup.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-config-segredos-por-ambiente/03-CONTEXT.md
@.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md
@.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md
@.planning/phases/03-config-segredos-por-ambiente/03-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos que o executor deve produzir/consumir. Não explorar o codebase para descobri-los. -->

`backend/src/config.js` — superfície pública exigida:
- `REQUIRED`: array de objetos `{ name, hint }`, constante de módulo em SCREAMING_SNAKE_CASE.
  Os 5 nomes, nesta ordem (D-04): `AGENDOR_TOKEN`, `JWT_SECRET`, `SMTP_PASS`, `ALLOWED_ORIGINS`,
  `ADMIN_USERS`.
- `findMissing(env)` → array dos itens de `REQUIRED` ausentes em `env`. Pura: não lê `process.env`,
  não loga, não lança. Presente-mas-vazio conta como ausente (`String(env[name] ?? '').trim()`).
- `buildMessage(missing)` → string em PT listando **todas** as faltantes, uma por linha, cada uma com
  seu `hint`. Nunca ecoa valores (ASVS V7).
- `validateEnv(env)` → `{ ok: true, missing: [] }` quando nada falta; caso contrário, se
  `env.NODE_ENV === 'production'` **lança** `new Error(buildMessage(missing))`, senão chama
  `logger.warn('[Config] ' + message)` e retorna `{ ok: false, missing: [<nomes>] }`.
- Efeito de boot no fim do arquivo, antes do `module.exports`: `validateEnv(process.env);`
- `module.exports = { validateEnv, findMissing, buildMessage, REQUIRED };`

`backend/src/logger.js` (já existe, consumido aqui):
- `logger.warn(...)`, `logger.info(...)`, `logger.error(...)`, `logger.debug(...)`

`backend/test/helpers/tmpDb.js` (já existe, usado no teste do dotenv só pelo padrão de tmpfile):
- `makeTmpDbPath()` → `{ path, cleanup }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar backend/src/config.js com validateEnv puro + testes dos dois ramos</name>

  <files>
    backend/src/config.js (novo),
    backend/test/config.validateEnv.test.js (novo),
    backend/test/setup.js (APENAS APPEND após a linha 32)
  </files>

  <read_first>
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pattern 1" (linhas 492-567)
      — o código de referência completo de `config.js`, com os comentários em PT já redigidos.
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §"backend/src/config.js"
      (linhas 47-111) — o que copiar de `secret.js` e o que deliberadamente NÃO copiar.
    - `backend/src/secret.js` (16 linhas, arquivo inteiro) — molde do fail-fast em PT com instrução
      de remediação; `throw new Error`, nunca `process.exit`.
    - `backend/src/logger.js` (40 linhas) — shape de módulo sem dependências + `module.exports` único.
    - `backend/test/agendor.pure.test.js` (linhas 1-18) — analog EXATO do arquivo de teste de função
      pura: cabeçalho em PT citando o requisito, `require('./setup')` na primeira linha executável,
      `const { test } = require('node:test')` + `node:assert/strict`.
    - `backend/test/auth.test.js` (linhas 9-26 e 53-62) — uso de `mock` do `node:test`.
    - `backend/test/setup.js` (32 linhas) — ler inteiro para entender os presets, mas **editar
      somente por acréscimo depois da linha 32**.
    - `backend/.c8rc.json` — o gate (`branches: 60`, `all: true`, `include: src/**/*.js`).
  </read_first>

  <behavior>
    - `findMissing({ AGENDOR_TOKEN:'t', JWT_SECRET:'s', SMTP_PASS:'p', ALLOWED_ORIGINS:'o', ADMIN_USERS:'a' })` → `[]`
    - `findMissing({})` → 5 itens, na ordem de `REQUIRED`
    - `findMissing({ ...todas, SMTP_PASS: '   ' })` → 1 item (`SMTP_PASS`) — vazio-com-espaços conta como ausente
    - `findMissing({ ...todas, ADMIN_USERS: undefined })` → 1 item — `?? ''` cobre `undefined` e `null`
    - `buildMessage(findMissing({}))` contém os 5 nomes E as 5 dicas, e NÃO contém nenhum valor de env
    - `validateEnv({ NODE_ENV:'production', ...todas })` → `{ ok: true, missing: [] }`, não lança
    - `validateEnv({ NODE_ENV:'production' })` → **lança** `Error` cuja mensagem cita os 5 nomes de uma vez
    - `validateEnv({ NODE_ENV:'development' })` → NÃO lança, retorna `{ ok:false, missing:[5 nomes] }`
      e chama `logger.warn` exatamente 1 vez com string começando por `[Config] `
    - `validateEnv({})` (sem `NODE_ENV`) → mesmo comportamento de `development` (avisa, não lança) —
      é o ramo em que a suíte e o CI caem, porque `npm run` não define `NODE_ENV`
  </behavior>

  <action>
    Criar `backend/src/config.js` exatamente com a superfície descrita em `<interfaces>`, seguindo
    verbatim o código de referência de 03-RESEARCH.md §Pattern 1 (linhas 506-566), incluindo os
    comentários em PT. Regras não-negociáveis: `require('./logger')` no topo (proibido `console.*`,
    CLAUDE.md); tag `[Config]` entre colchetes no warn; seções demarcadas por
    `// ── Nome ──────────`; `throw new Error(...)`, nunca `process.exit(1)`; `module.exports = {...}`
    único no fim, DEPOIS da chamada `validateEnv(process.env)`.

    As 5 dicas (`hint`) devem dizer COMO obter o valor, não só que ele falta — usar as redigidas em
    03-RESEARCH.md linhas 522-526 (`openssl rand -hex 32` para JWT_SECRET, "painel Agendor ›
    Configurações › API" para AGENDOR_TOKEN, etc.). A mensagem final termina orientando o
    `backend/.env.example` como referência.

    NÃO carregar `dotenv` dentro de `config.js`: a suíte requer este módulo e o `.env` real de
    produção jamais deve ser lido pelos testes (é a razão de existir do `test/setup.js`).
    NÃO adicionar `require('./config')` ao `index.js` — isso é o plano 03-02, que depende do
    checkpoint humano de D-13.

    Criar `backend/test/config.validateEnv.test.js` cobrindo os 8 casos de `<behavior>`. Estrutura:
    cabeçalho em PT explicando que o arquivo protege CFG-03/CFG-04 e D-04/D-05; `require('./setup')`
    primeiro; **antes** de `require('../src/config')`, definir `process.env.SMTP_PASS`,
    `process.env.ALLOWED_ORIGINS` e `process.env.ADMIN_USERS` com valores de teste inertes, para que
    o efeito de boot do módulo caia no ramo `ok: true` e não polua a saída da suíte com um warn
    (`setup.js` força `SMTP_PASS=''` sem guarda, então a atribuição vem DEPOIS do `require('./setup')`).
    Para o ramo `development`, interceptar o warn com `mock.method(logger, 'warn', () => {})` e
    inspecionar `mock.calls`; restaurar com `mock.restoreAll()` no `finally`.

    Anexar ao FIM de `backend/test/setup.js` (após a linha 32, sem tocar nas linhas 1-32 — Pitfall 10:
    re-adicionar a linha 15 no diff faz o job `secrets` barrar o próprio PR desta fase) um bloco
    comentado em PT definindo `process.env.NODE_ENV = 'test'` **sem guarda**, com a justificativa: a
    validação de config só derruba o processo em `production` (D-05), e um `NODE_ENV=production`
    exportado no shell faria `require('../src/config')` lançar e quebrar testes sem relação com
    produção. Seguir o mesmo estilo de comentário do bloco das linhas 26-31.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && npm run test:coverage</automated>
  </verify>

  <acceptance_criteria>
    - `backend/src/config.js` existe e `node -e "const c=require('./backend/src/config'); if(c.REQUIRED.map(r=>r.name).join(',')!=='AGENDOR_TOKEN,JWT_SECRET,SMTP_PASS,ALLOWED_ORIGINS,ADMIN_USERS') process.exit(1)"` sai 0 (executado a partir da raiz, com as 5 vars definidas no shell).
    - `grep -c "console\." backend/src/config.js` retorna `0`.
    - `grep -c "process.exit" backend/src/config.js` retorna `0`.
    - `grep -q "logger.warn('\[Config\]" backend/src/config.js` OU o warn é montado com a string
      `'[Config] '` concatenada — em qualquer forma, `grep -q "\[Config\]" backend/src/config.js` passa.
    - `grep -c "require('dotenv')" backend/src/config.js` retorna `0`.
    - `grep -c "require('./config')" backend/src/index.js` retorna `0` (a ligação é do plano 03-02).
    - `git diff -- backend/test/setup.js | grep -c '^-'` retorna `0` além do cabeçalho `---` do diff,
      ou seja: nenhuma linha existente foi removida/reescrita. Comando exato:
      `git diff -U0 -- backend/test/setup.js | grep -E '^-[^-]' | wc -l` → `0`.
    - `npm run test:coverage` sai 0 e o relatório mostra `config.js` com **100 %** de branches
      (a função pura é integralmente exercitada; o gate global de `branches: 60` não pode regredir).
    - `npm test` continua com os 35 testes anteriores verdes, mais os novos.
  </acceptance_criteria>

  <done>
    `config.js` existe com a função pura, os dois ramos de D-05 estão testados, o gate de cobertura
    segue verde, e nem `index.js` nem as linhas 1-32 de `setup.js` foram tocados.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tornar o carregamento do dotenv determinístico (D-13 / Pitfall 1) + teste</name>

  <files>
    backend/src/index.js (somente a linha 1),
    backend/test/config.dotenvPath.test.js (novo)
  </files>

  <read_first>
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pitfall 1" (linhas 749-787)
      — o mecanismo completo: `cwd: '/opt/agendor'` no `ecosystem.config.js:6` vs `.env` em
      `/opt/agendor/backend/.env`, e o fato de o dotenv retornar `{ error: ENOENT }` sem lançar.
    - `backend/src/index.js` (linhas 1-12) — ponto de inserção; `path` já é requerido na linha 7, por
      isso a linha 1 precisa de `require` inline.
    - `ecosystem.config.js` (arquivo inteiro, ~20 linhas) — confirmar o `cwd` que motiva a correção.
    - `backend/test/helpers/tmpDb.js` (linhas 22-40) — padrão de arquivo temporário + `cleanup`.
    - `backend/test/smoke.test.js` — analog de teste que faz I/O simples.
  </read_first>

  <behavior>
    - `dotenv.config({ path: '<caminho inexistente>' , processEnv: {} })` retorna objeto com
      `error.code === 'ENOENT'` e **não lança** — este é o modo de falha silencioso do Pitfall 1
    - `dotenv.config({ path: <absoluto de um .env temporário>, processEnv: out })` popula `out` mesmo
      com `process.cwd()` apontando para um diretório sem `.env`
    - `process.env` NÃO é poluído quando `processEnv` é passado (dotenv >= 16.4; instalado: 16.6.1)
    - A linha 1 de `backend/src/index.js` casa `/require\('dotenv'\)\.config\(\{[^)]*path:[^)]*__dirname/`
    - `backend/src/index.js` NÃO contém mais `require('dotenv').config()` sem argumentos
  </behavior>

  <action>
    Substituir a linha 1 de `backend/src/index.js` por uma chamada com caminho explícito derivado de
    `__dirname`: `require('dotenv').config({ path: require('path').join(__dirname, '..', '.env') })`.
    Acima dela, um comentário em PT de 3 linhas explicando o *porquê* (padrão do projeto: comentar a
    razão, não a ação): o `.env` mora ao lado do `package.json` do backend; sem o `path` explícito o
    carregamento depende do `cwd`, e sob PM2 (`ecosystem.config.js` define `cwd: '/opt/agendor'`) o
    dotenv procuraria `/opt/agendor/.env`, que não existe, falhando em SILÊNCIO. Citar D-13.

    `require('path')` inline é necessário porque a linha 1 vem antes do `const path = require('path')`
    da linha 7 — não reordenar os requires (o Biome está com `assist` off justamente para não
    reordenar requires).

    Não adicionar nada mais ao `index.js`: o arquivo está em `exclude` do `.c8rc.json` e lógica nova
    ali não é coberta (PATTERNS §riscos, item 2).

    Criar `backend/test/config.dotenvPath.test.js` com três testes cobrindo `<behavior>`:
    (a) mecanismo do silêncio — `dotenv.config` com path inexistente devolve `error.code === 'ENOENT'`
    sem lançar; (b) independência do `cwd` — criar diretório temporário com um `.env` de uma chave
    inerte (ex.: `GSD_DOTENV_PROBE=ok`), fazer `process.chdir(os.tmpdir())` para um diretório
    comprovadamente sem `.env`, carregar pelo caminho absoluto com a opção `processEnv` e afirmar que
    a chave chegou no objeto e que `process.env.GSD_DOTENV_PROBE` continua `undefined`; restaurar o
    `cwd` original e apagar o temporário no `after()`; (c) asserção de fonte — ler
    `backend/src/index.js` com `fs.readFileSync`, afirmar que casa o regex do `<behavior>` e que NÃO
    casa `/require\('dotenv'\)\.config\(\s*\)/`.

    Cabeçalho do arquivo em PT explicando que o teste protege CFG-03/CFG-04 contra a regressão do
    Pitfall 1 — o único achado desta fase capaz de causar indisponibilidade em produção.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && npm run test:coverage</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "require('dotenv')" backend/src/index.js` mostra exatamente 1 ocorrência, na linha
      contendo também `path:` e `__dirname`.
    - `grep -cE "require\('dotenv'\)\.config\(\s*\)" backend/src/index.js` retorna `0`.
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/config.dotenvPath.test.js` sai 0
      com 3 testes verdes.
    - `npm run test:coverage` sai 0 (o gate global não regride; nenhum arquivo novo em `src/`).
    - Nenhuma linha nova de lógica em `backend/src/index.js` além da linha 1 e do comentário:
      `git diff --stat -- backend/src/index.js` mostra no máximo 5 linhas adicionadas.
  </acceptance_criteria>

  <done>
    `index.js:1` carrega o `.env` por caminho absoluto derivado de `__dirname`, o modo de falha
    silencioso está documentado por teste, e o fail-fast continua DESLIGADO (nenhum
    `require('./config')` no boot).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → processo Node | `backend/.env` é lido do disco no boot; conteúdo não versionado e não inspecionável pelo repositório |
| operador → processo Node | O conteúdo do ambiente é definido por humano no deploy; ausência silenciosa é o vetor real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `config.js` mensagem de erro | mitigate | `buildMessage` monta a string a partir de `REQUIRED[].name` e `.hint` — nunca de `env[name]`. Teste afirma que a mensagem contém os nomes e não valores (ASVS V7) |
| T-03-02 | Denial of Service | boot em produção | mitigate | O `throw` NÃO é ligado neste plano (D-13). `index.js` não requer `./config`; a ligação depende do checkpoint humano do plano 03-02 |
| T-03-03 | Information Disclosure | suíte de testes lendo `backend/.env` real | mitigate | `config.js` proibido de requerer `dotenv`; teste do dotenv usa `processEnv` + arquivo temporário, nunca o `.env` do repositório |
| T-03-04 | Spoofing | `ALLOWED_ORIGINS` ausente cai em `localhost` (CORS permissivo em prod) | mitigate | Entra em `REQUIRED` (D-04); a mitigação efetiva chega no plano 03-02 |
| T-03-05 | Elevation of Privilege | `ADMIN_USERS` vazio libera gestão de usuários a qualquer autenticado | transfer | Fechado por *configuração* aqui (entra em `REQUIRED`). O fail-open de `requireAdmin()` no código é SEC-03 / Fase 6 — explicitamente fora do escopo |
| T-03-SC | Tampering | instalação de pacotes | accept | Nenhum pacote npm novo neste plano nem nesta fase (03-RESEARCH.md §Standard Stack). `dotenv` 16.6.1, `node:test` e `c8` já instalados; nenhum `npm install` a executar → `Package Legitimacy Gate` não se aplica |
</threat_model>

<verification>
```bash
export PATH="$HOME/bin:$PATH"
cd backend && npm run lint && npm run test:coverage
# fail-fast ainda DESLIGADO neste plano:
grep -c "require('./config')" src/index.js   # deve retornar 0
# setup.js só cresceu:
cd .. && git diff -U0 -- backend/test/setup.js | grep -E '^-[^-]' | wc -l   # deve retornar 0
```
</verification>

<success_criteria>
- `backend/src/config.js` exporta `validateEnv`, `findMissing`, `buildMessage` e `REQUIRED` com os 5 nomes de D-04.
- Os dois ramos de D-05 (production lança / dev avisa) têm teste próprio e passam.
- `backend/src/index.js:1` carrega o `.env` por caminho absoluto derivado de `__dirname`.
- `npm run test:coverage` sai 0; `config.js` com 100 % de branches; gate global sem regressão.
- Nenhuma linha de `backend/test/setup.js` foi removida ou reescrita (só append).
- `index.js` ainda NÃO requer `./config` — o fail-fast permanece desligado até 03-02.
</success_criteria>

<output>
Create `.planning/phases/03-config-segredos-por-ambiente/03-01-SUMMARY.md` when done
</output>
