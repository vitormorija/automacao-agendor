---
phase: 03-config-segredos-por-ambiente
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db.js
  - backend/src/emailer.js
  - backend/test/db.smtpPassMigration.keep.test.js
  - backend/test/db.smtpPassMigration.clear.test.js
  - backend/test/emailer.smtpPass.test.js
autonomous: true
requirements: [CFG-01]

must_haves:
  truths:
    - "A senha SMTP usada para enviar e-mail vem de process.env.SMTP_PASS, nunca da tabela config (D-01)"
    - "No boot, se SMTP_PASS está presente, a chave smtp_pass do banco é zerada (D-02)"
    - "No boot, se SMTP_PASS está ausente/vazio, a senha antiga do banco é PRESERVADA e um aviso é logado (D-02)"
    - "Depois de zerada, a chave não é re-semeada pelo seeder no boot seguinte (Pitfall 3)"
  artifacts:
    - path: "backend/src/db.js"
      provides: "Migração idempotente de smtp_pass + remoção da chave do objeto defaults"
      contains: "smtp_pass"
    - path: "backend/src/emailer.js"
      provides: "createTransporter lendo a senha do ambiente"
      contains: "process.env.SMTP_PASS"
    - path: "backend/test/db.smtpPassMigration.keep.test.js"
      provides: "Ramo defensivo de D-02 (env ausente → preserva)"
    - path: "backend/test/db.smtpPassMigration.clear.test.js"
      provides: "Ramo ativo de D-02 (env presente → zera) + regressão do re-seed"
    - path: "backend/test/emailer.smtpPass.test.js"
      provides: "Prova de que o transporte usa o env e não o banco"
  key_links:
    - from: "backend/src/emailer.js"
      to: "process.env.SMTP_PASS"
      via: "auth.pass do createTransport"
      pattern: "pass:\\s*\\(process\\.env\\.SMTP_PASS"
    - from: "backend/src/db.js"
      to: "config.smtp_pass"
      via: "setConfig('smtp_pass', '') condicional no boot"
      pattern: "setConfig\\('smtp_pass'"
---

<objective>
Tirar a senha SMTP do SQLite: `emailer.js` passa a lê-la de `process.env.SMTP_PASS` e `db.js` ganha
uma migração defensiva e idempotente que zera a chave antiga — **só quando o ambiente realmente tem
a senha**.

Purpose: fecha o núcleo de CFG-01. `smtp_pass` é o único valor da tabela `config` que é de fato um
segredo, e `deploy/backup.sh` copia o `.db` inteiro — a senha ia parar em até 30 cópias diárias em
disco. D-01/D-02 mudam comportamento, e a constraint do projeto exige teste cobrindo o novo fluxo:
os três testes deste plano são obrigatórios, não opcionais.

Output: `db.js` e `emailer.js` alterados + 3 arquivos de teste novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-config-segredos-por-ambiente/03-CONTEXT.md
@.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md
@.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md
@.planning/phases/03-config-segredos-por-ambiente/03-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos existentes que o executor consome. Não explorar o codebase para descobri-los. -->

`backend/src/db.js` (já existe):
- `getConfig(key)` → `string | null` (definida na linha 126, com hoisting disponível acima)
- `setConfig(key, value)` → grava via `INSERT OR REPLACE`
- `getAllConfig()` → objeto chave→valor
- `closeDb()` → fecha o singleton
- Tabela: `config (key TEXT PRIMARY KEY, value TEXT NOT NULL)`
- Caminho do banco: `process.env.DB_PATH` lido no load do módulo (seam 01-01)

`backend/test/helpers/tmpDb.js` (já existe):
- `makeTmpDbPath()` → `{ path, cleanup }`
- `openRaw(dbPath)` → conexão `better-sqlite3` crua para o MESMO arquivo

`backend/src/emailer.js` (já existe):
- `createTransporter()` — privada, não exportada; monta o `nodemailer.createTransport({ host, port, secure, auth: { user, pass } })`
- `verifySmtp()` e o handler de `POST /api/config/test-smtp` passam por `createTransporter()` e
  herdam a mudança automaticamente — nada a fazer neles
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migração idempotente de smtp_pass em db.js + os dois testes de ramo</name>

  <files>
    backend/src/db.js,
    backend/test/db.smtpPassMigration.keep.test.js (novo),
    backend/test/db.smtpPassMigration.clear.test.js (novo)
  </files>

  <read_first>
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §Q4 (linhas 654-743) — a
      tabela dos **4 pontos de toque**, o código da migração já redigido (linhas 680-710) e a tabela
      de preparação/asserção dos testes (linhas 730-736).
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pitfall 3" (linhas 802-816)
      — por que remover do `defaults` E zerar (não deletar) são as duas metades da mesma correção.
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §"backend/src/db.js"
      (linhas 161-238) — o padrão de migração idempotente com `catch (_) {}` comentado, o objeto
      `defaults` e o loop de seed, verbatim.
    - `backend/src/db.js` linhas 1-140 — o topo do arquivo (requires), o DDL da tabela `config`
      (linhas 8-11), o `defaults` (103-115), o loop de seed (117-124) e `getConfig`/`setConfig`
      (126-136).
    - `backend/test/db.dedup.test.js` linhas 1-40 e 62-82 — analog EXATO: `DB_PATH` em arquivo
      temporário definido ANTES do `require('./setup')`, `require('../src/db')` depois, `after()` com
      `closeDb()` + `cleanup()`, e seed por segunda conexão com `openRaw`.
    - `backend/test/setup.js` linhas 26-32 — `process.env.SMTP_PASS = ''` é **sem guarda**.
    - `backend/src/logger.js` — `db.js` ainda não requer o logger; adicionar no topo é seguro
      (logger só depende de `process.env`, não há ciclo).
  </read_first>

  <behavior>
    - Ramo PRESERVAR (`keep`): banco semeado com `config.smtp_pass = 'senha-antiga'`, `SMTP_PASS`
      ausente/vazio no ambiente → após o load de `db.js`, `getConfig('smtp_pass') === 'senha-antiga'`
      e `logger.warn` foi chamado
    - Ramo ZERAR (`clear`): banco semeado com `config.smtp_pass = 'senha-antiga'`,
      `process.env.SMTP_PASS = 'senha-do-env'` → após o load, `getConfig('smtp_pass') === ''`
    - Ramo ZERAR, segundo boot: fechar a conexão, limpar o `require.cache` de `../src/db`, requerer de
      novo o MESMO arquivo → `getConfig('smtp_pass') === ''` (o seeder NÃO re-inseriu a senha do env)
    - `getAllConfig()` continua devolvendo a chave `smtp_pass` (zerada, não removida) — o
      mascaramento do GET em `routes/config.js:38` continua correto sem alteração
    - Banco recém-criado, sem a chave: a migração não lança e não cria a chave
  </behavior>

  <action>
    Em `backend/src/db.js`:
    (a) Adicionar `const logger = require('./logger');` ao bloco de requires do topo, solto, sem
    separação por grupos (padrão do projeto).
    (b) **Remover a linha 110** (`smtp_pass: process.env.SMTP_PASS || '',`) do objeto `defaults`.
    Sem isso, um `DELETE` da linha ou um banco recriado faz o seeder re-inserir a senha do ambiente
    no SQLite no próximo boot, desfazendo a migração em silêncio (Pitfall 3).
    (c) Imediatamente **depois** do loop de seed (que termina na linha 124) — nunca antes, porque a
    chave pode ainda não existir — inserir a migração de D-02, exatamente como redigida em
    03-RESEARCH.md linhas 680-709, incluindo o cabeçalho `// ── Migração: a senha SMTP sai do banco
    (D-01/D-02, CFG-01) ──` e o comentário que documenta a **exceção deliberada** ao padrão "toda
    config runtime mora na tabela config" (o CONTEXT.md exige que essa exceção seja documentada no
    código, não implícita). Lógica: `envPass = (process.env.SMTP_PASS || '').trim()`,
    `dbPass = getConfig('smtp_pass')`; se `envPass && dbPass` → `setConfig('smtp_pass', '')` +
    `logger.info('[DB] ...')`; se `!envPass && dbPass` → `logger.warn('[DB] SMTP_PASS ausente ...
    senha antiga PRESERVADA ...')`. Envolver num `try { } catch (_) { /* banco recém-criado ou chave
    inexistente — nada a migrar */ }`, seguindo o padrão de silêncio justificado das linhas 79-94.
    Zerar (`setConfig(k, '')`) e não `DELETE`: a linha continua existindo, então nem o seeder nem
    nada mais a recria.

    Criar `backend/test/db.smtpPassMigration.keep.test.js`. Preâmbulo copiado quase literalmente de
    `db.dedup.test.js:12-32`: `makeTmpDbPath()` → `process.env.DB_PATH = ...` **ANTES** do
    `require('./setup')` (o preset de `DB_PATH` é guardado, então o temporário vence); NÃO definir
    `SMTP_PASS` (o `setup.js` já força `''`). Antes de `require('../src/db')`, abrir `openRaw(DB_PATH)`
    e criar a tabela com o DDL exato (`CREATE TABLE IF NOT EXISTS config (key TEXT PRIMARY KEY, value
    TEXT NOT NULL)`) e inserir `('smtp_pass','senha-antiga')`, fechando a conexão de seed no
    `finally`. Só então `require('../src/db')`. Asserção: `getConfig('smtp_pass') === 'senha-antiga'`.
    `after()` com `db.closeDb()` + `cleanup()`.

    Criar `backend/test/db.smtpPassMigration.clear.test.js` com o mesmo preâmbulo, **mas** com
    `process.env.SMTP_PASS = 'senha-do-env'` colocado **DEPOIS** do `require('./setup')` e **ANTES**
    do `require('../src/db')`. Comentar essa inversão de ordem no arquivo: `DB_PATH` é preset
    guardado (vem antes do setup) e `SMTP_PASS` é sobrescrito sem guarda em `setup.js:31` (tem que vir
    depois) — inverter faz o teste passar no ramo errado, que é o pior resultado possível.
    Dois testes no arquivo: (1) `getConfig('smtp_pass') === ''`; (2) regressão do re-seed —
    `db.closeDb()`, `delete require.cache[require.resolve('../src/db')]`, re-`require('../src/db')`
    apontando para o mesmo arquivo, afirmar `getConfig('smtp_pass') === ''` e que a chave ainda
    aparece em `getAllConfig()`.

    Cabeçalho em PT nos dois arquivos, no padrão de `agendor.pure.test.js`, citando CFG-01 e D-02 e
    dizendo o que o teste protege. Proibido `console.*` em código novo.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && npm run test:coverage</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "smtp_pass" backend/src/db.js` NÃO mostra nenhuma linha dentro do objeto `defaults`
      (entre `const defaults = {` e o `};` que o fecha); mostra apenas ocorrências no bloco da migração.
    - `grep -c "process.env.SMTP_PASS" backend/src/db.js` retorna `1` (só dentro da migração).
    - `grep -q "require('./logger')" backend/src/db.js` passa.
    - `grep -c "console\." backend/src/db.js` não aumentou em relação ao `HEAD`
      (`git show HEAD:backend/src/db.js | grep -c 'console\.'` == valor atual).
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/db.smtpPassMigration.keep.test.js`
      sai 0.
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/db.smtpPassMigration.clear.test.js`
      sai 0 com 2 testes.
    - Em `clear.test.js`, a atribuição de `process.env.SMTP_PASS` aparece **depois** da linha
      `require('./setup')`: verificável com
      `awk '/require\(.\.\/setup.\)/{s=NR} /process.env.SMTP_PASS *=/{if(NR<s) exit 1}' backend/test/db.smtpPassMigration.clear.test.js`.
    - `npm run test:coverage` sai 0 — as ~4 branches novas de `db.js` estão cobertas pelos dois
      arquivos e o gate global (`branches: 60`, folga de ~10) não regride.
    - `backend/agendor.db` intocado: `git status --porcelain backend/agendor.db` vazio.
  </acceptance_criteria>

  <done>
    `smtp_pass` saiu do `defaults`, a migração condicional roda depois do seed, e os dois ramos de
    D-02 mais a regressão do re-seed estão fixados por teste.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: emailer.js lê a senha de process.env.SMTP_PASS + teste da borda nodemailer</name>

  <files>
    backend/src/emailer.js,
    backend/test/emailer.smtpPass.test.js (novo)
  </files>

  <read_first>
    - `backend/src/emailer.js` linhas 1-40 — `createTransporter()` (linhas 5-15, `pass:` na linha 12)
      e `getPublicBaseUrl()` (linhas 23-40), que é o analog local de "helper privado que lê
      `process.env` com comentário explicando o *porquê*", incluindo a normalização
      `(process.env.X || '').trim()`.
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §"backend/src/emailer.js"
      (linhas 241-282) — o ponto exato e o molde do comentário.
    - `backend/test/helpers/fakeAxios.js` (arquivo inteiro, 17 linhas) — padrão de stub de borda
      externa com `mock.method`, e o formato de cabeçalho "Helper de teste (NÃO define testes)".
    - `backend/test/agendor.getStaleDeals.test.js` linhas 23-43 — ordem de instalação do stub e o
      comentário que a explica.
    - `backend/test/setup.js` linhas 26-32 — `SMTP_PASS` é forçado a `''` sem guarda.
  </read_first>

  <behavior>
    - Com `process.env.SMTP_PASS = 'env-pass'` e `config.smtp_pass = 'db-pass'` no banco, as opções
      passadas a `nodemailer.createTransport` têm `auth.pass === 'env-pass'`
    - E, explicitamente, `auth.pass !== 'db-pass'` — a asserção negativa é o que prova D-01
    - `auth.user`, `host` e `port` continuam vindo da tabela `config` (o híbrido de D-01 não é
      "tudo no ambiente")
    - Com `SMTP_PASS` ausente, `auth.pass` é `''` (string vazia normalizada), não `undefined` nem o
      valor do banco
  </behavior>

  <action>
    Em `backend/src/emailer.js`, trocar a linha 12 de `pass: getConfig('smtp_pass'),` para
    `pass: (process.env.SMTP_PASS || '').trim(),` — mesma normalização usada em
    `getPublicBaseUrl()` (linha 28). Acima de `createTransporter()`, um comentário em PT de 4-5 linhas
    no estilo de `getPublicBaseUrl()` (explica o *porquê*, não o *quê*): esta é a **exceção
    deliberada** ao padrão "toda config runtime vem da tabela `config`" — host/porta/usuário/remetente
    continuam no banco e editáveis pela UI, mas a senha é o único valor aqui que é de fato um segredo
    e `deploy/backup.sh` copia o `.db` inteiro para até 30 cópias diárias. Citar D-01 / CFG-01.

    Não alterar `verifySmtp()` nem nada relacionado a `POST /api/config/test-smtp`: passam por
    `createTransporter()` e herdam a mudança.

    Criar `backend/test/emailer.smtpPass.test.js`. `require('./setup')` primeiro; depois definir
    `process.env.SMTP_PASS = 'env-pass'` (obrigatoriamente DEPOIS do setup, que zera sem guarda);
    requerer `../src/db` e semear `setConfig('smtp_pass', 'db-pass')` mais `smtp_host`/`smtp_port`/
    `smtp_user` com valores inertes (`DB_PATH` fica no `:memory:` do setup — não é preciso arquivo
    temporário aqui, porque não há segunda conexão). Instalar o stub com
    `mock.method(nodemailer, 'createTransport', (opts) => { captured = opts; return { sendMail: async () => ({}) , verify: async () => true }; })`
    — `emailer.js` requer `nodemailer` no load mas só chama `createTransport()` dentro de
    `createTransporter()`, então o stub pode ser instalado depois do `require`, folga que o caso do
    axios não tinha.

    `createTransporter()` não é exportada. Disparar a chamada pelo caminho público mais barato:
    invocar `verifySmtp()` do `emailer` (que chama `createTransporter()` e depois `verify()` no objeto
    stubbado). Se isso se mostrar frágil ao ler o código, a alternativa é anexar um seam de teste em
    `emailer.js` no padrão de `routes/auth.js:359-369` (`module.exports.createTransporter =
    createTransporter;`) com o mesmo comentário "Seams de teste (…) expostos sem alterar o contrato".
    Escolher uma das duas e justificar no cabeçalho do teste.

    Restaurar os mocks com `mock.restoreAll()` no `after()`. Cabeçalho em PT citando CFG-01 e D-01.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && npm run test:coverage</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "getConfig('smtp_pass')" backend/src/emailer.js` retorna vazio (exit 1).
    - `grep -q "process.env.SMTP_PASS" backend/src/emailer.js` passa e a ocorrência está dentro de
      `createTransporter()`.
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/emailer.smtpPass.test.js` sai 0.
    - O teste contém uma asserção negativa explícita comparando com `'db-pass'`
      (`grep -q "db-pass" backend/test/emailer.smtpPass.test.js`).
    - `host`, `port` e `user` continuam vindo de `getConfig`:
      `grep -c "getConfig('smtp_host')\|getConfig('smtp_port')\|getConfig('smtp_user')" backend/src/emailer.js` >= 3.
    - `npm run test:coverage` sai 0; suíte inteira verde.
  </acceptance_criteria>

  <done>
    O transporte SMTP usa a senha do ambiente e a asserção negativa contra o valor do banco está no
    teste — D-01 provado, não presumido.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → backup em disco | `deploy/backup.sh` copia `backend/agendor.db` inteiro; até 30 cópias em `/opt/agendor/backups` |
| ambiente do processo → transporte SMTP | A credencial cruza aqui; é o novo caminho único |
| tabela `config` (editável pela UI) → uso operacional | Continua sendo fonte de host/porta/usuário — superfície mantida de propósito |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SMTP-01 | Information Disclosure | `config.smtp_pass` em texto puro no `.db` e nos backups | mitigate | D-01/D-02: a senha sai do banco; migração zera a chave quando o env tem o valor. **Resíduo aceito:** os backups já existentes continuam com a senha antiga — só a rotação da senha SMTP resolve; registrar no SUMMARY |
| T-03-SMTP-02 | Tampering | seeder re-inserindo a senha do env no banco no boot seguinte | mitigate | Remover `smtp_pass` do objeto `defaults` (db.js:110) **e** zerar em vez de deletar; teste de regressão explícito no `.clear.test.js` (Pitfall 3) |
| T-03-SMTP-03 | Denial of Service | envio de e-mail parar por `.env` incompleto no deploy | mitigate | D-02 defensivo: a migração só zera se `SMTP_PASS` estiver presente e não-vazio; caso contrário preserva e loga `warn`. Ramo coberto pelo `.keep.test.js` |
| T-03-SMTP-04 | Information Disclosure | senha vazando para o SQLite de teste a partir do shell/CI | mitigate | `backend/test/setup.js:31` já força `SMTP_PASS=''` sem guarda; os testes que precisam de valor o definem DEPOIS do setup, com valor descartável |
| T-03-SMTP-05 | Information Disclosure | senha aparecendo no `GET /api/config` | accept | `routes/config.js:38` já mascara; com o valor zerado o ternário devolve `''`. Mexer ali é risco sem ganho |
| T-03-SC | Tampering | instalação de pacotes | accept | Nenhum pacote npm novo neste plano; `nodemailer` e `better-sqlite3` já são dependências |
</threat_model>

<verification>
```bash
export PATH="$HOME/bin:$PATH"
cd backend
npm run lint && npm run test:coverage
node --test test/db.smtpPassMigration.keep.test.js test/db.smtpPassMigration.clear.test.js test/emailer.smtpPass.test.js
grep -n "smtp_pass" src/db.js src/emailer.js
```
</verification>

<success_criteria>
- `emailer.js` não chama mais `getConfig('smtp_pass')`; a senha vem de `process.env.SMTP_PASS`.
- `smtp_pass` não está mais no objeto `defaults` de `db.js`.
- A migração de D-02 roda depois do seed, é idempotente e tem os dois ramos cobertos por arquivos de
  teste separados (um processo por variação de ambiente).
- O teste de regressão prova que o seeder não re-insere a senha no boot seguinte.
- `npm run test:coverage` verde; `backend/agendor.db` intocado.
</success_criteria>

<output>
Create `.planning/phases/03-config-segredos-por-ambiente/03-03-SUMMARY.md` when done
</output>
