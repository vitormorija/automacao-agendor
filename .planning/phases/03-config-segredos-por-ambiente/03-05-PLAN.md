---
phase: 03-config-segredos-por-ambiente
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env.example
  - backend/test/envExample.test.js
  - README.md
autonomous: true
requirements: [CFG-02, CFG-03]

must_haves:
  truths:
    - "Toda variável lida via process.env em backend/src/ está documentada no .env.example (CFG-02)"
    - "O .env.example não documenta nenhuma variável que o código não lê (STALE_DAYS sai — D-12)"
    - "O .env.example marca explicitamente quais variáveis são obrigatórias e o que muda entre dev e prod (D-07/D-10)"
    - "Nenhum placeholder do .env.example tem entropia alta a ponto de disparar o gitleaks (Pitfall 9)"
    - "A drift entre código e .env.example passa a ser detectada por teste, não por revisão manual"
  artifacts:
    - path: "backend/.env.example"
      provides: "Contrato de configuração: blocos OBRIGATÓRIAS/OPCIONAIS com notas dev vs prod"
      contains: "BASE_URL_FRONTEND"
    - path: "backend/test/envExample.test.js"
      provides: "Meta-teste anti-drift src/ ↔ .env.example"
    - path: "README.md"
      provides: "Tabela de variáveis coerente com o .env.example corrigido"
  key_links:
    - from: "backend/test/envExample.test.js"
      to: "backend/.env.example"
      via: "leitura do arquivo + comparação com o grep de process.env em src/"
      pattern: "\\.env\\.example"
---

<objective>
Completar o contrato de configuração: `.env.example` passa a documentar exatamente as variáveis que o
código lê — nem mais, nem menos — com marcação de obrigatoriedade (D-04) e de diferença dev vs prod
(D-07), e um meta-teste impede que ele volte a divergir.

Purpose: fecha CFG-02 e a metade documental de CFG-03. Hoje faltam `DB_PATH`, `LOG_LEVEL` e
`BASE_URL_FRONTEND`, e sobra `STALE_DAYS` — variável fantasma que nenhum código lê (o valor efetivo
vem da tabela `config`). Foi revisão manual que produziu esse estado; o meta-teste é o que torna
CFG-02 permanente em vez de pontual.

Output: `.env.example` reorganizado, `backend/test/envExample.test.js` novo, tabela do README
sincronizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-config-segredos-por-ambiente/03-CONTEXT.md
@.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md
@.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Inventário fechado das 18 variáveis lidas em backend/src/ (03-RESEARCH.md §Q5, grep exaustivo). -->

OBRIGATÓRIAS (D-04 — boot aborta em produção se faltar):
`AGENDOR_TOKEN`, `JWT_SECRET`, `SMTP_PASS`, `ALLOWED_ORIGINS`, `ADMIN_USERS`

OPCIONAIS (default sensato no código):
`NODE_ENV` (default `development`), `PORT` (3001), `LOG_LEVEL` (`info`) [FALTA hoje],
`DB_PATH` (`backend/agendor.db`) [FALTA hoje], `BASE_URL` (`''`),
`BASE_URL_FRONTEND` (`http://localhost:5173`) [FALTA hoje], `ADMIN_EMAIL` (`''`),
`SMTP_HOST` (`smtp.gmail.com`), `SMTP_PORT` (`587`), `SMTP_USER` (`''`), `SMTP_FROM` (`''`),
`SEED_ADMIN_EMAIL` (`''`), `SEED_ADMIN_PASSWORD` (`''`)

FANTASMA (remover — D-12): `STALE_DAYS` — nenhum `process.env.STALE_DAYS` existe no código; o valor
efetivo é `getConfig('stale_days')`, default literal `'15'` em `db.js:105`, editável pela UI.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorganizar o .env.example (D-07/D-10/D-12) + meta-teste anti-drift</name>

  <files>
    backend/.env.example,
    backend/test/envExample.test.js (novo)
  </files>

  <read_first>
    - `backend/.env.example` (41 linhas, arquivo inteiro) — a convenção já presente: seções
      `# ── Nome ───`, marcador `# OBRIGATÓRIO:`, instrução de geração (`openssl rand -hex 32`) e
      notas dev vs prod nas linhas 22-25. Reorganizar preservando esse estilo, não inventar outro.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §Q5 (linhas 432-471) — a
      tabela das 18 variáveis com arquivo:linha e default, e os achados extras (fantasma
      `STALE_DAYS`, README desatualizado).
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"`.env.example` — comentários
      dev vs prod" (linhas 1052-1075) — o bloco já redigido e **verificado como não-disparador do
      gitleaks**.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pitfall 9" (linhas 916-937)
      — por que placeholders realistas de alta entropia barram o próprio PR desta fase, e quais
      placeholders foram testados em sandbox.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Meta-teste anti-drift"
      (linhas 1008-1050) — o código do teste, pronto, com o `walk()` recursivo.
    - `backend/test/agendor.pure.test.js` linhas 1-18 — cabeçalho e convenções de arquivo de teste.
  </read_first>

  <behavior>
    - Para todo `process.env.X` que apareça em qualquer `.js` sob `backend/src/` (recursivo), existe
      uma linha `X=` no `backend/.env.example` → o conjunto de faltantes é `[]`
    - Nenhuma variável documentada no `.env.example` deixa de ser lida pelo código: o conjunto
      inverso também é `[]` (com allowlist explícita e vazia — se alguém quiser manter uma variável
      só documental, tem que declarar a exceção no próprio teste, o que torna a decisão visível)
    - Nenhum valor do `.env.example` casa o padrão de segredo realista
      `[A-Za-z0-9/+_-]{24,}` — guarda contra reintroduzir o erro do commit `13905d4`
  </behavior>

  <action>
    Reescrever `backend/.env.example` em três blocos, preservando as seções `# ── Nome ───` e o
    tom em PT já usados no arquivo:

    1. `# ── Ambiente ──` com `NODE_ENV`, e um comentário de 2 linhas explicando o que `development`
       e `production` mudam de fato (fail-fast vira aviso vs aborta; log em texto vs JSON; serving
       do `frontend/dist`). É a formalização de D-07: um `.env` único por máquina, `NODE_ENV` decide.
    2. `# ── OBRIGATÓRIAS (o boot aborta em produção se faltar) ──` com as 5 de D-04, cada uma com o
       comentário de fim de linha dizendo COMO obter o valor (mesmo texto das `hint` de
       `backend/src/config.js`) e, quando aplicável, a nota `[dev: ...]`.
    3. `# ── OPCIONAIS (têm default sensato no código) ──` com as 13 restantes, cada uma anotando o
       default e a diferença dev/prod quando houver. **Adicionar** `LOG_LEVEL`, `DB_PATH` e
       `BASE_URL_FRONTEND` (as 3 faltantes de D-10). **Remover** `STALE_DAYS` inteiro (D-12) — nenhum
       código lê essa variável e documentá-la contraria CFG-02.

    Usar como base literal o bloco de 03-RESEARCH.md linhas 1054-1074, que foi commitado num repo
    sandbox e escaneado pelo gitleaks 8.30.1 com resultado *no leaks found*. Placeholders em
    português, óbvios e de baixa entropia — nunca valores realistas (Pitfall 9). Manter
    `JWT_SECRET=troque-por-um-segredo-forte-e-aleatorio` como está.

    Criar `backend/test/envExample.test.js` a partir do código de 03-RESEARCH.md linhas 1010-1047,
    adaptando o cabeçalho ao padrão de `agendor.pure.test.js` (em PT, dizendo que substitui a revisão
    manual que deixou 3 variáveis de fora e 1 fantasma dentro, e citando CFG-02). Três testes,
    conforme `<behavior>`: faltantes, sobrantes (com `const SOMENTE_DOCUMENTAIS = []` explícito e
    comentado) e entropia dos placeholders. Usar `fs`/`path` nativos e `require('./setup')` na
    primeira linha executável. Nada de `console.*`.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && npm run test:coverage && node --test test/envExample.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^STALE_DAYS=" backend/.env.example` retorna `0`.
    - `grep -c "^LOG_LEVEL=" backend/.env.example` retorna `1`; idem `^DB_PATH=` e `^BASE_URL_FRONTEND=`.
    - `grep -cE "^[A-Z0-9_]+=" backend/.env.example` retorna `18`.
    - `grep -q "OBRIGATÓRIAS" backend/.env.example` e `grep -q "OPCIONAIS" backend/.env.example` passam.
    - `grep -cE "^[A-Z0-9_]+=[A-Za-z0-9/+_-]{24,}$" backend/.env.example` retorna `0` (nenhum
      placeholder com cara de segredo real — Pitfall 9).
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/envExample.test.js` sai 0 com
      3 testes verdes.
    - `npm run test:coverage` sai 0; nenhum arquivo novo em `src/`.
  </acceptance_criteria>

  <done>
    O `.env.example` documenta as 18 variáveis reais com marcação de obrigatoriedade e dev/prod, sem
    fantasma e sem placeholder que dispare o gitleaks — e um teste impede a regressão.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar a tabela de variáveis do README com o .env.example</name>

  <files>README.md</files>

  <read_first>
    - `README.md` linhas 70-95 — a tabela atual, que lista `STALE_DAYS` e omite `DB_PATH` e
      `BASE_URL_FRONTEND`, e marca `SMTP_HOST/PORT/USER/PASS/FROM` como um bloco único obrigatório.
    - `backend/.env.example` (após a Task 1) — a fonte de verdade a espelhar.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §Q5, tabela de achados
      adicionais (linhas 460-467) — "deixar o README contradizendo o `.env.example` recém-corrigido é
      dívida imediata".
  </read_first>

  <action>
    Atualizar a tabela de variáveis do `README.md` (linhas 77-95) para espelhar o `.env.example`:
    remover a linha de `STALE_DAYS`; acrescentar `DB_PATH` e `BASE_URL_FRONTEND` como opcionais com
    seus defaults; ajustar a coluna "Obrigatória" para refletir D-04 — apenas `AGENDOR_TOKEN`,
    `JWT_SECRET`, `SMTP_PASS`, `ALLOWED_ORIGINS` e `ADMIN_USERS` recebem `✅`, e a linha agrupada de
    SMTP passa a distinguir `SMTP_PASS` (obrigatória, vem do ambiente) dos demais campos SMTP
    (opcionais, editáveis pela UI). Acrescentar uma frase curta abaixo da tabela dizendo que em
    `NODE_ENV=production` a ausência de uma obrigatória **aborta o boot** (CFG-04) e que a senha SMTP
    **não** é mais editável pelo painel (D-01/D-03).

    Manter o formato de tabela markdown já existente. Não reescrever outras seções do README —
    DOC-01 é a Fase 8; aqui só se corrige a contradição criada por esta fase.
  </action>

  <verify>
    <automated>test "$(grep -c 'STALE_DAYS' README.md)" -eq 0 && grep -q 'BASE_URL_FRONTEND' README.md && grep -q 'DB_PATH' README.md && test "$(grep -cE '^\|[^|]*(AGENDOR_TOKEN|JWT_SECRET|SMTP_PASS|ALLOWED_ORIGINS|ADMIN_USERS)[^|]*\|[^|]*✅' README.md)" -eq 5 && test "$(grep -cE '^\|.*✅' README.md)" -eq 5 && echo "OK README sincronizado"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "STALE_DAYS" README.md` retorna `0`.
    - `grep -c "BASE_URL_FRONTEND" README.md` retorna `>= 1`; idem `DB_PATH`.
    - A tabela do README marca `✅` **exatamente** para as 5 obrigatórias de D-04. Verificação
      automatizada em duas metades, para pegar tanto falta quanto excesso:
      `grep -cE '^\|[^|]*(AGENDOR_TOKEN|JWT_SECRET|SMTP_PASS|ALLOWED_ORIGINS|ADMIN_USERS)[^|]*\|[^|]*✅' README.md`
      retorna `5` (todas as obrigatórias marcadas) **e**
      `grep -cE '^\|.*✅' README.md` também retorna `5` (nenhuma variável opcional marcada por engano).
      As duas condições juntas provam a igualdade de conjunto; uma sozinha não prova.
    - `git diff --stat -- README.md` mostra alterações confinadas à seção de variáveis.
  </acceptance_criteria>

  <done>
    README e `.env.example` contam a mesma história sobre quais variáveis existem e quais são
    obrigatórias.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório público → leitor anônimo | `.env.example` e `README.md` são versionados e mundialmente legíveis |
| documentação → operador de deploy | Documentação incompleta ou mentirosa produz um `.env` de produção incompleto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information Disclosure | placeholder realista no `.env.example` virando segredo real | mitigate | Placeholders em PT de baixa entropia validados contra o gitleaks em sandbox (Pitfall 9); acceptance criteria com regex `^[A-Z0-9_]+=[A-Za-z0-9/+_-]{24,}$` → 0 ocorrências. É exatamente assim que o token do `13905d4` entrou |
| T-03-10 | Denial of Service | `.env` de produção incompleto por documentação faltante | mitigate | As 3 variáveis ausentes entram; o bloco OBRIGATÓRIAS espelha `REQUIRED` de `config.js`; meta-teste impede nova drift |
| T-03-11 | Repudiation | documentar variável que o código não lê (`STALE_DAYS`) | mitigate | Removida (D-12). O teste inverso, com allowlist vazia e explícita, obriga qualquer exceção futura a ser declarada no código do teste |
| T-03-SC | Tampering | instalação de pacotes | accept | Nenhum pacote npm novo; o meta-teste usa apenas `node:fs`/`node:path` |
</threat_model>

<verification>
```bash
export PATH="$HOME/bin:$PATH"
cd backend && npm run test:coverage && node --test test/envExample.test.js
grep -cE "^[A-Z0-9_]+=" .env.example      # 18
grep -c "^STALE_DAYS=" .env.example       # 0
cd .. && grep -c "STALE_DAYS" README.md   # 0
```
</verification>

<success_criteria>
- `.env.example` tem exatamente 18 atribuições, incluindo `DB_PATH`, `LOG_LEVEL` e
  `BASE_URL_FRONTEND`, e sem `STALE_DAYS`.
- Blocos OBRIGATÓRIAS/OPCIONAIS com notas dev vs prod, no estilo já existente do arquivo.
- `backend/test/envExample.test.js` passa e detectaria drift em qualquer direção.
- Nenhum placeholder dispara o padrão de segredo realista.
- README sincronizado com o `.env.example`.
</success_criteria>

<output>
Create `.planning/phases/03-config-segredos-por-ambiente/03-05-SUMMARY.md` when done
</output>
