---
phase: 03-config-segredos-por-ambiente
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/config.js
  - backend/test/config.route.smtpPass.test.js
  - frontend/src/components/ConfigPanel.jsx
autonomous: true
requirements: [CFG-01]

must_haves:
  truths:
    - "PUT /api/config não grava mais smtp_pass no banco, venha de onde vier o payload (Pitfall 4)"
    - "O formulário de configuração não tem mais campo de senha SMTP; no lugar há uma nota explicando de onde vem o valor (D-03)"
    - "O save() do frontend continua enviando o objeto inteiro sem quebrar — o backend simplesmente ignora a chave"
  artifacts:
    - path: "backend/src/routes/config.js"
      provides: "ALLOWED_KEYS como constante de módulo, sem smtp_pass, exposta como seam de teste"
      exports: ["ALLOWED_KEYS"]
    - path: "backend/test/config.route.smtpPass.test.js"
      provides: "Asserção de que smtp_pass está fora da allowlist do PUT"
    - path: "frontend/src/components/ConfigPanel.jsx"
      provides: "Nota substituindo o campo de senha; sem showPass, sem Eye/EyeOff órfãos"
  key_links:
    - from: "backend/test/config.route.smtpPass.test.js"
      to: "backend/src/routes/config.js"
      via: "seam module.exports.ALLOWED_KEYS"
      pattern: "ALLOWED_KEYS"
---

<objective>
Fechar a superfície de **escrita** da senha SMTP: remover `smtp_pass` da allowlist do
`PUT /api/config` e tirar o campo do formulário, substituindo-o por uma nota que explica de onde o
valor vem.

Purpose: sem isto, a migração de 03-03 se desfaz na primeira vez que alguém salvar a aba
Configurações — o `save()` do frontend envia `{...config}` inteiro, incluindo o `smtp_pass` que veio
do GET (Pitfall 4). Este é o 4º ponto de toque que o CONTEXT.md não listava e o RESEARCH encontrou.

Output: allowlist içada para constante de módulo + seam de teste, 1 arquivo de teste, e o
`ConfigPanel.jsx` limpo (campo, state `showPass` e imports `Eye`/`EyeOff`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-config-segredos-por-ambiente/03-CONTEXT.md
@.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md
@.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos existentes. Não explorar o codebase para descobri-los. -->

`backend/src/routes/config.js` (já existe):
- `module.exports = router` (última linha) — o padrão de seam do repo anexa props ao router
- `VALIDATORS` — mapa chave→validador, já existente no topo (linhas ~14-32)
- Handler do PUT (linha 43) com `const allowed = [...]` local (linhas 44-54); `'smtp_pass'` é a
  linha 51
- Handler do GET (linha 35) mascara: `{ ...config, smtp_pass: config.smtp_pass ? '••••••••' : '' }`
  — **não alterar**

Padrão de seam a copiar — `backend/src/routes/auth.js:359-369`:
`module.exports = router;` seguido de `module.exports.<nome> = <nome>;` com o comentário
"Seams de teste (…): expostos sem alterar o contrato HTTP — app.use() só precisa que module.exports
seja a função router; estas props são ignoradas pelo Express."
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remover smtp_pass da allowlist do PUT, içar ALLOWED_KEYS e fixar por teste</name>

  <files>
    backend/src/routes/config.js,
    backend/test/config.route.smtpPass.test.js (novo)
  </files>

  <read_first>
    - `backend/src/routes/config.js` (arquivo inteiro) — requires do topo (linhas 4-6: `../db`,
      `../scheduler`, `../emailer`), `VALIDATORS`, o GET com mascaramento (34-40), o PUT e sua
      allowlist (43-55), e o `module.exports = router` do fim.
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §"backend/src/routes/config.js"
      (linhas 285-344) — o ponto exato, o que NÃO mexer, e a recomendação concreta do seam
      `ALLOWED_KEYS`.
    - `backend/src/routes/auth.js` linhas 359-369 — o único precedente de seam no repo, com o
      comentário a reusar.
    - `backend/test/auth.test.js` linhas 9-26 — analog do teste que importa um router e desestrutura
      os seams; note o comentário explicando por que `require('./setup')` vem primeiro.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pitfall 4" (linhas 817-826).
  </read_first>

  <behavior>
    - `require('../src/routes/config').ALLOWED_KEYS` é um array e `ALLOWED_KEYS.includes('smtp_pass')`
      é `false`
    - `ALLOWED_KEYS` contém exatamente as 9 chaves restantes: `stale_days`, `admin_email`,
      `notify_author`, `smtp_host`, `smtp_port`, `smtp_user`, `smtp_from`, `cron_schedule`,
      `notifications_enabled`
    - `require('../src/routes/config')` continua sendo uma função (o router do Express) — o seam não
      quebra o contrato de `app.use()`
  </behavior>

  <action>
    Em `backend/src/routes/config.js`: içar o array `allowed` de dentro do handler do PUT para uma
    constante de módulo `const ALLOWED_KEYS = [...]` em SCREAMING_SNAKE_CASE, posicionada acima do
    handler (junto das outras constantes de topo, ao lado de `VALIDATORS`), **sem** a entrada
    `'smtp_pass'`. O corpo do handler passa a iterar `ALLOWED_KEYS`. Acrescentar um comentário curto
    em PT acima da constante explicando por que `smtp_pass` não está ali: a senha saiu do banco
    (D-01) e deixar a chave gravável faria a migração se desfazer na primeira vez que alguém salvasse
    o formulário (CFG-01).

    Anexar o seam no fim do arquivo, depois de `module.exports = router;`:
    `module.exports.ALLOWED_KEYS = ALLOWED_KEYS;` com o comentário no molde de
    `routes/auth.js:359-362`, explicando que props anexadas ao router são ignoradas pelo Express e
    permitem testar sem subir servidor (não existe `supertest` no projeto e não é para introduzir).

    **Não alterar** o mascaramento do GET (linha 38): com o valor zerado o ternário já devolve `''`;
    mexer ali é risco sem ganho. Não adicionar validador novo — remover a chave da allowlist apenas
    **estreita** a superfície (ASVS V5).

    Criar `backend/test/config.route.smtpPass.test.js`: `require('./setup')` na primeira linha
    executável (obrigatório — `routes/config.js` requer `../db`, `../scheduler` e `../emailer` no
    load, e sem o setup abriria o `backend/agendor.db` de produção), depois
    `const configRouter = require('../src/routes/config');` e as asserções de `<behavior>`, incluindo
    `assert.equal(typeof configRouter, 'function')` para provar que o seam não quebrou o router.
    Cabeçalho em PT citando CFG-01, D-01 e o Pitfall 4.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && npm run test:coverage</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "'smtp_pass'" backend/src/routes/config.js` mostra ocorrências **apenas** no GET
      (mascaramento, linha ~38) e/ou em comentários — nenhuma dentro de `ALLOWED_KEYS`.
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/config.route.smtpPass.test.js`
      sai 0, com asserções cobrindo os 3 itens de `<behavior>`.
    - `git diff -U0 -- backend/src/routes/config.js | grep -cE "^-.*safe"` retorna `0` — o
      mascaramento do GET não foi tocado.
    - `npm run test:coverage` sai 0; nenhum arquivo novo em `src/`, então o gate não é pressionado.
  </acceptance_criteria>

  <done>
    A API não aceita mais gravar `smtp_pass`, e isso está fixado por uma asserção de uma linha que
    não depende de HTTP.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remover o campo de senha do ConfigPanel.jsx e pôr a nota de D-03</name>

  <files>frontend/src/components/ConfigPanel.jsx</files>

  <read_first>
    - `frontend/src/components/ConfigPanel.jsx` linhas 1-70 e 210-319 — imports (linha 2:
      `Save, TestTube, Eye, EyeOff`), o bloco de state (14-20, com `showPass` na 18), o `save()`
      (45-63, que envia `{...config}` inteiro), o `<Field label="Senha / App Password">` (241-258) e
      os sub-componentes locais `Section`/`Field` (299-319).
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §"ConfigPanel.jsx"
      (linhas 347-409) — o bloco exato a remover, verbatim, e as classes Tailwind da família a usar.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Nota que substitui o campo"
      (linhas 1077-1089) — o JSX da nota já redigido.
    - `CLAUDE.md` §Code Style — o diretório `frontend/` omite `;` no fim das statements; seguir o
      estilo do arquivo, não impor o do backend.
  </read_first>

  <action>
    Em `frontend/src/components/ConfigPanel.jsx`:
    (a) Remover o `<Field label="Senha / App Password">` inteiro (linhas 241-258, incluindo o
    `<input type={showPass ? 'text' : 'password'}>` e o botão de olho).
    (b) No lugar, inserir a nota de D-03 (JSX pronto em 03-RESEARCH.md linhas 1080-1086): um `<div>`
    com `rounded-lg border border-gray-200 bg-gray-50 px-3 py-2 text-sm text-gray-600` dizendo que a
    senha SMTP é lida da variável de ambiente `SMTP_PASS` no servidor e não pode ser alterada por ali,
    e que para trocá-la é preciso editar o `.env` e reiniciar o serviço. Acima do JSX, um comentário
    `{/* D-01: ... o campo foi removido de propósito — não é um bug de UI. */}`.
    (c) Remover o state `const [showPass, setShowPass] = useState(false)` (linha 18) — fica órfão.
    (d) Remover `Eye` e `EyeOff` do import da linha 2, mantendo `Save` e `TestTube`.
    (e) **Não mexer** no `save()` (linhas 45-63): ele continua enviando `{...config}` e o backend
    passa a ignorar `smtp_pass` pela allowlist. Nenhuma mudança adicional é necessária.

    O Biome tem `noUnusedImports`/`noUnusedVariables` em `warn` (não quebra o lint), então os itens
    (c) e (d) não são pegos automaticamente — são obrigação do executor.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd frontend && npm run lint && npm run build && ! grep -qE "showPass|EyeOff|handleChange\('smtp_pass'" src/components/ConfigPanel.jsx && grep -q "SMTP_PASS" src/components/ConfigPanel.jsx && echo "OK ConfigPanel limpo"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "showPass" frontend/src/components/ConfigPanel.jsx` retorna `0`.
    - `grep -cE "EyeOff|Eye," frontend/src/components/ConfigPanel.jsx` retorna `0`.
    - `grep -c "handleChange('smtp_pass'" frontend/src/components/ConfigPanel.jsx` retorna `0`.
    - `grep -q "SMTP_PASS" frontend/src/components/ConfigPanel.jsx` passa (a nota cita a variável).
    - `export PATH="$HOME/bin:$PATH" && cd frontend && npm run build` sai 0.
    - `export PATH="$HOME/bin:$PATH" && cd frontend && npm run lint` sai 0.
    - O `save()` está inalterado: `git diff -U0 -- frontend/src/components/ConfigPanel.jsx | grep -c "^-.*JSON.stringify"` retorna `0`.
  </acceptance_criteria>

  <done>
    O painel não oferece mais um campo que não faz nada, e explica por escrito de onde a senha vem.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → `PUT /api/config` | Entrada não confiável; a allowlist é o controle de superfície |
| navegador → backend | O `save()` do painel envia o objeto inteiro, incluindo campos que o backend deve ignorar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SMTP-06 | Tampering | `PUT /api/config` regravando `smtp_pass` no banco | mitigate | Remover a chave de `ALLOWED_KEYS`; teste de seam fixa a ausência (Pitfall 4). Estreita a superfície sem validador novo (ASVS V5) |
| T-03-SMTP-07 | Information Disclosure | senha renderizada no navegador via `value={config.smtp_pass}` | mitigate | Campo removido (D-03); o GET já mascara e, após 03-03, devolve `''` |
| T-03-08 | Tampering | seam de teste alterando o contrato HTTP do router | accept | Props anexadas ao `module.exports = router` são ignoradas pelo Express; padrão já em uso em `routes/auth.js:359-369`; teste afirma `typeof router === 'function'` |
| T-03-SC | Tampering | instalação de pacotes | accept | Nenhum pacote npm novo; explicitamente NÃO introduzir `supertest` (usar o padrão de seam) |
</threat_model>

<verification>
```bash
export PATH="$HOME/bin:$PATH"
cd backend && npm run lint && npm run test:coverage
cd ../frontend && npm run lint && npm run build
grep -n "smtp_pass" ../backend/src/routes/config.js src/components/ConfigPanel.jsx
```
</verification>

<success_criteria>
- `ALLOWED_KEYS` existe como constante de módulo, tem 9 chaves e não inclui `smtp_pass`.
- O seam `module.exports.ALLOWED_KEYS` permite testar sem HTTP; o router continua sendo função.
- `ConfigPanel.jsx` sem campo de senha, sem `showPass`, sem `Eye`/`EyeOff`, com a nota de D-03.
- `npm run build` e `npm run lint` do frontend saem 0; backend com cobertura verde.
</success_criteria>

<output>
Create `.planning/phases/03-config-segredos-por-ambiente/03-04-SUMMARY.md` when done
</output>
