---
phase: 03-config-segredos-por-ambiente
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - backend/test/secrets.grep.test.js
autonomous: true
requirements: [CFG-01]

must_haves:
  truths:
    - "Todo PR passa por uma varredura automática de segredos nas linhas que ele adiciona (D-08)"
    - "O token histórico do commit 13905d4 NÃO deixa o job vermelho — o escopo por range de PR o exclui estruturalmente"
    - "Existe uma verificação independente do gitleaks provando que não há token/segredo em código e configuração (D-15)"
    - "A verificação de CFG-01 é escopada e não falha contra os próprios documentos de .planning/ que citam o token de propósito"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Job `secrets` com gitleaks-action@v3 escopado ao PR"
      contains: "gitleaks"
    - path: "backend/test/secrets.grep.test.js"
      provides: "Grep escopado como prova independente de CFG-01 (D-15)"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "gitleaks/gitleaks-action@v3"
      via: "uses: com env GITHUB_TOKEN e GITLEAKS_VERSION fixa"
      pattern: "gitleaks/gitleaks-action@v3"
---

<objective>
Colocar a varredura de segredos no CI como job próprio `secrets`, e criar a verificação
**independente** que o gitleaks sozinho não consegue dar.

Purpose: fecha a parte automatizável de CFG-01. D-08 escolheu a action do gitleaks em vez de script
próprio. D-15 registrou o limite honesto: o gitleaks **não detecta** o token em headers
`Authorization: Token` (medido nos três modos de scan), então CFG-01 precisa também de um `git grep`
escopado e documentado.

⚠️ Este plano **apenas adiciona o job**. Torná-lo required status check é o plano 03-07 e só pode
acontecer DEPOIS que este PR for mesclado na `main` (D-14): com `enforce_admins: true`, um contexto
exigido que ainda não existe nunca fica verde e trava o merge permanentemente.

Output: job `secrets` no `ci.yml` + `backend/test/secrets.grep.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-config-segredos-por-ambiente/03-CONTEXT.md
@.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md
@.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md
@.planning/todos/pending/sec-01-rotate-agendor-token.md
@CLAUDE.md
</context>

<interfaces>
<!-- Configuração do job já verificada linha a linha contra o código-fonte da action (v3). -->

Job `secrets` (03-RESEARCH.md §Code Examples, linhas 970-989) — todos os itens são obrigatórios:
- `runs-on: ubuntu-latest` (o runtime da action é `node24`; `ubuntu-latest` atende)
- bloco `permissions:` PRÓPRIO com `contents: read` + `pull-requests: read` — o
  `permissions: contents: read` do topo do workflow **zera** todos os outros escopos
- `actions/checkout@v7` com `fetch-depth: 0` — `--log-opts=base^..head` precisa do histórico local
- `gitleaks/gitleaks-action@v3` — SHA da tag em 2026-07-29: `e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e`
  (registrar em comentário na mesma linha; pinagem por major mantém o estilo do `ci.yml` da Fase 2)
- `env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` — a action faz `process.exit(1)` sem esta variável
- `env: GITLEAKS_VERSION: '8.24.3'` — fixa; `latest` faria uma regra nova quebrar o gate sem mudança
  de código
- `env: GITLEAKS_ENABLE_COMMENTS: 'false'` — comentar em PR exigiria `pull-requests: write`
- `env: GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'` — artefato SARIF sem uso aqui
- SEM `GITLEAKS_LICENSE`: `vitormorija` é `type: "User"`, a action detecta e dispensa

Comando de grep escopado que já passa hoje (03-RESEARCH.md §Pitfall 8, linhas 905-908):
`git grep -nI -e "c57f59ef" -- backend/src frontend/src deploy '*.example' '*.json' '*.sh' ':!.planning'`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar o job `secrets` (gitleaks) ao ci.yml</name>

  <files>.github/workflows/ci.yml</files>

  <read_first>
    - `.github/workflows/ci.yml` (48 linhas, arquivo inteiro) — o `on:` (6-9), o `permissions:` de
      topo (11-13), e a estrutura dos jobs `backend` (16-31) e `frontend` (33-48), incluindo o
      comentário `# id do job = contexto do required status check (Pitfall 3) — NÃO adicionar name:
      custom.` que aparece 2× (linhas 16 e 33).
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §Q1 completo (linhas 275-362)
      — como a action decide o range por evento, os achados reais medidos neste repositório, e a
      tabela de `permissions`/`fetch-depth`/env que quebram o job.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Auditoria da GitHub Action"
      (linhas 248-259) — a licença proprietária da action (o CLI é MIT), por que este repo não precisa
      de `GITLEAKS_LICENSE`, e o risco residual de "missing license" por flakiness.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pitfall 7" (linhas 873-891)
      e §"Pitfall 11" (linhas 951-962).
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §".github/workflows/ci.yml"
      (linhas 643-688) — convenções obrigatórias a herdar.
  </read_first>

  <action>
    Acrescentar um terceiro job ao `.github/workflows/ci.yml`, depois do job `frontend` (após a linha
    48), com id **`secrets`** (o id É o nome do contexto do status check — **não** adicionar
    `name:` custom). Copiar o bloco pronto de 03-RESEARCH.md linhas 971-989 na íntegra, com todos os
    comentários de fim de linha justificando cada valor — é a convenção do arquivo.

    Repetir acima do id do job o comentário exato já usado nas linhas 16 e 33:
    `# id do job = contexto do required status check (Pitfall 3) — NÃO adicionar name: custom.`
    Acrescentar 2 linhas de comentário explicando por que o job não nasce vermelho: escaneia apenas o
    range de commits do PR/push, então o token histórico do commit `13905d4` fica fora do range.

    NÃO alterar o `on:` (linhas 6-9): `pull_request` + `push: branches: [main]` é exatamente o escopo
    que mantém o token histórico fora da varredura. NÃO adicionar `workflow_dispatch` nem `schedule`
    — em varredura de histórico completo aparecem 2 achados e seria preciso um `.gitleaksignore`
    que silenciaria o único aviso automático da exposição do `sec-01` (Open Question 4: decidido
    **não** nesta fase). NÃO criar `.gitleaksignore`. NÃO usar `dir` mode em hipótese alguma: medido,
    ele lê o `backend/.env` real e o despeja no log público do CI.

    NÃO alterar os jobs `backend` e `frontend` — em particular, não adicionar `fetch-depth: 0` a
    eles, que só atrasaria os testes.

    Registrar no SUMMARY as duas limitações conhecidas, sem eufemismo: (a) `listCommits` não pagina,
    então PR com mais de 30 commits é escaneado só parcialmente — manter o PR desta fase abaixo de 30
    commits (são ~7 planos, factível); (b) a action pode falhar com `missing gitleaks license` por
    falha transitória da chamada `GET /users/{username}` — o remédio é re-run, não mudança de código.
  </action>

  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const need=['  secrets:','fetch-depth: 0','gitleaks/gitleaks-action@v3','GITHUB_TOKEN','GITLEAKS_VERSION','pull-requests: read',\"GITLEAKS_ENABLE_COMMENTS: 'false'\"]; const bad=['workflow_dispatch','schedule:','GITLEAKS_LICENSE','name: ']; let f=0; for(const n of need){ if(!y.includes(n)){ console.error('FALTA:',n); f=1; } } for(const b of bad){ if(y.includes(b)){ console.error('PROIBIDO PRESENTE:',b); f=1; } } if(f===0) console.log('OK job secrets'); process.exit(f);"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^  secrets:$" .github/workflows/ci.yml` retorna `1`.
    - `grep -c "gitleaks/gitleaks-action@v3" .github/workflows/ci.yml` retorna `1`, e a linha contém o
      SHA `e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e` em comentário.
    - `grep -c "fetch-depth: 0" .github/workflows/ci.yml` retorna `1`.
    - `grep -c "pull-requests: read" .github/workflows/ci.yml` retorna `1`.
    - `grep -c "GITLEAKS_VERSION" .github/workflows/ci.yml` retorna `1` e o valor é `'8.24.3'`, nunca
      `latest`.
    - `grep -c "GITLEAKS_LICENSE\|workflow_dispatch\|schedule:" .github/workflows/ci.yml` retorna `0`.
    - `ls .gitleaksignore` falha (arquivo não deve existir).
    - `git diff -U0 -- .github/workflows/ci.yml | grep -cE "^-[^-]"` retorna `0` — o job foi
      **acrescentado**, nada foi removido dos jobs existentes.
    - Após o push, o job `secrets` aparece verde no PR: `gh pr checks` lista `secrets` com conclusão
      `success` (registrar a saída no SUMMARY — é a prova de que o token histórico não deixa o job
      vermelho).
  </acceptance_criteria>

  <done>
    O `ci.yml` tem 3 jobs paralelos e o `secrets` roda verde no PR desta fase. O contexto `secrets`
    passa a existir assim que o PR for mesclado — pré-requisito de D-14 para o plano 03-07.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verificação escopada de CFG-01 como teste (D-15, Pitfall 8)</name>

  <files>backend/test/secrets.grep.test.js (novo)</files>

  <read_first>
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"Pitfall 8" (linhas 893-914)
      — por que um `grep -r` na raiz **falha** contra `.planning/` (6 ocorrências legítimas, que
      documentam a exposição) e qual é o escopo correto, já verificado como `OK` hoje.
    - `.planning/phases/03-config-segredos-por-ambiente/03-RESEARCH.md` §"gitleaks NÃO é prova de zero
      segredos" (linhas 304-315) — a medição que motiva D-15.
    - `.planning/todos/pending/sec-01-rotate-agendor-token.md` (arquivo inteiro) — o que a exposição é,
      o que NÃO a resolve, e a decisão registrada de adiar a rotação.
    - `backend/test/agendor.pure.test.js` linhas 1-18 — cabeçalho e convenções.
    - `.planning/phases/03-config-segredos-por-ambiente/03-PATTERNS.md` §"No Analog Found"
      (linhas 834-844) — este arquivo não tem analog no repo; nenhum teste existente roda subprocesso
      ou varre a árvore.
  </read_first>

  <behavior>
    - Um grep escopado a `backend/src`, `frontend/src`, `deploy`, `*.example`, `*.json`, `*.sh`,
      excluindo `.planning`, pelo prefixo do token da Agendor retorna **zero** ocorrências
    - O mesmo escopo, buscando o padrão de segredo literal
      `(password|secret|token|api_key)\s*[:=]\s*['"][A-Za-z0-9/+_-]{16,}['"]`, retorna zero
      ocorrências em `backend/src` e `frontend/src`
    - O teste NÃO falha por causa de `.planning/**`, que cita o prefixo do token de propósito
    - O teste NÃO lê `backend/.env` nem `.claude/**` (gitignored, contêm segredos reais) — o escopo é
      só o que é versionado em código/configuração
  </behavior>

  <action>
    Criar `backend/test/secrets.grep.test.js`, que é a **verificação independente** exigida por D-15
    (o gitleaks não detecta o token em headers `Authorization: Token` — medido nos três modos).

    Implementação: usar `child_process.execFileSync('git', ['grep', ...])` a partir da raiz do
    repositório (calculada com `path.join(__dirname, '..', '..')`, não do `process.cwd()`), com o
    escopo exato de 03-RESEARCH.md linha 906: pathspecs `backend/src frontend/src deploy '*.example'
    '*.json' '*.sh'` mais o exclusor `:!.planning`. `git grep` sai com código 1 quando não encontra
    nada — tratar isso como **sucesso** e qualquer saída não vazia como **falha**, imprimindo as
    ocorrências no `assert` para o desenvolvedor ver o que apareceu.

    Dois testes, conforme `<behavior>`: (1) prefixo do token da Agendor; (2) padrão genérico de
    literal de segredo em `backend/src` e `frontend/src`. Um terceiro teste opcional, defensivo:
    afirmar que o escopo do grep **exclui** `.planning` — por exemplo, verificando que a mesma busca
    SEM o exclusor encontra ocorrências, o que documenta por que o escopo existe e impede que alguém
    "conserte" o teste apagando documentação de segurança legítima.

    Cabeçalho em PT explicando: este arquivo é a prova de CFG-01 que o gitleaks não dá; as
    ocorrências em `.planning/` são referências truncadas deliberadas rastreando a exposição do
    `sec-01`, não vazamento novo; e o token histórico continua ativo até a rotação — o teste prova
    "nenhum segredo no código versionado em HEAD", nada além disso.

    Se `git` não estiver disponível no ambiente de execução, o teste deve **falhar de forma
    explícita** com mensagem clara, não pular em silêncio.
  </action>

  <verify>
    <automated>export PATH="$HOME/bin:$PATH" && cd backend && npm run lint && node --test test/secrets.grep.test.js && npm run test:coverage</automated>
  </verify>

  <acceptance_criteria>
    - `export PATH="$HOME/bin:$PATH" && cd backend && node --test test/secrets.grep.test.js` sai 0.
    - `grep -q ":!.planning" backend/test/secrets.grep.test.js` passa — o escopo está no código do
      teste, não implícito.
    - `grep -c "process.cwd()" backend/test/secrets.grep.test.js` retorna `0` — a raiz é derivada de
      `__dirname`, para o teste funcionar de qualquer diretório.
    - O teste passa também quando invocado pela suíte inteira:
      `export PATH="$HOME/bin:$PATH" && cd backend && npm test` sai 0.
    - `npm run test:coverage` sai 0 (nenhum arquivo novo em `src/`).
    - Verificação manual de contraste, para registrar no SUMMARY:
      `git grep -c "c57f59ef" -- .planning | wc -l` retorna > 0, provando que o exclusor é necessário.
  </acceptance_criteria>

  <done>
    CFG-01 tem prova independente do gitleaks, escopada de forma que não colida com a documentação de
    segurança do próprio projeto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contribuidor → repositório público | Linhas adicionadas num PR podem conter credenciais; é o vetor que já se materializou no commit `13905d4` |
| GitHub Actions runner → binário externo | A action baixa o CLI do gitleaks de um release; superfície de supply chain |
| log do CI → leitor anônimo | O repositório é público, então tudo que o job imprime é público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-LEAK-01 | Information Disclosure | segredo novo entrando por PR | mitigate | Job `secrets` com `gitleaks-action@v3` escopado ao range do PR (D-08). Vira gate de merge no plano 03-07 |
| T-03-LEAK-02 | Information Disclosure | token da Agendor já exposto no histórico público | transfer | Fora do alcance de código: só a rotação no painel da Agendor encerra. Rastreado em `sec-01`; adiada conscientemente para preservar o gate de CI-02. O SUMMARY da fase deve dizer isso com todas as letras |
| T-03-LEAK-03 | Information Disclosure | scan em `dir` mode lendo `backend/.env` e `.claude/**` reais (33 achados medidos) e imprimindo no log público | mitigate | Nunca usar `dir` mode; a action em `pull_request`/`push` usa `git` com `--log-opts` e `--redact`. Sem `workflow_dispatch`/`schedule` |
| T-03-SC | Tampering | supply chain da GitHub Action e do binário baixado | mitigate | Pinar `gitleaks/gitleaks-action@v3` com o SHA `e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e` registrado em comentário; `GITLEAKS_VERSION` fixa em `'8.24.3'` (nunca `latest`); `permissions:` mínimas; sem `GITLEAKS_LICENSE` em secret. Action auditada pelo código-fonte, não pelo README (03-RESEARCH.md §Auditoria) |
| T-03-LEAK-04 | Repudiation | falsa segurança em PR com mais de 30 commits | accept | `listCommits` não pagina (medido). Mitigação processual: manter o PR desta fase abaixo de 30 commits e registrar a limitação no SUMMARY, em vez de fingir que o gate é hermético |
| T-03-LEAK-05 | Denial of Service | job vermelho por `missing gitleaks license` sem mudança de código | accept | Falha transitória da chamada `GET /users/{username}`; remédio é re-run. Saída de emergência documentada: CLI MIT direto (contraria D-08) |
| T-03-LEAK-06 | Repudiation | tratar o gitleaks como prova de CFG-01 | mitigate | D-15: `secrets.grep.test.js` como verificação independente, porque o gitleaks não detecta a exposição em headers `Authorization: Token` |
</threat_model>

<verification>
```bash
export PATH="$HOME/bin:$PATH"
cd backend && npm run lint && npm run test:coverage
node --test test/secrets.grep.test.js
cd .. && gh pr checks   # job `secrets` deve aparecer com success
```
</verification>

<success_criteria>
- `ci.yml` tem o job `secrets` com `fetch-depth: 0`, `permissions` próprias, `GITHUB_TOKEN`,
  `GITLEAKS_VERSION: '8.24.3'`, comentários desligados e o SHA da tag registrado.
- Nenhum `.gitleaksignore`, nenhum `workflow_dispatch`/`schedule`, nenhum `dir` mode.
- O job roda **verde** no PR desta fase (prova de que o token histórico está fora do range).
- `backend/test/secrets.grep.test.js` passa com escopo correto e não colide com `.planning/`.
- SUMMARY registra as duas limitações (>30 commits; flakiness de licença) e a exposição ainda ativa
  do `sec-01`.
</success_criteria>

<output>
Create `.planning/phases/03-config-segredos-por-ambiente/03-06-SUMMARY.md` when done
</output>
