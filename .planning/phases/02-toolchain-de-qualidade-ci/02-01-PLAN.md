---
phase: 02-toolchain-de-qualidade-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/test/agendor.futureTasks.test.js
autonomous: true
requirements: [WR-02]
must_haves:
  truths:
    - "getDealsWithFutureTasks inclui deal com tarefa aberta e dueDate futuro"
    - "Exclui tarefa finalizada e tarefa com dueDate no passado (ou == now, > estrito)"
    - "Paginação para em tasks.length < 100 (chama api.get 2x no caso de 2 páginas)"
    - "cd backend && npm test fica verde com a suíte ampliada"
  artifacts:
    - path: "backend/test/agendor.futureTasks.test.js"
      provides: "Caracterização de getDealsWithFutureTasks (0% -> coberto)"
      contains: "installFakeAxios"
      min_lines: 60
  key_links:
    - from: "backend/test/agendor.futureTasks.test.js"
      to: "backend/src/agendor.js getDealsWithFutureTasks"
      via: "installFakeAxios stub de /tasks + mock.timers relógio fixo"
      pattern: "getDealsWithFutureTasks"
---

<objective>
Fechar WR-02: fixar por teste de caracterização o comportamento ATUAL de
`getDealsWithFutureTasks` (backend/src/agendor.js:173-204), hoje 0% coberto e usado
pelo scheduler para decidir quem NÃO é notificado. Pré-requisito do gate de cobertura
(WR-03) — precisa estar verde antes de flipar o threshold.

Purpose: uma regressão silenciosa (off-by-one em dueDate/now, ou na condição de break
da paginação) hoje não seria pega por nenhum teste — lacuna direta contra o Core Value.
Output: backend/test/agendor.futureTasks.test.js (verde), suíte ampliada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md   # § "WR-02 Characterization Test — detalhe concreto"
@.planning/phases/02-toolchain-de-qualidade-ci/02-PATTERNS.md   # § agendor.futureTasks.test.js (PRIMARY analog)

<interfaces>
SUT — backend/src/agendor.js (linhas 173-204), comportamento a PINAR (não corrigir):
- now = new Date(); yesterday = now-24h enviado como param dueDateGt.
- api.get('/tasks', { params: { dueDateGt, per_page:100, page } }); lê data.data || [].
- adiciona t.deal.id ao Set sse: !t.finishedAt && t.deal?.id && new Date(t.dueDate) > now  (> ESTRITO).
- break quando tasks.length === 0 OU tasks.length < 100; page++ só quando exatamente 100.
- catch interno -> console.error + break (não propaga). Retorna Set de deal ids.

Helper — backend/test/helpers/fakeAxios.js:
  installFakeAxios(routeHandler(url, config)) => fakeInstance { get: mock.fn }
  routeHandler retorna { data: <envelope Agendor> }; para /tasks -> { data: { data: <tasksArray> } }.
  fakeInstance.get.mock.callCount() disponível para asserir paginação.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de caracterização de getDealsWithFutureTasks</name>
  <files>backend/test/agendor.futureTasks.test.js</files>
  <read_first>
    - backend/test/agendor.getStaleDeals.test.js  (analog EXATO — copiar estrutura: header de intenção PT, require('./setup') PRIMEIRO, installFakeAxios ANTES de require('../src/agendor'), mock.timers before/after)
    - backend/test/helpers/fakeAxios.js  (assinatura routeHandler(url, config) + callCount)
    - backend/test/setup.js  (por que require('./setup') vem primeiro)
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "WR-02 Characterization Test" (tabela de casos a–e)
    - .planning/phases/02-toolchain-de-qualidade-ci/02-PATTERNS.md § agendor.futureTasks.test.js
  </read_first>
  <behavior>
    - Caso (a): task finishedAt:null, dueDate FUTURO (> FIXED_NOW), deal.id X -> result.has(X) === true
    - Caso (b): task finishedAt:<data>, dueDate futuro, deal.id Y -> result.has(Y) === false
    - Caso (c): task finishedAt:null, dueDate PASSADO (< FIXED_NOW), deal.id Z -> result.has(Z) === false
    - Caso (c2) blindagem `>` estrito: dueDate === FIXED_NOW exato, deal.id W -> result.has(W) === false
    - Caso (d) paginação: página1 = 100 tasks (inclui P1), página2 = <100 (inclui P2) -> has(P1) && has(P2) && fake.get.mock.callCount() === 2
    - Caso (e) opcional: task sem deal.id -> não adiciona (guard t.deal?.id)
  </behavior>
  <action>
    Criar o arquivo espelhando 1:1 a estrutura de agendor.getStaleDeals.test.js. Ordem obrigatória:
    require('./setup') na 1a linha; destructure { test, before, after, mock } de node:test; assert de
    node:assert/strict; installFakeAxios de ./helpers/fakeAxios. Declarar FIXED_NOW =
    new Date('2026-06-01T00:00:00.000Z').getTime(). Instalar installFakeAxios((url, config) => …) ANTES
    de require('../src/agendor'): para url === '/tasks', ramificar por config?.params?.page retornando
    { data: { data: PAGE_1 } } (100 itens) vs { data: { data: PAGE_2 } } (<100); fallback { data:{data:[]} }.
    Capturar o retorno de installFakeAxios como `fake` para asserir fake.get.mock.callCount(). before ->
    mock.timers.enable({ apis:['Date'], now: FIXED_NOW }); after -> mock.timers.reset(). Fixtures de task
    INLINE (arrays no próprio arquivo; não criar fixture externo) com dueDate como literais ISO relativos a
    FIXED_NOW e comentários PT auto-descritivos por caso. Asserções via node:assert/strict, uma por
    comportamento, com comentário PT "explica o porquê" e uma nota de "blindagem" no caso c2 documentando
    que `>` estrito exclui igualdade. Disciplina de CARACTERIZAÇÃO: pinar o comportamento atual (inclusive o
    `>` estrito) — NÃO corrigir. Comentários em português, 2 espaços, aspas simples, ponto-e-vírgula (estilo
    do backend CommonJS).
  </action>
  <verify>
    <automated>cd backend && node --test test/agendor.futureTasks.test.js</automated>
    <automated>cd backend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && node --test test/agendor.futureTasks.test.js` sai com exit 0, todos os subtests ok.
    - `cd backend && npm test` sai exit 0; contagem total >= 32 (28 anteriores + >=4 novos), 0 fail.
    - grep confirma `require('./setup')` na primeira linha executável e `installFakeAxios` antes de
      `require('../src/agendor')`.
    - grep confirma `mock.timers.enable` e `callCount()` presentes (paginação asserida).
    - Nenhuma chamada de rede real (teste roda sub-segundo) — prova indireta de que o stub está instalado.
  </acceptance_criteria>
  <done>getDealsWithFutureTasks caracterizado (casos a–e), suíte verde, gap de 0% fechado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| teste -> borda HTTP Agendor | Nenhuma chamada real; installFakeAxios stuba axios.create. Sem rede, sem segredo. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01-01 | Tampering | teste tocar rede/prod real | accept | installFakeAxios isola a borda HTTP; setup força DB_PATH=:memory: e AGENDOR_TOKEN='test'. Sem segredo real, sem I/O externo. |
| T-2-SC | Tampering | npm installs | n/a | Este plano NÃO instala pacotes (auditoria de legitimidade tratada em 02-02). |
</threat_model>

<verification>
- cd backend && npm test  (>=32/…, 0 fail)
- cd backend && node --test test/agendor.futureTasks.test.js  (exit 0)
</verification>

<success_criteria>
getDealsWithFutureTasks tem teste de caracterização verde cobrindo inclusão/exclusão
(finalizada, passado, `>` estrito) e parada de paginação. Pré-requisito de WR-03 satisfeito.
</success_criteria>

<output>
Create .planning/phases/02-toolchain-de-qualidade-ci/02-01-SUMMARY.md when done
</output>
