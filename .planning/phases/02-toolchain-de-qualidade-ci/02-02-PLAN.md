---
phase: 02-toolchain-de-qualidade-ci
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - biome.json
  - backend/package.json
  - backend/package-lock.json
  - frontend/package.json
  - frontend/package-lock.json
  # + commit de formatação ISOLADO: reformat whitespace-only em backend/src/**, frontend/src/**
  #   e demais arquivos rastreados (não enumeráveis; um único `biome format --write .`)
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "biome.json na raiz configura lint+format para backend (CJS) e frontend (ESM)"
    - "npm run lint fica verde (exit 0) em backend E frontend no estado atual"
    - "npm run format existe nos dois package.json; biome format --check . passa"
    - "Scripts lint/format/test presentes nos dois package.json (QUAL-03)"
    - "Reformatação preserva comportamento: suíte backend permanece verde; vite build passa"
  artifacts:
    - path: "biome.json"
      provides: "Config única de lint+format (root:true, assist:false, quoteStyle single, indent space 2)"
      contains: "\"root\": true"
    - path: "backend/package.json"
      provides: "scripts lint/format + devDep @biomejs/biome@2.5.5"
      contains: "biome lint"
    - path: "frontend/package.json"
      provides: "scripts lint/format/test(no-op) + devDep @biomejs/biome@2.5.5"
      contains: "biome format"
  key_links:
    - from: "backend/package.json / frontend/package.json"
      to: "biome.json (raiz)"
      via: "biome resolve config subindo a árvore a partir do CWD do pacote"
      pattern: "biome (lint|format)"
---

<objective>
Adotar Biome 2.5.5 como ferramenta ÚNICA de lint+format (D-01), com uma biome.json na raiz
(D-02), padronizar scripts nos dois package.json (QUAL-03), aplicar UM passe de formatação
isolado guardado pela suíte da Fase 1 (D-04/D-05), e estabelecer o baseline de lint warn-tolerante
medindo os diagnostics reais (D-06) para deixar `npm run lint` verde no estado atual (QUAL-01).

Purpose: qualidade verificável localmente sem trocar stack nem alterar comportamento.
Output: biome.json, scripts nos dois pacotes, commit de formatação isolado, baseline de lint documentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md  # § Code Examples (biome.json, scripts), § Pattern 2/3, § Package Legitimacy Audit, Pitfalls 2/4
@.planning/phases/02-toolchain-de-qualidade-ci/02-PATTERNS.md  # § biome.json / package.json (No Analog — mirror conventions)
@CLAUDE.md   # convenções: PT, 2 espaços, aspas simples, imports ungrouped/inline anti-circular, zero-dep
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Biome + biome.json raiz + scripts nos dois package.json</name>
  <files>biome.json, backend/package.json, backend/package-lock.json, frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Code Examples -> biome.json" e "Scripts padronizados" (usar VERBATIM como baseline)
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Package Legitimacy Audit" (@biomejs/biome = Approved; sem postinstall; binário via optionalDependencies)
    - backend/package.json e frontend/package.json (blocos scripts/devDependencies atuais)
    - CLAUDE.md (aviso: imports deliberadamente ungrouped + requires inline anti-circular -> assist DEVE ficar off)
  </read_first>
  <action>
    Com PATH dos wrappers Node do projeto (export PATH="$HOME/bin:$PATH"), instalar em CADA pacote
    (sem workspaces — o CI faz npm ci por pacote): `cd backend && npm i -D -E @biomejs/biome@2.5.5` e
    `cd frontend && npm i -D -E @biomejs/biome@2.5.5`. NÃO instalar na raiz (Pitfall 1: quebra npm ci por
    pacote). `-E` pina a versão exata. Criar biome.json na RAIZ conforme o baseline do RESEARCH: "$schema"
    do node_modules, "root": true, vcs.useIgnoreFile true, files.includes com negação `!` para
    node_modules/dist/coverage/backend/agendor.db/backend/test/fixtures/**, formatter.indentStyle "space" +
    indentWidth 2, javascript.formatter.quoteStyle "single", linter.rules.recommended true, e
    "assist": { "enabled": false } (desliga organize-imports — Pitfall 4). NÃO adicionar overrides de warn
    ainda (isso é medido na Task 3). Adicionar aos scripts do backend: "lint":"biome lint .",
    "format":"biome format --write ." (manter test/test:coverage intactos). Adicionar aos scripts do
    frontend: "lint":"biome lint .", "format":"biome format --write .", e um "test" no-op documentado
    (echo + exit 0) — D-03/A5. Confirmar que @biomejs/biome aparece em devDependencies dos dois package.json
    e nos dois lockfiles. Legitimidade: RESEARCH audita @biomejs/biome como Approved (11.4M/sem, repo oficial,
    sem postinstall) — sem checkpoint bloqueante necessário.
  </action>
  <verify>
    <automated>cd backend && npx @biomejs/biome --version | grep -q 2.5.5</automated>
    <automated>cd frontend && npx @biomejs/biome --version | grep -q 2.5.5</automated>
    <automated>node -e "const b=require('./backend/package.json'),f=require('./frontend/package.json');const j=require('./biome.json');if(!b.scripts.lint||!b.scripts.format||!f.scripts.lint||!f.scripts.format||!f.scripts.test)process.exit(1);if(j.root!==true||j.assist.enabled!==false)process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - @biomejs/biome@2.5.5 (exato, sem ^) em devDependencies de backend/package.json E frontend/package.json.
    - Ambos os lockfiles contêm @biomejs/biome (npm ci reprodutível no CI).
    - biome.json na raiz existe com "root": true e "assist": { "enabled": false }.
    - backend/package.json e frontend/package.json têm scripts "lint" e "format"; frontend tem "test" (no-op).
    - `npx @biomejs/biome --version` imprime 2.5.5 nos dois pacotes.
  </acceptance_criteria>
  <done>Biome instalado nos dois pacotes, biome.json raiz criado, scripts padronizados (QUAL-02/QUAL-03).</done>
</task>

<task type="auto">
  <name>Task 2: Passe de formatação isolado + guard da Fase 1 (D-04/D-05)</name>
  <files>backend/src (whitespace), frontend/src (whitespace) — commit de formatação isolado</files>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Pattern 2: Format-then-guard" (sequência exata) e Pitfall 4 (nada de organize-imports)
    - CLAUDE.md (o guard existe porque não se altera comportamento sem teste cobrindo)
  </read_first>
  <action>
    Rodar UMA vez, a partir da raiz, `backend/node_modules/.bin/biome format --write .` (usa a biome.json
    raiz; `.` restringe ao repo). Inspecionar `git diff` e CONFIRMAR que só há mudança de whitespace/estilo —
    se aparecer QUALQUER linha de `require(`/`import` movida/reordenada, PARAR e reconfigurar (assist deve
    estar off; Pitfall 4). Commitar SOMENTE a formatação, isolada (nenhuma outra mudança junto), mensagem
    style(02): passe único de formatação Biome (whitespace-only, sem mudança de comportamento). Imediatamente
    rodar o guard: `cd backend && npm test` e confirmar a contagem ATUAL da suíte verde (>=32 após 02-01,
    0 fail) — prova D-05 de que a reformatação preservou comportamento. Guard do frontend (sem testes):
    `cd frontend && npm run build` (vite build) deve passar. Se algo quebrar, é investigação BLOQUEANTE antes
    de prosseguir — não seguir para a Task 3.
  </action>
  <verify>
    <automated>backend/node_modules/.bin/biome format --check . </automated>
    <automated>cd backend && npm test</automated>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `biome format --check .` sai exit 0 (repo já formatado — idempotente).
    - `cd backend && npm test` exit 0, mesma contagem de 02-01, 0 fail (guard D-05 verde).
    - `cd frontend && npm run build` exit 0 (dist gerado).
    - `git log -1 --stat` do commit de formatação mostra apenas mudanças de whitespace; `git show` não contém
      linhas de require/import reordenadas.
  </acceptance_criteria>
  <done>Repo formatado em commit isolado; suíte Fase 1 verde + build frontend verde comprovam comportamento preservado.</done>
</task>

<task type="auto">
  <name>Task 3: Baseline de lint warn-tolerante (measure-first, D-06)</name>
  <files>biome.json</files>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Pattern 3: Baseline warn vs error" e § "Open Questions #2" (medir antes de decidir o conjunto de regras)
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § Pitfall 2 (não usar `biome ci` nem --error-on-warnings)
  </read_first>
  <action>
    MEASURE-FIRST — não hardcodar o conjunto de regras. Rodar `cd backend && npm run lint` e
    `cd frontend && npm run lint` uma vez, coletar TODOS os diagnostics de regras recomendadas que hoje
    falhariam. Para CADA regra ofensora, adicionar override "warn" em linter.rules.<grupo>.<regra> em
    biome.json (ex.: suspicious.noDoubleEquals, style.noParameterAssign — só as que realmente aparecerem).
    Regras que o código já satisfaz permanecem em error via recommended. Documentar a lista de overrides
    como "baseline D-06" num comentário PT no biome.json e no SUMMARY. NÃO promover warn->error que exija
    mudança de código (isso é fase futura com teste). Rerodar até `npm run lint` sair exit 0 nos DOIS pacotes
    (warns presentes são aceitáveis; erros = 0). Confirmar que nenhum flag --error-on-warnings é usado.
  </action>
  <verify>
    <automated>cd backend && npm run lint</automated>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm run lint` sai exit 0 (erros=0; warns permitidos).
    - `cd frontend && npm run lint` sai exit 0.
    - biome.json contém overrides "warn" apenas para regras que apareceram como ofensoras (measure-first),
      com comentário PT "baseline D-06".
    - Nenhuma regra de estilo reescreveu semântica; nenhum override "error" novo exige mudança de código.
  </acceptance_criteria>
  <done>npm run lint verde no estado atual em ambos os pacotes; baseline documentado (QUAL-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry -> devDependency (Biome) | Binário de terceiro instalado no toolchain local e no CI. Supply-chain (ASVS V14). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02-01 | Tampering | @biomejs/biome (slopsquat / binário malicioso) | mitigate | Versão PINADA `-E` 2.5.5; sem postinstall (verificado em RESEARCH § Package Legitimacy Audit: Approved, 11.4M/sem, repo oficial biomejs/biome); binário por plataforma via optionalDependencies. |
| T-2-02-02 | Tampering | organize-imports reordenando requires (quebra anti-circular) | mitigate | assist.enabled=false em biome.json; usar `biome format` (não `check`); revisar git diff do commit de formatação (Pitfall 4). |
| T-2-02-03 | Repudiation | mudança de comportamento escondida no passe de formatação | mitigate | Commit de formatação ISOLADO whitespace-only + guard suíte Fase 1 verde (D-05) + vite build. |
| T-2-SC | Tampering | npm installs desta fase | mitigate | Nenhum pacote [ASSUMED]/[SUS]/[SLOP] (audit RESEARCH); @biomejs/biome e c8 = Approved; sem checkpoint bloqueante requerido. |
</threat_model>

<verification>
- biome format --check .  (exit 0)
- cd backend && npm run lint && npm test
- cd frontend && npm run lint && npm run build
</verification>

<success_criteria>
Biome único cobre backend+frontend; scripts lint/format/test presentes nos dois package.json;
format idempotente com config versionada; npm run lint verde no estado atual (baseline warn documentado);
comportamento preservado (guard Fase 1 verde). QUAL-01/QUAL-02/QUAL-03 satisfeitos.
</success_criteria>

<output>
Create .planning/phases/02-toolchain-de-qualidade-ci/02-02-SUMMARY.md when done
</output>
