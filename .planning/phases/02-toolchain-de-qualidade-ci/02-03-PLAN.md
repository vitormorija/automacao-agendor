---
phase: 02-toolchain-de-qualidade-ci
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - .github/workflows/ci.yml
  - backend/.c8rc.json
autonomous: true
requirements: [CI-01, WR-03]
must_haves:
  truths:
    - "ci.yml roda 2 jobs paralelos backend e frontend em pull_request e push"
    - "backend job: npm ci -> biome lint . -> npm run test:coverage (node --test sob c8)"
    - "frontend job: npm ci -> biome lint . -> npm run build (vite build)"
    - "workflow tem permissions: contents: read (least-privilege)"
    - ".c8rc.json check-coverage:true com lines global just-below-observed (per-file:false); npm run test:coverage passa no estado atual"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pipeline de CI (2 jobs, node20, cache por lockfile)"
      contains: "contents: read"
    - path: "backend/.c8rc.json"
      provides: "Gate de cobertura (check-coverage) sobre a rede de segurança"
      contains: "check-coverage"
  key_links:
    - from: ".github/workflows/ci.yml (job backend)"
      to: "backend scripts test:coverage + biome lint"
      via: "run: npm run ... com working-directory: backend"
      pattern: "npm run (test:coverage|build|lint)"
    - from: "backend/.c8rc.json"
      to: "npm run test:coverage (c8)"
      via: "check-coverage falha o processo abaixo do threshold"
      pattern: "check-coverage"
---

<objective>
Criar o pipeline de CI (CI-01): .github/workflows/ci.yml com dois jobs paralelos (backend, frontend),
disparado em pull_request e push, node 20, permissions:contents:read. E fechar WR-03: flipar o gate
de cobertura em .c8rc.json AGORA que a lacuna de getDealsWithFutureTasks foi coberta (02-01) — threshold
global modesto, medido logo abaixo do observado, para a rede de segurança não erodir em silêncio.

Purpose: cada PR passa a rodar lint+testes(+coverage)+build automaticamente.
Output: ci.yml, .c8rc.json com check-coverage. (A prova de CI verde em PR real ocorre em 02-04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md  # § Code Examples (ci.yml skeleton), § Coverage Gating (WR-03), Pitfalls 2/3
@.planning/phases/02-toolchain-de-qualidade-ci/02-PATTERNS.md  # § ci.yml / .c8rc.json (No Analog / self-modify)
@backend/.c8rc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: .github/workflows/ci.yml (2 jobs paralelos, CI-01)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Code Examples -> ci.yml (skeleton)" (usar como base)
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § Pitfall 2 (usar `biome lint .`, nunca `biome ci` nem --error-on-warnings) e Pitfall 3 (id do job == contexto do required check)
    - backend/package.json e frontend/package.json (nomes de scripts que o CI invoca)
  </read_first>
  <action>
    Criar .github/workflows/ci.yml conforme o skeleton do RESEARCH. name: CI; on: pull_request + push
    (branches [main]); permissions: contents: read (least-privilege, supply-chain). Dois jobs com ids
    EXATAMENTE `backend` e `frontend` (SEM name: custom divergente — o id vira o contexto do status check,
    Pitfall 3), runs-on: ubuntu-latest, defaults.run.working-directory apontando o pacote. Steps de cada job:
    actions/checkout@v4, actions/setup-node@v4 (node-version '20' — D-09; cache: npm com cache-dependency-path
    apontando o lockfile do pacote), npm ci. backend: `npm run lint` (biome lint . warn-tolerante) depois
    `npm run test:coverage`. frontend: `npm run lint` depois `npm run build`. CONFIRMAR a major tag atual de
    actions/checkout e actions/setup-node antes de commitar (A1 — via gh api /repos/actions/checkout/tags ou
    docs); se @v4 não for a atual, ajustar. Comentários PT onde couber.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');['contents: read','working-directory: backend','working-directory: frontend','node-version','pull_request','npm run test:coverage','npm run build'].forEach(s=>{if(!y.includes(s)){console.error('missing',s);process.exit(1)}})"</automated>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!y.includes('backend:')||!y.includes('frontend:'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml existe; jobs de id `backend` e `frontend` (contextos = ids, Pitfall 3).
    - on: pull_request e push; permissions.contents == read.
    - Cada job: checkout + setup-node node-version '20' + cache por lockfile + npm ci; backend chama
      `npm run lint` e `npm run test:coverage`; frontend chama `npm run lint` e `npm run build`.
    - Nenhum `biome ci` nem `--error-on-warnings` (baseline warn não pode falhar — Pitfall 2).
    - Major tags de checkout/setup-node confirmadas como atuais.
  </acceptance_criteria>
  <done>Pipeline de CI criado (2 jobs paralelos, least-privilege). CI-01 estruturalmente pronto; prova de verde em PR real em 02-04.</done>
</task>

<task type="auto">
  <name>Task 2: Flip do gate de cobertura c8 (WR-03, measure-first)</name>
  <files>backend/.c8rc.json</files>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Coverage Gating (WR-03) — detalhe concreto" (c8 NÃO suporta threshold por-path; usar global modesto per-file:false; medir DEPOIS do WR-02)
    - .planning/todos/pending/wr-03-enforce-coverage-thresholds.md (NÃO copiar lines:60/per-file:true — per-file:true falharia nos arquivos intencionalmente descobertos)
    - backend/.c8rc.json (estado atual report-only)
  </read_first>
  <action>
    MEASURE-FIRST — WR-02 já fechou a lacuna (02-01), então medir agora. Rodar `cd backend &&
    npm run test:coverage` e ler a linha "All files" (aggregate lines %). Editar backend/.c8rc.json
    adicionando "check-coverage": true, "per-file": false e "lines": <N> onde N é um piso logo ABAIXO do
    aggregate observado (margem contra flutuação; ex.: observado ~23% -> N=20). Manter all:true /
    include:src/**/*.js / exclude test+src/index.js / reporter text+lcov intactos. NÃO usar per-file:true
    (falha nos arquivos sem cobertura desta fase). NÃO mudar o comando do CI — o mesmo
    `npm run test:coverage` passa a bloquear via .c8rc.json. Opcionalmente adicionar branches/functions/
    statements com pisos análogos se os valores observados derem margem segura; se algum for arriscado,
    ficar só em lines. Confirmar que `npm run test:coverage` sai exit 0 no estado atual (gate passa, não
    falha pela lacuna já fechada). Comentar em PT no SUMMARY qual N foi escolhido e por quê.
  </action>
  <verify>
    <automated>cd backend && npm run test:coverage</automated>
    <automated>node -e "const c=require('./backend/.c8rc.json');if(c['check-coverage']!==true||c['per-file']!==false||typeof c.lines!=='number')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - backend/.c8rc.json tem "check-coverage": true, "per-file": false, "lines": <número medido, just-below>.
    - `cd backend && npm run test:coverage` sai exit 0 no estado atual (gate ativo mas NÃO falha).
    - N escolhido é menor que o aggregate observado (documentado no SUMMARY); NÃO é o 60/per-file:true do todo.
    - Chaves originais (all/include/exclude/reporter) preservadas.
  </acceptance_criteria>
  <done>Gate de cobertura ativo (bloqueia abaixo do piso) sem falhar o estado atual. WR-03 fechado; a rede de segurança não erode em silêncio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> repo/ações de terceiros | Workflow executa código de terceiros (checkout/setup-node) e instala deps. Supply-chain (ASVS V14). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03-01 | Elevation of Privilege | permissões do workflow | mitigate | `permissions: contents: read` (least-privilege) no topo do ci.yml. |
| T-2-03-02 | Tampering | actions/checkout, actions/setup-node (ação de terceiro comprometida) | mitigate | Pinar major tag `@v4` (confirmada atual); `npm ci` usa lockfile (instalação determinística). |
| T-2-03-03 | Information Disclosure | secret exposto no workflow | accept | Nenhum secret é necessário (lint/test/build são públicos); ci.yml não referencia secrets. |
| T-2-03-04 | Tampering | erosão silenciosa da rede de segurança | mitigate | c8 check-coverage bloqueia PRs que baixem a cobertura abaixo do piso (WR-03). |
| T-2-SC | Tampering | npm installs no CI | mitigate | `npm ci` + lockfiles atualizados em 02-02; pacotes auditados (Approved). |
</threat_model>

<verification>
- node valida ci.yml (jobs backend/frontend, contents:read)
- cd backend && npm run test:coverage  (exit 0 com check-coverage:true)
</verification>

<success_criteria>
Pipeline de CI roda lint + testes(+coverage) + build em 2 jobs paralelos a cada PR/push, least-privilege.
Gate de cobertura ativo sem falhar o estado atual. CI-01 estruturado; WR-03 fechado.
</success_criteria>

<output>
Create .planning/phases/02-toolchain-de-qualidade-ci/02-03-SUMMARY.md when done
</output>
