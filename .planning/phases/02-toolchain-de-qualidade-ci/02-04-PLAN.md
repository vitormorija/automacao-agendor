---
phase: 02-toolchain-de-qualidade-ci
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - deploy/branch-protection.md
autonomous: false
requirements: [CI-02, CI-01]
user_setup:
  - service: github
    why: "Branch protection é config de repositório (não versionada) e exige admin no repo"
    dashboard_config:
      - task: "Confirmar required status checks backend+frontend + enforce_admins na main"
        location: "GitHub -> Settings -> Branches -> main (ou via gh api)"
must_haves:
  truths:
    - "Um PR real dispara ci.yml e os checks backend+frontend ficam VERDES (prova CI-01)"
    - "main exige status checks backend e frontend antes do merge (enforce_admins:true)"
    - "Um PR com falha proposital tem o botão de merge BLOQUEADO (prova CI-02); depois revertido"
    - "deploy/branch-protection.md documenta o passo-a-passo exato (gh api) reproduzível"
  artifacts:
    - path: "deploy/branch-protection.md"
      provides: "Runbook D-10: comando gh api + verificação + evidência CI-02"
      contains: "required_status_checks"
  key_links:
    - from: "branch protection da main"
      to: "contextos de status check backend/frontend (ids dos jobs em ci.yml)"
      via: "gh api PUT .../branches/main/protection contexts:[backend,frontend]"
      pattern: "required_status_checks"
---

<objective>
Fechar CI-02: tornar os checks backend e frontend REQUIRED na branch protection da main (via gh api),
provar que um PR real fica VERDE (evidência de CI-01), e provar com um PR de falha proposital que o
merge é BLOQUEADO (D-11). Documentar o passo-a-passo exato num runbook (D-10), já que branch protection
é config de repositório não versionada.

Purpose: transformar o CI de "existe" em "gate obrigatório e comprovado".
Output: deploy/branch-protection.md + estado de branch protection configurado e verificado.
Non-autonomous: exige interação humana no GitHub (admin, confirmação visual do merge bloqueado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md  # § Code Examples (branch protection gh api, verificação CI-02), Pitfall 3
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runbook deploy/branch-protection.md (D-10)</name>
  <files>deploy/branch-protection.md</files>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Branch protection — required status checks (runbook D-10)" e § "Verificação CI-02 (D-11)"
    - deploy/instalar.sh (estilo dos docs/scripts de deploy existentes, PT)
  </read_first>
  <action>
    Criar deploy/branch-protection.md (PT) documentando de forma reproduzível: (1) pré-requisitos (gh CLI
    autenticado, admin em github.com/vitormorija/automacao-agendor); (2) o comando exato
    `gh api --method PUT .../repos/vitormorija/automacao-agendor/branches/main/protection` com o body JSON
    das 4 chaves de topo (required_status_checks.strict:true + contexts ["backend","frontend"],
    enforce_admins:true, required_pull_request_reviews:null, restrictions:null) — conforme RESEARCH; (3) o
    comando de verificação `gh api .../branches/main/protection/required_status_checks`; (4) o procedimento
    de prova CI-02 (abrir PR de falha proposital, confirmar merge bloqueado, reverter). Nota Pitfall 3:
    contexts DEVEM casar com os ids dos jobs (backend/frontend). Nota A3: se o gh api retornar 422, ajustar
    o shape e registrar o corrigido aqui.
  </action>
  <verify>
    <automated>test -f deploy/branch-protection.md && node -e "const t=require('fs').readFileSync('deploy/branch-protection.md','utf8');['required_status_checks','enforce_admins','backend','frontend','branches/main/protection'].forEach(s=>{if(!t.includes(s))process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - deploy/branch-protection.md existe, em PT, com o comando gh api PUT completo (4 chaves de topo).
    - Contém contexts ["backend","frontend"], enforce_admins, comando de verificação e o procedimento de prova CI-02.
  </acceptance_criteria>
  <done>Runbook reproduzível de branch protection documentado (D-10).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Configurar required checks + provar CI verde em PR (CI-01)</name>
  <what-built>
    ci.yml (02-03) + runbook (Task 1). Claude vai: criar branch, push, abrir PR via gh, aguardar os checks
    backend+frontend rodarem, e então aplicar o gh api PUT de branch protection (contexts backend/frontend,
    enforce_admins:true) conforme o runbook.
  </what-built>
  <read_first>
    - deploy/branch-protection.md (Task 1)
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § Code Examples (gh api)
  </read_first>
  <how-to-verify>
    1. Claude executa: criar branch (ex.: ci/enable), push, `gh pr create --fill --base main`.
    2. Aguardar e conferir `gh pr checks <PR>` — os contextos `backend` e `frontend` devem aparecer e ficar
       VERDES (prova de CI-01: pipeline roda verde no estado atual).
    3. Claude executa o `gh api --method PUT .../branches/main/protection` do runbook.
    4. Confirmar `gh api .../branches/main/protection/required_status_checks` lista `backend` e `frontend`
       em contexts.
    5. Humano confirma no GitHub que a proteção está ativa (Settings -> Branches -> main).
  </how-to-verify>
  <resume-signal>Digite "approved" quando os checks aparecerem verdes E a proteção estiver configurada, ou descreva o problema (ex.: 422 do gh api, contexto não casou).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prova CI-02 — PR de falha proposital barrado (D-11)</name>
  <what-built>
    Com a proteção ativa (Task 2), Claude abre um PR com uma falha barata e reversível (ex.: um teste
    quebrado proposital) para comprovar que o merge é BLOQUEADO — evidência concreta de que o gate funciona.
  </what-built>
  <read_first>
    - .planning/phases/02-toolchain-de-qualidade-ci/02-RESEARCH.md § "Verificação CI-02 (D-11)"
  </read_first>
  <how-to-verify>
    1. Claude cria branch test/ci-gate-proof com uma falha proposital (teste quebrado), commita e abre PR.
    2. Conferir `gh pr checks` — o check backend (ou frontend) fica VERMELHO.
    3. Humano confirma no GitHub que o botão de merge está BLOQUEADO por status check obrigatório.
    4. Claude fecha o PR e descarta a branch (NÃO mergear); confirmar que nada da falha entrou na main.
  </how-to-verify>
  <resume-signal>Digite "approved" após confirmar visualmente que o merge ficou bloqueado e o PR foi fechado/revertido, ou descreva o que aconteceu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| contribuidor -> main | Código só entra na main via PR; sem gate, código não-verificado poderia ser mesclado. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04-01 | Bypass/Tampering | merge de código não-verificado na main | mitigate | Required status checks (backend+frontend) + enforce_admins:true; provado por PR de falha proposital barrado (D-11). |
| T-2-04-02 | Repudiation | config de proteção não reproduzível/auditável | mitigate | Runbook deploy/branch-protection.md com comando gh api exato + verificação (D-10). |
| T-2-SC | Tampering | — | n/a | Nenhum npm install neste plano. |
</threat_model>

<verification>
- gh pr checks (PR real): backend+frontend verdes (CI-01)
- gh api .../branches/main/protection/required_status_checks: contexts [backend,frontend]
- PR de falha proposital: merge bloqueado (CI-02), depois revertido
</verification>

<success_criteria>
main protegida por required status checks backend+frontend (enforce_admins). PR verde comprova CI-01;
PR de falha proposital comprova o bloqueio (CI-02). Runbook D-10 documentado.
</success_criteria>

<output>
Create .planning/phases/02-toolchain-de-qualidade-ci/02-04-SUMMARY.md when done
</output>
